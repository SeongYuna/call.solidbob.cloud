# 6주차 코어 기준선 판정문 — 초안

> **초안이다 — 09-22 에 빈칸을 다 채웠다.** 판정일(2026-09-30)에 모델 구성이 붙어 있는지만 확인하고 `jekyll/_logs/2026-09-30-NN-seongyun.md` 로 옮긴다.
> 진행 기록은 한 번 쓰면 고치지 않으므로(`CLAUDE.md` §4) 초안은 여기(비공개)에 둔다. 옮긴 뒤 이 파일은 지운다.
> 티켓: `w6-core-baseline-check` · 근거 규칙: `CLAUDE.md` §1 범위 관리 · 절대 원칙 2·4·5·10 · `decisions/121` 5번

---

## 1. 무엇을 판정하는가

6주차 종료 시점에 **코어(A~E, C-5) 기준선을 통과했는가.** 통과하지 못했으면 F·G·H·I 를 착수하지 않는다.
**성공 조건은 F-2 가 아니라 STT 오류 내성 실험 + 검색 품질 개선 수치다**(기획서 8절). 이 문장을 판정문에 그대로 둔다.

## 2. 판정 대상 구성 — 둘 중 하나를 고르고 이유를 적는다

- [ ] **운영 구성**(규칙 마스킹 + BM25 + 스니펫) — 09-30 에 모델이 운영 경로에 붙어 있지 않으면 이것이다
- [x] **모델 구성**(규칙+NER · KoE5 임베딩, **리랭커 없음**) — ✅ **09-22 채움**: 09-22 12:10 부터 운영에 붙어 있고(`/health` spokes `pii_ner`·`retrieval_dense`), 운영 구성으로 다시 잰 값(run_id 4)이 나란히 있다. **판정일에 아직 붙어 있으면 이것으로 판정한다** — 떨어져 있으면 운영 구성(run_id 4)으로
- 모델 배치 방식: `decisions/124`(09-22) — **B안, NER·임베딩만 운영 노드에 CPU 로** 싣는다(리랭커 제외). `121`(전용 GPU)을 대체했다. 판정일에 실제로 붙어 있는지로 구성을 고른다
- **떠 있지 않은 구성의 수치로 통과를 선언하지 않는다.** 붙어 있지 않은 구성의 값은 「로컬 측정 참고치」로만 나란히 적는다

## 3. 기준과 값

값 하나마다 **측정일 · 커밋 · 명령 · 표본 수** 넷을 붙인다(§5). 넷 중 하나라도 없으면 그 값으로 판정하지 않는다.
여러 번 실행한 값 중 **최저치**를 쓴다(절대 원칙 4).

| 항목 | 기준 | 모델 구성 — 운영에 붙은 것 (run_id 3) | 운영 구성 참고치 (run_id 4) | 통과 |
|---|---|---|---|---|
| B 검색 Recall@5 (오류 없음) | ≥ 0.70 | **0.969** (93/96) | 0.812 (78/96) | ✅ 둘 다 |
| B 검색 Recall@5 (오류 10%) | ≥ 0.60 | **0.969** (93/96, dense 곡선) | 0.792 (76/96) | ✅ 둘 다 |
| B 검색 MRR | 기록 | 0.868 | 0.635 | 기록 |
| C-5 누락 (오류 없는 전사) | **0건 — 절대 규칙** | **0** · n 28 · P1~P7 표본 전부 | 0 · n 28 | ✅ |
| C-5 누락 (오류 5% / 10%) | 기록 — 한계로 적는다 | 규칙+NER 곡선(`2026-09-22-all.json`) | 2 / 3 | 한계 1 |
| F-2 판정 (규칙표 안) | 100% — 절대 규칙 | **1.0** · n 99 | 1.0 · n 99 | ✅ 단서 4 |
| B-6 정답 없음 기권 | 기록 | 0 · n 24 (문턱 미적용) | 0 · n 24 | 기록 — 문턱 값은 팀 결정 대기 |
| E 하네스 | 실제로 돌고 네 조건이 붙는다 | **run_id 3** · 09-22 · `e966b62` · 3회 최저 · `--record` | run_id 4 · 같음 | ✅ — ⚠ **로컬 기록 DB**(류준 맥 Postgres 5434) |

**09-22 에 채운 값의 네 조건** — 측정일 2026-09-22 · 커밋 `e966b62`(깨끗한 트리) · 골든셋 `v1-150.1` (B 96 · C-5 28 · F-2 99 · B-6 24) · 지식베이스 98조항 · ES 9.5.1 + nori · 각 3회 최저 ·
`ELASTICSEARCH_URL=http://127.0.0.1:9200 DATABASE_URL=<로컬> python scripts/run_eval.py --runs 3 --retriever dense --record`(run_id 3) ·
`… --no-ner --retriever bm25 --record`(run_id 4) · 구성 내보내기 `data/processed/eval-export/2026-09-22-run-3-4.json`(류준 05번 기록, `STATE.md` 「실측값」).
오류 곡선은 `scripts/measure_error_tolerance.py`, 시드 3개 최저, `data/processed/error-tolerance/2026-09-22-all.json`.

⚠ **골든셋이 09-22 에 156 → 279건으로 늘었다**(F-2 99 · 「정답 없음」 포함). 판정일 재측정은 표본 수가 달라지므로 위 「지금 있는 값」과 **같은 표에 나란히 두되 표본 수를 반드시 같이 적는다.**

지금 있는 값의 출처: 2026-09-21 · 커밋 `261bce4-dirty` · 골든셋 `v1-150`(156건) · 지식베이스 98조항 · ES 9.5.1 + nori
· `ELASTICSEARCH_URL=http://127.0.0.1:9200 python scripts/run_eval.py --runs 3 --no-ner --retriever bm25`
· 오류 내성은 `scripts/measure_error_tolerance.py`, 시드 3개(`STATE.md` 「실측값」 · `jekyll/assets/error-tolerance-2026-09-21.json`)

~~⚠ **E 가 지금 가장 약하다** — 09-21 값은 기록 DB 없이 쟀다.~~ → **09-22 해소**: run_id 3·4 가 `--record` 로 남았다. 남은 단서는 「로컬 기록 DB」 하나 — 운영 RDS 의 `eval_run` 에는 없다. 판정문에 그렇게 적고 통과로 본다(값·커밋·명령·표본이 다 있다).
⚠ `eval_run` 에 검색·NER 구성 컬럼이 없어 run 3·4 가 DB 만으로는 구분되지 않는다 — 구성은 내보내기 JSON 으로 남겼다(장민석 님 컬럼 추가 대기).

## 4. 측정 불가 — 통과도 실패도 아니다

- ~~F-2 필요서류 판정 — 골든셋 `f2_case` 0건~~ → **09-22 정정: 이제 잰다.** 골든셋에 `f2_case` 99건이 들어왔고(`f3f284b`) `decisions/125` 가 `118`(설계 문서 전환)을 되돌려 「구현 완결」로 갔다. 판정문에는 **「팀이 만든 규칙표 안에서의 정확도」**라는 단서를 붙인다(절대 원칙 10). 값은 아직 기록되지 않았다 — 판정일 재측정에서 나온다
- **트리거** — 골든셋에 도착 시각이 없어 일부러 채점하지 않는다(채점하면 가짜 만점)
- **A-5 통번역** — 외국인 음성 데이터 미확보(AI Hub 505·71479)
- **생성(B-4·B-5) · 음성 인식(A-5) · 통화 온도(D-5)** — 하네스가 부르지 않는다(`w6-harness-silent-metrics` 가 끝나면 「측정 불가」로 찍힌다)
- 컴플라이언스·콜 가드의 1.0 은 **상한이지 성능이 아니다**(골든셋을 보고 쓴 규칙). 판정 근거로 쓰지 않는다

## 5. 판정문에 반드시 같이 적는 한계

1. C-5 「누락 0」은 **오류 없는 전사 한정**이다 — 오류 5% 에서 이미 2건이 샌다
2. 주입기가 실측 STT 오류의 **46.9%** 를 흉내 내지 못한다 — 곡선이 낙관 쪽이다
3. 쌍둥이 조항 2건은 구분 단서가 없는 기준으로 채점된다(`decisions/123`)
4. F-2 의 정확도는 **팀이 만든 규칙표 안에서의 값**이다(`decisions/125` · 절대 원칙 10)

## 6. 결론 (판정일에 채운다)

- 판정: **통과 (09-22 값 기준 — 판정일에 모델 구성이 아직 붙어 있는지만 확인한다)**
- 통과한 것: B 검색(오류 없음·10% 둘 다, 모델·운영 구성 둘 다) · C-5 누락 0(오류 없는 전사, P1~P7) · F-2 1.0(규칙표 안) · E 하네스(run_id 3·4)
- 측정 불가인 것: 트리거 · A-5 · 생성(B-4·B-5) · D-5 통화 온도 (4절 목록 그대로)
- F·G·H·I: **착수하지 않는다 — 판정과 무관한 범위 관리 판단**(`decisions/132`, 09-22). `w8-extension-pick` 은 닫았다

## 7. 판정일 전에 끝낼 것

- [x] 모델 구성 재측정 + `--record` — run_id 3(09-22, 류준)
- [x] 운영 구성 재측정 + `--record` — run_id 4(09-22, 류준)
- [x] 판정 대상 구성 결정 — 2절(모델 구성, 09-22)
- [ ] **판정일에 할 것 하나** — 운영 `/health` 의 spokes 에 `pii_ner`·`retrieval_dense` 가 있는지 본다. 있으면 이 초안을 그대로 `_logs/2026-09-30-NN-seongyun.md` 로 옮긴다. 없으면 2절을 운영 구성으로 바꾸고 run_id 4 열로 판정한다
- [ ] 판정일 명령(재측정은 **선택** — 09-22 값으로 판정해도 네 조건이 다 있다)

```bash
ELASTICSEARCH_URL=http://127.0.0.1:9200 python scripts/run_eval.py --runs 3 --record --no-ner --retriever bm25   # 운영 구성
ELASTICSEARCH_URL=http://127.0.0.1:9200 python scripts/run_eval.py --runs 3 --record                               # 모델 구성(모델 파일이 있을 때)
python scripts/measure_error_tolerance.py                                                                          # 곡선
git rev-parse --short HEAD                                                                                         # 커밋
```
