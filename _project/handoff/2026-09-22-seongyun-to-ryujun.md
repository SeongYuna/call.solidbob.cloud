# 류준님께 — 09-21 전달 문서(§1)에 대한 답 (2026-09-22, 정성윤)

> 비공개(`_project/`). 정본은 결정 기록·티켓·미결 항목이고, 여기는 그것을 순서대로 옮긴 것입니다.

## 정한 것

| § | 요청 | 답 |
|---|---|---|
| 1-1 | 09-30 판정 구성 | **모델 구성으로 시도합니다** — `decisions/124`. 단 전용 GPU 가 아니라 **운영 노드(t3.large)에 CPU 로 NER + 임베딩만** 싣습니다(리랭커 제외). 09-30 까지 `/health` `spokes` 에 붙지 않으면 `121` §5 대로 운영 구성으로 판정합니다. 순서는 티켓 `w5-models-on-node`. |
| 1-8 | AI Hub 71479 S3 경로 | **제안 그대로 승인** — `s3://assist-apne2/datasets/aihub-71479/validation/{label,wav}/`, zip 그대로, `--sse AES256`. 버킷은 퍼블릭 차단 4종 ON 확인(09-22), 팀 IAM 만 읽습니다. `CHECKSUMS.md5` 도 같은 경로에. |
| 1-10 | `roles/speech.client` | **붙였습니다**(09-22, `callguard-stt@…`). Chirp 3 재측정 돌려도 됩니다. |
| 1-4 | SYN-010 운영 투입 | **허용합니다.** 쓰기 문은 09-22 에 잠겼습니다(`ingest_guard: locked`). 미디에이터 안에는 `CORE_API_TOKEN` 이 들어 있어 미디에이터를 거치는 재생기 경로는 그대로 됩니다. 서버를 **직접** 부르는 `--core-url …/close` 는 토큰이 필요합니다 — 값은 파일 아닌 방법으로 따로 드립니다. 투입 뒤 넷(`recommendation_card`·`closure`·`compliance_flag`·`compliance_rule`)이 쌓이는지 확인 부탁드립니다. 이것이 잠근 뒤 실제 통화가 저장되는지의 확인이기도 합니다. |
| 1-3 | TTS 콘솔 할당량 | 아직 안 봤습니다. gcloud 로그인은 됐으니 다음에 봅니다. |

## 새로 부탁드릴 것 (124 의 1단계)

- **모델 두 폴더를 S3 에 올려 주세요** — `s3://assist-apne2/models/koelectra-ner/` · `s3://assist-apne2/models/koe5/`. 로컬 측정에 쓴 바로 그 폴더이고, 해시를 `w5-models-on-node` ①에 적어 주세요. 리랭커는 이번엔 올리지 않습니다. `models/` 프리픽스는 지금 비어 있습니다.

## 아직 못 정한 것

- **1-2 `118` 되돌림** — 오늘 정합니다(사용자 판단 대기).
- **1-6 STT 캡 날짜** — 리허설 날짜가 없어서 못 정합니다. 합성 통화와 판정 측정은 STT 를 안 타니 당장은 영향이 없습니다.
- **1-9 A-5 차별점 유지** — Chirp 3 값을 보고 정합니다.
- **1-11 절차 고르기 규칙** — 제안(규칙 없는 `TERM` 1순위면 판정하지 않는다)에 동의합니다. 콜 미디에이터 `src/` 라 태그가 오르니 124 의 ② PR 과 같이 가면 태그 한 번으로 끝납니다. 별도로 먼저 내셔도 됩니다.
- **1-7 `121` 열린 질문** — `124` 로 질문 1·2·4 가 사라졌습니다(원격 없음). NER 을 서버 파드에 싣자는 제안 그대로입니다.

## 참고 — 09-22 에 고친 운영 것

- 랜딩(www)이 09-15 이후 배포되지 않고 있었습니다(Vercel 설정 실수). 고쳤고, 홍보 페이지의 라이브 통화 주소도 넣었습니다.
- 프론트 Vercel 프로젝트에 들어 있던 서버 비밀값 62개를 지웠습니다. 화면 동작엔 영향 없습니다.
- 09-21 「남은 일 목록」 셋째 판이 적은 결정 125~128 은 저장소에 없습니다. 그 문서의 「끝났다」 항목 중 일정·배정 호출·공개 데모·스키마 대조는 아직 미결입니다.
