# 성윤님께 — 류준 쪽에서 막혀 있는 것과 부탁드릴 것 (2026-09-21)

> 비공개(`_project/` — 사이트에 안 올라감). 근거는 전부 `jekyll/open-items.markdown`·`_project/STATE.md`·결정 기록에 있고,
> 여기는 그걸 **사람이 읽게 순서를 매겨 옮긴 것**입니다. 이 문서가 정본이 아니라, 정본은 미결 항목·결정 기록입니다.
> 6주차 판정(09-30)이 9일 남았습니다. 그 전에 결정이 필요한 것을 맨 위에 뒀습니다.

---

## 0. 한 줄 요약

- **제 쪽에서 혼자 할 수 있는 건 오늘 다 돌렸습니다**(§5). 남은 건 전부 ① 남의 회신 ② 팀 결정 ③ 외부 데이터 셋 중 하나에 걸려 있습니다.
- 성윤님께 부탁드릴 건 **결정 7건 + 손 작업 3건**(§1 — TTS 키는 받아서 확인까지 끝냈습니다). 그중 시급한 건 **GPU EC2 를 09-30 전에 띄울지**와 **`decisions/118`(F-2) 되돌릴지** 둘입니다.

---

## 1. 성윤님이 해주셔야 할 것 (급한 순)

### 1-1. [결정] 6주차 판정(09-30)까지 GPU EC2 를 띄울지 — `decisions/121` §5

- **왜 급한가**: 121 이 "판정일까지 GPU 가 운영 경로에 붙어 있으면 모델 구성, 아니면 운영 구성(규칙+BM25)이 기준선" 이라고 정했습니다.
  어느 쪽이든 괜찮지만 **어느 쪽인지 지금 정해야** 제가 그에 맞춰 수치를 준비합니다.
- **제 의견**: 09-30 전에 인스턴스를 세우고 `ai/` 를 서비스로 떼는 건(121 열린 질문 1~4) 9일 안에 무리라고 봅니다.
  **이번 판정은 운영 구성으로 가고, 모델 구성은 로컬 참고치로 나란히 적는 쪽**을 권합니다 — 그 참고치는 오늘 이 맥에서 쟀습니다(§5-1).
- **정해 주시면 제가 할 것**: 운영 구성 기준선 표 + 모델 구성 참고치 표를 `jekyll/docs/06`(평가 설계)에 나란히 넣겠습니다.

### 1-2. [결정] `decisions/118`(F-2 설계 문서 전환)을 되돌릴지

- 118 은 "골든셋에 F-2 케이스가 생기고 하네스가 실제로 채점하면 되돌린다" 고 문을 열어 뒀습니다.
- **오늘 케이스를 만들었습니다**(§5-2). 하네스에서 `closure_gate` 가 `NO_SAMPLES` 가 아니라 실제 값으로 나옵니다.
- **정할 것**: `w8-f2-wrapup` 을 「구현 완결」 갈래로 되돌릴지, 아니면 118 대로 설계 문서로 가고 케이스는 회귀 방지용으로만 둘지.
  둘 다 정직한 선택입니다. **주의**: 케이스 기대값은 규칙표를 만든 것과 같은 편(류준)이 적었으니 「팀이 만든 규정 안에서의 정확도」입니다(절대 원칙 10). 발표에서 그 단서 없이 100% 라고 쓰면 안 됩니다.
- 118 을 쓰신 분이 성윤님이라 판단도 성윤님께 맡깁니다. 되돌리시면 `w7-f2-checkpoint`·`w8-f2-wrapup` 두 티켓의 갈래 문구를 함께 고쳐 주세요(둘 다 성윤님·민석님 티켓이라 제가 안 건드렸습니다).

### 1-3. ~~[손 작업] Google Cloud TTS API 키 발급~~ — ✅ 받았습니다, 확인 끝

- 09-21 `--prefetch SYN-001` 로 7턴 381자 합성 성공(상담원 Wavenet-B · 고객 Wavenet-C, 재실행은 캐시 히트로 호출 0, 장부 381/900,000자). 티켓 `w5-persona-tts-google` 닫았습니다.
- **성윤님께 하나만**: 콘솔에서 Text-to-Speech **문자 할당량을 낮출 수 있는지**(2차 캡은 앱에 있지만 1차 캡은 콘솔뿐입니다) 한 번 봐 주세요. SSML `volume` 이 귀에 들리는지는 제가 mp3 를 들어 보겠습니다.

### 1-4. [결정·팀] 운영에 합성 통화 한 건(SYN-010)을 태울지

- 09-19 SSM 실측에서 운영 DB 의 `recommendation_card`·`closure`·`compliance_flag`·`compliance_rule` 이 **전부 0행**이었습니다. 코드는 `0.1.18` 에 다 들어 있는데 **그 경로를 타는 통화를 운영에서 아직 한 번도 안 걸어 본 것**입니다.
- 시연 전에 한 번은 봐야 합니다. 대가는 운영 DB 에 `syn-` 통화 행이 남는 것뿐입니다(지우면 됩니다).
- 정해 주시면 제가 재생기(`replay_persona_call.ts --core-url …`)로 태우고 넷이 쌓이는지 확인하겠습니다. 콜 미디에이터 `0.2.2` 의 `CORE_API_TOKEN` 을 저도 알아야 하니 그때 알려 주세요(파일로 주고받지 말고).

### 1-5. [손 작업] 기록 DB 가 있는 환경에서 하네스를 `--record` 로 한 번

- 09-21 재측정이 `eval_run`/`eval_result` 에 안 남았습니다(run_id 없음). §5 규칙(날짜·커밋·명령·표본)은 글로 남겼지만 DB 행이 있어야 CI 게이트(`w2-baseline-gate`)를 걸 수 있습니다.
- 명령은 `STATE.md` 「실측값」의 그것 그대로에 `--record` 만 붙이면 됩니다. Neon 자격증명이 있는 머신이 성윤님 쪽뿐이라 부탁드립니다.

### 1-6. [결정] STT 일 캡 600초 — 시연 리허설 전에

- 통화 1건 STT 가 **하한 146초**(추정)라 지금 캡(일 600s·월 3,600s)이면 **하루 4통화**입니다. 리허설 날 걸립니다.
- 리허설·시연 날짜가 잡히면 그 이틀만 캡을 올리는 걸 권합니다(COST-1 의 GCP 쿼터 1차 캡은 그대로). 합성 통화는 STT 를 안 타니 거기엔 영향이 없습니다.

### 1-7. [같이] `decisions/121` 이 연 질문 다섯 — 인스턴스 만들기 전에

- 제 몫이 걸린 건 **1번(`ai/` 가 라이브러리에서 서비스가 된다)** 과 **4번(GPU 가 꺼졌을 때 규칙·BM25 로 내려가야 한다)** 입니다.
- 제 제안: 포트는 그대로 두고 **어댑터만 원격 호출(HTTP)로 바꾸는 얇은 층**을 `ai/` 안에 둡니다. 원격이 안 붙으면 지금처럼 규칙·BM25 로 내려갑니다(`/health` `spokes` 가 말합니다).
  마스킹(C-5)만은 자막 앞단이라 왕복이 모든 발화에 붙습니다 — 그래서 NER 은 원격보다 **서버 파드에 CPU 로 싣는 게 낫다**고 봅니다(0.45GB, 09-15 이 맥 측정 p95 수십 ms). 121 을 조금 열어 「NER 만 예외」로 둘지 같이 정하고 싶습니다.
- 정해지면 제가 결정 기록(2xx)으로 적겠습니다. 인스턴스 자체(비용·자동 중지·모델 파일 위치)는 성윤님 몫입니다.

### 1-8. [결정] AI Hub 71479 — 받았습니다. S3 경로만 정해 주세요

- 오늘 Validation 라벨 3개 + 발화 원천 1개(**총 20 GB**, Training 266 GB 는 안 받음)를 받아 `data/raw/aihub-foreign-proficiency-71479/` 에 넣었습니다. 라벨에 정답 전사·숙련도 4등급(Beginner 13,710 / Intermediate 4,764 / Advance 3,647 / Fluent 326)·TOPIK 급이 있어 **A-5 등급별 WER/CER 을 잴 수 있습니다.** 온라인 녹음이라 8 kHz 로 낮춰 재고 「상한」 단서를 붙입니다.
- **S3 에 올리는 게 맞습니다** — `decisions/210`(데이터는 S3 에 두고 측정용) 그대로이고, 용량도 문제 없습니다: 버킷 `assist-apne2` 는 지금 `uploads/` 객체 1개(31 KB)뿐이고 런북 예산이 S3 100 GB ≈ $4/월입니다. 20 GB 면 월 $0.5 안팎, 업로드는 무료, 다른 곳에서 내려받을 때만 GB 당 $0.09 입니다.
- **정할 것 둘**: ① 경로 — 210 이 「버킷·경로는 정성윤 님이 정한다」고 했습니다. 제안: `s3://assist-apne2/datasets/aihub-71479/validation/{label,wav}/` 에 **zip 그대로**(풀지 않은 20 GB 하나 + 라벨 zip 3개) ② 접근 — AI Hub 재배포 금지라 **버킷은 비공개 그대로, 팀 IAM 만** 읽게. 퍼블릭 읽기·프리사인 공유는 안 됩니다. 정해 주시면 제가 올리고 티켓에 S3 키를 적습니다(수치 기록에 데이터 버전으로 남깁니다).
- **원본을 어떻게 드리나 — 파일을 보내지 않습니다.** 20 GB 를 드라이브·USB 로 옮기는 것보다 **제 맥에 있는 AWS 자격증명으로 성윤님이 정한 S3 경로에 제가 직접 올리는 것**이 빠르고, AI Hub 조항(재배포 금지)에도 맞습니다(버킷 밖으로 사본이 안 생깁니다). 성윤님은 ① 경로 ② 버킷이 비공개·팀 IAM 한정인지만 답해 주시면 됩니다. 올린 뒤 아래 md5 로 대조하실 수 있게 `CHECKSUMS.md5` 를 같은 경로에 같이 올립니다.

  ```
  58351c25ef65daf15f0bf2284fabe9fc   55M  label/VL_pronunciation_lab.zip
  d18ebd315473769f5f809e5542fdcecc   35M  label/VL_pronunciation_tag.zip
  9831e9e4152c55bfc1e4f0b7cfb61b8a   16M  label/VL_Speech_lab.zip
  97573d12fa01cf81f1246ebd5003a859   20G  wav/VS_Speech_sound.zip
  ```

  올리는 명령(경로만 바꾸면 됩니다 — zip 그대로, 풀지 않고):

  ```bash
  aws s3 sync data/raw/aihub-foreign-proficiency-71479/validation \
    s3://assist-apne2/datasets/aihub-71479/validation --exclude "label/VL_*/*" --sse AES256
  ```
- 하나 더: STT 일 캡 600초(§1-6)에 A-5 표본(등급당 20건 × 4 ≈ 600초)이 딱 걸립니다. 측정 날 캡을 하루만 올리거나 이틀에 나눠 잽니다.
### 1-9. [결정·팀] A-5 를 차별점으로 유지할지 — 첫 수치가 나왔습니다

- §5-6: 초급 화자 WER 0.41(상한). 「어눌한 한국어를 정확히 전사」가 이 수치로 차별점이 되는지, 아니면 범위 ⓑ 를 「측정·기록」으로 낮춰 발표할지. 판단 재료는 티켓 `w3-a5-translation-spike` 09-21 절에 다 있습니다. Chirp 3 로 다시 재면 달라질 수 있어 그 값을 보고 정하셔도 됩니다(내일 ~390초).

### 1-10. [손 작업] 서비스 계정에 「Cloud Speech 클라이언트」 역할 — Chirp 3 재측정용

- A-5 를 Chirp 3 로 다시 재려 했더니 `403 speech.recognizers.recognize` 로 막혔습니다(STT 0초 사용). v1 은 역할 없이 되지만 v2(Chirp)는 서비스 계정에 **`roles/speech.client`** 가 있어야 합니다. `callguard-506606` 프로젝트 IAM 에서 서비스 계정에 이 역할 하나만 붙여 주시면 됩니다. 운영 STT 경로와는 무관한 측정 전용입니다. 붙으면 제가 한 줄로 돌립니다(약 390초).

### 1-11. [결정] 필요서류 절차 고르기 규칙 — 검색이 좋아지면 엉뚱한 절차가 뜹니다

- 로컬에서 dense 를 켜고 E2E 를 돌리니 남아 있던 ❌2(SYN-011·021)는 **dense 만으로 풀렸습니다**(리랭커 불필요). 대신 **SYN-023 이 새로 깨졌습니다** — 옥내 배관(규칙 없는 절차)을 묻는 통화에 첫 발화부터 「누수 요금 감면」 체크리스트가 떴습니다. 09-18 에 제가 정한 「규칙 있는 첫 카드」 규칙이 규칙 없는 1순위를 건너뛴 탓입니다.
- 제안: 1순위가 규칙 없는 `TERM` 이면 멈추고 판정하지 않는다(건너뛰는 건 `POLICY`·`MANUAL` 만). 콜 미디에이터 `src/` 라 태그가 오르니 성윤님 확인 뒤 바꾸고, 24건을 BM25·dense 두 구성으로 다시 돌리겠습니다. 운영은 BM25 라 지금 당장은 안 드러납니다 — GPU 인스턴스를 붙이기 전에만 고치면 됩니다.


---

## 2. 민석님 검토 대기 — 제가 `server/` 를 고친 것들 (`decisions/302`)

성윤님이 전해 주셔도 되고, 미결 항목 그대로입니다. 전부 **운영에 이미 떠 있는 코드**라 검토가 늦어져도 동작엔 지장 없습니다.

1. **C-5 이름·주소 규칙 보강**(09-17·18, `server/apps/masking`) — `name_detector.py` 네 갈래 · `_NOT_NAMES` 목록이 실제 발화에서 걸린 것만 넣었는지 · 가족 호칭 문맥 · P4 재라벨.
2. **`closure_rule` 25→29**(09-18) — `decisions/305` 가 EXCLUDED 로 뒀던 2.6·4.5·4.9·4.12 를 되살렸습니다(갈래 공통 서류만 필수). 305 취지와 맞는지.
3. **`compliance_flag` 저장 배선 신설**(09-18) — `compliance_rule` 카탈로그를 저장 직전 UPSERT 로 채우는 방식 · `default_severity` 전부 `medium`(등급 정의 없음) · 저장 실패를 200+로그로 삼키는 정책.
4. 4.20 과태료 이의신청·4.8·3.3·6.12 를 규칙표에 넣을지(「— 필요서류」 조항이 아니라 일부러 뺐습니다).

---

## 3. 서희님 몫으로 넘어가 있는 것

1. **대시보드 컴플라이언스 경고**가 서버 결과가 아니라 프론트 로컬 규칙입니다. 서버·콜 미디에이터는 검사·저장까지 하니 콜 미디에이터 `compliance` 메시지 파서만 받으면 `announceCompliance` 를 켤 수 있습니다(켜는 건 성윤님 `main.ts`).
2. **`AgentCallBox` 모달**이 합성 통화 라이브 보기를 가립니다 — 재생기일 때 닫는 방법.
3. C-6 갈래 이름이 프론트(한글 3종)·백엔드(영문 4종, `distress` 포함)에서 다릅니다.
4. 상담기록 목록이 `customer_id` 해시를 그대로 보여 줍니다.

---

## 4. 팀이 정해야 하는 것 (성윤님이 회의에 올려 주시면 됩니다)

| 무엇 | 왜 지금인가 | 제 권고 |
|---|---|---|
| **「감정분석」 대체 용어** | D-5 코드·화면 라벨·발표 자료에 다 들어갑니다. `decisions/203` 이 그 말을 안 쓰기로 했는데 대체어가 없습니다 | 「통화 온도(Call Temperature)」 |
| **C-4 라벨의 뜻** | 기획서 C-4(대체 표현 제시·기능) ≠ 골든셋 C-4(의학적 안심 발언·위반 갈래). `compliance_rule` 카탈로그에 무엇을 넣을지 갈립니다 | 초안 `_project/decisions/211`(제안) — §5-4 |
| **합성 대본 24건 사람 검토** | 매뉴얼 5.2 종료·5.4 위기 응대 해석과 어눌한 한국어 과장 여부는 AI 검수(QA 페르소나 3종)만 거쳤습니다 | 한 사람이 1시간이면 됩니다. 시연 대본이 될 SYN-004·010·020 만이라도 |
| **`distress_count` 미저장 판단**(`decisions/205` ④) | 정신건강 정보라 컬럼을 뺐는데 팀 확인이 없습니다 | 유지 |

---

## 5. 오늘(09-21) 제 쪽에서 한 것 — 성윤님 판단에 필요한 결과

값은 전부 하네스·측정 스크립트가 낸 것이고 조건(날짜·커밋·명령·표본)은 각 티켓에 있습니다.

### 5-1. 모델 계열 오류 내성 곡선 (미결 「모델 계열 곡선이 그림에 없다」) — ✅ 쟀습니다

- 조건: 2026-09-21 · 커밋 `97f7b25` · `scripts/measure_error_tolerance.py --device mps --golden-set <09-21 시작 시점 스냅샷>` · 시드 3개(09-21 운영 실행과 같음) · 검색 n 96 · C-5 판정 24. **운영 계열(bm25·규칙) 값은 성윤님 09-21 JSON 과 시드별로 완전히 같아서** 한 그림에 5계열이 얹혔습니다 — `jekyll/assets/error-tolerance-2026-09-21-models.png`(+`.json`). 티켓 `w5-error-curve-model-series`.
- 검색 Recall@5 / MRR (시드 최저):

  | 오류율 | bm25(운영) | dense | dense+리랭커 |
  |---|---|---|---|
  | 0% | 0.833 / 0.666 | 0.979 / 0.885 | 0.979 / 0.919 |
  | 10% | 0.812 / 0.653 | 0.979 / 0.878 | 0.979 / 0.908 |
  | 20% | 0.792 / 0.621 | 0.969 / 0.873 | 0.969 / 0.898 |

- C-5 누락(판정 24, 시드 최대): 규칙만 0 → 2 → 3 → 4 → 4 / **규칙+NER 0 → 0 → 0 → 1 → 1**. 「누락 0」이 오류 있는 전사에서 성립하는 구성은 NER 을 켠 쪽뿐이고 그것도 10% 까지입니다.
- 하네스 모델 구성 3회 최저치: NER+dense 0.979/0.885 · NER+dense+리랭커 0.979/0.919 · masking 0/0/0.0 (n 28).
- **읽을 때 붙일 단서**: ① 주입기가 실측 오류의 46.9% 를 못 흉내 내 곡선 전체가 낙관 쪽 ② 골든셋은 작성자가 본 뒤의 값 ③ M5/mps 값이라 지연·메모리는 무관, 품질만 유효 ④ **운영에는 dense·NER 이 없습니다** — 기준선은 여전히 규칙+bm25 행. 리랭커는 Recall 을 한 건도 더 못 맞히고 MRR 만 +0.03 이라 `208`·`121`의 「리랭커는 뒤로」와 같은 방향입니다.

### 5-2. F-2 골든셋 케이스 (118 되돌림 조건) — ✅ 99건, 하네스가 실제로 채점합니다

- `golden-set/v1-150.json` 에 `GS-601~699`(module `F-2`, 발화 없이 `f2_case` 만 — 검색·마스킹 채점에 안 섞입니다). 규칙표 29조항 × (전부 안내 / 하나 누락 / 전부 누락) 87건 + 경계 12건. `EXCLUDED` 6조항은 규칙이 없어 채점 대상에서 뺐습니다.
- 기대값은 규칙표를 **손으로 옮겨 적어** 먼저 정하고 코드와 대조했습니다 — 어긋남 0. 하네스: `closure_gate: accuracy 1.0 · absolute_rule_passed true · n 99`(커밋 `97f7b25`). 서버 골든 테스트가 `v1-50` 을 읽고 있어 skip 이 안 풀리길래 경로 한 줄만 `v1-150` 으로 고쳤습니다(199 passed) — `server/` 파일이라 민석님께도 알립니다.
- **인용 주의**: 1.0 은 「코드가 규칙표를 그대로 따르는가」의 상한이지 「안내가 실제로 충분했는가」가 아닙니다. 발화 → evidence 자동 판정(`detection.py`)은 이번 케이스로 재지 않았습니다(발화 붙은 케이스가 따로 필요).
- `DASAN-POLICY-1` 문구를 305 에 맞췄습니다(ID 유지). 골든셋에 POLICY-1 참조는 0건이라 검색 라벨 영향은 없지만 **다음 ES 재색인 뒤 검색 결과가 달라질 수 있습니다** — 09-30 전엔 재색인하지 않는 게 안전합니다.
- ⚠ `check_session_end.py` 가 `w8-f2-wrapup`(민석님 티켓, todo)이 `closure_gate` 테스트 경로를 소관으로 둬서 경고합니다. 118 판단과 함께 그 티켓의 status·갈래를 정리해 주세요.

### 5-3. D-5 통화 온도 하네스 배선 + 어댑터 — ✅ 둘 다, 채점은 「측정 불가 — 표본 없음」

- 하네스 리포트에 `call_temperature` 항목이 생겼고, 판정 포트 `VoiceOutlierPort`(server)와 구현체 `WavVoiceOutlierAdapter`(ai — wav → F0 중앙값 → 화자별 로버스트 z → 이상치)를 만들어 `run_eval.py` 에 꽂았습니다. 실제 리포트는 이제 「모듈 미구현」이 아니라 **「골든셋에 채점 대상이 없다」** 로 찍힙니다 — 그게 정직한 값입니다. 판정 로직·임계값은 한 글자도 안 바꿨습니다.
- 골든셋 D-5 케이스 형식(화자 · 발화별 오디오 경로 · 튀어야 할/차분한 발화)은 `golden-set/README.md` 끝에 있습니다. ai 테스트 전부 통과, 계약 3+4종 KEPT.
- **남은 것**: ① 음성 골든셋 0건 — 통화 단위 + 톤 라벨이 필요한데 다산콜DB 는 발화 단위 클립이라 못 씁니다. 출처가 없으면 **D-5 는 시연에선 동작, 수치로는 「측정 불가」** 로 갑니다 ② 용어(§4) ③ 실제 통화 음성이 이 판정으로 흐르는 서버 경로는 없습니다(서버는 텍스트만 받음 — 콜 미디에이터 쪽 설계가 먼저).

### 5-4. C-4 라벨 결정 기록 초안 (`_project/decisions/211`, 제안 — 팀 합의 전) — ✅ 썼습니다

- 확인한 사실: 저장소 안에 C-4 의 뜻이 **둘 공존**합니다. 골든셋(GS-312·318)·`ai/apps/compliance` 규칙·서버 `compliance_rule` 카탈로그(「의학적 안심 발언 · MANUAL-4.1」)는 **위반 갈래**로, 기획서 2.4절·MANUAL 1.4·DTO 주석·ERD 는 **기능(대체 표현 제시)** 으로 씁니다. 하네스는 C 를 위반 있음/없음 이진으로만 재서 **점수엔 영향이 없습니다.**
- **권고 ①: 기획서를 고친다** — C-4 를 「근거 없는 안전 보장(의학적 안심 발언 등) 탐지, MANUAL-4.1」 위반 갈래로 재정의하고, 「대체 표현 제시」는 C-1~C-4 공통 출력(`alternative_source`)으로 표 아래로 옮깁니다. 이유: 점수·코드·골든셋이 한 자리도 안 움직이고(`123` 의 「판정 전 채점 기준 불변」과 같은 방향), 「대체 표현 제시」는 탐지 대상이 없어 재현율을 낼 수 없는 항목이라 표에 두면 측정 불가가 영구히 남습니다(절대 원칙 10).
- 채택 시 고칠 곳 12곳 — 전부 문서·주석이고 코드 동작 0·골든셋 0. MANUAL 4.1/1.4 문구 수정은 ES 재적재가 따라오니 **09-30 뒤로** 미루자고 적었습니다.

### 5-5. B-6 「관련 문서 없음」 — ✅ 케이스 24건 + 점수 분포, 문턱은 팀 결정으로

- 골든셋에 정답 없음 케이스 24건(`GS-701~724`, module `B-6` — 타 기관 소관 15 · 시 범위 밖 2 · 잡담 4 · 지식베이스 공백 3). **B 96건의 Recall@5/MRR 은 전후 동일**(0.833/0.666)이라 09-30 기준선엔 영향 없습니다. 하네스에 `no_answer` 항목(지금은 문턱이 없어 기권 0 이 정직한 값).
- 측정(`scripts/measure_no_answer_threshold.py`, 이 맥): **세 계열 모두 문턱 하나로 깨끗이 못 가릅니다.** BM25 는 정답 1건 잃는 값에서 24건 중 5건만 걸러져 권하지 않고, 임베딩이 켜지면(`121`) dense 0.67 안팎에서 손실 1/94 로 15/24 를 거릅니다. 남는 9건은 「요금·신고·서류·지원금」 어휘가 겹치는 타 기관 문의라 어휘 검색으로는 못 가립니다.
- **정할 것**: ① 문턱을 둘지·어느 계열·어느 값(결정 기록으로, 채택 전 새 케이스로 재측정) ② 타 기관 문의에 「관련 문서 없음」 카드가 맞는지 「소관 안내(MANUAL-2.2)」 카드가 맞는지 — 프론트 카드 형식과 같이(서희님).
### 5-6. A-5 숙련도 등급별 WER/CER — ✅ 쟀습니다 (데이터 받은 날 바로)

- 조건: 2026-09-21 · 커밋 `97f7b25-dirty` · `scripts/measure_a5_proficiency.py`(시드 20260921, 등급당 20건, 3~6초, 화자당 ≤2, 8 kHz 다운샘플, Google STT **v1 기본 모델**) · STT 482초(오늘 캡 600 안, 이달 996/3,600). 공개 요약 `jekyll/assets/a5-wer-2026-09-21.json`(건별 전사 없음).

  | 등급 | n | WER | CER |
  |---|---|---|---|
  | Beginner | 20 | **0.412** | 0.221 |
  | Intermediate | 20 | 0.266 | 0.198 |
  | Advance | 20 | 0.225 | 0.087 |
  | Fluent | 20 | 0.198 | 0.067 |

- 방향은 일관됩니다(등급 낮을수록 오류 큼). 원본 48 kHz 대비 8 kHz 는 전체 WER +0.065(20건). **이 수치는 상한입니다** — 온라인 녹음을 다운샘플만 했고(코덱·잡음 없음), 과제가 낭독·질문답변이라 민원 어휘가 아니며, 짧은 발화만 골랐습니다. 정답 전사가 학습자 오류를 그대로 적어 STT 의 「교정」이 오류로 잡히는 것도 있습니다.
- **정할 것(§1-9)**: A-5 를 코어 차별점으로 유지할지. Chirp 3 재측정은 다음 날 캡으로 가능합니다.

### 5-7. 로컬 dense E2E · 합성 음성 음량 · 대본 AI 검토 — ✅

- **E2E**: dense 만 켜면 SYN-011·021 ✅, 리랭커까지 켜면 24건 ✅23·❌1(SYN-023 — §1-11). 보고서 `data/processed/persona-e2e/2026-09-21-*`.
- **합성 음성**: 고함이 평온과 같은 크기였습니다. WaveNet 이 SSML 음량 «올리기»를 무시해서입니다. 기준을 -8 dB 로 낮춰 평온 대비 고함 +7 dB 가 들리게 고쳤고, 시연 대본(SYN-004·010·020) 캐시를 새로 채웠습니다. 시연 때 스피커를 조금 올려 주세요.
- **대본 24건 AI 검토**: 부록 A 금지 표현 0건, 고친 발화 3건(라벨 변경 0), 71479 실제 학습자 발화와 대조해 과장 없음. 사람이 꼭 볼 항목 8개는 `scripts/persona_sim/REVIEW-2026-09-21.md` 에 있습니다(특히 SYN-009 위기 응대 말투).


---

## 6. 제(류준)가 사람으로서 직접 해야 하는 것 — 참고

- **A-5 Chirp 3 재측정**(IAM 역할 뒤, §1-10) · S3 업로드(경로 답 뒤) · 절차 고르기 규칙 수정(§1-11 답 뒤) · 시연 전 음성 한 번 듣기.
- 합성 대본 — AI 검토는 끝났고(§5-7) 사람이 볼 8개 항목만 남았습니다.
- 위 §1 결정이 나오면 그에 따른 코드·문서 반영.
