# Requirement: E-3
"""STT 오류 주입기 — [4.2절](/docs/04/) STT 오류 내성 실험(`w5-stt-error-injector`).

전사 텍스트에 **실측에서 뽑은 유형의** 합성 오류를 넣는다. 아무 글자나 바꾸면 그건 STT 오류가
아니라 오타이고, 그 위에서 낸 곡선은 아무것도 말하지 않는다(절대 원칙 10).

    hangul.py     음절 ↔ 자모 (순수 계산)
    classify.py   (정답, STT 출력) → 편집 유형 — 실측 쌍에서 프로파일을 뽑는 쪽
    profile.py    실측 프로파일 — 유형별 건수 · 자모 혼동표 (출처가 주석에 있다)
    injector.py   프로파일대로 주입하고 **실제 WER 을 재서** 목표와 함께 돌려준다
"""
