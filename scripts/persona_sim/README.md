# 합성 통화 대본 — `dasan-v0`

> ⚠ **결정 기록이 아직 없다**([미결](/open-items/)). 2026-09-16 Claude Code 로 생성하고 **QA 페르소나 3종으로 검수**했다 — 사람 검토는 아직이다.
> 티켓: [w5-persona-sim-scripts](/backlog/w5-persona-sim-scripts/)
>
> ⚠ **내용 경고** — SYN-005·006·007 에 폭언·성적 표현, SYN-009 에 **극단적 선택을 암시하는 발화**가 있다. 콜 가드(C-6)
> 검증에 필요한 만큼만 썼다. 실제로 힘든 상황이면 자살예방상담전화 **109**(24시간)로 연락할 수 있다.

상담원·고객 **페르소나 대화 대본**이다. 두 AI 가 실시간으로 말하는 것처럼 **재생**한다 — 대본은 미리 만들어 고정하고
(같은 통화를 반복 재생할 수 있어야 한다), 재생기가 턴마다 콜 미디에이터에 글자를 흘리고 이 맥의 `say` 로 소리를 낸다.
**상담원 대시보드(`apps/call`)는 고치지 않는다** — 받는 메시지가 실제 통화와 똑같다.

## 파일

| 파일 | 내용 |
|---|---|
| `dasan-v0/personas.json` | 상담원 **6**(신입·일반·베테랑 + 09-18 무뚝뚝·사무적 A04 · 과잉 친절·단정형 A05 · 외국어 응대 지향 A06) · 고객 **19**(09-16 9 · 09-17 D-5 용 4 · 09-18 급한 직장인 C14 · 의심 많음 C15 · 일본 출신 초급 한국어 C16 · 옆길 고령 C17 · 조용히 비꼼 C18 · 가족 대리인 C19) · 톤 5종 · `say_voice`(이 맥의 한국어 음성) · `tts_voice_hint`(「여성」/「남성」 — Google TTS 음성 배분 폴백 단서) |
| `dasan-v0/SYN-001.json` ~ `SYN-024.json` | 통화 1건 = 파일 1개 |
| `generate_dasan_v0.py` | **정본.** JSON 은 이걸로 만든다. 라벨 문구가 발화에 글자 그대로 없거나 같은 화자가 연달아 말하면 멈춘다. 페르소나 정의는 여기 |
| `scenarios_v0_a.py` · `scenarios_v0_b.py` | 대본 정의 — ① SYN-001~014(09-16·17, 생성기에서 그대로 옮김) ② SYN-015~024(09-18 성격 다양화). 생성기가 import 한다 |
| `../../services/call-mediator/scripts/replay_persona_call.ts` | 재생기 |
| `TTS.md` | **Google Cloud TTS(Wavenet)** — 키 발급 절차(정성윤 님 전달용) · `--speak google` · `--prefetch` · 무료 한도 리밋 2단(09-18) |
| `E2E.md` · `e2e_check.py` | **왕복 검사** — 대본 → 콜 미디에이터 → 서버 → DB → `/ws` → `GET /hub/calls/{id}/transcript`·`/record` 를 규칙으로 판정(09-18) |

```bash
python3 scripts/persona_sim/generate_dasan_v0.py      # JSON 을 손으로 고치지 말고 생성기를 고쳐 다시 만든다
```

> **왜 `ai/` 가 아니라 `scripts/` 인가** — `scripts/check_release_tags.py` 가 `ai/` 변경을 **서버 이미지 변경**으로 친다.
> 대본을 `ai/` 에 두면 대본 한 줄 고칠 때마다 서버 태그를 올려야 한다.

---

## 시연 — 대시보드에서 실제 대화처럼 보기

### A. 로컬 (서버·콜 미디에이터를 이 맥에서)

```bash
cd server && uvicorn main:app --env-file ../.env                 # 1. 서버 :8000
cd services/call-mediator && npm start                                 # 2. 콜 미디에이터 :8080 (루프백이라 토큰 불필요)
cd apps/call && VITE_CALL_MEDIATOR_WS_URL=ws://localhost:8080/ws npm run dev   # 3. 대시보드 :5173
```

4. 브라우저에서 대시보드를 열고 **「통화 시작」** 을 누른다 — 화면이 비워지고 콜 미디에이터에 붙는다
5. 재생한다

```bash
cd services/call-mediator
node scripts/replay_persona_call.ts SYN-004 --speak            # 소리까지
node scripts/replay_persona_call.ts SYN-004 --speak google     # Google TTS(Wavenet) — .env 의 GOOGLE_TTS_API_KEY, 캐시 있으면 키 없이도 (TTS.md)
node scripts/replay_persona_call.ts SYN-004 --speed 2 --watch  # 빠르게, 터미널에서 대시보드가 받는 것도 본다
node scripts/replay_persona_call.ts SYN-004 --close --core-url http://localhost:8000   # 끝나면 통화 후 요약 초안까지
```

`--close` 는 `/ws` 로 받은 **마스킹본만** `POST /hub/calls/{id}/close` 에 싣는다(대본 원문을 보내지 않는다 — SEC-1).
⚠ 이 API 는 통화의 `ended_at`·`status` 를 바꾸지 않는다 — 서버에 그 경로가 아직 없다(2026-09-17, [미결](/open-items/)).

### B. 운영 (`server.solidbob.cloud` 콜 미디에이터 `0.1.6` 이상 배포 후)

1. 대시보드를 라이브 모드로 연다 — 빌드 설정 없이 런타임 오버라이드(허용 목록 통과 주소)로 붙는다:
   ```
   https://call.solidbob.cloud/?call_mediator=wss%3A%2F%2Fserver.solidbob.cloud%2Fcall-mediator%2Fws%3Ftoken%3D<뷰 토큰>
   ```
   한 번 열면 브라우저에 저장된다. 되돌리기: `?call_mediator=clear`. 뷰 토큰은 정성윤 님께(런북 19-1).
2. **「통화 시작」** 을 누른다
3. 이 맥에서 재생한다 — **글자 입력 토큰은 환경변수로만** 준다(명령줄·URL 에 싣지 않는다):
   ```bash
   cd services/call-mediator
   export CALL_MEDIATOR_INGEST_TOKEN=...      # 과금 문 토큰 — 진짜 비밀. 셸 기록에 남기지 않게 주의
   node scripts/replay_persona_call.ts SYN-004 --url wss://server.solidbob.cloud/call-mediator --speak
   ```

### 시연 순서 (QA-3 권고)

| 순서 | 대본 | 보이는 것 |
|---|---|---|
| 1 | **SYN-004** | 외국인 고객 · 상담원 C-1 위반 → 정정 · 인증번호·계좌·전화번호 마스킹 · 필요서류 |
| 2 | **SYN-003** | 13턴 — 대리 발급 서류 잘못 안내(C-3) → 필요서류 카드 → 정정 |
| 3 | **SYN-006 → SYN-007** | 콜 가드 → 통화 종료 → 「블랙리스트 전환 요청」(사람이 누른다) → 관리자 승인 → 같은 번호 재인입 베테랑 배정 |

SYN-001(정상 대조군)은 보여 줄 것이 없고, SYN-009 는 위기 발화 때문에 발표장에서는 틀지 않는다.
SYN-007 은 **SYN-006 의 요청이 승인돼 그 번호가 `active` 일 때만** 의미가 있다 — 재생기는 배정을 하지 않는다(J-5 는 서버 API 몫).

---

## 24건 구성

| ID | 턴 | 상담원/고객 | 무엇을 보나 |
|---|---|---|---|
| SYN-001 | 7 | 일반/중국 유학생 | 정상 대조군 — 아무 경고도 없어야 한다 |
| SYN-002 | 19 | 일반/베트남 결혼이민자 | 조건 확인 후 필요서류 · 한글 수사 전화번호(P4) · 베트남어 단어 한 개 |
| SYN-003 | 13 | 신입/직장인 | C-3 위반 → 정정 · 붙은 주민번호(P1) |
| SYN-004 | 19 | 신입/필리핀 부모 | C-1 위반 · P3·P4·P5·P6 · 쉬운 말로 바꿔 설명 |
| SYN-005 | 25 | 일반/자영업자 | 콜 가드 insult·threat → 진정 · **블랙리스트 요청 안 함**(경계 사례) |
| SYN-006 | 12 | 신입/반복 악성 | sexual·insult·threat → 1차·2차 안내 → 종료 → **블랙리스트 요청** |
| SYN-007 | 9 | **베테랑**/같은 민원인 | 같은 발신 번호 재인입 → **J-5 베테랑 배정** |
| SYN-008 | 16 | 일반/재문의 | **욕설 없이 톤만 튄다** — 텍스트 모듈은 못 잡는 게 정상(D-5 대조군) · C-1 |
| SYN-009 | 21 | 신입/고령 | 사칭 사기 · **distress(블랙리스트 사유 아님)** · C-2 → 정정 · P1·P2·P5 · 톤이 **가라앉는** 방향 |
| SYN-010 | 5 | 베테랑/청년 | 소관 아님 — 필요서류 없음 |
| SYN-011 | 26 | 베테랑/차분→폭발 두 번 | **D-5** — shouting 두 턴만 이상치, 콜 가드 0 |
| SYN-012 | 24 | 베테랑/내내 화남 | **D-5** — 튀는 구간 0 (기준선 자체가 높다) |
| SYN-013 | 26 | 베테랑/지친 보호자 | **D-5** — weary 두 턴, 톤이 **아래로** 튄다 · 대리 신청 |
| SYN-014 | 22 | 베테랑/차분 | **D-5** 대조군 — 가짜 이상치가 나오지 않는지 |
| SYN-015 | 9 | **무뚝뚝**/급한 직장인 | 초본 대리 발급 — 본인/대리 조건 먼저 · **태도는 라벨이 아니다**(매뉴얼에 태도 조항 없음) |
| SYN-016 | 15 | **단정형**/의심 많음 | 자동차세 연납 — 「무조건」·「확실히」 **C-1 두 번** → 정정 · 세 번 되묻는 고객 |
| SYN-017 | 15 | **외국어 응대**/일본 출신 | 미성년 자녀 도서관 가입 — 보호자 동의서·가족관계 서류 · **통역 안내 먼저** · 일본어 단어 |
| SYN-018 | 24 | 일반/옆길 고령 | 수도 명의변경 — 체납 확인 먼저 · 본론 되돌리기 · 톤 변화 0 |
| SYN-019 | 16 | **무뚝뚝**/조용히 비꼼 | 지방세 환급 — 계좌 명의자 다르면 위임장 · **콜 가드 0건이 정상**(비꼼 ≠ 욕설) |
| SYN-020 | 23 | 신입/가족 대리인 | 우대용 교통카드 대리 — **묻지 않은 P1·P2·P3** · C-3 위임장 면제 추측 → 정정 |
| SYN-021 | 9 | **단정형**/급한 직장인 | 대중교통 분실물 — 「틀림없이」 보관 단정 C-1 → 정정 |
| SYN-022 | 15 | **외국어 응대**/필리핀 부모 | 아동돌봄 지원 — 중복 지급 단정 안 함 · 사칭 문자 P5 · 통역 안내 |
| SYN-023 | 9 | 베테랑/조용히 비꼼 | 옥내 배관 — 소관 아님 · 업체 알선 안 함 · 필요서류 없음 |
| SYN-024 | 15 | 일반/의심 많음 | 취득세 생애최초 감면 — 「제가 알기로는」 C-3 → 정정 · 세 번 확인 |

라벨 합계(24건): PII P1 3 · P2 2 · P3 3 · P4 17 · P5 3 · P6 16 · P7 5 / 콜 가드(턴×갈래) insult 3 · threat 2 · sexual 2 · distress 1 /
위반 C-1 5 · C-2 1 · C-3 3. 골든셋 `v1-150` 에서 **표본 0건이던 P1·P2·P3·P5** 를 채운다.
09-18 추가분(SYN-015~024)은 폭언·성적 표현을 **새로 넣지 않았다**(수위 논의 미결) — 콜 가드 라벨은 09-16 것 그대로다.

## 라벨 규칙

- **라벨은 LLM 판단이 아니라 시나리오 명세다**(절대 원칙 1·9). 생성기가 라벨 문구가 발화에 **글자 그대로** 있는지 검사한다
- **콜 가드는 «턴 × 갈래» 로 센다** — 한 턴에 같은 갈래 문구가 둘이어도 1. 문구(span) 경계로 채점하지 않는다 — 규칙은 같은 협박을
  「찾아가」·「가만 안 둬」 둘로 잡기도 한다(QA-2)
- `customer_non_calm_turns` 는 톤이 calm 이 아닌 고객 턴 전부다. **의도한 연기이지 측정값이 아니다**
- **`procedure.required_documents` 는 규칙표(`server/apps/closure_gate/domain/value_objects/closure_rule.py`)의 기본 서류 이름을 그대로 쓴다**(2026-09-18, E2E 「서류 목록 일치」 판정 기준). 조건부 서류(대리·미성년자·세대주 아님 등)는 `conditional_documents` 에 따로 둔다 — 규칙표가 conditional 로 다루어 필수로 세지 않기 때문이다. 괄호 안 보충(「(외국인등록증 원본)」)은 판정에서 뺀다. 규칙표에 없는 조항(4.5·4.9·2.6 등)은 TERM.md 문구 그대로

## 이 대본으로 **말할 수 없는 것** (절대 원칙 2·10)

- **STT 정확도·A-5 WER/CER** — 글자를 직접 흘리므로 STT 를 거치지 않는다. 인식 오류가 0 이라 검색·마스킹 수치는 **상한**이다
- **트리거 지연 p95** — 발화 시각은 재생기가 지어낸 값이다(`persona_replay/plan.ts`)
- **D-5 통화 온도의 실제 성능** — 톤은 `say` 의 말 빠르기로만 흉내 낸다(목소리 크기는 못 바꾼다)
- **블랙리스트 승인 판단** — `admin_decision_scenario` 는 데모 시나리오다. 승인은 관리자 몫이다(`decisions/204`)
- 수치를 낼 때는 `source: synthetic` 으로 실제·작성 골든셋과 **섞지 않는다**

## QA 페르소나 검수 (2026-09-16)

LLM 을 **채점자가 아니라 테스트 데이터 검수자**로 썼다 — 시스템 성능 점수를 매기지 않았다(절대 원칙 1).

| 페르소나 | 관점 | 반영 |
|---|---|---|
| QA-1 상담 품질 감사관 | 매뉴얼·TERM 대조 | C-2 출처 1.4→1.2 · 위기 신호 뒤 연결을 먼저 · 지식베이스에 없는 안내 5곳(세대주 일반화·휴대폰 화면·위임장 작성자·고지서 기한·원본 안내 누락) · 성적 표현 직후 1차 안내 · 사전납부 감경 안내 |
| QA-2 개인정보·라벨 검수 | 라벨 누락·오분류, 현재 규칙을 대본에 직접 실행 | 라벨 누락 3(「몇 살이야」·「너 같은 게 무슨 상담을」·SYN-009 문구) · 실제 은행 형식과 같은 계좌번호 교체 · 연속 발화 수정 + 생성기 검사 추가 · 턴×갈래 집계 |
| QA-3 외국인 당사자·심사위원 | 희화화·쉬운 말·수위·시연 흐름 | 초급 한국어 일관성 · 영어 감탄사 축소·이름 교체 · 통역 안내를 앞으로 · 「년」 비하 표현 제거 · 위기 발화 수위 낮춤 · 연령 표기 제거 · C-2 정정 턴 |
| **2026-09-18 재검수(SYN-015~024)** QA-1 | 매뉴얼·TERM 대조 | SYN-023 옥내 누수 «계량기가 도는지 보는 점검법» 은 TERM-3.15 에 방법이 없어 **삭제**(확인 후 안내로) · 초본 대리(4.3) 「세대원도 위임 없이 불가」·명의변경(3.5) 「체납 정산 먼저」·환급(4.11) 「계좌 명의자 다르면 위임장」·우대카드(2.6) 「수수료는 발급 기관」 조항 그대로 · A04 무뚝뚝함은 매뉴얼에 태도 조항이 없어 **라벨을 만들지 않음** |
| 09-18 QA-2 개인정보·라벨 | 현재 규칙(`server/apps/masking`·`ai/apps/call_guard`·`ai/apps/compliance`)을 새 대본 10건에 직접 실행 | SYN-020 카드번호가 **Luhn 통과**해 `…0120`→`…0121` 로 교체 · 주민번호 체크섬 불통과 확인 · 콜 가드·컴플라이언스는 라벨과 전부 일치 · **규칙 쪽 결함 5건은 대본을 안 고치고 아래에 적음** |
| 09-18 QA-3 외국인 당사자·심사위원 | 희화화·수위 | 일본어 단어 4개·초급 한국어 일관(조사 누락·짧은 문장) · 고령(C17)·비꼼(C18)에 비하 표현 없음 · 폭언 미추가 |

**09-18 QA-2 가 찾은 «대본이 아니라 시스템» 문제 — 대본을 규칙에 맞추지 않았다** (서브에이전트 로컬 실행, 하네스 측정값 아님):
- **C-5 누락(SEC-1)** — SYN-020#16 「저는 강민재고요, 아버지는 강영식이에요」 **두 이름 다 원문 노출.** 09-17 규칙은 「~이고요」 만 알고 **모음으로 끝나는 이름 + 「고요」** 와 **「아버지는 ○○○이에요」**(가족 호칭 문맥)를 모른다
- **P4→P3 오분류** — 한글 수사 휴대전화 「공일공 공공공공 공일일육」 이 P3(계좌)로 잡힌다(SYN-016·018·021, 09-17 SYN-011~014 도 같다). 쉼표로 끊은 SYN-002 는 P4 로 맞게 잡힌다 — `sino_to_digits` 뒤 자릿수 규칙 순서 문제로 보임. 글자는 가려지므로 누락은 아니다
- **P6 과잉 3종** — 「고객님이요, 아이요?」(SYN-017#05) · 「다르면 위임장과 계좌 명의자」 세 어절(SYN-019#05, 「명의자」 앞을 이름으로) · 「위임장과 고객님」(SYN-020#19, 「고객님」 앞 어절을 이름으로) — 자막이 지워진다
- 외국인 두 어절 이름 「다나카 유이」 가 `*** *이` 로 남는다(끝 글자 「이」 를 조사로 본다) — 라벨은 P6 로 잡혀 누락은 아니지만 이름 조각이 남는다


**QA-2 가 찾은 «대본이 아니라 시스템» 문제는 고치지 않고 [미결](/open-items/)로 넘겼다** — 정상 대조군(SYN-001)이
현재 마스킹 규칙으로 **통과하지 못한다**(「신분증으로 하시면」 이 P7 주소로 잡힘). 대본을 규칙에 맞춰 바꾸지 않는다.

## 데이터 취급

- 개인정보는 **전부 실존하지 않는 가짜 값**이다 — 전화번호 `010-0000-XXXX`, 주민번호는 체크섬이 틀리고 뒷자리 `000000`,
  카드는 Luhn 불통과, 계좌 `000000…`, 주소는 가상 지자체 「한별시」
- AI Hub 원문을 넣지 않았다(재배포 금지). 근거는 팀이 쓴 가상 지식베이스 `knowledge-base/dasan/` 뿐이다
