---
date: 2026-09-21
author: "류준"
person: ryujun
seq: 7
---

- **남은 류준 작업을 다 돌렸다**(사용자 지시 — S3 업로드만 제외). 도중에 세션 한도로 에이전트 3개가 멈췄고, 남긴 것을 이어받아 끝냈다.
- **로컬 dense E2E** — `decisions/121` 판단용. SYN-011·021(09-18 의 ❌2, BM25 순위)은 **dense 만 켜도 ✅**(리랭커 불필요). 리랭커까지 켠 24건은 ✅23·❌1. **새로 깨진 SYN-023** 은 검사 DB 로 시각을 맞춰 원인을 확정했다 — 고객 두 번째 발화 「수도관에서 물이 새요」 직후 TERM-3.7(누수 감면)이 `incomplete` 로 판정됐다. 1순위 TERM-3.15(옥내 배관)에 규칙이 없어 09-18 에 내가 정한 「규칙 있는 첫 카드」 규칙이 건너뛴 탓이다(대본 검토 에이전트의 「상담원의 감면 언급이 켰다」 가설은 시각이 맞지 않아 기각). 라벨은 안 고쳤다. 제안(1순위가 규칙 없는 TERM 이면 멈춤)은 미결·전달 문서 §1-11. 보고서 `data/processed/persona-e2e/2026-09-21-{1105,1116,1434}.md`, 띄운 서버·콜 미디에이터는 내렸다.
- **합성 음성 음량** — mp3 를 재 보니 고함이 평온과 같은 크기였다. 같은 문장 변형 합성으로 **WaveNet ko-KR 이 SSML `volume` 올리기(+dB)를 무시**함을 확인(`+8dB` 만 준 것이 평온과 평균·최대 음량까지 동일, 내리기·속도·높이는 반영). `TONE_PROSODY` 를 기준 -8 dB 배치로 바꿔 평온 대비 격앙 +3.8 · 고함 +7.1 · 지침 -5.0 dB 확인. 테스트 3건 추가(콜 미디에이터 134 통과 · `tsc` 통과, `scripts/` 라 태그 불변). 톤 힌트는 「평온 대비 dB」로. 시연·수정 대본 7건 캐시를 새 음량으로 채움(이달 11,256/900,000자). 사람 귀로는 아직 안 들었다.
- **대본 24건 AI 검토** — `scripts/persona_sim/REVIEW-2026-09-21.md`. 부록 A 금지 표현 0 · 발화 3건 고침(SYN-006·013·024, 라벨 diff 0, 생성기 24건 통과) · AI Hub 71479 학습자 발화와 패턴 대조(원문 인용 0건 확인) — 과장 없음, 오히려 조사 «바꿔 쓰기» 오류가 빠져 있다. 사람이 볼 8개 항목 남김. 검토 에이전트는 MANUAL 5.2 가 「통화 종료 기준」(폭언·협박 시)이지 인사 절차가 아님을 바로잡았다.
- **Chirp 3 재측정 — 권한에 막혔다**(STT 0초). 스크립트에 `--stt-model`·`--region`·`--probe` 와 v2 호출을 붙이고(모델별 캐시·결과 파일 분리, 프로젝트 ID 는 서비스 계정 JSON 에서) 테스트 3건 추가(11 통과). `--probe` 가 `us`·`us-central1`·`asia-southeast1` 모두 `403 speech.recognizers.recognize` — 서비스 계정에 `roles/speech.client` 가 없다. IAM 은 정성윤 님 몫이라 요청만 남겼다. 캡은 이 실행에만 프로세스 환경변수 900초, `.env` 는 그대로(가드가 `.env` 캡 600 으로 막는 것도 확인).
- 남은 것(전부 남의 손 또는 사람 귀): IAM 역할 → Chirp 3 · S3 경로 → 업로드 · 절차 고르기 규칙 결정 → 수정·재검사 · 시연 전 음성 듣기 · 대본 사람 확인 8개.
