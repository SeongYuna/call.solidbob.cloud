---
date: 2026-09-22
author: "정성윤"
person: seongyun
seq: 5
---

- **운영 RDS 에 `db/migrations/2026-09-22-blacklist-request-note-unmeasured.sql` 을 넣었다**(`a866ff4`, 장민석 `decisions/316`).
  SSM → 서버 파드 → `DATABASE_URL` 경로(런북 17-2 방식). 결과 — `blacklist_request.temperature_outliers` NULL 허용 ·
  `decision_note` VARCHAR(500) 신설 · 주석 4건. 테이블 수 29 그대로. 넣기 전 `blacklist_request` 는 **0행**이라 소급 문제는 없다.
- **넣은 뒤 `scripts/compare_prod_schema.py` 로 컬럼 단위 대조 — 29 테이블 199 컬럼, 타입·길이·NULL 여부까지 어긋남 0.**
  (09-20 의 198 컬럼에서 `decision_note` 하나 늘었다.)
- ⚠ **순서가 또 뒤집혔다 — 09-14 와 같은 사고다.** PR #111 머지로 `release.yml` 이 **10:26 KST 에 server `0.1.25` 를 먼저 올렸고**
  마이그레이션은 **10:37 KST** 에 들어갔다. 약 11분 동안 블랙리스트 요청 생성·목록·결정이 500 이었을 것이고 `/health` 는 그동안 `ok` 였다
  (실제 요청이 있었는지는 확인 안 했다 — 행 0건). 런북 19장의 「이미지보다 먼저」는 이번에도 사람이 끼어들 틈이 없었다.
  `decisions/128`(릴리스 워크플로가 대조하고 멈춘다)이 **구현 전**이라 막지 못했다 — `w6-deploy-schema-precheck` 에 이 사례를 적었다.
- **첫 시도는 실패했다** — 노드 `/tmp/mig.sql` 에 **09-14 마이그레이션 파일이 ec2-user 소유로 남아 있어**(7,607바이트) root 의 curl 이
  덮어쓰지 못했고(`curl: (23)`), 남은 옛 파일과 sha256 을 비교해 「MISMATCH」로 멈췄다. 파일명을 `mig-2026-09-22.sql` 로 바꿔 넣었다.
  **sha256 대조가 있어서 옛 파일이 들어가지 않았다** — 대조 없이 넣었으면 09-14 파일이 다시 들어가 「이미 적용된 DB」로 멈추거나 헷갈렸을 것이다.
  노드의 옛 `/tmp/mig.sql` 은 지우지 않았다(tmpfs 라 재부팅이면 사라진다).
- 방법 메모: 클로드 코드 자동 모드가 운영 DB 쓰기(`aws ssm send-command`)를 막아 **마지막 적용 명령은 사용자가 `!` 로 직접 돌렸다.**
  읽기 전용 점검(연결·테이블 수·컬럼 목록)은 통과됐다.
- 남은 것: `0.1.25` 의 `/close`·카드 피드백 토큰 문(`315`)은 프론트 PR #112 가 같은 날 들어갔으니 **운영 상담원 화면이 새 번들인지** 확인
  (Vercel 자동배포 미확인 상태 그대로다). `w6-admin-agent-mapping`(장민석) 운영 확인은 관리자가 결정 한 번 눌러 보면 된다.
