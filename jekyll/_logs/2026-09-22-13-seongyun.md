---
date: 2026-09-22
author: "정성윤"
person: seongyun
seq: 13
---

- **장민석 님 운영 확인 요청 셋을 API 로 돌렸다 — 프론트 재배포 없이 됐다.** 운영 서버(`/openapi.json` 44 경로)에 `/hub/admin-stats`·`/hub/blacklist-requests/mine` 이 이미 있고, 화면 쪽은 운영 빌드에도 main 에도 둘을 부르는 코드가 아직 없어서(현황판 숫자·「내 요청」 화면 미구현) 배포 여부와 무관하게 API 로만 볼 수 있다. 승인·해제 화면은 PR #120 빌드에 들어 있어 운영 관리자 화면으로 된다.
- **② 요청 → 승인.** 상담원 「최효원」(`agent-5b3a88e63348`) 토큰으로 오늘 합성 통화 `syn-prod-syn-006-20260922-132934` 에 요청 생성 → **201, `request_id` 1**(운영 첫 요청). 근거 스냅샷 모욕 1·위협 2·성적 발언 3, 온도 이상 **null(미측정, `316` 대로)**. 관리자 화면에서 승인(06:04 UTC, `admin-1`, 180일) → `entry_id` 1 생성, 오류 없음 — **관리자 행 자동 생성(`314`) 이 SQL 없이 됐다.** 해제는 자동 모드 권한에 막혀 못 했다 — 등록 1 이 `expires_at` 2027-03-21 로 **살아 있다. 관리자 화면 등록 목록에서 해제할 것.**
- **③ 내 요청.** 생성 전 빈 목록 → 생성 뒤 `mine` 에 1건 `pending` 으로 보임(200). 승인 뒤 상태는 다시 안 봤다.
- **① 현황판 집계.** 200. `calls_total` 14 · `calls_closed` 0 · `call_guard_flags` 7 · `pending_requests` 0 · `active_entries` 1 · routing 셋 0. ⚠ **「오늘 통화 4건」은 이 API 로 읽을 수 없다** — 여덟 칸에 날짜 구분이 없고 14 는 09-11~15 테스트 통화 10건을 포함한 누적이다. 오늘 4건은 `GET /hub/calls` 목록으로 따로 셌다(13:19·13:19·13:01·13:29 KST, 전부 `syn-prod-`). 현황판에 「오늘」 칸이 필요하면 장민석 님과 정할 것.
- **C-5 과잉 마스킹 1건 발견.** 요청 사유 「운영 점검용 테스트 요청 (정성윤, 09-22) — …」 가 저장 뒤 「운영 점검용 테스트 ** ****, *****) — …」 가 됐다. 이름·날짜는 의도대로인데 **일반 단어 「요청」이 같이 가려졌다**(재현율 우선이라 실패는 아님). 류준·장민석 님께 전달 — 「~요청 (이름」 꼴이 이름 규칙에 걸린 것으로 보이나 확인 안 했다.
- 남은 것: 등록 1 해제 · 현황판 「오늘」 칸 여부 · 과잉 마스킹 재현 확인 · `mine` 의 승인 뒤 상태 표시 확인.
