---
date: 2026-09-23
author: "정성윤"
person: seongyun
seq: 12
---

- **미결 1번 묶음(확인만 하면 닫히는 것) 9건을 근거와 함께 닫았다.** 굵은 것 셋 —
  ① `statefulset.apps/elasticsearch configured` 는 **표시만** 그렇다(노드에서 `kubectl diff` 차이 0 · ES 파드는 09-08 생성 그대로)
  ② 운영 첫 검색 3,300ms 는 예열이 맞다 ③ 09-21 미커밋 결정 `125`~`128` 은 전부 회수돼 있다.
- **2번 묶음(코드) 착수 — 오늘 네 건을 끝냈다.**
  - **k8s 프로브를 정했다**: readiness 가 `/docs`(스웨거)였던 것을 `/health` 로 바꾸고 liveness 를 새로 달았다.
    **`/health/ready` 는 프로브에 안 쓴다** — DB·ES 를 찔러 503 을 내므로 ES 가 잠깐 죽으면 자막·마스킹까지 멈춘다.
    검색만 죽는 편이 낫다(`main.py` 의 같은 판단). 진단용으로는 그대로 쓴다. 태그 `0.1.41`.
  - **D-2 유형 이름표 두 벌을 하나로 맞췄다**: `ai/postcall_summary` 의 `CHAPTER_TO_TYPE` 값을 운영 유형표
    (`decisions/323`)와 **글자까지** 같게. 어느 장이 투표하는지는 그대로 뒀다(6장은 여전히 표 밖).
    운영 영향 0 — 모델 스포크가 안 꽂혀 있어 규칙 어댑터만 돈다. **모델을 켜는 순간 섞일 자리**를 미리 막았다.
  - **테스트·CI 위생**: 남은 skip 5건은 CI 에 모델·ES 가 없어서인 **설계된 skip** 임을 확인하고 닫았다.
  - **「머지 = 배포」 구조 문제**: `decisions/128` 의 배포 전 스키마 대조가 막고 있고 오늘 네 릴리스 전부 초록이었다.
- **결정 둘을 기록했다** — AI Hub 71479 원본 S3 경로는 `s3://assist-apne2/datasets/aihub-71479/validation/`(업로드는 원본을 가진 류준),
  DNS 공지 문구는 「DNS 는 클라우드플레어에서, 가비아에서 고치면 반영 안 됩니다」.
- 미결 111 → **96**. 남은 것 중 `apps/` 7건은 조서희 전담이라 손대지 않았고, 새 측정이 필요한 24건은 발표의 「측정 불가」로 간다.
