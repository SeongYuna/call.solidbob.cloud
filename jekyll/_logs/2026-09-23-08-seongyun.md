---
date: 2026-09-23
author: "정성윤"
person: seongyun
seq: 8
---

- **Q-44 고침을 운영에서 재 보니 안 걸려 있었다 — 한 번 더 고쳤다(`0.1.40`).** `0.1.39` 배포 뒤 운영 파드에서
  `POST /hub/search` 를 직접 쳤다(서비스 토큰은 파드 안에서만 읽었다).
- **문턱은 맞았다**: `ㅁㄴㅇㄹ` 0.540 · `ㅂㅈㄷㄱ` 0.527 · `ㅋㅋㅋㅋ` 0.532 / `전입신고` 0.792 ·
  「주민등록초본 떼려면 뭐가 필요해요」 0.835. **0.67 이 무의미와 진짜 질의를 정확히 가른다** —
  `decisions/135` 에 「아직 재지 않았다」고 적어 둔 것이 이걸로 답을 얻었다.
- **틀린 것은 게이트였다.** `wrap_no_answer` 가 층 목록이 `["retrieval_dense"]` 와 **정확히 같은지**로 봤는데
  운영은 캐시까지 켜서 `["retrieval_dense", "retrieval_cache"]` 다. 테스트를 `cache_size=0` 으로만 조립해서
  못 잡았다 — **운영 구성으로 조립하는 테스트가 없던 것**이 진짜 원인이다. 그 테스트를 더했다.
- 배운 것: 「조립이 맞는지」 보는 테스트는 **운영과 같은 옵션으로** 조립해야 한다. 기본값으로 조립하면
  운영에만 있는 층이 조건을 빗나가도 초록이 된다.
- 남은 것: `0.1.40` 배포 뒤 같은 질의로 다시 재서 `ㅁㄴㅇㄹ` 이 0건이 되는지 확인 → Q-44 재시험 ✅.
