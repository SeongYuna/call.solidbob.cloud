---
date: 2026-09-15
author: "정성윤"
person: seongyun
seq: 1
---

- **5~8주차 칸반 티켓을 전부 만들었다 — 27건.** 마지막 주차까지 보드가 비어 있지 않게 채웠다.
  이전에는 **w5 1건 · w6 1건 · w7 4건 · w8 0건**이었다 — 남은 절반의 일정이 칸반에 없었다.
  뽑은 곳은 셋뿐이다: [8주 마일스톤](/docs/08/) 로드맵 · `STATE.md` 「지금 막혀 있는 것」 ·
  [미결 항목](/open-items/). **새로 지어낸 일은 없다.**
- **4주차 로드맵 목표에 티켓이 0건이었다 — 이게 이번에 찾은 것 중 제일 크다.**
  4주차는 「nori · dense_vector · RRF 병합 · 청킹 3종 비교」인데 `w4-` 티켓 27건 중
  **해당하는 것이 하나도 없었다.** 4주차에 실제로 한 일(게이트웨이·블랙리스트·관리자 로그인·배포·
  스키마 따라잡기)은 전부 로드맵에 없던 일이고, 로드맵에 적힌 일에는 티켓이 없었다.
  **양쪽 다 정상 작동했는데 서로를 안 보고 있었다** — 칸반은 실제 진행을, 로드맵은 계획을 정확히 적고 있었다.
  네 건(`w4-dense-vector-index`·`w4-rrf-hybrid`·`w4-reranker`·`w4-chunking-compare`)을 만들었고,
  **주차는 로드맵 그대로 뒀다** — 미룰지는 팀이 정할 일이라 미결에 올렸다.
- **주차별로 만든 것** — w4 5건 · w5 5건 · w6 6건 · w7 6건 · w8 5건.
  - **w5 가 핵심 주차다**(성공 조건의 절반). [C-5 NER](/backlog/w5-ner-p6-p7/)을 **곡선보다 앞에 뒀다** —
    지금 오류율 0% 에서 이미 **누락 2건(P6 인명)** 이라, 그 위에 곡선을 얹으면 「오류 때문에 뚫렸다」와
    「원래 뚫려 있었다」가 섞인다. `depends_on` 으로 순서를 박았다.
  - **w6 에 [코어 기준선 판정](/backlog/w6-core-baseline-check/)을 세웠다.** F·G·H·I 동결은
    **6주차 «종료 시점»** 조건인데 그 판정을 «누가 언제 하는가» 가 어디에도 없었다.
    2026-08-27 에 이 문장에서 시점이 빠져 확장 논의가 6건 멈춘 적이 있다(`decisions/022`) — 그 자리를 티켓으로 고정했다.
  - **w7 [F-2 체크포인트](/backlog/w7-f2-checkpoint/)의 무게가 옮겨 갔다.** 「만들까」가 아니라
    **「잴 수 있는가」**다 — 스포크는 09-14 에 다산 규칙표로 다시 만들어져 있고(`decisions/305`),
    막힌 것은 **골든셋 F-2 케이스 0건**과 `verdict` `blocked`→`incomplete` 미반영이다.
- **담당자는 «제안»이라고 명시했다.** 역할표·주 담당 디렉터리를 따라 적었지 본인이 가져간 게 아니다.
  §4 가 *"`assignee` 는 실제로 손대는 사람"*이라고 정하므로 착수할 때 본인이 고치는 것을 전제로 뒀다.
  ⚠ **류준 16건으로 쏠렸다** — `ai/` 가 4~6주차 로드맵의 대부분이라 그렇게 됐다.
  `decisions/302` 로 잠금이 풀렸으니 나눌 수 있다는 것까지 미결에 적었다.
- **슬러그 접두어 충돌을 피해 이름을 잡았다.** `check_session_end.py` 의 중복 검사는
  **접두어 일치**(`na.startswith(nb)`)라 `w7-postcall-summary` 는 기존 `w2-postcall-summary-g2` 에 걸린다 —
  `w7-postcall-spoke` 로 바꿨다. `w6-baseline-*` 도 `w2-baseline` 에 걸려 `w6-core-baseline-check` 로 갔다.
  **일부러 나눈 단계(w5 곡선 · w8 F-2)는 `depends_on` 을 블록 형식으로 적어** 경고에서 뺐다.
- 확인: `check_session_end.py` **새 중복 경고 0건**(남은 1건은 조서희 님의 기존 쌍) ·
  `jekyll build` 통과 · `check_site_links.py` **184 페이지 / 깨진 내부 링크 0**.
- **09-14 의 미푸시 커밋 2건을 함께 푸시하고 PR 을 올렸다** — `5c7175a`(A-6 키를 `.env.example` 에 ·
  `.env` 사본 gitignore) · `2bef122`(`CLAUDE.md` §3 워크플로 목록 · `decisions/111` 교차 경고의 한계).
- 남은 것:
  - **담당자 확인** — 27건의 `assignee` 는 본인들이 고쳐야 한다. 특히 류준 쏠림
  - **4주차를 로드맵대로 갈지 미룰지** — 지금 4주차 검색 품질 티켓 4건이 `todo` 인 채로 4주차가 끝나 간다
  - 룰셋에 `tag-check`·`gateway` 추가(콘솔, admin 몫)는 09-14 부터 그대로 열려 있다
