---
date: 2026-09-22
author: "장민석"
person: minseok
seq: 2
---

- **백엔드 남은 티켓 여섯 중 다섯을 진행했다(`server` 브랜치, 커밋·배포 전).** 01번 기록의 이어짐이다.
- **결정 셋** — `314` 관리자마다 전용 `agent` 행(`admin-<id>`)을 붙여 블랙리스트 결정을 기록한다(409 해소) · `315` `/close` 는 상담원 토큰 또는 서비스 토큰, 카드 피드백은 상담원 토큰을 확인하되 신원을 버린다, **상담원 토큰은 PC 에 남긴다**(사용자: 「관리자가 너무 번거롭다 — PC 한 대당 한 번」) · `316` 블랙리스트 요청의 D-5 온도 이상을 0 대신 **null(미측정)**, 반려 사유 필수(`decision_note`), `display_hint` 는 채우지 않는다(전화번호 뒷자리도 P4).
- **D-5 추정 확인** — 「늘 0」이 맞았다. `voice_outlier` 에 쓰는 곳이 서버 요청 경로에 없다(`ai/` 평가 쪽뿐). 배선은 오디오를 가진 콜 미디에이터 몫이고 음성 골든셋이 없어 붙여도 측정 불가라, 이번엔 「0 으로 보이지 않게」까지만 했다. 지금까지 저장된 0 은 소급해 바꾸지 않았다.
- **스키마** — `db/migrations/2026-09-22-blacklist-request-note-unmeasured.sql`(순증, 29 테이블 그대로). **옛 `schema.sql` 에 적용 → 새 `schema.sql` 과 199컬럼 타입·NULL·주석까지 일치, 두 번 넣으면 멈춤**을 도커 `postgres:17` 로 확인했다. ⚠ `schema.sql` 을 처음엔 손으로 고쳤는데 **`db/generate_schema_docs.py` 가 생성하는 파일**이었다 — 생성기를 고쳐 다시 생성했고 주석 한 줄 순서 말고는 같다. `ERD.png` 는 graphviz 가 없어 다시 못 그렸다(`erd.dot` 는 갱신). 런북 19장에 「이미지보다 먼저 + 프론트보다 늦게」를 적었다.
- **남의 변경 사후 검토**(`w5-server-change-review` done) — 고친 것 `resolve_or_create` 동시 발급 PK 위반 500(`ON CONFLICT` + 재조회, 관리자 행 ID 면 404). 나머지는 봤고 그대로 둔다 — 상세는 티켓.
- **검증** — server **1,282 통과**(+integration **27**, 새로 띄운 DB) · ai 440 · 계약 server 4 · ai 3 KEPT · 콜 미디에이터 `tsc --noEmit` 통과(재생기 `--close` 가 `CORE_API_TOKEN` 을 싣는다). ⚠ 중간에 zsh 가 변수 속 명령을 안 쪼개 **빈 결과끼리 비교해 「일치」가 찍힌 가짜 초록**이 한 번 있었다 — 스크립트로 다시 돌려 실제 값으로 확인했다.
- 티켓: `w5-server-change-review`·`w7-blacklist-leftovers` done · `w6-admin-agent-mapping`(운영 확인 남음)·`w6-open-write-guards`·`w7-d5-server-wiring`(프론트 남음) in-progress · `w8-f2-wrapup` 은 손대지 않았다(남은 일이 발표 문구라 코드 작업이 없다).
- 남은 것: **조서희 님 넷**(토큰 `localStorage` · `closeCall`·`submitCardFeedback` 토큰 · 반려 사유 입력 · `null`=미측정) — 이것 뒤에 머지. 서버 태그(`0.1.25`)는 PR 때 올린다(태그 = 배포). 운영은 마이그레이션 → 이미지 순서(정성윤 님). 제안만 한 것: 쓰기 일곱 경로 ingress 외부 차단(미결).
