---
date: 2026-09-11
author: "정성윤"
person: seongyun
seq: 19
---

- **운영 ES 에 지식베이스를 적재했다 — 운영 검색·추천이 503 에서 200 이 됐다.** 서버 이미지에 `knowledge-base/`·`scripts/` 가
  없어서, GitHub `main` tar 를 서버 파드 안에서 풀고 이미지의 `ai/apps` 와 파드의 `ELASTICSEARCH_URL` 로 적재하는 절차를
  설계했다. 사용자가 SSM 세션에서 실행했다. 절차는 런북 15-1 에 넣었다.
  - 사전 로컬 검증: 실제 GitHub tar 에서 두 가지만 꺼내기 · 이미지와 같은 배치에서 스크립트 import·경로 · 청크 98개.
    ES 쓰기는 이 머신에 ES 가 없어 운영에서 처음 돌았다.
  - 운영 출력: `analysis-nori 9.5.1` · `청크 98개 (조항 98개)` · `생성된 인덱스: callguard-kb-single` · `98건` ·
    `_cat/indices` `green … docs.count 98`. ES 는 StatefulSet + 볼륨이라 재시작·자동 중지 뒤에도 남는다.
  - 밖에서 확인: `POST /hub/search` **200**. 「주민등록등본 발급 대리 신청 서류」 → `DASAN-TERM-4.1 증명서 발급` ·
    `4.3 주민등록초본` 순. `POST /hub/recommendations` **200** — `fired "true"`, 카드에 `source.doc_id` 가 붙는다.
    통화 `test-deploy-202609111813-kb` 가 남았다.
- **정직하게 — 적재가 됐다는 것이지 검색 품질이 좋다는 뜻은 아니다.** 「여권 재발급 서류가 뭐예요」 1위가
  `DASAN-MANUAL-4.1`(의학적 판단 금지)이었고, 추천 1위 카드는 `3.14 수도 폐전`이었다. 원인은 검색이 아니라
  **지식베이스에 여권 조항이 없는 것**이다(98조항 중 「여권」은 신분 확인 서류 목록에만 나온다). `decisions/201` 이
  다산을 고른 근거가 「서류 문의 상위 단어 `여권` 4위」라 이 공백은 크다 → [미결](/open-items/)에 올렸다.
- 이번에도 세션 중 `K` 변수가 비어 `$K exec …` 가 bash 내장 `exec` 로 바뀌었다(아무것도 실행되지 않았다).
  빈 변수에 기대지 않게 적재 절차는 `sudo k3s kubectl -n callguard …` 전체 명령으로 적었다.
- 남은 것: 여권 조항 공백 결정 · 검색 품질(4주차 nori 사전·dense·RRF — 평가 하네스로 잰다) · 게이트웨이 배포(게이트웨이 세션).
  이 기록·런북 15-1·미결·18 기록·`w4-rds-prod-db` done 은 아직 커밋하지 않았다.
