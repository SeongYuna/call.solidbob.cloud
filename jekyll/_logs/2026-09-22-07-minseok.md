---
date: 2026-09-22
author: "장민석"
person: minseok
seq: 7
---

- **통합 수동 QA 를 프로젝트 게이트로 세웠다 — [w5-manual-qa-full-stack](/backlog/w5-manual-qa-full-stack/) · `decisions/319`**
  - 5주차가 끝나 가는데 서버·AI·콜 미디에이터·상담원 화면·관리자 화면을 붙여 놓고 **사람이 화면을 눌러 본 적이 없다.**
  - 그동안의 검사(`e2e_check.py` · 서버 pytest · `/health`)는 전부 API 왕복이라 **화면에 무엇이 보이는지는 보지 않는다.**
  - 그래서 세 영역 모두 다음 작업의 전제를 확인하지 못한 채 멈춰 있다.
  - 이 확인은 개인 요청이 아니라 **프로젝트 진행 조건**으로 두었다.
  - 수행은 배포·토큰·주소를 쥐고 있는 정성윤 님이 맡는다.
- **게이트를 걸었다**
  - 통과 전에는 정성윤 님 티켓 넷(`w7-latency-budget` · `w6-deploy-schema-precheck` · `w7-demo-infra-readiness` · `w7-infra-loose-ends`)과 `w8-demo-rehearsal` 을 시작하지 않는다.
  - 다섯 티켓 모두 `depends_on: w5-manual-qa-full-stack` 을 달았다.
  - `_project/STATE.md` 「현재」에도 같은 내용을 올렸다.
- **체크리스트 `Q-00a`~`Q-69`**: 운영 주소에서 직접 누른다.
  - 준비 → 관리자 로그인·토큰 발급 → 상담원 로그인
  - 손으로 넣는 발화 15문장: P1~P7 · 필요서류 · 콜가드 · 컴플라이언스 · 과탐지 대조
  - 통화 중 버튼 → 통화 후 처리 → 관리자 처리 → SEC-1 원문 잔존 확인
  - 알려진 mock 목록도 적어 두었다. 버그로 넘기지 않는다.
- **규칙**
  - 직접 누른다. Claude 나 스크립트는 판정을 대신하지 않는다 — 이 게이트가 생긴 이유가 지금까지의 검사가 전부 자동 API 왕복이었다는 것이다.
  - 찾기만 하고 고치지 않는다. ❌ 는 영역 담당에게 넘긴다.
  - 하루 단위로 `_logs/` 에 끊어 남긴다.
- **체크리스트를 만들며 본 것**
  - 체크리스트는 `apps/call`·`apps/admin` 코드에서 버튼 라벨과 호출 API 를 읽어 만들었다.
  - 상담원 마이크 박스는 토큰 없음·미지원·오류 때 **안내 없이 모달만 닫힌다**(`useAgentCallSession.ts` 의 `errorMessage` 를 그리는 곳이 없다) → `Q-48`
  - 운영 `call.solidbob.cloud` 가 실서버 번들인지 mock 번들인지는 STATE 와 미결 항목이 서로 다르게 적고 있다 → `Q-00d` 에서 가장 먼저 확인한다
- 남은 것: 정성윤 님 착수. ❌ 중 `server/` 쪽은 내가 받는다.
