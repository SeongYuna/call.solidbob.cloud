---
date: 2026-09-21
author: "정성윤"
person: seongyun
seq: 6
---

- **결정 넷을 기록했다** — 사용자(PM)에게 선택지를 묻고 고른 대로 썼다.
  - `decisions/129` — 8스프린트는 10-14 에 끝나고 10-15 ~ 10-27 은 새 기능 없는 「마감 기간」. ⚠ 팀에 묻지 않고 PM 이 기본값으로 정했다(사용자 지시 「그냥 진행해」) · 마일스톤·`STATE.md` 「현재」 반영 · `w5-schedule-gap-decision` done
  - `decisions/126` — 배정 판정은 콜 미디에이터가 통화 시작 직후 부른다 · 통화 종료는 화면 버튼 그대로. 구현은 열쇠 꽂기 뒤. 구현하며 정할 것(후보 상담사 목록 · 발신 번호 없는 테스트 콜은 늘 「블랙리스트 아님」)을 함께 적었다
  - `decisions/127` — 공개 데모는 라이브로 둔다(09-17 부터의 상태를 기록). 확인: 번들 뷰 토큰은 마스킹 자막 구독만 열고, 발화 전송 토큰(`call_token` = INGEST)은 URL 로만 받으며 그 경로는 Google STT 를 안 쓴다 → `w7-call-ui-loose-ends` 의 「STT 한도를 태운다」 걱정은 해당 없음
  - `decisions/128` — 배포 전 운영 스키마 대조는 `release.yml` 이 SSM 으로 하고 어긋나면 멈춘다. 구현 전
  - **`124` 는 비워 두었다** — 「모델을 운영 노드에 올린다」 계획을 쓴 다른 세션이 그 번호를 쓰기로 했다
- **토대를 깔았다** (운영은 읽기만 했다 — 바꾼 것 없음)
  - 09-30 판정문 초안 `_project/baseline-check-2026-09-30-draft.md` — 기준 · 지금 값과 네 조건 · 측정 불가 · 한계 넷 · 판정일 명령. **가장 약한 것은 E(`run_id` 없음)**
  - `w5-ingest-auth-fail-closed` 에 **열쇠 꽂기 실행 절차** — 백업 → 토큰 생성 → 미디에이터 먼저 → 서버 → 확인 → 되돌리기. 이름은 실물로 맞췄다. **명령은 실행하지 않았다**(사용자가 직접 한다)
  - `w7-latency-budget` — `e2e_check.py` 는 REST·DB 만 읽어 `retrieval_ms`·`e2e_latency_ms` 를 못 받는다 → 재생기 `--watch` 에서 방송 값을 모으는 순서
  - `w7-infra-loose-ends` · `w4-aws-resource-hygiene` — 확인 사실: 서버 준비 프로브는 `/docs`(생존 프로브 없음) · S3 수명 주기 규칙은 `datasets/` 하나 · RDS 백업 7일 · 퍼블릭 꺼짐 · Enhanced Monitoring 꺼짐 · 권장 2건은 둘 다 informational(`enhanced_monitoring_off`·`multi_az_instance`) · 7월 잔재 VPC 는 인터페이스 0. **지우는 명령은 적기만 하고 실행하지 않았다**
- **정정** — `w7-infra-loose-ends` 의 「S3 `datasets/` 에 음성이 있다」는 틀렸다. 버킷 최상위는 `uploads/` 하나뿐이다. 운영 검색 인덱스에는 벡터가 없어 **임베딩을 켜기 전에 벡터 재적재가 필요하다**(`scripts/index_knowledge_base.py` 24~30줄) — 모델 계획이 「미확인」으로 남긴 부분이다
- 미결 여섯을 닫거나 ◐ 로 표시(공개 데모 · 스키마 · J-5 · TTS 키 · 일정 · `CORE_API_TOKEN`). TTS 키와 AI Hub 신청은 **사용자 확인**이고 저장소 기록은 없었다
- 남은 것: 모델 계획의 리랭커 포함 여부(A/B) 미정 · 결정 기록 `124` 와 GPU 티켓 셋 취소는 그 계획을 쓴 세션 몫 · STATE 「막혀 있는 것」의 AI Hub 행은 류준 님 행이라 그대로 · 커밋 안 함
- 번호 정정(09-22 main 동기화 때): 일정 결정은 처음 `125` 로 썼는데, 다른 세션이 같은 날 `125`(118 되돌림)를 main 에 넣어 **`129` 로 옮겼다.** 이 기록의 파일 이름도 같은 이유로 `05` → `06` 이다(원격의 `05` 는 다른 작업).
- **09-22 main 동기화** — 로컬 PM 을 `origin/main`(`acbb32e`)으로 빨리 감고 이 세션의 미커밋 변경을 다시 얹었다. 충돌 둘은 **main 쪽을 살렸다**: `w5-ingest-auth-fail-closed`(다른 세션이 09-22 에 실제로 잠갔다 — 이 세션의 「실행 절차」는 커밋된 적 없고 「아직 실행 안 함」이 틀린 말이 돼 뺐다) · 미결의 TTS 키 항목(main 이 키 수령·합성 확인까지 적었다). 새 결정 `124`(B안 — NER·임베딩만 CPU)·`125`(118 되돌림, F-2 구현 완결)와 골든셋 279건(`f2_case` 99)에 맞춰 판정문 초안과 `w6-core-baseline-check` 덧붙임을 고쳤다. **결정 126~129 와 판정문 초안은 여전히 커밋 전이다** — 09-22 체크리스트 3번이 「어느 이력에도 없다」고 한 것이 이 파일들이다(이 머신에 있다)
