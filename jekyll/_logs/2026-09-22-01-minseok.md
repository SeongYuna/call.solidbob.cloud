---
date: 2026-09-22
author: "장민석"
person: minseok
seq: 1
---

- **main 을 `server` 로 당겨왔다**(fast-forward, `3e95514` → `acbb32e`, 25커밋). 푸시는 안 했다.
- **`w6-admin-agent-mapping` — 관리자 블랙리스트 승인·해제 409 를 푸는 코드를 넣었다(커밋 전).** 선택지 ①(관리자마다 `agent` 행)을 사용자가 골랐다. 처음 결정할 때 `admin-<admin_account.id>`·`role='admin'` 행을 만들고 `admin_account.agent_id` 가 비어 있을 때만 채운다(`COALESCE` — 덮어쓰지 않는다). `406` 의 `resolve_or_create` 를 쓰지 않은 이유: 이름으로도 찾아서, 관리자와 이름이 같은 상담원이 있으면 그 상담원 이름으로 결정이 기록된다. 그래서 `AgentDirectoryPort.ensure_admin`(ID 로만 찾는다)을 따로 두고, 상담원 목록·이름 찾기는 `role='agent'` 만 보게 했다. 스키마 변경 없음(`db/schema.sql` 은 주석 한 줄). 유닛 76 통과 · 새로 띄운 `postgres:17` 에 현재 `schema.sql` 을 넣고 `agent_auth` integration 5 통과. **결정 기록 `314` 는 아직 안 썼다.**
- **`w6-open-write-guards` — `/close` 와 카드 피드백에 문을 달았다(커밋 전).** `/close` 는 상담원 토큰 **또는** 서비스 토큰(재생기 `--close` 용, 재생기가 `CORE_API_TOKEN` 을 싣게 고쳤다). 카드 피드백은 상담원 토큰을 확인하되 `dependencies=[...]` 로 걸어 agent_id 가 라우트 함수에 들어오지 않는다(부록 A-1, 사용자 선택). hub adapter + 루트 테스트 전부 통과. ⚠ **프론트(`apps/call` `closeCall`·`submitCardFeedback`)가 아직 토큰을 안 싣는다** — 조서희 님 쪽이 먼저 바뀌어야 머지할 수 있다(사용자 결정: 서버만 하고 넘긴다). **결정 기록 `315` 는 아직 안 썼다.**
- **`w5-server-change-review` — 0.1.14→0.1.24 리뷰를 에이전트로 돌렸다.** 확인한 것: `resolve_or_create` 동시 호출 시 PK 위반 500(실제 결함, 아직 안 고침) · 동명이인이 같은 `agent_id` 로 합쳐진다(406 설계, 미결로 올릴 것) · `retrieval_ms`·`generation_ms` 가 「안 쓰인다」는 지적은 **오판**(응답 스키마로 미디에이터에 나간다) · 저장 실패 정책이 콜 가드·컴플라이언스(200+로그)와 추천·필요서류(500)로 다르다 — 의도인지 문서가 없다.
- 사용자가 「토큰 방식보다 나은 방법」을 물어 셋을 제안했다: ① 쓰기 일곱 경로를 ingress 에서 외부 차단(미디에이터는 클러스터 안 `http://callguard-server` 로 부른다) ② 상담원 토큰을 URL 쿼리 대신 1회 교환 → `HttpOnly` 세션 쿠키 ③ 통화 단위 권한 토큰. 답을 기다리는 중.
- 남은 것: 결정 `314`·`315` · `resolve_or_create` 충돌 수정 · `w7-d5-server-wiring`(「미측정」 — `temperature_outliers` NULL 허용 마이그레이션) · `w7-blacklist-leftovers` · `w8-f2-wrapup` · 미결·STATE 갱신 · 전체 pytest·lint-imports. 로컬 integration 용 컨테이너 `cg-it-pg`(포트 55432)가 떠 있다.
