---
date: 2026-09-15
author: "정성윤"
person: seongyun
seq: 20
---

- **여러 갈래로 병행한 09-15 작업을 한 브랜치로 모아 푸시했다.** 공유 작업 트리에 쌓여 있던
  변경 17개 파일 + 진행 기록 9건을 네 커밋으로 가르고, 이미 커밋을 끝낸 갈래(프로브 삭제)를 그 위에 얹었다.
  - `chore(ci)` 보호 설정 사본 둘 · `code(ci)` 결정 114(CI 결함 다섯) · `docs(sync)` 문서 동기화 ·
    `log(w4)` 기록 9건 · 머지.
- **⚠ 프로브가 되살아날 뻔한 자리를 확인했다.** 삭제 커밋(`ef7360c`)은 **다른 작업 트리**에서
  나갔고, 이쪽 트리에는 `auth_router.py` 의 `probe`·`PROBE_MARKER` · `AuthProbeResponse` ·
  `tests/test_auth_probe_router.py` 가 **그대로 남아 있었다.** 손대지 않은 상태였기에 머지가
  조용히 지워 갔지만, 이쪽에서 그 파일들을 한 줄이라도 고쳤다면 되살아났을 것이다.
  머지 뒤 `grep`·`ls` 로 넷이 다 없어진 것을 확인했다.
- **충돌 둘을 풀었다.**
  - `kustomization.yaml` — server `0.1.12`(이쪽) vs `0.1.13`(프로브 삭제). **`0.1.13`.**
    둘 다 아직 레지스트리에 없고 머지된 소스 한 벌로 한 이미지를 굽는다 — 높은 쪽 하나면 된다.
    저쪽이 `0.1.12` 를 「진행 중인 다른 작업」으로 비켜 준 자리라 §4 대로 먼저 손댄 쪽을 살렸다.
  - `STATE.md` 상태 표 — 네 줄을 각자 최신으로. `server/`·`ai/`·`apps/` 는 main, **인프라 줄만 이쪽**
    (main 쪽은 「필수 검사 아직 셋뿐」인 옛 서술이다).
- **갈래 사이에서 어긋난 것 하나를 고쳤다** — `.claude/rules/rfp-harness.md` 가 CI 트리거 축소를
  `decisions/112`(admin 서브도메인·Redis)로 인용하고 있었다. 실제 근거는 **`114`** 이고
  `CLAUDE.md`·`test.yml` 주석은 둘 다 `114` 로 맞다. 옮겨 적다 어긋난 쪽을 고쳤다(§1 파생 문서).
- 검증: server 783 passed · ai 392 passed · 계약 4·3종 KEPT · gateway typecheck·테스트 fail 0 ·
  지킬 197페이지 깨진 링크 0 · 태그 게이트 통과(server `0.1.13` · gateway `0.1.5` 둘 다 레지스트리에 없음).
- 남은 것: **PM → main PR 은 아직 안 열었다**(사용자 판단). 지금 `origin/PM` 이 main 보다 앞서 있고,
  머지하면 `0.1.13`(프로브 없는 이미지 + `.dockerignore` 정리)과 `0.1.5` 가 함께 구워진다.
