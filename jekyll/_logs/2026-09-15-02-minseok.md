---
date: 2026-09-15
author: "장민석"
person: minseok
seq: 2
---

- **`server` 브랜치를 푸시했다** — PR #86(정성윤 님이 연 것, 본문 없음)에 커밋 셋이 올라갔다. CI `server`·`ai`·`gateway`·`jekyll` 통과,
  **`tag-check` 실패** — 「server 코드가 바뀌었는데 `newTag` 가 `0.1.7` 그대로」. 운영 DB 에 `agent_token` 마이그레이션을 먼저 넣으려고 **일부러 안 올린 것**이다.
  `tag-check` 는 필수 검사가 아니라 PR 은 머지 가능 상태로 뜬다 — 마이그레이션 전에 머지되지 않게 해야 한다.
  ⚠ 푸시 때 GitHub 가 저장소가 `SeongYuna/call.solidbob.cloud` 로 옮겨졌다고 알렸다(옛 주소 리다이렉트로 들어감). 로컬 `origin` 은 아직 옛 주소다.
- **§7.3 계약 정본(`_project/plan.md`)을 코드와 다시 맞췄다.** 전사·카드 예시가 옛 값(`documents`·`score`·`source.clause`·`e2e_latency_ms`, `segment_id` 없음)이라
  실제 응답 모양으로 바꾸고(`fired`·`similarity_score`·문자열 값), **「응답은 전부 문자열」 규칙을 절 머리에 올렸다** — 전에는 일부 예시 주석에만 있었다.
  허브 HTTP 표면에 통화 후 초안 · 카드 피드백 · 상담원 토큰 관리 · 블랙리스트 요청의 `Authorization` 헤더를 추가했다.
  게이트웨이 WS `payload` 가 서버 응답 그대로라는 서술은 `services/gateway/src/app/call_registry.ts` 로 확인하고 적었다.
- **「전부 문자열」 에서 빠진 응답이 하나 있었다** — `POST /hub/cards/{id}/feedback` 의 `feedback_id`·`card_id` 가 정수였다.
  `/openapi.json` 의 2xx 응답 모델을 전부 훑어 **정수·불리언·실수가 남은 곳이 이 두 필드뿐**임을 확인하고 `StrField` 로 바꿨다. `server` 696 passed.
- **공개 사이트 7.3절(`docs/07`)은 2026-08-25 v2 기록 그대로라** 프론트가 옛 `closure_type`·`approved/blocked` 를 기준으로 삼을 수 있다.
  본문은 기록이라 고치지 않고 절 머리에 «지금 코드와 다르다 — 정본은 `plan.md` 7.3 · `/openapi.json`» 경고만 달았다. 지킬 빌드 · 깨진 링크 0.
- 관찰: 카드 피드백은 **상담원 ID 를 일부러 받지 않는다**(부록 A-1 — 상담원 단위 집계 금지). 상담원 토큰을 어디까지 걸지 정할 때 이 API 는 빼야 한다 — [미결](/open-items/)에 붙였다.
- 남은 것: 프론트 `apps/call/src/types/contract.ts` 는 여전히 `is_final: boolean` 등 원래 타입이다(조서희) · PR #86 머지 순서(마이그레이션 → 태그 → 머지) · 통화 후 초안 저장
