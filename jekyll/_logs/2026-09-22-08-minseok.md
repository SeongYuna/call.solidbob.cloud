---
date: 2026-09-22
author: "장민석"
person: minseok
seq: 8
---

- **`w6-ingest-guard-fail-closed` — 쓰기 경로 문을 「없으면 닫힌다」로** (`decisions/120` 「전환 순서」 4번). 정성윤 님이 09-22 오전에 1~3단계를 운영에서 끝내(`ingest_guard: locked`) 남은 서버 몫이다.
  - `require_ingest_service` 의 「미설정이면 통과」 한 줄을 걷었다 — 미설정이면 쓰기 여덟 경로(`routing-decisions` 포함)가 **전부 401**. `/health` 의 `ingest_guard` 는 미설정이면 `open` 대신 **`unset`** 을 낸다(「열림」은 이제 거짓이라서)
  - **401 이지 기동 거부가 아니다.** 티켓은 둘 다 허용했고 런북 12-2-b 끝줄은 「서버가 안 뜬다」로 적혀 있었다. 업로드 문(`110`)·`/close`(`315`)와 같은 모양이 되고, 키 하나 때문에 읽기 경로·관리자 화면·로컬 `uvicorn`·CI 까지 죽이지 않는다. 런북 그 줄을 이 판단으로 고쳤다
  - 테스트: 「미설정이면 연다」 → 「미설정이면 401(헤더를 보내도)」 · `/health` `unset`/`locked`. **라우터 테스트 11개 파일이 「열림」에 기대 헤더 없이 쓰기 경로를 부르고 있었다** — 전부 401 로 깨져서 `hub/tests/adapter/_ingest_auth.py`(토큰 상수) + 같은 폴더 `conftest.py`(환경변수) 로 헤더를 붙였다
  - 검증: server **1,301 passed** · import 계약 4종 KEPT · ai 454 passed
  - 주석·문서: `config.py`·`ingest_guard.py`·`main.py` 이행기 주석 정리 · `.env.example`·`secret.example.yaml`(비우면 닫힌다, 로컬은 두 값 다 채운다) · 런북 12-2-b·9번 · `decisions/120` 상태 줄
  - server 태그 `0.1.32` → **`0.1.33`**
- ⚠ **로컬에서 미디에이터를 서버에 붙이는 사람은 이제 `INGEST_SERVICE_TOKEN`·`CORE_API_TOKEN` 을 같은 값으로 둘 다 채워야 한다** — 안 채우면 통화 시작부터 401
- 남은 것: PR 머지 → 배포 뒤 `/health` 가 여전히 `locked` 인지 확인(티켓 마지막 완료 조건). 세션 종료 검사가 `secret.example.yaml`·런북 수정으로 정성윤 님 todo 티켓 셋(`w7-demo-infra-readiness`·`w7-infra-loose-ends`·`w8-project-closeout`)을 경고했는데, 주석 한 줄씩이라 그 티켓들의 착수는 아니다 — 건드리지 않았다
