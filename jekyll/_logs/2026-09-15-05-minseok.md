---
date: 2026-09-15
author: "장민석"
person: minseok
seq: 5
---

- **운영 DB 스키마를 확인했다 — 26 테이블, 이름이 현재 `db/schema.sql` 과 완전 일치.** 정성윤 님이 서버 파드에서 찍은 `pg_tables` 출력을 받아 `schema.sql` 의 `CREATE TABLE` 26개와 diff 했다.
  `agent_token`(09-15) · `admin_account`·`admin_refresh_token`·`closure_item`(09-14) 이 모두 있다 — 09-14 마이그레이션 이후 아무도 찍지 않던 확인이 이것으로 채워졌다.
- 범위: **테이블 이름만** 봤다. 컬럼·제약은 운영에서 직접 보지 않았다 — 두 마이그레이션 파일이 한 트랜잭션이라 테이블이 있으면 파일 전체가 들어간 것이고,
  로컬에서 적용 결과가 새 스키마와 349항목 일치함은 앞서 확인했다.
- 남은 것: PR #86 머지(= `0.1.8` 운영 배포) 뒤 `/health` spokes `postcall` · `/openapi.json` `/admin/agent-tokens` 확인
