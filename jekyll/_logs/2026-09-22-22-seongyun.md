---
date: 2026-09-22
author: "정성윤"
person: seongyun
seq: 22
---

- **`w6-segment-id-reuse`(재연결하면 전사를 덮어쓴다 — 수동 QA ❌ 「(데이터)」) 를 장민석 님에게서 넘겨받아 고쳤다**(운영 확인 전). 원인 둘을 다 막았다 — ① 콜 미디에이터가 통화를 다시 열 때 발화 번호를 1부터 다시 셌다 ② 서버 UPSERT 가 다른 화자의 발화로도 글자만 덮고 화자는 그대로 뒀다.
  - 서버: `POST /hub/calls` 응답에 `last_segment_id`(이미 있던 통화에 저장된 가장 큰 번호) · 전사 UPSERT 에 화자 조건 → 다르면 **409**(조용히 덮지 않는다)
  - 콜 미디에이터: 그 번호 다음부터 센다. 필드 없는 옛 서버면 지금처럼 1부터
  - 검증: server **1,434 passed** · import-linter **5 kept** · call-mediator **157/157** · `tsc` 0. 이어 세는 한 줄을 끄면 새 미디에이터 테스트 2건이 실패함을 확인. 서버 integration 1건(실 DB 에서 다른 화자 덮기 거절 · 같은 화자 재전송은 덮음)은 로컬에 DB 가 없어 CI 에 맡긴다
  - 태그 server `0.1.37` · call-mediator `0.2.7` — 다른 브랜치·레지스트리에 없음(`decisions/131` 오류 규칙 첫 적용)
- ⚠ 안 고친 것: 다시 연 통화의 `utterance_end_ms` 기준 시각(새로 연 시각). 화면은 번호순이라 드러나지 않는다 — 티켓에 적었다.
- 남은 것: PR · 배포 뒤 운영에서 test-qa-05 방식(여섯 줄 → 채널 닫기 → 같은 통화 다시 열어 한 줄)으로 **7건** 확인 → 티켓 done · 수동 QA 「(데이터)」 ❌ 를 ✅ 로.
