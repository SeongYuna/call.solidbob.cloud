---
date: 2026-09-14
author: "류준"
person: ryujun
seq: 1
---

- **main 을 `ai` 로 받아 푸시했다** — `ai` 가 이미 main 에 들어가 있어 fast-forward(`9970fba`). 보낼 커밋이 없어 PR 은 올리지 않았다
- **칸반 카드에 한 줄 메모(`note`)를 띄우게 했다** — `kanban.markdown` 이 배지·제목만 그려서 메모를 달 자리가 없었다. 진행 중인 A-5 스파이크·통화 온도(D-5)와, 이번에 진행 중으로 옮긴 오류 주입기(E-3)에 **「모델 학습 후 진행 예정 — 정확도 측정」** 을 달았다(사용자 지시). J-1·J-2·J-4 · J-5 는 남은 일이 서버 어댑터·배선이라 달지 않았다
- **C-6·D-5 저장 경로를 만들고 C-6 은 전사 수신에 배선했다** ([티켓](/backlog/w4-c6-d5-persistence/) → done). 고객 확정 발화가 마스킹·저장된 **뒤에** 콜 가드가 돌고 `call_guard_flag` 에 발화 단위로 갈아끼운다 — 탐지기가 받는 것이 `event.text`(마스킹본)라 폭언 기록에 개인정보가 들어갈 경로가 없다(MANUAL-5.5). 콜 가드가 없는 배포에서는 **501 이 아니라 건너뛴다** — 전사 수신에 얹혀 있어 501 이면 자막까지 멈춘다. `/health` `spokes` 에 `call_guard` 가 붙는다
  - 리포지토리를 티켓 `paths`(`ai/…`)가 아니라 `server/…/postgres/` 에 뒀다 — `ai/CLAUDE.md` §0 이 DB 저장은 `ai/` 몫이 아니라고 정한다
  - **탐지기 `strip()` 버그** — 앞 공백을 자른 문자열 기준으로 오프셋을 내서, 저장하면 공백 수만큼 구간이 밀릴 수 있었다. 고쳤다
  - D-5 는 포트·리포지토리·`record_speaker_temperature`(기준선을 못 만들면 **부르지 않는다** — 빈 목록은 「튄 구간 없음」으로 기록된다). ⚠ **부르는 실시간 경로가 없다** — 톤 특징값은 오디오에서 나오는데 `server/` 는 텍스트만 받는다. `robust_z` 는 HTTP 표면에 나타나면 실패하는 테스트로 막았다(부록 A-1)
  - **`document` 테이블을 채우는 경로가 아예 없었다** — `scripts/seed_documents.py`(98조항 UPSERT, 지식베이스에서 빠진 조항은 지우지 않고 알린다). ⚠ **운영 RDS 에 안 돌렸다.** 안 돌리면 욕설이 걸린 고객 발화의 `POST /hub/transcripts` 가 FK 위반으로 500 이다 → 런북 17-3 에 절차를 넣었다
  - 검증: 로컬 `postgres:17` + 현재 `schema.sql` 에 적재 스크립트 2회(멱등) · integration 6 통과 · 실제 앱으로 「전화번호 + 욕설 + 위기 신호」 발화를 넣어 `call_guard_flag` 에 `insult`(5.1)·`distress`(5.4) 가 **마스킹된 자막 구간**으로 남는 것을 확인
- **E-3 STT 오류 주입기를 만들었다 — 곡선은 아직 안 냈다** ([티켓](/backlog/w5-stt-error-injector/) → in-progress). 유형을 지어내지 않으려고 분류 규칙을 코드로 고정하고 실측 40쌍(09-09 협대역 실험의 정답 ↔ Chirp)에 돌려 프로파일을 뽑았다(`scripts/build_stt_error_profile.py`). 편집 81건 중 **흉내 내지 못하는 몫이 46.9%(38/81)** — 환청·의미 붕괴라 넣을 말을 지어내야 한다. **같은 WER 에서 실제보다 덜 파괴적이므로 곡선은 낙관 쪽으로 기운다**
  - 캘리브레이션(주입기 검증이지 품질 수치 아님): 골든셋 고객 발화 136건·815단어·시드 20260914 에서 목표 0.05/0.10/0.15/0.20 → 실제 0.0503/0.1018/0.1509/0.2000
  - **틀린 것 둘** — ① 숫자 읽기 과대 집계(숫자로 시작한 환청까지 셌다, 6→2건) ② **주입본이 정규화된 텍스트여서 0% 에서도 `010-2345-6789` 의 하이픈이 사라졌다** — C-5 곡선의 주 실패 모드가 구분자 부재라 곡선 전체가 교란될 뻔했다. 둘 다 고치고 테스트로 고정
- 검증: `server` 341(+14) · `ai` 226(+32) · 계약 4+3종 KEPT
- 남은 것: 운영 RDS `seed_documents.py`(런북 17-3) · E-3 곡선(Recall@5·MRR·C-5, **모델 학습 후**) · D-5 실시간 호출 경로 · `w2-baseline-gate` 의 `paths` 가 `ai/apps/evaluation/*` 전체를 잡아 오류 주입기 파일에도 경고가 뜬다(티켓 주인 확인)
