---
date: 2026-09-21
author: "류준"
person: ryujun
seq: 8
---

- **PR #108(`ai → main`, 정성윤 님이 연 것)에 오늘 커밋을 실었다** — 새 PR 을 열지 않고 같은 브랜치의 기존 PR 을 쓴다. 푸시 전에 정성윤 님의 `main → ai` 머지(11a8e7c)를 받아 충돌 3개를 풀었다(티켓 2건은 양쪽 기록 보존, STATE 는 줄마다 공통 조상과 비교).
- **`tag-check` 실패를 고쳤다** — 이번 PR 이 서버 이미지에 들어가는 코드(D-5 판정 포트·DTO, 평가 기록 모듈 ID)를 바꿨는데 `newTag` 가 `0.1.23` 그대로였다. 런북 13·16·「만들지 말 것」을 읽고 `infra/k8s/base/kustomization.yaml` 의 server 태그를 **`0.1.24`** 로 올렸다(다른 열린 PR #109 도 0.1.23 이라 겹치지 않음). 머지하면 `release.yml` 이 새 이미지를 굽고 배포한다 — 요청 경로 동작 변경은 없다. STATE 서버 줄에 적었다(장민석 님 검토 전).
- PR 제목·본문을 오늘 내용으로 채웠다.
