---
date: 2026-09-21
author: "정성윤"
person: seongyun
seq: 4
---

- **`STATE.md` 의 낡은 서술 열두 곳을 실물에 맞췄다** — 같은 날 다른 세션이 인프라 줄의 「`0.2.2`(운영 반영 전)」 한 대목을 고쳤는데, 같은 파일의 다른 줄이 여전히 반대로 말하고 있었다(검토에서 나온 것).
  - 서버 줄 `0.1.19` 「운영 반영 전」 → **09-20 릴리스로 운영 반영, 장민석 님 사후 검토 대상** · 「Neon 스키마 미확인」은 취소선(Neon 은퇴 · 운영 RDS 컬럼 단위 일치)
  - 막힘 표 세 행을 취소선으로 닫음 — 「A(STT) 코드 0줄」·「트리거 스포크 배선」·「`postcall/` 미생성」(운영 `/health` 의 `spokes` 에 `trigger`·`postcall` 이 있다, 09-21 실측)
  - 회신 대기 세 행을 닫음 — `ClosureType`(`decisions/305` 로 계약이 바뀜) · `segment_id` 타입(응답 전부 문자열, `test_segment_id_contract.py`) · 800 vs 1,500ms(`decisions/202`)
  - 구조 절 — `ai/` 모듈 넷 → **여덟** · `server/` 앱 **일곱** · DB 22 → **29 테이블** · 결정 기록 번호 「106·205 까지」 → **123 · 210 · 313 · 406**
  - 테스트 수 09-21 값(ai 421 · server 1,058)을 09-18 값 옆에 나란히 · 상담원 데모 「mock 모드」 → 09-17 부터 라이브
- 옛 문구는 지우지 않고 취소선·날짜로 남겼다(절대 원칙 8). 「실측값」 절은 09-21 값이 최신이라 손대지 않았다.
- 남은 것: 「프론트(Vercel) Git 자동배포 미확인」은 **확인할 근거를 못 찾아 그대로 뒀다** — PR #96 의 자동 배포 확인은 Pages 이고 Vercel 이 아니다.
