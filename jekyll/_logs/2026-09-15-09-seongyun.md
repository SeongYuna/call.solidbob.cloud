---
date: 2026-09-15
author: "정성윤"
person: seongyun
seq: 9
---

- **미결 항목을 정리했다 — 열린 항목 115건 → 103건.** 지운 것은 없다(절대 원칙 8).
- **오늘 작업으로 이미 닫힌 9건이 열린 채 남아 있었다.** `- [x]` 로 바꾸고 **「닫힘 — 근거」 한 줄**을 붙였다:
  구글 OAuth 클라이언트 · `admin_account` 첫 행(2건, 서로 다른 절에 중복 기재돼 있었다) · 운영 Redis 방식 ·
  `CUSTOMER_REF_HMAC_KEY`(**이미 있었다**) · 09-14 스키마 적용(Neon 부분은 항목 자체가 낡았다) ·
  `document` 채우는 경로 · 운영 `document` 행 · 보존 정리를 누가 부르나(`decisions/113`).
  **이게 실제로 위험한 종류의 낡음이다** — 다음 사람이 이미 끝난 일을 다시 조사하거나, 더 나쁘게는
  「아직 안 됐다」고 믿고 중복 작업을 한다.
- **내가 만든 중복 2건을 먼저 쓴 항목에 합쳤다.** 오늘 `agent_id`·J-5 를 새 절로 썼는데,
  둘 다 **09-14·09-15 에 장민석이 이미 적어 둔 항목**이 있었다. §4 의 「먼저 손댄 쪽이 산다」대로
  먼저 쓴 항목을 살리고 내 분석(코드 근거·선택지)을 그 아래로 접어 넣은 뒤, 내가 만든 두 절은 지웠다.
  **원인은 쓰기 전에 검색하지 않은 것이다** — 오늘 두 번째 같은 실수다(앞의 고아 테이블 오인도
  「이미 있는 것을 못 보고 단정」이었다).
- **정리하며 드러난 것**: 「조서희 님께」로 시작하는 전달 항목이 **8건** 쌓여 있다. 미결 항목은 전달함이
  아닌데 그렇게 쓰이고 있다 — 프론트 담당에게 실제로 닿는 경로가 따로 없어서다. 다음에 정할 만하다.
- 남은 것(내 영역): J-5 를 데모에서 부를지 · 룰셋에 `tag-check`·`gateway` 등록 ·
  `branch-protection.json` 이 라이브와 어긋남 · OIDC 신뢰 정책 확인(콘솔 30초) ·
  ES 이미지를 CI 가 굽지 않음 · `converge.sh` 가 게이트웨이 롤아웃을 안 봄 ·
  `docs` CNAME 과 자리표시자 `ai` 레코드 정리.
