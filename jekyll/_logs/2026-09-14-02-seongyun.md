---
date: 2026-09-14
author: "정성윤"
person: seongyun
seq: 2
---

- **PR #73(frontend→main) 머지로 릴리스가 실패한 원인을 찾았다.** `release.yml` 의 `plan` 잡,
  「이미지를 다시 구워야 하는지 본다」 스텝이 exit 1 — **가드가 설계대로 작동한 것이지 CI 버그가 아니다.**
  `#59`·`#61` 에 이어 세 번째다.
  - 원인: #73(`3e5868a`)이 170개 파일을 가져왔는데 그 안에 `server/apps/admin_auth/`(관리자 구글 로그인
    슬라이스 전체)·`server/core/config.py`·`server/main.py`·`server/requirements.txt`·`db/schema.sql`
    이 들어 있다. 그런데 **`infra/` 는 한 파일도 안 바뀌었다**
    (`git diff --name-only b6137d6 3e5868a -- infra/` → 빈 결과) — `newTag` 가 `0.1.3` 그대로.
  - `seongyuna/callguard-server:0.1.3` 은 Docker Hub 에 09-11 08:45 로 이미 있다(확인함).
    `imagePullPolicy: IfNotPresent` 라 같은 태그로 다시 구워도 노드는 캐시된 옛 이미지를 쓴다.
  - 게이트웨이는 정상적으로 안 걸렸다 — #73 이 건드린 건 `services/gateway/README.md` 하나뿐이고
    gateway 경로 정규식은 `src/`·`package*.json` 만 본다. `deploy` 가 skipped 된 것도
    09-10 #61 때 넣은 `needs.plan.result == 'success'` 조건이 의도대로 돈 것이다.

- **태그를 내가 올리려다 되돌렸다 — 장민석이 먼저 했다(§4 「먼저 손댄 쪽이 산다」).**
  `kustomization.yaml` 을 `0.1.4` 로 고쳤다가, `origin/server` 의 `fb627d4`(13:02)가 이미 server
  `0.1.4` · gateway `0.1.2` 로 올려 둔 것을 확인하고 `git checkout --` 로 되돌렸다.
  - **내가 먼저 머지했으면 장민석 머지가 같은 가드에 걸렸을 것이다(네 번째).** 태그만 올린 내 PR 이
    머지되면 `0.1.4` 가 #73 시점 코드로 구워져 레지스트리에 박히고, 그 뒤 `server` 머지는
    「코드 변경 true + 태그 있음 true」가 되어 exit 1 이다. 태그는 브랜치 사이의 공유 자원인데
    결정 기록 번호(§4)와 달리 담당자별 번호대가 없다.

- **`origin/server` 머지 — 릴리스 네 잡 모두 통과. 예측대로였다.**
  머지 전에 `decide()` 를 그대로 옮겨 시뮬레이션해 «넷 다 통과» 로 예측했고, PR #74(`dc203b0`)가
  04:14 UTC 에 머지돼 `plan`·`image`·`gateway-image`·`deploy` 전부 success, 117초.
  운영 `/health` 확인: `spokes` 4종(`masking closure_gate retrieval trigger`) 그대로.
  - fast-forward 다(`origin/server..origin/main` 이 비어 있다) — 충돌 없음.
  - server `0.1.4`·gateway `0.1.2` 둘 다 Docker Hub 에 없다(확인함) → 둘 다 `build=true`.
  - `/health` 스포크 4종 유지: `IsFinalTrigger.__init__` 시그니처가 그대로라 `provider.py`
    (`IsFinalTrigger(**kwargs)`, 이번에 안 바뀜)가 계속 붙는다. `SPOKES` 에 추가·삭제 없음.
    0.1.2 때 trigger 가 조용히 빠진 그 자리를 확인했다.
  - `TranscriptEvent.received_at_ms` 는 `int | None = None` 이라 하위 호환. 매니페스트는
    `kustomization.yaml` 만 바뀌어 새 환경변수·시크릿이 필요 없다.

- **#73 의 관리자 로그인은 배포해도 500 이다 — 아직 구축 전이라 의도된 상태이고 이번 범위 밖이다**(사용자 확인).
  `admin_auth/dependencies/providers.py` 의 포트 배선이 요청 스코프라 앱 기동·`/health`·게이트웨이
  스모크는 영향받지 않는다(= 배포 판정은 정상). 남은 것 셋: ① 클러스터에 Redis 없음
  (`infra/k8s/base/` 에 매니페스트 0건) ② `server-env` 에 `GOOGLE_OAUTH_CLIENT_ID`·`ADMIN_JWT_SECRET`
  ·`REDIS_URL` 없음 ③ RDS 에 `admin_account`·`admin_refresh_token` 테이블 없음.

- 남은 것: `newTag` 누락을 **머지 후가 아니라 PR 단계에서** 막을지 — `decisions/108` 「남은 것」이
  이미 물어둔 질문인데 이번이 세 번째 재발이다. `release.yml` 은 main push 전용이고 필수 통과 검사에
  일부러 안 넣었다(넣으면 배포가 머지를 막는다). 배포를 하지 않는 **태그 검사 잡만** `test.yml` 로
  떼어내면 필수로 걸어도 안전하다. 오늘 겪은 「두 사람이 같은 태그를 집는」 문제도 같이 봐야 한다.
