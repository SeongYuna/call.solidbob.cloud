---
date: 2026-09-23
author: "조서희"
person: seohee
seq: 10
---

- **w7-plaintext-reveal-sec1 (done)** — 칸반 순서 14번. 티켓의 권고안("기능을 걷는다")을
  그대로 택해 `decisions/408`로 기록하고 구현했다.
  - `MaskedText.tsx`를 대폭 단순화 — `plainText`·`authorized`·`revealAll`·`openedIds`·
    `onToggle`·`revealSpansFor`/`RevealSpan`을 전부 지웠다. 마스킹 스팬은 이제 항상
    마스킹본만 보여주는 비대화형 `<mark>`다.
  - `TranscriptPanel.tsx`의 "권한 확인 (데모)"·"원문 보기" 버튼, `toggleSpan`·
    `toggleLineSpans`·`allRevealIds`를 지웠다. 「⚠ 경고」 배지는 `.claude/rules/call.md
    §2`가 요구하는 정적 표시라 남기되 비대화형 `<span>`으로 바꿨다.
  - `plain_text` 필드를 `contract.ts`(`TranscriptEvent`·`TranscriptQuerySegment`)·
    `callStore.ts`(`Utterance`)에서 뺐다 — 실서버 계약엔 애초에 없던 mock 전용 필드였다.
  - `maskSensitiveText.ts`의 `logPlainReveal`(열람 감사 로그 목업)·`SensitiveMaskResult.plain`
    삭제. mock 데이터 생성 쪽(`callHistory.ts`·`scenarios/helpers.ts`·`scenarios/dasan.ts`
    실제 원문 3건: 주민번호·연락처·주소)도 같이 걷었다. 죽은 CSS도 정리.
  - `_project/STATE.md`의 `apps/` 줄에 남아 있던 "ko-masking은 권한 확인 후 스팬 클릭으로
    원문 토글" 서술도 정정했다.
  - 서버 쪽 단언 테스트(완료 조건 2번)는 장민석 몫이라 손대지 않음.
  - 검증: `tsc --noEmit`·`vite build`(번들 JS 약 3.4KB 감소)·`npm test`(11/11) 통과.
- 사용자 지시로 이번 항목은 커밋하지 않고 수정만 남겨 뒀다.
