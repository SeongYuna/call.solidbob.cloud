---
date: 2026-09-14
author: "조서희"
person: seohee
seq: 1
---

- **관리자 화면 블랙리스트 연장 버튼 문구를 정리했다** — "지금부터 {개월}개월로 재설정" →
  "재설정"(`EntriesTab.tsx`). 옆의 입력 칸에 이미 개월 수가 보이므로 버튼 문구가 그 값을
  다시 읽어줄 필요가 없었다.
- **`apps/dashboard` → `apps/call`로 폴더 이름을 바꿨다**(`git mv`, 히스토리 보존). 파생되는
  경로 에러를 미리 잡았다 —
  - `.claude/rules/dashboard.md`를 `.claude/rules/call.md`로 같이 옮겼다. **frontmatter의
    `paths: apps/dashboard/**` 글롭을 안 고쳤으면 이 규칙이 조용히 다시는 안 걸렸을 것**이라
    이번 건에서 가장 중요한 수정이었다.
  - 실제 명령이 박혀 있던 `docs/harness.md`의 `cd apps/dashboard && ...`, `CLAUDE.md` §3
    저장소 구조, `rfp-harness.md` 6곳, `README.md`, `_project/plan.md`(수정 이력 남김),
    `_project/STATE.md`, `_project/dns-solidbob-cloud.md`를 고쳤다.
  - `services/gateway/*`·`apps/admin/*` 9개 파일·`server/core/config.py`·
    `server/tests/test_main_cors.py`·`.gitignore`·`.env.example`의 주석 언급도 맞췄다.
  - `jekyll/_logs/`·`_project/decisions/`·`STATE-archive.md`·`jekyll/_backlogs/`는 과거
    시점 기록이라 소급 수정하지 않고 그대로 뒀다.
  - **직접 못 고친 것 하나**: 운영 Vercel 프로젝트(`call-solidbob-cloud`)의 Root Directory
    설정이 여전히 `apps/dashboard`를 가리킨다(`decisions/106`, vercel.com 쪽 설정). main
    머지 전에 정성윤이 `apps/call`로 바꿔야 한다 — 안 바꾸면 다음 배포가 깨진다.
- 남은 것: 위 Vercel Root Directory 변경. `jekyll/open-items.markdown`에 적었다.
