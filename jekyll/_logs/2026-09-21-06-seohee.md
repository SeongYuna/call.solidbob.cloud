---
date: 2026-09-21
author: "조서희"
person: seohee
seq: 6
---

- **컴플라이언스(C-1~C-4) 서버 방송 연동의 파서 단계만 먼저 들어갔다.** 운영
  call-mediator는 `announceCompliance`가 꺼져 있고(`services/call-mediator/src/app/call_registry.ts:96-99`,
  검사·저장은 계속 돌지만 대시보드로는 안 보냄) 프론트 파서에 `compliance` 타입이
  아예 없었다 — 서버가 방송을 켜기 전에 프론트가 먼저 받을 준비를 해 두는 순서.
- 조사(1단계): `ports.ts:153` 메시지 정의, `call_registry.ts:487-500`의 발행 조건
  (`announceCompliance === true && findings.length > 0` — 위반이 없을 때도 안 보내고,
  검사 실패 시에도 `log.warn`만 남기고 아무 신호도 안 보냄. "탐지 미동작"을 알려주는
  필드·메시지는 서버 어디에도 없음 — 확인함), 프론트 로컬 규칙(`detectComplianceRisk.ts`,
  `TranscriptPanel.tsx:713`·`888` — 컴포넌트 로컬 state만 쓰고 스토어 없음), 부록 A-1
  금지 표현, 알 수 없는 `type` 처리 방식(`onError`로 떨어짐, 화면이 깨지지 않음)을
  전부 파일:라인으로 정리해 사용자에게 보여줌.
- 수정(2단계, 파서·스토어만):
  - `types/contract.ts`: `ComplianceFinding` 타입 추가(서버 `compliance_schema.py`
    필드명 그대로 — `rule_code`·`phrase`·`alternative_source?`)
  - `lib/ws/realCallMediatorClient.ts`: `ParsedMessage`에 `compliance` 추가,
    `parseCallMediatorMessage`·`parseByKind`·`handleMessage`에 기존 `call_guard`와
    같은 방식으로 배선, `parseCompliance()`를 `parseCallGuard`/`parseClosure`의
    검증 방식(`readString`·`isRecord`·`parseSource`) 그대로 따라 신설 — 모르는 필드는
    무시하지 않고 형식이 틀리면 `null`(→ `onError`)로 떨어짐
  - `lib/ws/types.ts`: `CallMediatorListener`에 `onCompliance?` 콜백 추가(기존
    `onCallGuard?`와 같은 선택적 패턴)
  - `hooks/useCallMediatorSession.ts`: `onCompliance` 리스너를 새 스토어 액션에 연결
  - `store/callStore.ts`: `compliance: Record<string, ComplianceFinding>` 상태 +
    `applyCompliance` 액션 추가(`callGuard`/`applyCallGuard`와 같은 모양).
    **어떤 컴포넌트도 이 값을 안 읽음** — grep으로 확인.
  - mock 클라이언트는 손대지 않음(이 메시지를 안 보내도 됨).
  - 화면·로컬 규칙(`detectComplianceRisk`·`ComplianceWarningBanner`)은 전혀 안 건드림.
- `apps/call`: `tsc --noEmit` + `vite build` 통과.
- 커밋·푸시는 안 함(사용자 지시). 이전 세션(05번 로그)의 `TranscriptPanel.tsx`
  변경도 아직 미커밋 상태로 같이 남아 있음 — 이번 작업과는 별개.
