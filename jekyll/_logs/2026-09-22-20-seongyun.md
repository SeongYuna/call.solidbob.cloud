---
date: 2026-09-22
author: "정성윤"
person: seongyun
seq: 20
---

- **정리 넷.** ① `ai.solidbob.cloud` DNS 레코드 삭제(정성윤, 클라우드플레어 콘솔) ② **운영 노드의 시크릿 백업 파일 5개 `shred -u -z`** — `/home/ssm-user/server-env.backup.yaml`·`…0328.yaml` · `/home/ec2-user/server-env.backup.0325.yaml`(0바이트) · `/root/secret-backups/20260921-183422/{server-env,call-mediator-tokens}.yaml`, 디렉터리도 지움. 남은 백업 0(`find` 확인). 되돌릴 백업이 더는 없다 — 시크릿을 다시 바꿀 때는 그 자리에서 새로 뜬다 ③ 저장소 루트의 `vercel link` 잔재(`.env.local` — Vercel OIDC 토큰 · `.vercel/` — `call-solidbob-cloud-kxu6` 링크) 삭제, `.env.local` 은 덮어쓴 뒤 지움 ④ **Vercel 브랜치 배포는 정성윤이 파일 대신 프로젝트 설정에서 직접 막았다** — `w6-vercel-json-branch-deploys` done, 미결 체크. 설정이 저장소 밖이라 프로젝트를 새로 만들면 다시 한다.
- **`STATE.md` `ai/` 줄의 낡은 문장 둘 정정**(류준 줄 — 사용자 지시로 직접): 「모델 구성은 아직 운영에 없다」 → 09-22 12:10 부터 운영에 붙어 있다(수치는 여전히 로컬 기록 DB) · 남은 막힘의 「모델 두 폴더 S3 업로드」 → 완료. 인프라 줄의 「`ai` DNS 레코드 살아 있다」도 지움.
- **안 한 것**: 콜 미디에이터 입력 토큰 교체 — 사용자가 하지 않기로 했다(오늘 대화에 한 번 드러났다는 사실만 18번 기록에 남아 있다). Vercel 옛 변수 둘(`VITE_GATEWAY_WS_URL`·`VITE_GATEWAY_DEMO_BASE_URL`) — 이 머신에 Vercel CLI·로그인이 없어 대시보드에서 지워야 한다(미결 그대로).
