---
date: 2026-09-22
author: "류준"
person: ryujun
seq: 8
---

- **PR #129 배포 뒤 운영 확인 — 티켓 셋을 닫았다.** #129(server `0.1.33` · call-mediator `0.2.4`) 릴리스 성공 뒤 장민석 님 #131(`0.1.34` · `0.2.5`, 우리 변경 포함)까지 배포가 끝난 것을 기다려 확인했다. 정성윤 님이 운영 ES 재적재(nori)를 마쳤다고 전해 왔다.
- **운영 SYN-010 재투입**(`syn-prod-syn-010-20260922-v2`, 16:05:13~16:05:50, 새 재생기 `--watch --save-ws`, `--close` 없음):
  - **절차 고르기 ✅** — 1순위 `TERM-2.12`(규칙 없음) → **필요서류 판정 0건**(`/record` `closures` 0 · `/ws` 원본 `closure` 0). 오전의 `TERM-2.9` 「신분증」 오판이 사라졌다 → `w6-procedure-pick-rule` done
  - **마지막 턴 ✅** — 저장 5/5, 재생기가 확정 5건의 `/ws` 귀환을 확인하고 닫음(207ms) → `w6-replay-last-turn` done. 원인은 미확정(미디에이터 로그 — 정성윤)
  - **지연** — 이번엔 `/ws` 로 `retrieval_ms` 를 직접 봤다: **775ms · 510ms**(둘 다 1,000 미만). 배포 직후 첫 요청도 775ms 라 오전의 3,300ms(모델 탑재 직후 첫 요청)와는 다르다
  - **nori** — 이 16:05 확인은 **재적재 전 인덱스**였다(재적재 완료는 그 뒤에 전달받음). 재적재 뒤 16:21 에 다시 흘려(`-v3`) dense 순서가 16:05 와 **완전히 같음**을 봤다 — 벡터가 살아 있다. 분석기는 밖에서 `_analyze` 를 못 해 직접 확인하지 못했고, BM25 경로는 이 통화에서 안 탄다 → `w6-nori-pos-filter` done. v3 도 저장 5/5 · 판정 0 · `retrieval_ms` 603 · 542
  - 맞장구 「아 네, 알겠습니다」에는 여전히 무관한 카드 5장이 뜬다 — B-6 문턱을 보류했으니 **예상대로**다(`w6-b6-conversational-recalibration`)
  - `/health` 전후 `ok` · 스포크 11개 · `ingest_guard: locked`. 저장 파일에 토큰 없음 확인. 원자료 `data/processed/prod-synthetic/2026-09-22/syn-010-v2-*`
- 작은 것: 재생기가 생성 모델이 없을 때도 `생성 0ms` 로 찍는다(서버가 `generation_ms: "0"` 을 보낸다) — `E2E.md` 는 「없는 값은 —」라 어긋난다.
