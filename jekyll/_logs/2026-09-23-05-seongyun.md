---
date: 2026-09-23
author: "정성윤"
person: seongyun
seq: 5
---

- **수동 QA(09-22)에서 나온 일을 사람별로 대조했다.** 류준: Q-44(무의미 검색에도 결과)는 티켓 둘이 `done` 이지만
  **고쳐진 게 아니다** — 문턱을 걸었다가 대화체의 60%가 기권해 보류했고(`decisions/215`), 맞장구 억제도 사전 등록한
  채택 조건에 미달해 넣지 않았다(`216`). 운영은 기본 꺼짐이라 재시험하면 그대로 ❌ 다. Q-21(절차 누적)은 절반
  (`w6-procedure-pick-rule` 완료 · `w6-procedure-noise-redesign` 남음). 장민석: Q-14 는 서버 몫이 `0.1.35` 로
  나가 있고 남은 것은 `READ_AUTH_REQUIRED=true` 한 줄(조서희 토큰 UI 뒤), Q-62 의 409 는 `decisions/314` 로 없어졌다.
- **Q-67(감사 로그에 만료일 재설정이 안 남는다) 서버 쪽을 넘겨받아 고쳤다.** `todo` 라 착수 기록이 없는 티켓이라
  가져왔다(`CLAUDE.md` §4). 쌓이는 것은 멀쩡했고 **읽는 문이 등록 1건짜리뿐**이라 감사 로그가 N+1 이 되는 게 원인이었다.
  `GET /hub/blacklist-expiry-changes?limit=50`(관리자 전용, 최근 순)을 슬라이스 한 벌로 추가했다 —
  포트·인터랙터·프로바이더·라우터·스키마 + 테스트 3종(라우터 401·정렬·`limit` 422 / 인터랙터 / 실제 DB 통합).
  `server` 전량 초록 · 계약 5종 KEPT · 태그 `0.1.39`. 화면 줄은 조서희 몫으로 티켓에 남겼다.
- **Q-45(상담원 보호 알림 배너)의 원인 진단을 정정한다.** 「실서버 `CallGuardFlag` 에 매칭 텍스트가 없어 못 채운다」는
  사실과 다르다 — 서버는 `phrase`(마스킹된 발화에서 잘라낸 표현)와 `span`·`source_doc_id` 를 내려보내고
  (`call_guard_check_schema.py`), 콜 미디에이터는 응답 본문을 **그대로** 방송한다(`announceCallGuard: true`).
  떨어뜨리는 곳은 프론트 계약 타입이다 — `apps/call/src/types/contract.ts` 의 `CallGuardFlag` 가
  `{segment_id, category}` 둘만 선언한다. **서버 변경 없이 화면에서 닫을 수 있다.**
- 남은 것: `0.1.39` 를 PR 로 넣고 배포 뒤 재설정 1회로 Q-67 을 다시 눌러 본다. 위 Q-45 정정은 조서희에게 전달.
