---
date: 2026-09-19
author: "정성윤"
person: seongyun
seq: 4
---

- **주차 표기가 두 주 낡아 있었다 — `STATE.md` 「3주차」 → 「5주차」.** 마일스톤의 1주차 경계(`08-20~08-26`)로
  세면 오늘은 **5주차 3일차**다. 이게 가려 놓고 있던 것이 하나 있다 — **F·G·H·I 동결 판정일이 6주차 종료
  = 09-30 이고 11일 남았다.** STATE 머리말에 주차 경계와 다음 마일스톤을 함께 박았다.
- **판정에 쓸 공식 수치가 없다는 것을 확인했다(가장 중요).** `STATE.md` 실측값 블록은 **09-09 값**이고
  거기서 **`masking` 이 ❌(누락 2)** 다 — **지금 판정하면 C-5 절대 규칙 미달로 동결이 걸린다.**
  그 뒤 들어온 dense·리랭커·NER·C-5 규칙 보강은 전부 **로컬 측정이고 하네스 기록이 아니라**
  절대 원칙 2·5 상 인용할 수 없다. 이 머신엔 ES·모델·`.venv` 가 없어 **내가 돌릴 수 없다** →
  류준 님께 재측정 요청을 **명령까지 적어** [미결](/open-items/)·[w6-core-baseline-check](/backlog/w6-core-baseline-check/)에 남겼다.
  같이 적은 것: **무엇을 「기준선 구성」으로 삼을지가 판정보다 앞선다** — 운영은 규칙+BM25, 로컬 최고는
  NER+dense+리랭커다. 최고 수치로 통과를 선언하면 **운영에서 성립하지 않는 통과**가 된다(`decisions/208` 과 같이 간다).
- **골든셋을 직접 세어 문서 둘을 고쳤다** (`golden-set/v1-150.json` 156건) —
  모듈별 **B 96 · C-5 25 · C-1~C-4 20 · C-6 15**, PII 표본 **P1 4·P2 2·P3 3·P4 5·P5 4·P6 7·P7 3**.
  ① `CLAUDE.md` §2 절대 원칙 5 와 ② `.claude/rules/rfp-harness.md` 의 **「C-5 채점 범위는 P4·P6·P7 뿐」은
  낡은 서술**이라 2026-09-19 갱신 주석을 달았다(원문은 지우지 않았다 — 절대 원칙 8).
  **`f2_case` 는 여전히 0건** — F-2 는 측정 불가 그대로이고, 7주차 체크포인트가 **잴 수 없는 것을 판정해야 하는 상태**다. 미결에 올렸다.
- **결정 기록 `117` — 배포 파이프라인.** 09-09~09-14 에 만들어 계속 돌고 있는데 **기록만 없었다**
  (예약해 둔 `108` 은 RDS 이관이, 태그 게이트는 `111` 이 가져갔다). 갈린 넷을 근거·되돌리는 법과 함께 남겼다 —
  ① 버전은 저장소가 정한다(`newTag`, `sha-` 자동 태그는 기각 — `converge.sh` 가 되돌린다)
  ② 인스턴스에서 git 을 쓰지 않는다(러너가 렌더한 YAML 을 SSM 으로) ③ 통로는 SSM(포트를 열지 않는다)
  ④ OIDC 임시 자격증명. ⚠ 티켓 본문의 **「Root 단독 운영이라 대안이 Root 액세스 키뿐」은 사실이 아니어서 정정**했다 —
  **루트 액세스 키는 0개**이고 장기 키는 IAM 사용자 `admin`(2026-07-24 생성, `AdministratorAccess`) 것 하나다.
  그 키는 **09-17 운영 점검에서 이미 SSM 에 쓰였다** — 「안 쓰는 시험용 사용자」가 아니었다.
- **보드에서 거짓말하던 티켓 셋을 닫았다** — `w3-aws-deploy` · `w3-cicd-release-pipeline` · `w3-k3s-image-and-manifests`.
  남아 있던 미체크 항목이 **거의 전부 `decisions/116` 으로 안 하기로 한 것**이었다(`ai` DNS 레코드 · 자동 중지 cron · AMI).
  하나 남았던 「파이프라인 결정 기록 없음」은 위 `117` 로 채웠다. **AMI 를 안 뜨는 대가**(인스턴스를 잃으면
  노드 재구축 반나절+)는 지우지 않고 티켓에 적어 두었다.
- 남은 것: 커밋·PR · Vercel 옛 변수 둘 삭제 · 류준 님 재측정 · `decisions/208` 합의
