---
date: 2026-09-22
author: "정성윤"
person: seongyun
seq: 7
---

- **모델 구성이 운영에 붙었다 — `decisions/124` ③·④·⑤ 끝** (`w5-models-on-node`). PR #114(server `0.1.28`, main 의 `0.1.27` 위에 머지) 배포 뒤 SSM 으로 순서대로:
  - ③ 노드 `/opt/callguard/models` 로 `aws s3 sync` → `sha256sum -c` **HASH-OK**(20파일). ⚠ 첫 검사는 홈 폴더에서 쳐서 전부 「No such file」이 났다 — 해시 목록의 경로가 상대 경로라 `cd /opt/callguard/models` 뒤에 쳐야 한다(런북 11장에 그렇게 적어 뒀다)
  - ④ 서버 파드 안에서 `index_knowledge_base.py --to-es --recreate --embed-model /models/koe5` → `callguard-kb-single` **100 청크 전부 `embedding`(dense_vector 1024, cosine, bbq_hnsw)**. 조항 98 + POLICY-1 이 셋으로 갈린 것
  - ⑤ `server-env` 백업 → `PII_NER_MODEL_DIR` → 재시작 → spokes 에 **`pii_ner`** → `RETRIEVAL_EMBED_MODEL_DIR`(이미 같은 값이 들어 있어 「no change」) → 재시작 → **`retrieval_dense`·`retrieval_cache`**. 파드 RESTARTS 0 · 파드 664Mi(모델은 첫 요청 때 더 오른다) · 노드 여유 3.2GB
- PR #114 는 main 의 ai PR(#115, 태그 `0.1.27`)과 `kustomization.yaml` 에서 충돌해 main 을 합치고 `0.1.28` 로 올렸다. 다른 세션의 미커밋 4개(STATE 인프라 줄 등)는 stash 로 비켰다가 되돌렸다
- 남은 것: ⑥ SYN-010 운영 투입(류준 — 검색 구간 ≤1,000ms · 이름만 넣은 발화 마스킹 · 첫 요청 뒤 메모리·RESTARTS 재확인) · ⑦ `--record` 재측정(류준) · 류준 님 로컬 모델 폴더와 해시 대조 · 넘으면 임베딩 키만 빼고 재시작
