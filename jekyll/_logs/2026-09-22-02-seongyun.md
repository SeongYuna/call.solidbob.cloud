---
date: 2026-09-22
author: "정성윤"
person: seongyun
seq: 2
---

- **쓰기 경로를 잠갔다** (`w5-ingest-auth-fail-closed` ③, `decisions/120` 전환 순서 그대로). 노드 안에서 토큰을 만들어 미디에이터 시크릿 → 롤아웃 → 파드 안 길이 확인 → `server-env` → 롤아웃. 결과 `/health` **`ingest_guard: locked`**, 토큰 없는 `POST /hub/calls` **401**, 파드 넷 재시작 0. 시크릿 백업은 노드 `/root/secret-backups/`. 토큰 값은 어디에도 찍지 않았다. 미디에이터를 거친 실제 통화 저장 확인은 류준 님 SYN-010 투입이 맡는다. 남은 ④(서버 fail-closed 코드)는 별도 PR.
- **결정 `124`** — 09-30 판정을 모델 구성으로 시도한다. 전용 GPU(`121`) 대신 **운영 노드에 CPU 로 NER + 임베딩만**(리랭커 제외, 약 2.3 GB — 노드 여유 4.5 GB). 붙지 않으면 `121` §5 대로 운영 구성 판정. 사용자 선택. 티켓 `w5-models-on-node` 신설, GPU 티켓 셋 취소, `121` 상태 줄을 「124 로 대체」로.
- **09-21 셋째 판이 「기록했다」는 결정 124~128·판정문 초안·`w5-models-on-node` 는 어느 브랜치 이력에도 존재한 적이 없다** — `git log --all --diff-filter=A` 0건, stash·reflog 0건. 결정 123 이 09-21 01:38 에 커밋된 뒤 다른 머신에서 쓴 것이라 그 작업 트리에만 있거나 파일이 안 만들어진 것. 오늘 쓴 124 가 첫 124 다.
- **류준 님 요청에 답했다** — `_project/handoff/2026-09-22-seongyun-to-ryujun.md`. S3 경로 승인(제안 그대로) · `roles/speech.client` 부여 · SYN-010 투입 허용 · 09-30 모델 구성 시도 + 모델 S3 업로드 요청. `118`·STT 캡·A-5 유지는 미정.
- **랜딩(www)에 `VITE_CALL_MEDIATOR_WS_URL` 을 넣고 재배포** — 상담원 화면 프로젝트의 값을 API 로 복사(값 미출력). 번들 `index-D9_EVbhr` 에 `wss://server.solidbob.cloud/call-mediator/ws` 가 구워졌다. 홍보 페이지 라이브 통화가 처음으로 주소를 갖는다.
- 남은 것: `118` 판단 · 런북 11장(GPU 원안) 정정은 `124` ② PR 에서 · 124 ①은 류준 님(모델 업로드) · SYN-010 결과 확인 · 이 배치 커밋·PR.
