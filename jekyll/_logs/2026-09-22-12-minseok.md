---
date: 2026-09-22
author: "장민석"
person: minseok
seq: 12
---

- **J-5 남은 일을 코드로 판단했다** — 셋 중 둘은 코드로 끝낼 수 없고, 대신 막힌 곳 하나를 찾았다
  - `routing_log` 실확인: 코드 경로는 단위·통합 테스트로 덮였다. 남은 것은 운영 통화 1건인데 이 머신에 미디에이터 토큰이 없다 → 미결로
  - `ROUTING_CANDIDATES` 값: `agent_id` 는 토큰 발급 때 넣은 이름 그대로라는 규칙까지만 코드로 안다. 실제 행은 운영 `GET /admin/agents` 몫
  - ⚠ **입사일(`agent.hired_on`)을 넣는 코드가 어디에도 없었다** — 상담원 행은 이름만으로 생기고(`406`) 화면에도 입력란이 없다. **운영의 모든 상담원이 근속 0년 → 베테랑이 나올 수 없었다.** SYN-007 은 늘 「일반 배정으로 떨어짐」이 된다
- **상담원 입사일 저장 API** — `decisions/321`, `w6-agent-hired-on-api`(done)
  - `PUT /admin/agents/{agent_id}/hired-on`(관리자) — 상담원만(관리자 행·없는 상담원 404) · 오늘보다 뒤 422 · `null` 이면 지운다. `agent_auth` 스포크에 슬라이스 전 층
  - `GET /admin/agents` 목록에 `hired_on`(없으면 `null`) — 필드 추가라 기존 파서 영향 없음
  - 검증: server **1,312 passed** · import 계약 4 kept · **통합 테스트 12건을 로컬 도커 PostgreSQL 17 + 현재 `schema.sql` 로 돌렸다**(새 입사일 테스트 포함 — 관리자 행은 안 바뀌는 것까지)
  - 기획서 7.3절에 올리고 수정 이력 · 태그 server `0.1.34` → **`0.1.35`**
- 조서희 님께 화면 요청 티켓 `w6-admin-agent-hired-on-ui`(담당 «제안») — 입사일이 없으면 「근속 0년으로 계산」을 보이게
- 남은 것(미결 「J-5 시연 준비」): 배포 뒤 시연 상담원 입사일 넣기 · `ROUTING_CANDIDATES` 운영 값 · 테스트 통화로 `routing_log` 확인
