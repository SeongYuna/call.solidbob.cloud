---
date: 2026-09-15
author: "장민석"
person: minseok
seq: 10
---

- **서버 남은 일 셋을 했다**(사용자 지시). 요약 확정은 선택지를 처음에 너무 짧게 적어 «상담원이 요약을 고친다고?» 라는 되물음이 왔다 — 배경(초안은 확정 전 상태이고 확정 API 가 없어 영원히 초안)을 설명한 뒤 다시 물었다.
- **블랙리스트 연장 누적 상한 — 승인일 + 365일**(`decisions/309` 추가, 사용자 선택). 규칙은 도메인 `blacklist/domain/services/expiry.py`, 저장소가 잠근 상태에서 승인일과 비교해 `ExpiryBeyondCap` → 422(응답에 가능한 마지막 시각).
  단축은 통과 · 경계 허용. integration 에 «366일 연장 거부 · 만료·이력 그대로» 를 넣었다. ⚠ 365 라는 값의 근거는 없다 — 승인 상한과 맞췄을 뿐.
- **요약 확정 API** `POST /hub/calls/{id}/summary-confirmation`(`decisions/310`, 사용자 선택: 고쳐서 확정). 상담원 토큰 필수 · 누가 확정했는지는 저장 안 함 · 고친 문구 마스킹 ·
  `summary_confirmed_at` 기록 · 후속조치 draft → confirmed · 확정은 한 번(409) · 확정 뒤 초안 재생성도 409. 스키마 변경 없음. 파일명이 기존과 겹치지 않는지 먼저 확인했다.
- **게이트웨이 알림 3종을 켰다**(`announcePending`·`announceCallGuard`·`announceClosure`) — 조건이던 프론트 파서가 PR #88 로 main 에 들어왔다. 켰을 때 동작은 기존 테스트가 이미 본다(gateway typecheck 통과 · 101 pass).
- 태그: 서버 `0.1.9` → `0.1.10` · 게이트웨이 `0.1.3` → `0.1.4`. 로컬 태그 검사 통과(둘 다 레지스트리에 없음 · 코드 변경 있음).
- 검증: `server` 753 passed · integration 17 · 계약 4종 KEPT / `ai` 225 / gateway 101. **마이그레이션 없음.**
- 남은 것: 운영 반영 전(커밋·PR·머지) · 프론트 연결(요약 확정 화면 · 연장 422 메시지 · 세 신호 표시)은 조서희 님께 · 확정 후 수정 경로 없음 · 이력 사유 보존 기간 미정.
