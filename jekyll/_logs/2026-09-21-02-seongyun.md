---
date: 2026-09-21
author: "정성윤"
person: seongyun
seq: 2
---

- **저장소 전수 조사 → 낡은 문서 정리** ([w5-stale-docs-sweep](/backlog/w5-stale-docs-sweep/)). 1,810개 파일을 네 영역(`server/` · `ai/`·데이터 · 프론트·미디에이터·인프라 · 기록)으로 나눠 읽고, **코드·실물과 어긋난 서술**만 골라 고쳤다. 옛 문구는 지우지 않고 날짜를 달아 「그때의 값」으로 남겼다.
  - `ai/CLAUDE.md` — 모듈 표에 **`call_guard`(C-6)·`voice_signal`(D-5)** 가 통째로 빠져 있었다 · 지식베이스 「조항 20개」 → **98개**(76·20·2) · `A-3` → `A-5` · §7 의 g4dn 전제에 **실물은 `t3.large`, 모델은 전용 GPU EC2**(`decisions/116`·`121`) 정정 · 「두 도메인인지」 질문은 09-03 에 답이 나온 것으로 닫음
  - 서버 주석 세 곳의 **「`.importlinter` 계약 5」는 없는 계약**이었다(계약은 넷) — 사실대로 고쳤다. `masking_provider.py` 의 「P6·P7 은 아직 없다」도 걷었다(규칙으로 부분 지원 중). `.importlinter` 의 「D-4 는 postcall 에 붙일 예정」 → hub `knowledge_gap_*` 로 구현됨. `server/CLAUDE.md` 「계약 3종」 → 4종
  - 콜 미디에이터 README — 알림 셋 「꺼 두었다」 → **09-15 부터 켜져 있다** · 「화자 분리는 하지 않는다」 → `speaker=auto` 로 한다(같은 파일 안에서 모순이었다) · 환경변수 「`.env.example` 에 아직 없다」 → 있다
  - `docs/architecture.md`(스포크 0개·501) · `docs/harness.md`(테스트 수 — **server 1,058 · ai 421 · 계약 4+3** 다시 셈) · 런북 머리말(**인스턴스 정정 네 번째**: 9-2·11·14·20·21장은 실물에 해당 없음) · 런북 19장(11번은 같은 날 뒤 세션에서 했다) · `infra/CLAUDE.md` §3(클론 주소는 `SeongYuna` 가 맞다) · `golden-set/README.md`(`f2_case` 0건) · `decisions/208` **상태줄만**(`121` 이 방향을 바꿨다 — 본문 그대로) · `plan.md` 수정 이력(09-17 개명 누락분)
- 확인: `cd server && pytest` 종료 코드 0 · 구조 계약 **4 kept**. 주석·문서만 바꿨다.
- ⚠ **`server/`·`ai/` 아래 파일을 고쳤으므로 PR 에서 `tag-check` 가 server 태그를 올리라고 한다**(`0.1.19` → `0.1.20`). 주석뿐이지만 게이트는 경로로 판정한다. 콜 미디에이터 `src/config.ts` 의 같은 주석은 **그래서 되돌렸다**(README 에만 반영) — 주석 한 줄로 미디에이터 이미지를 다시 굽지 않으려는 것이다.
- 남은 것: 고치지 않고 미결로 올린 것 여섯 — 개발기간(10-27) vs 8스프린트 종료(10-14) · `/close`·카드 피드백에 상담원 토큰 가드가 없다 · 새 클러스터에서 `CORE_API_TOKEN` 이 비는지 · 하네스가 부르지 않는 지표 셋 · D-5 저장소 미배선 · 허브가 스포크를 직접 import 하는 것을 계약으로 막을지. `jekyll/docs/14`(08-28 시점 문서, `A-3` 표기)는 그때의 기록이라 두었다.
