---
date: 2026-09-15
author: "정성윤"
person: seongyun
seq: 2
---

- **배포 확인용 프로브 `GET /admin/auth/test` 를 넣었다** ([티켓](/backlog/w4-swagger-deploy-probe/)).
  목적은 기능이 아니라 **경로 확인**이다 — `server/` 를 고쳐 main 에 머지하면 운영의
  `/docs`·`/openapi.json` 이 실제로 바뀌는지 한 번 완주해 본다. 인증·DB·Redis 를 타지 않는다.
- **머지 전 운영 상태를 먼저 찍어 뒀다** — `/openapi.json` 경로 **27개**, `/admin/auth/*` 는 넷
  (`google`·`refresh`·`logout`·`me`). `/health` 의 spokes 는 여섯(`masking`·`closure_gate`·
  `retrieval`·`trigger`·`call_guard`·`uploads`). **비교 대상이 없으면 «바뀌었다»를 말할 수 없다.**
  ⚠ 이 값으로 `STATE.md` 의 「이미지 태그 server 0.1.4」가 낡은 것도 드러났다 — 운영에는 이미
  `uploads`·관리자 로그인이 떠 있으니 `0.1.6` 이다. 해당 줄을 덮어썼다.
- **이미지 태그를 같은 커밋에서 올렸다**(`0.1.6` → `0.1.7`). 파드가 `imagePullPolicy: IfNotPresent`
  라 태그를 그대로 두면 **배포는 초록인데 노드가 캐시된 옛 이미지를 계속 쓴다.**
  `scripts/check_release_tags.py` 로 미리 돌려 확인했다 — `0.1.7` 레지스트리에 없음 · 코드 변경 있음 → 통과.
- **테스트는 «스웨거에 실리는가»까지 본다** (`test_auth_probe_router.py`, 3건).
  라우트가 200 을 돌려주는 것과 `/openapi.json` 에 공표되는 것은 다른 문제다
  (`include_in_schema=False` 면 앞은 통과하고 뒤는 실패한다) — 이번 작업의 목적이 뒤쪽이라 따로 고정했다.
  응답 말단은 전부 문자열이고 비밀은 싣지 않는다(7.3절 계약 · SEC-2).
- 확인: `cd server && pytest` **667 passed · 3 skipped** · `lint-imports` **계약 4종 KEPT**.
- 남은 것:
  - **머지 후 `/openapi.json` 대조** — 경로가 27 → 28 이 되고 `/admin/auth/test` 가 보이면 완주다.
    안 보이면 볼 곳은 셋이다: `release.yml` 의 `paths` 필터 · `plan` job 의 태그 판정 · SSM 적용 로그.
  - **프로브를 걷어내는 시점** — 임시 엔드포인트다. 걷어낼 다섯 곳을 티켓에 적어 뒀고,
    상시 유지할지 지울지는 [미결](/open-items/)에 올렸다. **이름이 `/test` 인 채로 남기지 않는다.**
  - **어느 이미지가 도는지 응답으로 알 방법이 아직 없다** — `marker` 는 손으로 올리는 값이고
    `app.version` 은 `0.1.0` 에 고정돼 있다. 빌드 태그 주입은 미결에 적었다.
