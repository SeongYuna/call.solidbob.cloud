---
date: 2026-09-16
author: "류준"
person: ryujun
seq: 1
---

- **합성 통화 대본 `dasan-v0` 10건 초안을 만들었다** — 상담원·고객 페르소나가 실시간 대화하는 것처럼 보이게 하자는 논의에서 출발했다.
  대본은 미리 만들어 고정하고 게이트웨이 `WS /dev/text` 로 재생하는 구조를 권고했다 — 턴마다 즉석 생성하면 재현이 안 되고(절대 원칙 4) 운영 T4 를 생성 모델과 나눠 쓰게 된다.
  `ai/apps/persona_sim/scenarios/` — 페르소나(상담원 3 · 고객 9 · 톤 5) · 대본 10건(짧음 3 · 중간 5 · 긺 2) · 생성기 `generate_dasan_v0.py`
- **무엇을 담았나** — 블랙리스트 요청(SYN-006) · 같은 발신 번호 재인입 베테랑 배정(SYN-007) · 폭발 후 진정해 요청 안 함(SYN-005) ·
  욕설 없이 톤만 튀는 대조군(SYN-008) · distress 는 블랙리스트 사유가 아님(SYN-009) · 정상 대조군(SYN-001·010).
  라벨 합계 PII P1 2·P2 1·P3 1·P4 5·P5 2·P6 9·P7 4 · 콜 가드 insult 4·threat 2·sexual 3·distress 1 · 위반 C-1 2·C-2 1·C-3 1 — **골든셋에서 0건이던 P1·P2·P3·P5 를 채운다**
- **라벨은 시나리오 명세에서 왔고, 생성기가 라벨 문구가 발화에 글자 그대로 있는지 검사한다**(첫 실행 통과). LLM 채점이 아니다
- **아무것도 측정하지 않았다.** 이 대본은 STT 를 거치지 않아 검색·마스킹 수치는 상한이고, `tone` 은 TTS 연기 지시라 D-5 성능 근거가 못 된다 — README 「말할 수 없는 것」에 적었다
- **비용 논의(추정, 실측 아님)** — 대본을 Claude Code 안에서 만들고 재생에서 STT 를 안 부르면 추가 청구 0. API(Sonnet 5)로 만들면 100건 약 $5 추정
- **팀 방향 메모(류준, 확정 아님)** — 「모델을 따로 학습시키지 않고 데이터는 S3 에 올려 정확도 측정용으로만 쓸 것 같다」. 이게 확정되면 칸반 메모 「모델 학습(정성윤 · 개인 PC) 후 진행」 2건과 파인튜닝 전제인 `w5-classifier-ner-benchmark` 를 고쳐야 한다 — [미결](/open-items/)에 적었다
- ⚠ 이 머신에서 `git` 이 Xcode 라이선스 미동의로 막혀(`xcodebuild -license`) 변경 목록을 git 으로 확인하지 못했다
- 남은 것: 사람 검토 · 결정 기록 `2xx` · 재생기 · 하네스 `source: synthetic` 분리 집계 — [w5-persona-sim-scripts](/backlog/w5-persona-sim-scripts/)
