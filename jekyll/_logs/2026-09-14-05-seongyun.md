---
date: 2026-09-14
author: "정성윤"
person: seongyun
seq: 5
---

- **「고쳐야 할 것」이 실제로 코드에 들어갔는지 전수 대조하고, 빠진 칸반을 채웠다.** 대조 기준은
  [미결 항목](/open-items/)·`STATE.md`「막혀 있는 것」·결정 기록이고, 확인은 문서가 아니라 `origin/main`·`origin/ai` 의
  **코드**로 했다.
  - **반영됨(main)**: 트리거 도착 시각 `received_at_ms`(게이트웨이 → 서버 DTO → `IsFinalTrigger` 3단 전부) ·
    ES 연결 실패 500→503 · 「검색 중」·통화 시작 §7.3 반영 · 모노 화자 분리 `speaker=auto`(`decisions/303`) ·
    `?gateway=` 허용 목록 · 관리자 구글 로그인 코드 · 블랙리스트 재범 분류 · 재상담 이력 카드 · 전사 복합키 UPSERT.
    **티켓도 전부 있다** — 09-14 에 장민석·조서희가 `w4-` 8건을 만들어 뒀다(`w4-trigger-arrival-time` 등).
  - **코드는 있는데 main 에 없다**: C-6·D-5 저장 + STT 오류 주입기 — `origin/ai` 가 **3커밋 앞서 있다**(미머지).
  - **여전히 구멍**: C-5 **P6·P7 NER**(`ai/` 에 NER 모듈 없음 — 절대 규칙 ❌ 그대로) ·
    **J 블랙리스트 서버 어댑터·라우터**(`server/apps/blacklist/adapter/outbound/` 가 `__init__.py` 뿐이고 hub 에 라우터 없음 —
    프론트 화면은 mock 위에서 돈다) · F-2 다산 규칙표. 셋 다 기존 티켓이 있어 새로 만들지 않았다.
- **새 티켓 2건 — 티켓이 아예 없던 것들이다.**
  - [w4-admin-auth-runtime](/backlog/w4-admin-auth-runtime/) — 관리자 로그인이 **운영에선 500** 이다.
    Redis 매니페스트 0건 · `server-env` 에 키 3종 없음 · RDS 에 테이블 2개 없음. 앱 쪽 티켓(`w4-admin-google-login`,
    조서희)과 갈래가 다르다. ⚠ **`/health` 는 정상이라 배포 판정으로는 안 드러난다** — 눌러 보기 전에는 모른다.
  - [w4-aws-resource-hygiene](/backlog/w4-aws-resource-hygiene/) — RDS 권장 사항 2건 · Enhanced Monitoring ·
    7월 잔재 VPC·보안그룹(01 기록의 「남은 것」).
- **중복 티켓 하나를 만들었다가 지웠다.** 태그 게이트 티켓(`w4-release-tag-guard`)을 쓴 뒤,
  **같은 트리의 다른 세션이 먼저 `w4-release-tag-gate` 를 만들어 코드까지 끝낸 것**을 보고 내 것을 지웠다
  (§4 「먼저 손댄 쪽이 산다」). 세션 중에 `tag-check.yml`·`decisions/111` 이 워킹 트리에 나타나 알았다.
- **내 진행 중 티켓 3건의 완료 조건을 실물에 맞췄다** — 체크만 옮긴 게 아니라 **틀어진 것을 적었다**.
  - `w3-cicd-release-pipeline`: 첫 배포 실검증 · 부팅 자동 수렴 systemd ✅. 예약해 둔 결정 기록 번호 `108` 은
    운영 DB 이관이, `111` 은 태그 게이트가 가져갔다 — **파이프라인 자체의 결정 기록은 아직 없다.**
  - `w3-k3s-image-and-manifests`: ES 인덱스 적재 ✅(09-11, 98조항 · green).
  - `w3-aws-deploy`: `terraform apply` 폐기 · `server` 레코드 · RDS 스키마 · `/health` ✅.
    **완료 조건의 뒤쪽 절반(`destroy` → `apply` 재현)이 콘솔 전환으로 없어진 것**을 지우지 않고 적어 뒀다(절대 원칙 8).
    **자리표시자 `ai` 레코드는 아직 살아 있다** — 실측 `ai.solidbob.cloud` → `216.198.79.1`(apex·Vercel).
    `decisions/105` 로 문서 서술만 지웠지 DNS 는 안 지웠다.
- **남의 티켓은 건드리지 않았다.** `w3-api-tunnel-handoff`(장민석, in-progress)는 09-11 엔드투엔드 성공·09-14 게이트웨이
  502 복구까지 갔으니 완료로 보이지만, 내가 이어받은 일이 아니라 상태를 옮기지 않았다(§4) — 장민석 확인이 필요하다.
- 확인: `cd jekyll && bundle exec jekyll build` 통과, `/kanban/` 에 새 티켓 2건 렌더 확인.
- 남은 것: `origin/ai` 3커밋 머지 · 새 티켓 2건 착수 · `ai` DNS 레코드 삭제.
