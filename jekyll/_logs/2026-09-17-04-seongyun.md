---
date: 2026-09-17
author: "정성윤"
person: seongyun
seq: 4
---

- **개명 브랜치에 main 을 받아 개명을 다시 얹었다** — 개명 작업(`227d529`)을 하는 사이 main 이 17건 앞서 나갔다(PR #98 류준 · PR #99 조서희 등). 두 분의 작업을 **먼저 머지하고 그 위에 개명을 얹는 순서**를 택했다 — 그래야 두 분이 각자 브랜치에서 디렉터리 이동·식별자 충돌을 풀지 않고 main 을 당겨 받기만 하면 된다. 개명이 스크립트라 다시 돌리는 비용이 작았다.
- **충돌 9건** — 내용 6(`STATE.md` · `open-items` · `kustomization.yaml` · `apps/call/src/App.tsx` · 미디에이터 `README.md`·`ws_server.ts`) + 위치 3(main 이 `services/gateway/` 아래 새로 넣은 합성 통화 재생기·테스트 — git 이 개명된 디렉터리로 옮길 것을 제안했고 그대로 받았다). **내 쪽 변경이 기계적 치환뿐인 파일은 main 쪽 내용을 받고 스크립트를 다시 돌렸다**(병합이 건드린 72개 파일만 대상, 치환 241회). 손으로 쓴 것 둘 — `STATE.md` 인프라 줄과 미결 항목 두 절 — 은 커밋에서 꺼내 그대로 되돌려 넣었다. main 은 인프라 줄을 안 건드렸다.
- **태그 정정 (세 번째 기록의 `0.1.14` 는 버린 값이다)** — 그 번호는 PR #98 이 이미 가져갔고, 이어 `0.1.15`(류준)·`0.1.16`(조서희 PR)이 찼다. 개명 브랜치의 server 는 **`0.1.17`**(레지스트리 404 · 다른 브랜치 미사용 확인), call-mediator 는 `0.2.0` 그대로. 결정 기록 115 의 숫자도 고쳤다.
- **조서희 님 PR #99 의 `tag-check` 실패를 풀었다** — main 이 `0.1.15` 를 구운 뒤였는데 `frontend` 가 서버 파일 15개를 고치고도 태그가 `0.1.15` 그대로였다. 사용자 지시로 `frontend` 브랜치에 server `0.1.16` 한 줄 커밋(`3c1a884`)을 별도 워크트리에서 만들어 푸시했다. 같은 스크립트로 통과를 먼저 확인했다. ⚠ 남의 브랜치에 올린 것이라 조서희 님은 다음 작업 전에 pull 해야 한다.
- **Docker Hub 레포가 생겼다** — 로컬 태그 검사에서 `callguard-call-mediator:0.2.0` 이 401(판정 불가)이 아니라 **404(없음)로 바뀌었다.** 전환 순서 1번이 끝난 것이다(티켓 체크).
- 검증(병합 뒤 전부 다시): call-mediator 타입 검사 · 테스트 **107/107**(재생기 테스트 6건 포함) · `apps/call`·`apps/platform` 빌드 · server·ai pytest · import 계약 4+3종 kept · 워크플로·매니페스트 파싱 · 사이트 빌드 + 링크 깨짐 0 · 태그 검사 통과(server `0.1.17` 새 태그 · call-mediator `0.2.0` 새 태그).
- 남은 것: **푸시·PR 은 아직이다.** PR 을 열면 곧바로 main 룰셋 필수 검사를 `gateway` → `call-mediator` 로 바꿔야 머지가 열린다. 그 앞에 EC2 시크릿 복사 · Vercel 새 변수 추가, 뒤에 헬스 확인 · 옛 오브젝트·옛 변수 삭제 · `.env` 키 변경(런북 19-1 「개명 전환」).
