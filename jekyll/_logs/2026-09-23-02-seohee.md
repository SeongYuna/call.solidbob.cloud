---
date: 2026-09-23
author: "조서희"
person: seohee
seq: 2
---

- 09-22 QA 결과표를 확인한 뒤 만들어진 칸반 열린 티켓 17건(조서희 몫)을 sprint·의존성
  기준으로 순서를 정해 정리했다 — 그중 5·6번 두 개를 처리했다.
- **w6-platform-unmeasured-numbers (done)** — `apps/platform`의 `PrivacySection.tsx` `STATS`
  세 항목(「38% 처리시간 단축」·「92% 정서 위기 감지」·「5개 언어 지원」)이 전부 실측한 적
  없는 값이었다. 그중 둘(C-6·D-1)은 지금 측정 자체가 없어 숫자를 지어내는 대신, 이미
  실측이 끝난 다른 세 값으로 교체했다 — 「0건 개인정보 마스킹 누락」(P1~P7, n=28,
  `e966b62`)·「100% 필요서류 판정 정확도」(n=99, `e966b62`, "팀 규정 기준" 단서)·
  「96.9% 검색 정확도 Recall@5」(n=96, run_id 7, `cc572b4`). 각주도 "디자인 목업입니다"에서
  실제 출처를 적는 문장으로 바꿨다. `#stats` 내비게이션 앵커·3열 레이아웃은 안 건드림.
  `tsc --noEmit`·`vite build` 통과 — 정적 카피 변경이라 운영 재확인 없이 `done`.
- **w6-frontend-tests-vitest (done)** — `apps/call`에 테스트가 0건이었다. `vitest ^5.0.1`
  devDependency 추가, `package.json`에 `"test": "vitest run"`. `apps/call/test/realCallMediatorClient.test.ts`에
  WS 파서 5종(자막·추천·컴플라이언스·종료 + 오늘 추가한 started) 11개 테스트 — 표본은
  §7.3 계약대로 숫자·불린을 문자열로 넣고(`"true"`·`"1500"`) 파서가 제대로 변환하는지,
  계약을 어긴 표본(불린이 boolean·verdict가 옛 `approved`)은 조용히 안 깨지고 `null`을
  돌려주는지 확인한다. `npm test` 11/11 통과. CI 잡·룰셋 추가는 정성윤 몫으로 남겨 뒀다
  (티켓이 애초에 그렇게 갈라 뒀다).
- 남은 것: 칸반 순서 7번(`w6-admin-agent-hired-on-ui`)부터 계속. `w6-read-path-token-ui`는
  의존 티켓(`w6-read-path-auth`, 장민석)이 아직 in-progress라 대기.
