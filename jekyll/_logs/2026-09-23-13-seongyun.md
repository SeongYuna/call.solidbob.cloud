---
date: 2026-09-23
author: "정성윤"
person: seongyun
seq: 13
---

- **장민석 님 몫 셋을 이어서 했다**(`decisions/302` — `server/` 는 누구나 본다. 이견 있으면 말해 주면 된다).
- **`agent.role` 인증 없음 — 09-09 의 위험은 이미 사라져 있었다.** J-4 결정 API 는 `require_admin` 을 지나고
  요청 본문에 `role` 필드가 없다. 역할은 저장소가 `role=ROLE_ADMIN` 으로 박아 넘긴다 — 호출자가 주장할 자리가 없다.
  ⚠ 그래서 `transitions.py` 의 역할 검사는 **런타임에 절대 실패하지 않는다**(도메인 보호막으로만 남는다) —
  「강제된다」의 근거는 가드지 그 검사가 아니다. 그 사실까지 적고 닫았다.
- **09-22 류준이 고친 `server/` 사후 검토 — 셋 다 통과.** ① 이름 끝 「이」 처리는 원칙 3(애매하면 가린다)과 방향이 같고
  근거가 주석에 남아 있다(⚠ `_FOREIGN_SURNAMES_1` 하드코딩 목록은 표본 밖 성씨에 약하다 — 한계로 함께 적는다)
  ② `EXCLUDED` 의 `4.23` 은 `decisions/217` 근거대로 **판정하지 않는다** ③ `_MODULE_ID` 두 줄은 다른 행과 같은 규칙이다.
- **D-1 요약을 고치고 같은 하네스로 다시 쟀다.** 발췌에 세 줄을 더했다 — 필요서류 안내(최대 3) · 후속 조치(최대 5) ·
  마무리 안내. **전부 자막에 있는 문장 그대로**라 지어낸 문장은 여전히 0이다.
  **핵심 항목 21/60 → 53/60**(micro 0.35 → **0.883**) · **서류 2/23 → 23/23 · 조치 1/10 → 9/10 · 요청 5/7 → 7/7** ·
  문의 10/10 유지 · 요약 길이 중앙값 160 → 373자. 측정: `scripts/run_eval.py --no-ner`, `postcall-v1.json` 10통화 60항목.
  ⚠ **「안내」는 4/10 로 남았다** — 못 잡은 것은 「소관 부서 심사로 정해짐」처럼 **여러 발화에 흩어진 설명**이라
  한 줄 발췌로는 안 된다. 생성 요약이 붙을 때 다시 잰다.
- 검증: `server` 전량 초록 · 계약 5종 KEPT · `ai` 전량 초록. 미결 96 → **93**. 커밋하지 않았다.
