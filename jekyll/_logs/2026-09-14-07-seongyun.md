---
date: 2026-09-14
author: "정성윤"
person: seongyun
seq: 7
---

- **PM 을 main 과 동기화했다(로컬만, 푸시 안 함).** 시작 시점에 PM 은 main 보다 **18 커밋 뒤·2 앞**이었고,
  워킹 트리에 커밋되지 않은 A-6 업로드·태그 게이트 작업이 남아 있어 `git merge` 가 곧바로 거부됐다
  (겹치는 파일 9개). **먼저 커밋하고 병합**하는 쪽을 택했다(사용자 확인) — 공유 워크트리에서
  stash 를 쓰면 pop 충돌과 다른 세션의 동시 편집이 겹친다.
- **커밋 3건으로 갈랐다** — `code(release)` 태그 게이트(`decisions/111`) · `code(upload)` A-6
  S3 보관(`decisions/110`) · `log(w4)` 09-14 기록 5건 + 런북 3차 정정 + STATE + 미결.
  한 덩어리로 넣지 않은 이유는 되돌릴 때 갈라져야 하기 때문이다.
- **병합을 두 번 했다 — 도중에 main 이 두 번 움직였다.** 첫 병합은 `379483f`(PR #78, `ai`),
  두 번째는 `1956c02`(PR #79, `server` — J 블랙리스트·통화 목록·고객 식별 HMAC·F-2 체크리스트)까지 받았다.
  결과: **PM 은 main 대비 0 behind · 7 ahead.** 로컬 `main` 브랜치도 `1956c02` 로 맞춰 뒀다.
- **충돌 9건은 전부 「양쪽이 끝에 덧붙인」 모양이라 둘 다 살렸다** — 09-14-06 기록의 예측 그대로였다.
  - `server/core/config.py`·`.importlinter`·`requirements.txt`·`test_connection.py`:
    A-6(`s3_bucket`·`upload_token`·`boto3`) + 관리자 로그인(`admin_jwt_secret`·`redis_url`·`jwt`·`redis`) 병존
  - `server/main.py`: `_wire_uploads`(A-6) 와 `_wire_call_guard`(C-6) 가 같은 자리에 들어왔다.
    둘 다 두고 `lifespan` 배선 튜플을 **넷**으로 폈다 — 한쪽만 살렸으면 `/health` 스포크가 조용히 빠졌을 자리다.
  - `infra/k8s/base/secret.example.yaml`: `UPLOAD_*` 넷 + `CUSTOMER_REF_HMAC_KEY` 병존
  - `_project/STATE.md`: **줄 단위로 갈랐다.** PM 은 「인프라·CI」줄만, main 은 나머지 셋만 갱신돼 있어
    각자 새 쪽을 골랐다. 인프라 줄에 남아 있던 `apps/dashboard` 는 main 의 개명(`apps/call`)에 맞췄다.
- ⚠ **`newTag` 를 `0.1.6` 으로 유지했다.** main 은 `0.1.5`(PR #79), 그 전에 `0.1.4` 가 배포돼 있다.
  오늘 하루에 태그 충돌이 **세 번째**다(`0.1.4`·`0.1.5`·`0.1.6`) — `decisions/111` ⑥ 의 교차 경고가
  잡아내라고 만든 바로 그 모양인데, **아직 룰셋 필수 검사가 아니라 막지는 못한다.**
- 확인: `server` 664 passed · `ai` 225 passed · 계약 **7종 전부 KEPT** · `jekyll build` 통과.
- 남은 것:
  - **푸시·PR 은 하지 않았다** — 사용자 요청이 로컬 반영까지였다. `origin/PM` 은 아직 `ecb395b`.
  - 배포하려면 `0.1.6` 이 필요하다: 버킷 CORS · `server-env` 에 `S3_BUCKET`·`UPLOAD_TOKEN`·`CUSTOMER_REF_HMAC_KEY`.
  - 룰셋에 `tag-check`·`gateway` 추가(콘솔, admin 몫) — 태그 충돌을 PR 단계에서 막는 유일한 경로다.
