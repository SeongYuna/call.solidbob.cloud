---
date: 2026-09-10
author: "조서희"
person: seohee
seq: 2
---

- `main`(류준 `ai` 브랜치 병합분)을 `frontend`로 가져왔다. `apps/dashboard/src/store/callStore.ts`·
  `CallSummaryPanel.tsx` 두 곳에서 충돌 — 방금 만든 `BlackConsumerAction`(고객 분류 카드)과
  J 블록의 `BlacklistRequestButton`(블랙리스트 전환 요청)이 같은 자리에 나란히 추가돼서다.
  **양쪽 추가분을 모두 유지**하는 방향으로 풀었다(어느 쪽도 지우지 않음). `tsc --noEmit` 통과 확인.
- J 블록을 09-09 스키마 QA(`decisions/205`)와 대조하다가 프론트 타입 불일치 2건을 찾아 고쳤다
  (`w4-blacklist-request-flow` 완료 조건에 걸리는 부분, 티켓은 류준 담당이라 상태는 안 건드림):
  - `BlacklistEntryItem.display_hint` — DB `blacklist_entry` 테이블엔 이 컬럼이 없다.
    표시 힌트는 `blacklist_request`에만 있어, `request_id`로 원 요청을 찾아 붙이도록
    `AdminBlacklistPanel.tsx`에 `entryDisplayHint()` 헬퍼를 추가했다.
  - `BlacklistRequestItem.evidence_snapshot_at` — 스키마에 있는데 프론트 타입에 빠져 있어 추가.
    `callStore.ts`의 `submitBlacklistRequest`에서 채운다.
- 남은 것: `BlackConsumerAction`(즉시 분류) vs J 블록(승인 게이트 있는 전환 요청)이 의미상
  겹친다는 건 [미결 항목](/open-items/)에 이미 적어뒀다 — 이번 세션에서는 그대로 뒀다.
  `CallGuardFlag.category` 한글 3종 vs 백엔드 4종(`distress` 포함) 계약 불일치도 아직 안 풀림
  (`w4-blacklist-request-flow` 티켓에 이미 적혀 있음).
