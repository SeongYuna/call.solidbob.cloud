---
date: 2026-09-23
author: "조서희"
person: seohee
seq: 7
---

- **w6-qa-call-screen-fixes (in-progress)** — 칸반 순서 11번, QA 결함 묶음 9개.
  - ❌(시연 전) 셋: **Q-47은 이미 고쳐져 있었다**(`StandbyBackButton.tsx`에 `ConfirmDialog`가
    정확히 요구된 그대로 있음, 코드 변경 없음). **Q-48**은 `useAgentCallSession.ts`의
    `no-token`·`unsupported`에도 `errorMessage`를 채우고, `App.tsx`에 별도 배너를 추가해
    `AgentCallBox`가 사라져도 안내가 남게 했다. **(화면)**은 `AgentCallBox.tsx`에 「닫기」
    버튼과 자막 도착 시 자동 숨김을 추가했다.
  - ⚠(되도록) 여섯: Q-54는 09-22에 이미 고침. Q-11은 정성윤이 서버에서 고침. Q-04는
    의도(내가 넣음). **Q-63**은 절반만 — 실제론 "등록일부터"가 아니라 "지금부터"인데
    입력칸이 전역 기본값으로 항상 채워져 있어 손 안 대고 눌러도 만료일이 바뀌는 게
    진짜 원인이었다 → 결과 날짜 미리보기 추가(CSS 찌그러짐은 못 고침, 브라우저 없음).
    **후속조치 빈 항목**은 프론트 송신 쪽은 이미 필터링돼 있음을 확인(서버 쪽은 확인
    불가). **Q-29·45(보호 알림 배너)**는 실서버 `CallGuardFlag`에 매칭 텍스트가 없어
    `CustomerRiskBanner`를 못 채운다 — 설계 결정이 필요해 미룸.
  - (시연) 재생 전 화면 종료 불가 항목은 운영 절차 결정이 먼저 필요해 손 안 댐.
  - 검증: `apps/call`(`tsc --noEmit`·`vite build`·`npm test` 11/11), `apps/admin`
    (`tsc --noEmit`·`vite build`) 통과. 운영 재확인 없어 `in-progress`.
- 칸반 5~11번 오늘 세션 완료. 커밋·푸시 예정.
