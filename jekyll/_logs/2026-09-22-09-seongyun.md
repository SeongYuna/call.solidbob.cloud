---
date: 2026-09-22
author: "정성윤"
person: seongyun
seq: 9
---

- **류준 님 답장(부탁 셋)을 받아 닫은 것** — `w5-models-on-node` ①·⑥·⑦ 체크, `STATE.md` 「현재」의 「운영 값은 아직 없다」를 run_id 3·4 로 덮어씀.
  - **모델 해시 대조 끝**: NER 은 HF 커밋까지 같다. KoE5 는 커밋이 다르지만(운영 `237b580` · 류준 `bc6d284`) HF tree API 로 두 커밋의 파일 해시를 대조하니 **README.md 하나만 다르다** — 가중치·설정 동일. 운영 모델 = 측정 모델
  - **3,300ms 예열 확인**: `0.1.31` 새 파드(검색 0건)에 `POST /hub/search` 를 서로 다른 질문 셋으로 보냈다 — **0.58 / 0.58 / 0.62s**, 같은 질문 반복 **0.05s**(캐시). 1순위 셋 다 정답(2.12·2.6·4.4). **단순 예열로는 재현되지 않았다** — 서버가 요청별 지연을 로그에 안 남기고 그 파드도 교체돼 SYN-010 의 3,300 은 원인 미해결로 남긴다. 임베딩은 켜 둔다(124 ⑥ 은 p95 기준)
  - 절차 고르기 규칙(「규칙 없는 조항이 1순위면 판정하지 않는다」)은 09-22 답장에서 동의한 그대로 — 미디에이터 코드라 류준 님 PR(태그 오름). PR #121 에는 안 들어왔다
- 남은 것: 미디에이터가 `retrieval_ms` 를 `/ws` 로 중계하지 않는다(류준 확인) → `w7-latency-budget` 수집 경로에 얹는다 · `eval_run` 구성 컬럼 없음(류준 로그) · 절차 고르기 규칙 PR(류준) · 재생기 마지막 턴 유실(류준) · 두 합성 통화는 시연용으로 남길지 정할 때까지 그대로
