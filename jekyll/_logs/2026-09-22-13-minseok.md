---
date: 2026-09-22
author: "장민석"
person: minseok
seq: 13
---

- **남은 일 한 번에 — 막히는 곳은 AI 페르소나 대화가 도는 완성본을 기준으로 예측해 먼저 만들었다**(사용자 지시). 가정은 결정 기록에 적었다 — 틀리면 다음에 고친다
- **① C-5 중간 자막 번호 앞자리** — `decisions/324`, `w6-c5-interim-number-mask`. 가설이 코드로 맞았다: 확정 규칙은 완성된 번호(10~16자리)만 잡는다. 중간 자막에만 `InterimMaskingAdapter` 를 얹어 3자리 이상 이어진 숫자(낭독형 포함)를 가린다 — 확정·DB·골든셋 채점은 그대로. NER 이 꽂혀도 그 위에 얹힌다(`get_masking_port` 교체 방식)
  - 내 테스트 예문(「3층 301호」)이 틀렸었다 — 확정 규칙도 P7 로 가리는 게 맞았다. 금액 예문으로 바꿨다
- **② 읽기 경로 문** — `decisions/322`, `w6-read-path-auth`. 부르는 쪽을 코드로 확인하고 갈랐다: 상담원 화면 읽기 넷은 **토큰을 안 싣는다** → `120` 처럼 두 단계(지금은 받기만, 틀리면 401 · `READ_AUTH_REQUIRED=true` 로 닫음 · `/health` `read_guard`). 관리자 화면은 이미 관리자 토큰을 싣는다 → 지식 공백 조회·상태 변경은 **바로 닫음**. 신고는 부르는 곳이 없어 상담원 토큰으로 바로 닫고 **설명을 마스킹 후 저장**. `e2e_check.py` 가 서비스 토큰을 싣는다. 조서희 님께 `w6-read-path-token-ui`(담당 «제안»)
- **③ D-2 문의 유형** — `decisions/323`, `w6-d2-inquiry-type-null`(done). 지식베이스 장(대중교통·상하수도·일반행정·감염병·재난·생계 지원금) 어휘 규칙표. 안 걸리면 **「미분류」**(티켓 조건 — NULL 은 「처리 전」) · `e2e_check.py` 가 NULL 을 ❌ 로. **측정 안 됨**(D 케이스 0건)
- **④ F-2 `blocked→incomplete`** — 이미 09-14(`305`)에 되어 있었다. 티켓이 낡아 있었고 `125` 로 남은 일은 발표 문구뿐 — 인용 규칙대로 적고 `w8-f2-wrapup` done. 평가 지표 테스트 픽스처의 옛 값만 맞췄다
- **J-5 시연 준비** — `seed_demo_agents.py`(페르소나 A01~A06 → `demo-*` 행 + 입사일, 토큰 즉시 폐기) · 매니페스트 `ROUTING_CANDIDATES` · 런북 12-2-c
- **정리** — 미결 7건 닫음(08-27 규칙 완화 · 포트 간극 · 검토 요청 4건 — `w5-server-change-review` 범위 · 쓰기 경로 인증) · `w6-open-write-guards` done(프론트가 이미 토큰을 싣는 것 확인)
- 검증: server **1,356 passed** · import 계약 4 kept · ai 464 passed · persona_sim 22 passed
- 남은 것: PR #132 머지 → 운영에서 ① 중간 자막 표본 ② 시드 스크립트 · `routing_log` ③ 조서희 님 토큰 뒤 `READ_AUTH_REQUIRED=true`
- **main 병합 중 티켓 중복을 정리했다** — 정성윤 님 #133(09-22 저녁 QA 티켓 19건)에 같은 일이 먼저 있었다: `w6-read-path-auth` · `w6-c5-interim-number-mask` · `w6-d2-inquiry-type-null` · `w6-read-path-token-ui`(조서희). **먼저 만들어진 쪽을 살리고** 내가 만든 넷(`w6-read-path-auth` 같은 이름 · `w6-c5-interim-mask` · `w6-d2-inquiry-type` · `w6-call-read-token`)은 지우고 진행을 옮겼다. `w6-routing-candidates-prod` 에도 시드 진행을 적었다
- 새로 내 몫으로 온 셋(`w6-admin-stats-today` · `w6-call-record-customer-id` · `w6-server-loose-ends`)은 이 기록 뒤에 본다
