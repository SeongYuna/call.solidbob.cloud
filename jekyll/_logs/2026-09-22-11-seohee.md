---
date: 2026-09-22
author: "조서희"
person: seohee
seq: 11
---

- `apps/call/src/mock/customerHistory.ts:15-23`의 낡은 주석을 정정했다(직전 조사
  결과 반영, 코드 동작은 손대지 않음 — 주석만).
- 정정 내용: "customer_id는 스키마에만 있고 어떤 경로도 채우지 않는다"는 더
  이상 사실이 아니다 — `GET /hub/calls?customer_id=...`로 과거 통화 이력을
  조회하는 기능은 `call_list_router.py`에 이미 있다. 지금 막힌 지점은 하나뿐:
  브라우저가 "지금 통화 중인 고객"의 `customer_id` 값을 알 방법이 없다
  (`POST /hub/calls` 응답은 `customer_linked: bool`만 주고 실제 HMAC은 안
  줌 — 평문 전화번호를 브라우저에 안 보내려는 의도된 설계. `/record`·
  `/transcript`·WS 이벤트 어디에도 `customer_id` 필드 없음). 이 연결이
  생기면(서버가 현재 통화의 customer_id를 알려줄 작은 엔드포인트/필드) 이
  mock 카드를 실 API로 교체 가능하다.
- 빌드 확인: `cd apps/call && npm run build`(`tsc --noEmit && vite build`) 통과 —
  번들 해시가 직전 빌드와 동일해(`index-BgbDpu9T.js`) 런타임 출력이 안 바뀐
  것도 확인했다.
- 이번 파일 외 다른 파일은 건드리지 않았다.
