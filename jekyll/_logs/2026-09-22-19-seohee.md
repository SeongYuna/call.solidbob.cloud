---
date: 2026-09-22
author: "조서희"
person: seohee
seq: 19
---

- 방금 만든 J-5 콜 미디에이터 배선을 **같은 세션에서 되돌렸다** — "백엔드는 일단
  돌려놔" 지시. 커밋 전이라 `git checkout --`으로 `services/call-mediator/`의
  8개 파일(`ports.ts`·`hub_http.ts`·`call_registry.ts`·`config.ts`·`main.ts`·
  테스트 3개)을 깔끔하게 원상복구했다 — `npm run typecheck` 클린,
  `npm test` **135/135**(J-5 추가 전 원래 개수 그대로) 확인.
- **프론트(`apps/call`)의 WS 파싱·스토어·화면 배너는 안 건드렸다** — 지시가
  "백엔드"만 짚었고, 콜 미디에이터가 이제 그 메시지를 안 보내니 무해하게
  잠들어 있을 뿐이다. 나중에 배선만 다시 켜면 바로 동작한다.
- **화면 문구는 되돌렸다** — `BlacklistRequestButton.tsx`(apps/call)·
  `EntriesTab.tsx`(apps/admin)에 방금 채워 넣은 "이후 통화부터 배정 판정이
  기록에 남습니다"는 백엔드가 없으면 다시 거짓이 되므로 같이 뺐다. 지시엔
  없었지만 이 프로젝트의 "측정 안 한 걸 측정한 것처럼 안 쓴다" 원칙과 바로
  어긋나서 판단해서 되돌렸다.
- `w7-j5-routing-caller.md` 티켓을 롤백 상태에 맞게 정정 — `status: in-progress`,
  `assignee: 정성윤`으로 되돌리고, 「구현(롤백됨)」 절에 시도한 내용을 그대로
  남겼다(절대 원칙 8 — 실패·중단한 시도를 지우지 않는다). 완료 조건에
  "콜 미디에이터가 실제로 판정을 부른다"를 미해결로 새로 추가했다.
- 빌드 재확인: `services/call-mediator`(typecheck·test 135/135),
  `apps/call`·`apps/admin`(각 `npm run build`) 전부 통과.
