---
date: 2026-09-22
author: "정성윤"
person: seongyun
seq: 18
---

- **수동 QA(`w5-manual-qa-full-stack`) 1일차 — 0장~3장.** 운영 주소 · 새 번들(`call index-4X0vR1JI` · admin `index-BVF9FZhk`) · server `0.1.35` · call-mediator `0.2.5`.
  판정은 정성윤이 화면에서 했다. 발화는 3장부터 스크립트로 넣었다(티켓 규칙 1 — 넣기만, 판정은 눈으로).

| # | 결과 | 메모 |
|---|---|---|
| Q-00a | ✅ | `/health`·`/health/ready`·`/call-mediator/health` 200, spokes 에 `pii_ner`·`retrieval_dense` |
| Q-00b | ✅ | `admin.solidbob.cloud` 구글 로그인 |
| Q-00c | ✅ | 입력 토큰 확보 |
| Q-00d | ✅ | 로그인 화면이 뜨고 통화 화면 헤더에 `REST` |
| Q-00e | ✅ | 창 둘(상담원 · 개발 페이지) 준비 |
| (준비) | ❌ → 고침 | 개발 페이지(`/call-mediator/dev`)가 1006. 콜 미디에이터 Origin 허용 목록에 `https://server.solidbob.cloud` 가 없어 403 이었다(토큰은 맞음 — 출처별로 붙어 보고 확인). 운영에 `set env` 로 넣었고 저장소는 로컬 커밋 `af3eac1`(미머지) — **머지 전에는 릴리스마다 되돌아간다**(16:57 PR #132 릴리스로 실제로 한 번 되돌아갔다) |
| Q-01 | ✅ | 현황판 |
| Q-02 | ✅ | 탭 7개 오류 없이 열림 |
| Q-03 | ✅ | `QA상담원1` 토큰 한 번만 보임 |
| Q-04 | ✅ ⚠ | 목록에 있음. 이름이 `Q****1`·`최*원` 으로 가려져 보인다 — 의도인지 조서희 님 확인 |
| Q-05 | ⚠ | 5분 넘게 두면 로그인이 풀린다 — 테스트 값(access 5분 · refresh 10분) 그대로. 운영 값은 `w7-demo-infra-readiness` 3번 |
| Q-10 | ✅ | 빈 칸 → 「토큰을 입력해 주세요.」 |
| Q-11 | ✅ ⚠ | `cga_wrong` → 「상담원 토큰이 없거나 폐기됐다 — 관리자에게 새 토큰을 받아라」. 다른 문구와 달리 **반말** → 조서희 |
| Q-12 | ✅ | 「안녕하세요, QA상담원1님」 |
| Q-13 | ✅ | 로그인 뒤 새로고침 — 유지 |
| Q-15 | ✅ | 로그아웃 → 로그인 화면 → 새 토큰으로 다시 로그인 |
| Q-14 | ⚠ | 목록은 뜬다. **새 상담원에게 남의 통화(16:25 SYN-010 등)가 보인다** → `w6-read-path-auth` |
| Q-20 | ✅(확인 필요) | 발화 넣기는 스크립트. 상담원 줄 |
| Q-21 | ✅ ⚠ | 초본 조항 1순위 · 「주민등록초본 발급」 절차. ⚠ Q-27 뒤 「수도 사용자 명의변경」이 절차로 잡혀 초본·수도가 발화마다 재판정 — `closures` 8건 전부 `incomplete`. 미결 「절차가 쌓이기만 한다」 · B-6 문턱 꺼짐의 운영 재현 |
| Q-22~Q-26 | ✅ | 확정 자막 전부 가려짐. 중간 자막 앞자리 노출은 캡처로 확인 못 함 |
| Q-27 | ✅ | 이름 `***` · 주소 전체 가려짐, 조각 없음 |
| Q-28 | ✅ | 한글 번호 줄 전체 가려짐 |
| Q-29·Q-30 | ✅ | 「콜가드」 배지 + 안내 문구. 위쪽 「상담원 보호 알림」 배너는 확인 필요 |
| Q-31~Q-33 | ❌ | 상담원 위반 세 줄에 경고가 없다 → `w6-compliance-alert-ui`(조서희, 화면이 서버 결과를 그리지 않음) |
| Q-34 | ✅ | 과탐지 없음 |
| (새) | ⚠ | 「⚠ 경고」 배지가 **고객의 개인정보 발화(Q-22~Q-28)** 에 붙는다 — 방향이 반대 → `w6-warning-badge-direction`(조서희, 새 티켓) |

| Q-40 | ✅ | `test-qa-03` `서류` 1/1 · `test-qa-05` 에서 Enter·Shift+Enter·Esc 확인 |
| Q-41 | ✅ | 「팝업창」 보기의 추천 카드 「사용 표시」 저장됨(필요서류 보기에는 버튼이 없다 — 처음엔 못 찾았다) |
| Q-42 | — | 누락 0개 절차 카드가 없어 버튼이 나오지 않는다(조건 미충족) |
| Q-43 | ❌ | 칩 둘(「주민등록초본 발급」·「수도 사용자 명의변경」)을 번갈아 누르면 **카드 제목과 서류 목록이 안 맞는다** — 초본의 「신분증」이 「3.4 수도요금 자동납부」 밑에, 명의변경 서류 셋이 「3.13 전자고지」 밑에 붙는다. 서버 판정(`closures` 4.3·3.5)은 맞다. `store/callStore.ts` `attachIndex` 가 판정을 **가장 최근 자동 카드**에 붙이고 조항 번호로 짝짓지 않는다 → `w6-closure-card-pairing`(조서희) |
| Q-45 | ⚠ | **욕설(Q-29) 뒤 「상담원 보호 알림」 배너가 뜨지 않는다** — 줄 안 「콜가드」 배지·안내 문구만. 닫을 배너가 없다 → 조서희 |
| Q-46 | ⚠ | 버튼은 떴다. 그런데 새 줄이 아래에 붙지 않고 **첫 줄을 덮어썼다** — 아래 (데이터) 행 |
| (데이터) | ❌ | **같은 통화의 채널이 닫혔다 다시 열리면 발화 번호가 1부터 다시 매겨져 저장된 전사를 덮어쓴다.** `test-qa-05`: 17:44 재연결 뒤 보낸 고객 발화가 segment 1(상담원 인사)을 덮었고, 화자는 **상담원 그대로** 남았다(`transcript_segment` UPSERT 가 `speaker` 를 안 바꾼다). 원래 인사는 사라졌다. 콜 미디에이터가 다시 뜨면(오늘 릴리스 세 번) 실제 통화에서 조용히 일어난다 → `w6-segment-id-reuse`(장민석) |
| Q-21 추가 | ⚠ | `test-qa-05` 에서도 초본 문의 통화에 「수도요금 자동납부 3.4」·「전자고지 3.13」·「수도 사용자 명의변경」 카드가 필요서류로 뜬다 — 절차 누적·오판정 재현 |
| Q-44 추가 | ❌ | `ㅂㅈㄷㄱ` → 「결과 2건을 아래에 추가했어요」 — 무의미 입력에도 결과(B-6 문턱 꺼짐) |
| (화면) | ⚠ | 실서버 화면의 「고객과 전화하기」 창은 **「통화받기」를 눌러야만 닫힌다** — 마이크를 쓰지 않는 통화(스크립트·합성)에서도 자막을 가린다. 오늘 오후 수정(`421f0d6`)은 mock 재생만 숨긴다 → 조서희 |
| Q-44 | ❌ | `전입신고` → 「결과는 이미 패널에 올라와 있어요」인데 전입신고 카드가 보이지 않는다(⚠). **`ㅁㄴㅇㄹ` → 「결과 4건을 아래에 추가했어요」** — 「관련 문서 없음」이어야 한다. B-6 문턱 꺼짐(`decisions/215` 보류)의 화면 쪽 결과 → 류준 `w6-b6-conversational-recalibration` · 문구는 조서희 |
| Q-47 | ❌ | 「대기화면」이 **확인창 없이 바로 나간다** — 통화 중 실수로 누르면 화면 상태를 잃는다 → 조서희 |
| (운영) | ⚠ | **QA 중 17:19 PR #135 머지 → 릴리스(server `0.1.36` · call-mediator `0.2.6`)가 진행 중 통화를 끊었다**(채널 둘 1006, 헤더 「끊김」). 콜 미디에이터 Origin 값도 main 으로 되돌아갔다(세 번째). 화면 결함은 아니다 — **QA 동안 머지를 멈추거나 `af3eac1` 을 먼저 넣어야 한다** |

| Q-48 | ❌ | 토큰 없이(`?call_id=test-qa-10`) 「통화받기」 → **안내 없이 창만 닫힌다.** 콜 미디에이터 로그에 test-qa-10 연결 시도·거절이 한 줄도 없다 — 화면이 연결을 시도하지 않고 닫는다(`useAgentCallSession.ts`). 상담원은 연결된 줄 안다 → 조서희 |
| Q-50 | ✅ | `test-qa-06` 「통화 후 처리」 · 규칙 발췌 요약 초안 · 유형 「일반행정」 |
| Q-51 | ✅ | 17:50:27 확정 |
| Q-52 | ✅ | 확정 버튼이 사라지고 「이미 확정된 요약입니다」 |
| Q-53 | ✅ | 17:51:40 재수정(사유 「재수정」) · 이력에 이전 요약. 사유 없으면 「재수정 확정」이 안 눌린다 ✅ |
| Q-54 | ✅ ⚠ | 진짜 버튼은 통화 후 처리 **맨 아래 「콜 라우팅 보호」** 에만 있고 상담기록 재조회에는 없다(`decisions/204`). 위쪽 「블랙컨슈머로 분류」(mock)가 **「관리자에게 전송됨」이라고 거짓 표시**한다(7장 알려진 mock — ⚠). 첫 시도(`test-qa-07`)는 QA 스크립트가 발신 번호 없이 통화를 열어 「고객이 식별되지 않았습니다」로 거절 — 안내 정확. `test-qa-08`·`09`(가짜 번호 `010-0000-90xx`)는 접수 |
| Q-55 | ✅ | 대기화면 · 최근 상담기록 맨 위 17:48 |
| Q-56 | ✅ | 요약에 원문 없음. 서버 저장본(전사 6 · 요약 · 기록)에도 `010-1234-5678`·`900101`·`홍길동`·`세종대로 110` 0회 |
| (후속조치) | 확인 필요 | 확정 전 「0건」이었는데 저장본에 `후속 조치` 항목 1건 — 직접 입력 여부 확인 |
| Q-60 | ✅ | `test-qa-08`·`09` 요청이 승인요청 탭에 뜸 |
| Q-61 | ✅ | 사유 없으면 「반려」가 안 눌린다 |
| Q-62 | ✅ | `08` 반려(사유 「반려」) · `09` 승인, 409 없음 |
| Q-63 | ✅ ⚠ | 사유 없으면 「재설정」 꺼짐 ✅. 2개월로 재설정 → 만료 2027-01-20 → 2026-11-21, 「변경 이력 보기」에 세 줄(18:15·18:16·18:30). ⚠ 개월 수가 **등록일부터** 세어져 같은 값 재설정(18:16)은 날짜가 안 바뀌는데도 이력에 남는다 — 의도 확인. ⚠ 이력을 펼치면 줄이 찌그러진다(버튼이 한 글자씩 세로로 꺾임) → 조서희 |
| Q-64 | ✅ | 오후 테스트 등록(15:04 승인) 18:00 해제 · QA 승인 건 18:31 해제 → 「해제된 기록」 둘, 등록 0 |
| Q-65 | ✅ | 18:16 기준 완료 통화 4 · 콜가드 22 · 승인 대기 0 · 활성 등록 1 |
| Q-66 | — | 「아직 쌓인 공백 신고가 없습니다」 — 항목이 없어 해제·다시 열기 불가. 검색 실패(Q-44)는 자동으로 쌓이지 않는다(상담원 신고 방식) |
| Q-67 | ⚠ | 승인 2 · 반려 1 · 해제 2 는 있다. **재설정 세 번이 감사 로그에 없다** — 항목별 변경 이력에만 남는다. 만료일 변경도 관리자 행위라 감사 로그에 있어야 한다 → 장민석·조서희 |
| Q-68 | ✅ | 근속 연차 값 새로고침 뒤 유지 |
| Q-69 | ✅ | `QA상담원1` 토큰 폐기 → 상담원 창 새로고침 시 로그인으로 튕김(이후 같은 이름으로 재발급) |

- 막힌 것과 우회: 이 PC 크롬 음성 인식이 두 탭에서 서로 끊어 글자가 안 나왔고, 휴대폰은 콜 미디에이터까지 닿지 않았다. 스크립트로 `test-qa-03` 에 문장 15개를 9초 간격 · 어절 단위 중간 자막과 함께 넣었다. 마이크 경로는 Q-48 에서 따로 본다.
- ⚠ 운영 콜 미디에이터 입력 토큰이 이 세션 대화에 한 번 드러났다(편집기 선택). **QA 뒤 교체한다**(런북 12-2).
- 남은 것: 4장 → 5장 → 6장, 빈칸(Q-02·Q-05·Q-10·Q-11·Q-13·Q-15), 마지막에 3→5→6 한 번에.

- **마지막 통과(3장 → 5장 → 6장 한 번에) — 끊김 없이 끝났다.** `test-qa-final`(가짜 번호 `010-0000-9010`): 발화 6 → 가려짐·콜가드 → 통화 종료 · 요약 확정 → 「블랙리스트 전환 요청」 → 관리자 승인 → 해제 → 현황판 반영.
  서버 저장본: `status closed` · `summary_confirmed true` · 전사 6 · 원문 개인정보·가짜 번호 0회. 17:19 뒤 릴리스 없음. 화면에는 카드 짝짓기·경고 방향 결함이 그대로 보였다(티켓 있음).
- **결과: 모든 칸이 채워졌다. ❌ 7 · ⚠ 14.** ❌ 는 전부 담당자 티켓으로 넘겼다 — 게이트(`decisions/319`) 완료 조건 중 **「❌ 가 고쳐진 뒤 그 번호만 다시 눌러 ✅」만 남았다**(QA 티켓은 `in-progress` 로 둔다).

| ❌ | 무엇 | 티켓 |
|---|---|---|
| Q-31~Q-33 | 상담원 위반에 경고가 안 뜬다 | `w6-compliance-alert-ui`(조서희) |
| Q-43 | 필요서류 목록이 엉뚱한 카드 제목 밑에 붙는다 | `w6-closure-card-pairing`(조서희) |
| Q-44 | 무의미 검색에도 결과 | `w6-b6-conversational-recalibration`(류준) |
| Q-47 | 대기화면이 확인 없이 나간다 | `w6-qa-call-screen-fixes`(조서희) |
| Q-48 | 마이크·토큰 실패에 안내가 없다 | `w6-qa-call-screen-fixes`(조서희) |
| (데이터) | 재연결하면 전사를 덮어쓰고 화자가 틀린 채 남는다 | `w6-segment-id-reuse`(장민석) |
| (준비) | 개발 페이지 Origin 403 | 로컬 커밋 `af3eac1`(정성윤, 미머지) |

  ⚠ 는 `w6-warning-badge-direction` · `w6-qa-call-screen-fixes` · `w6-audit-log-blacklist-reset` · `w6-read-path-auth`(Q-14) · `w7-demo-infra-readiness`(Q-05) · 미결 「절차가 쌓이기만 한다」(Q-21) 로 갔다.
- **QA 방법 메모(다음에 할 사람에게)**: ① 한 PC 두 탭에서 크롬 음성 인식을 동시에 켜면 서로 끊어 글자가 안 나온다 — 발화는 스크립트로 넣었다(`/dev/text`, 어절 단위 중간 자막 · 확정, 가짜 `X-Caller-Phone`) ② 블랙리스트 요청은 발신 번호가 있어야 한다 ③ **QA 중에는 main 머지를 멈춘다** — 릴리스마다 콜 미디에이터가 다시 떠 진행 중 통화가 끊겼다(오늘 세 번) ④ 운영 DB 에 QA 통화 `test-qa-01~10`·`test-qa-final` 이 남아 있다(`decisions/130` 과 같은 이유로 둔다).
