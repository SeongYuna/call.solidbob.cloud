---
date: 2026-09-15
author: "정성윤"
person: seongyun
seq: 10
---

- **OIDC 신뢰 정책을 실물로 확인했다 — 유효하다. 미결 항목을 닫았다.**
  09-14 에 네 세션이 전부 AWS 자격증명이 없어 못 봤던 건이다. 이번 세션에 자격증명이 붙어 있어 확인했다.
  - `callguard-deploy-role` 의 `sub` 조건:
    `repo:SeongYuna@280208914/call.solidbob.cloud@1344602683:ref:refs/heads/main`
    처음엔 깨진 값으로 보였으나 숫자가 맞다 — `gh api repos/SeongYuna/call.solidbob.cloud` 의
    `owner_id`(280208914)·`repo_id`(1344602683)와 일치한다. **이름이 아니라 숫자 ID 로 박힌 형태**라
    저장소 이름 변경·동명 재생성으로 사칭되지 않는다. 이름만 쓰는 것보다 강하다.
  - 함께 본 것 — ① `StringLike` 지만 값에 `*` 가 없어 사실상 정확 일치 ② `aud` 는 `sts.amazonaws.com` 고정
    ③ **GitHub OIDC 공급자를 신뢰하는 역할은 이것 하나뿐**(전 역할 전수 조사) ④ 오늘 05:33 에 실제로 쓰였다 —
    릴리스 #90(`34933095943`)의 `k3s-deploy` → 「AWS 자격증명(OIDC)」 success, `RoleLastUsed` 도 같은 시각.
  - `#54`·`#55` 가 그 스텝에서 거부당한 건 역할 생성(09-09 04:37) 직후 과도기였다. 지금은 문제 없다.

- **그래서 09-14 에 «ⓑ보다 우선순위가 높을 수 있다»고 올렸던 위험은 성립하지 않는다.**
  `release.yml` 에 `pull_request` 를 더해도 `k3s-deploy` 는 OIDC 스텝에서 죽는다 — 머지 안 된 코드가
  운영에 배포되지 않는다. **다만 이미지 push 는 남는다** — `image`·`gateway-image` 는 AWS 를 거치지 않고
  `DOCKERHUB_*` 만 쓴다. 그쪽은 판정만 하는 `tag-check.yml` 을 따로 뗀 것으로 이미 닫혀 있다(`decisions/111`).

- 남은 것: `open-items` 185행(2026-09-03)이 아직 «지금은 이미지를 손으로 굽고 손으로 올린다»로 적혀 있다.
  `release.yml` 이 도는 지금은 낡은 서술이다 — 그 항목을 손볼 사람이 같이 정리하면 좋겠다.
