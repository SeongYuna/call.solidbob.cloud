---
date: 2026-09-23
author: "정성윤"
person: seongyun
seq: 1
---

- **화면에 뜨는 서버 오류 문구를 존댓말로 고쳤다 (수동 QA Q-11).** 조서희 님이 「이건 프론트가 아니라 서버 쪽」이라고 짚어 준 것이 맞았다 — `apps/call`·`apps/admin` 의 API 클라이언트가 응답의 `detail` 을 **그대로** 오류 메시지로 띄운다(`coreClient.ts:80-85`). 그래서 화면만 고쳐서는 문구가 안 바뀐다.
- **고친 곳 9개** — 상담원 문 2(`agent_auth/.../agent_guard.py` 21·31) · 통화 종료 문 1(`hub/dependencies/close_guard.py`) · 관리자 문 2(`admin_auth/.../admin_guard.py`) · 관리자 로그인 2(`auth_router.py` 403·401) · 구글 토큰 검증 2(`google_id_token_verifier.py`). 예: 「…관리자에게 새 토큰을 **받아라**」 → 「…관리자에게 새 토큰을 **요청해 주세요**」.
- **콜 미디에이터·개발자만 보는 문구는 그대로 뒀다** — `ingest_guard`(「토큰이 없거나 틀렸다」)·전사 409 등. 사람이 읽는 화면이 아니다.
- **회귀 테스트 4건 신설**(`agent_auth/tests/test_user_facing_messages.py`) — 화면에 닿는 문 넷의 `detail` 이 「…니다/…세요」로 끝나는지 본다. 옛 문구로 되돌려 보니 실제로 실패했다. server **1,438 passed** · 계약 5 kept.
- 남은 것: PR · 조서희 님 티켓(`w6-qa-call-screen-fixes`)에서 Q-11 을 지웠다고 알리기.
