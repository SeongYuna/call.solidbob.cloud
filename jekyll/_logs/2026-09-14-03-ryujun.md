---
date: 2026-09-14
author: "류준"
person: ryujun
seq: 3
---

- **블랙리스트 남은 작업을 시작하려다 멈췄다 — 장민석 님이 이미 만들었다.** `server` 브랜치 `b1dade6`(15:12, PR 전)에 J 블랙리스트 API 5종·PostgreSQL 저장소·관리자 로그인 연동·§7.3 등록이 들어 있다. [J-1·J-2·J-4 티켓](/backlog/w4-blacklist-request-flow/)을 done 으로 옮기고 **실제 서버 작업은 `w4-blacklist-api`(server 브랜치) 의 것**이라고 본문에 적었다
- **C-6 배선이 두 벌이었다** — 내 PR #76(전사 수신 안에서 탐지, 이미 main 머지)과 민석 님 브랜치(게이트웨이가 `POST /hub/call-guard-checks`)가 같은 날 따로 만들어졌다. 둘 다 들어가면 **같은 발화를 두 번 탐지·저장**하고, 리포지토리 파일 이름도 같아 머지 충돌이 난다. **사용자 결정으로 민석 님 방식을 남기고 내 쪽을 걷어냈다** — 결과를 대시보드로 보낼 수 있는 쪽이 그쪽이다
  - 걷어낸 파일은 **민석 님 브랜치의 기준 커밋(`61a3d35`)과 같은 내용으로** 되돌렸다. 되돌리기 전에 그 기준과 PR #76 직전 main 사이에 이 파일들을 고친 사람이 없는지 확인했다(차이 0) — 남의 변경을 되돌리지 않았다
  - 남긴 것: D-5 저장 경로 · `robust_z` 비노출 테스트 · `document` 적재 스크립트 · 탐지기 `strip()` 오프셋 수정(도메인 테스트로 옮겼다)
  - **내 문서가 틀리게 됐던 것을 고쳤다** — 런북 17-3·미결 항목의 「조항 적재 안 하면 전사 저장 500」은 걷어낸 내 배선 기준이었다. 민석 님 저장소는 조항이 없으면 근거를 NULL 로 넣는다
- **J-5 베테랑 배정은 민석 님 브랜치 머지 뒤로 미뤘다**(사용자 결정) — 고객 식별(발신 번호 HMAC)이 그 브랜치에만 있다. 그 뒤에도 「지금 받을 수 있는 상담원」 목록의 출처를 먼저 정해야 한다. 카드에 메모를 달았다
- 검증: `server` 331 · `ai` 224 · 계약 4+3종 KEPT
