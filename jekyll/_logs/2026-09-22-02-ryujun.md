---
date: 2026-09-22
author: "류준"
person: ryujun
seq: 2
---

- **남은 류준 티켓을 서브에이전트 6개로 돌렸다**(사용자 지시 — 계획을 먼저 승인받고 1차 병렬 5 · 2차 순차 1). 1차 다섯은 각자 워크트리에서 일하고 제가 `ai` 에 차례로 머지했다(푸시 안 함). 도중에 **다섯 개 모두 API 500 으로 한 번 끊겼고**, 맥락을 살린 채 이어서 돌려 끝냈다.
- **하네스 침묵 누락 해소**(`w6-harness-silent-metrics`, done) — 생성(B-4·B-5)·A-5 가 `run_eval` 리포트에 줄로 나온다. 값이 없으면 사유가 붙은 「측정 불가」이고 「모듈 미구현」·`NO_SAMPLES` 와 섞이지 않는다(테스트 15). **D-5 는 09-21 `f3f284b` 에 이미 배선돼 있었다** — 티켓이 낡았던 것이다. 생성은 `--ollama-url` 로만 꽂힌다(기본 꺼짐). ES 없이 돌린 리포트를 전후 diff 하면 두 섹션 추가뿐이다.
- **골든셋의 AI Hub 원문 14건을 바꿔 썼다**(`w6-aihub-text-exposure`, done · `decisions/212`) — 미결이 적은 12건이 아니라 14건이었다(GS-101·102 는 notes 로만 원문이고 `source` 는 `authored` 였다). 같은 의도의 새 문장으로 바꾸고 `source: "aihub_rewritten"`, 판 `v1-150.1`. **채점 라벨과 나머지 265건은 바이트 단위로 같다.** 옛 판 v1-10·v1-50 의 GS-101·102 도 교체. 마스킹 테스트·탐지기의 짧은 조각은 두었다. git 이력은 다시 쓰지 않았다. 비용: 「실제 발화 문체 vs 작성 문체」 비교를 이 골든셋으로는 더 못 한다.
- **GS-205 원인**(`w6-golden-set-loose-ends`, in-progress) — 질의 「이건 시에서 결정해주시는 건가요」의 내용어는 `결정` 하나이고, **nori 분석기에 품사 필터가 없어** 조사·어미가 순위를 정한다: `해지` 를 `해/VV`+`지` 로 쪼갠 `해` 가 `해주시는` 의 `해` 와, `서울시` 의 `시`(NNG)가 `발생 시` 의 `시`(NNB)와 맞는다. BM25 상위 5가 전부 내용어 매칭 0 이고 정답(TERM-1.2)은 26위, dense 9위. 임시 인덱스에 `nori_part_of_speech` 를 넣으면 2위로 오르지만 GS-206·241 을 잃는다 — **골든셋 한 번으로 고르는 것이라 채택하지 않았다**(미결). `compare_retrievers.py` 가 항목별 상위 k(`<날짜>.items.json`)를 남기게 했다(테스트 4). ①B-6 문턱·②C-4 는 팀 결정 대기.
- **ai/ 모델 HTTP 표면**(`w6-ai-model-http-surface`, in-progress · `decisions/213`) — `ai/apps/model_serving` + 합성 루트 `ai/model_server.py`, FastAPI, `MODEL_SERVICE_TOKEN` 이 없으면 전부 거절(fail-closed). NER 은 **구간만** 주고 마스킹 판정은 서버 규칙과 합쳐서 한다(절대 원칙 9). 미적재 503 `model_not_loaded` · 생성 백엔드 전멸 503 `model_unavailable` · 422 가 입력값을 되돌려주지 않는다(SEC-1). `server/` 무수정. 로컬 CPU 스모크(홉 없음, 참고치) ner 11ms · embed 39ms · rerank(6) 85ms — **네트워크 홉 지연은 미측정**이라 `121` 질문 2 는 열려 있다. 남은 것: 서버 원격 어댑터(장민석) · CI `ai` job 에 fastapi 류 설치(정성윤).
- **다산콜DB 시나리오 수를 파일로 셌다** — 고유 56(1~56 빠짐없음) · (시나리오,역할) 112 · wav 6,614. 공개 페이지의 「56」이 맞다. 파일명 형식이 셋이라 `decisions/203` 의 「민원인 5,549건」은 실제 **5,570건**이다.
- **D-5 음성 이상치 저장은 배선하지 않았다 — 새 API 가 필요하다.** 콜 미디에이터의 hub 호출 6개 중 오디오·특징값을 싣는 것이 없어 블랙리스트 근거의 `voice_outlier` 건수는 운영에서 늘 0 이다. 제안(`POST /hub/voice-samples`, 서버에서 F0 만 뽑고 오디오는 즉시 버림)은 미결로 — 결정은 `w7-d5-server-wiring`(장민석).
- **두 구성 재측정**(`w6-model-config-remeasure`, done) — 골든셋 v1-150.1 · 커밋 `121157e` · 로컬 Postgres(127.0.0.1:5434 — 5432 는 다른 프로젝트가 씀) · 각 3회 최저 `--record`. **운영 구성(규칙+BM25) run_id 1: Recall@5 0.812 · MRR 0.635** / **모델 구성(dense+리랭커+규칙+NER) run_id 2: 0.969 · 0.914** (n 96). C-5 누락 0(n 28, P1~P7 전부 표본) · F-2 1.0(n 99, 규칙표 상한) · 컴플라이언스·콜 가드 1.0(상한) · B-6 기권 0(n 24). 09-21·09-15 보다 −0.021 · −0.010 이지만 **질의 14건이 바뀐 판이라 항목 단위 비교는 안 된다.** 오류 10%: BM25 0.792 · rerank-dense 0.969. C-5 는 규칙만이면 5% 1 → 10% 2 → 15% 4, 규칙+NER 은 15% 부터 2(GS-414·415, P7). kanana 생성 1회(기록 안 함): 카드 480 · 출처 1.0 · 출하 환각 0. **모델 구성은 운영에 없다 — 로컬 참고치.**
- **최종 측정은 절차만 준비했다**(`w8-final-eval-run`, todo) — 동결 커밋에서 돌려야 의미가 있어 실행하지 않았다. 명령·체크리스트를 티켓에 채웠다.
- **⚠ server 이미지 태그를 못 올렸다.** 이번 변경이 서버 이미지 코드를 바꿔 `tag-check` 가 `0.1.24` 로는 막는다. `0.1.25` 는 이미 레지스트리에 있어 쓸 수 없고, 다음 빈 번호를 확인하려던 조회가 권한에 막혔다 — `0.1.24` 로 되돌려 두고 미결에 올렸다.
- 검증(머지 후 한 트리): ai pytest 496 passed · server 1,257 passed · scripts 15 passed · 계약 3+4 KEPT · `jekyll build` 통과.
- 남은 것(전부 남의 손 또는 사람): 태그 번호 · 품사 필터 결정 · B-6 문턱 · C-4 · 용어 · 음성·대본 사람 검토 · 서버 원격 어댑터 · D-5 API · GPU EC2 · Chirp 3 IAM.
