---
date: 2026-09-23
author: "정성윤"
person: seongyun
seq: 10
---

- **QA 게이트와 무관한 범위만 골라 문서·대조 작업을 했다**(`decisions/319` 가 막는 것은 내 티켓 넷과 시연 리허설이고, `w8-final-docs` 는 거기 없다).
- **런북 자원 이름을 AWS 실물과 대조했다 — 전부 일치.** EC2 `i-034cda2423f65c8bd` · `t3.large` · 키 `callguard-key` ·
  `ap-northeast-2b` / RDS `callguard-pg` · `db.t4g.micro` · PostgreSQL `17.11` · 20GiB / S3 `assist-apne2`.
  티켓에 「SSH 키 이름이 아직 어긋나 있다」고 적혀 있었는데 **런북의 실물 대조 표가 이미 정정해 둔 상태**였다 — 그 줄을 닫았다.
- **공개/비공개 경계를 훑었다 — 자격증명 0건.** 공개 트리 전체에서 AWS 키·PEM·상담원 토큰·슬랙 토큰 패턴 0건.
  사이트에 남은 `_project/` 문자열은 결정 기록 **경로 이름 인용**이고 내용은 지킬 루트 밖에 있다.
- **스키마·ERD 재생성 — 차이 0**(29 테이블). 저장소 정의가 DB 와 어긋나 있지 않다.
  ⚠ `ERD.png` 는 이 머신에 graphviz 가 없어 못 만들었다.
- **`STATE.md` 의 `server/`·인프라 줄을 지금 상태로 덮어썼다** — 운영 `0.1.40` · `read_guard: locked` · 되돌리는 법.
- **미결 두 건 갱신** — 「읽기 경로 인증」은 2단계까지 켜서 닫되 **바깥문만**이라는 단서와 후속 티켓을 적었고,
  B-6 문턱 항목에는 수동 검색 경로 적용과 운영 실측값을 붙였다.
- 남은 것: `w8-final-docs` 의 1·2·3번(문서 대조 · 숫자 넷 · 「측정 불가」)은 하네스 값이 걸려 **6주차 기준선 판정과 같이** 본다.
