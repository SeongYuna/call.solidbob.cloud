---
date: 2026-09-11
author: "정성윤"
person: seongyun
seq: 4
---

- **발표 자료 4종을 마지막으로 두 방향에서 교차검증했다.** ① 문서끼리 같은 사실을 다르게 적은 곳을
  찾았고 ② 이 대화 맥락이 없는 새 에이전트에게 발표에서 입으로 말할 핵심 주장 20개를 1차 자료(git·코드·
  GitHub Actions API·결정 기록)로 처음부터 다시 확인하게 했다. 숫자는 전부 맞았고, 표현·표기 7건을 고쳤다.
- **03번 기록을 보강한다** — 03은 "PR #59·#61은 실패다"라고만 적었는데, 실행 전체 결과는 failure가 맞지만
  멈춘 곳은 `plan` job의 「이미지를 다시 구워야 하는지 본다」 step이고(주석: "server/ai 코드가 바뀌었는데
  newTag 가 '0.1.1' 그대로입니다"), **`deploy` job은 둘 다 success**였다(`release.yml`의 `if: always() && …
  needs.image.result == 'skipped'`). 즉 "배포가 안 됐다"가 아니라 **"새 이미지 없이 옛 `0.1.1`이 다시
  적용됐다"**가 정확하다. 결론(09-10 변경이 운영에 없다)은 같다.
- 고친 것:
  - 위 배포 표현 — 네 문서 모두 "배포 실패" → "새 이미지가 구워지지 않고 옛 이미지가 재적용됨"
  - 501 — 스포크 구현 자체가 없어 늘 501인 것은 compliance·통화 종료 둘이 맞지만, ES나 `ai/`가 없으면
    search·recommendations도, PostgreSQL이 없으면 DB를 쓰는 엔드포인트 5개도 501이라는 조건을 붙였다
  - "STT 티켓은 `w2-stt-batch` 하나뿐"은 틀렸다 — STT 관련 티켓은 5개(요금 상한·V3/V4 실측·배치 전사·
    A-5 스파이크·오류 주입기)이고, **스트리밍 STT를 구현하는 티켓**이 없는 것이다
  - C-1~C-4 상태 아이콘이 기능명세서 안에서 🟡/⬜로 갈려 있던 것을 "⬜ 판정 로직 미착수(허브 배선·채점
    케이스 20건은 있음)"로 통일
  - C-6 범위 — 요약표 세 곳이 "욕설·폭언"만 적고 있어 위기 신호(`distress`)를 더했다(`96d88ac`)
  - onboarding이 최신 상태의 출처로 STATE.md를 먼저 가리키던 것을 `/progress/`로 바꾸고, STATE.md 인프라
    줄이 09-03에서 멈춰 있다는 주의를 붙였다
  - progress-briefing 파트 C의 "3주차인 지금까지도" → "3주차를 마친 지금까지도"(제목의 4주차 초입과 맞춤)
- 남은 것: 03의 "남은 것"이 그대로다 — 이미지 태그를 올려 09-10 변경 운영 반영, `knowledge_gap` FK 누락
  공유, STATE.md 인프라 줄 갱신, CLAUDE.md 세 곳.
