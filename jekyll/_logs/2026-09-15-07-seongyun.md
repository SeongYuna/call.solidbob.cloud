---
date: 2026-09-15
author: "정성윤"
person: seongyun
seq: 07
---

- **PR #92 의 배포 태그를 올렸다 (`ai` 브랜치 `c9fa746`)** — `tag-check` 가 유일한 빨간불이었다.
  PR #92 는 `server/main.py`·`server/core/config.py`·`ai/apps/**` 를 고쳐 server 이미지 경로에
  걸리는데 `infra/k8s/base/kustomization.yaml` 의 `newTag` 가 `0.1.10` 그대로였고, 그 태그는
  PR #90(`75eb0c5`)이 이미 레지스트리에 구워 뒀다. `imagePullPolicy: IfNotPresent` 라 그대로
  머지하면 **배포는 초록인데 노드는 캐시된 옛 이미지를 계속 쓴다** — `decisions/111` 이 막으려던 상황이다.
  `0.1.11` 로 올렸다. 게이트웨이는 `services/gateway/src/` 를 안 건드려 `0.1.4` 유지.
- **`0.1.11` 선점 확인** — main·PM·ai·server 전부 `0.1.10`, frontend 만 `0.1.9` 였다.
  `decisions/111` ⑥ 의 「두 브랜치가 같은 태그를 집는」 경우는 없었다.
- **⚠ `origin/frontend` 가 `0.1.9` 에 멈춰 있다** — 조서희 님이 main 을 받아올 때 이 줄에서
  충돌한다. 잘못 풀면 태그가 되돌아간다. 다음 머지의 지뢰다.
- **⚠ PR #92 는 지금 CONFLICTING 이라 `tag-check` 가 아예 돌지 않는다.** 07:45 에 PR #91(PM)이
  main 에 들어가면서 어긋났다. GitHub 이 머지 커밋을 못 만들어 `pull_request` 워크플로가 생성되지 않는다 —
  **태그를 고쳐도 검사는 여전히 옛 SHA 의 실패로 남아 있다.** 충돌은 `_project/STATE.md` 1곳
  (`ai/` 줄은 류준·인프라 줄은 main 이 최신) · `jekyll/open-items.markdown` 1곳(양쪽 순수 추가, 둘 다 살리면 된다) 둘뿐이다.
- 남은 것: 류준 님이 main 을 `ai` 로 머지해 위 두 곳을 풀면 `tag-check` 가 새 SHA 에서 돈다.
