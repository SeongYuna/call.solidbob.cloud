---
date: 2026-09-22
author: "정성윤"
person: seongyun
seq: 11
---

- **Vercel 배포 한도에 다시 걸렸다 — 09-15 에 건 Ignored Build Step 은 한도를 막지 못한다.** 그 설정은 빌드만 취소하고 **배포 생성은 그대로 일어나며, 한도는 생성 기준**이다(API 오류 `api-deployments-free-per-day`, `total 100 · remaining 0`). 실측: 24시간(09-21 13:39 ~ 09-22 13:39 KST)에 세 프로젝트 합쳐 **147건** 생성 — main 44 · frontend 32 · PM 29 · ai 27 · server 15, 빌드된 건 24건이고 123건이 `Canceled`. 다섯 브랜치 × 세 프로젝트라 push 한 번이 3건, 머지 한 번이 6건이다.
- **운영 상담원·관리자 화면이 `a5450d8`(PR #120, 12:15 KST) 에 멈춰 있다.** 상담원(`kxu6`)은 12:38, 관리자는 12:18 KST 이후 배포가 하나도 안 만들어졌고 PR #121~#124 의 main 커밋 넷 다 GitHub 체크가 `Deployment rate limited`. 안 나간 프론트 커밋 5건: `9c2f90d` 온도 null 표시 · `6bff18a` 지역자원 mock 게이팅 · `421f0d6` 합성 통화 모달 · `86a3c27` 고객 해시 라벨 · `063d13c` 주석. 서희 님 오늘 정리 8건 중 이 넷이 운영엔 아직 없다. 랜딩은 최신(오전 강제 배포 뒤 변경 없음).
- **강제 배포 실패.** `POST /v13/deployments`(git 소스) 가 같은 한도로 거부됐고, CLI `vercel deploy --prod` 는 같은 생성 경로인 데다 루트에서 작업 트리 2.5GB 를 올리다 `Upload aborted` 가 나 멈췄다. 해제 시각 **09-23 13:38 KST** — 명령은 런북 18-4 에 적었다. `vercel link` 가 남긴 `.gitignore` 의 `.env*` 한 줄은 되돌렸고, `.env.local`(OIDC 토큰)·`.vercel/` 은 gitignore 대상이라 트리에 남아 있다(지워도 된다).
- **근본 대책은 `vercel.json` 의 `git.deploymentEnabled`** 로 PM·ai·server 브랜치 배포를 끄는 것(147 → 76). 세 앱 다 넣어야 하고 `apps/` 는 조서희 전담이라 미결에 올렸다. 런북 18-4 에 「Ignored Build Step 이 막았다」 는 서술을 정정했다.
- 남은 것: ① 09-23 13:38 KST 이후 `kxu6`·`admin` 강제 배포 ② `vercel.json` 셋 넣기(누가 넣을지 미결) ③ 서희 님 요청 「통화 `call_id` → `customer_id` 조회」 는 `GET /hub/calls/{id}/record` 에 필드 하나 얹는 일로 장민석 님 몫 — 티켓은 아직 안 만들었다.
