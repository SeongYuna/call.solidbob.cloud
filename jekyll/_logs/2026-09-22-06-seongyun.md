---
date: 2026-09-22
author: "정성윤"
person: seongyun
seq: 6
---

- **모델을 운영 노드 CPU 에 싣는 준비 — `decisions/124` ①·② 끝** (`w5-models-on-node`)
  - ① 모델 받기·S3 업로드는 **류준 님이 아니라 이 머신(WSL)에서** 했다 — 류준 님 09-22 메시지(학원이 계정 관리자라 자격증명을 못 준다 · AI Hub 정책상 각자 받는 게 맞다)로 방향을 바꿨다. `uv pip install huggingface_hub` 뒤 `hf download` 로 `nlpai-lab/KoE5`(2.2GB)·`monologg/koelectra-base-v3-naver-ner`(430MB)를 받아 `s3://assist-apne2/models/` 에 올렸다(파일 20 + 해시 목록 1, 2.7GB, `--delete` 안 씀). ⚠ **류준 님 로컬 폴더와 해시 대조는 아직** — shasum 결과를 요청한다
  - ② 저장소: `server.Dockerfile` 에 CPU 판 torch(`download.pytorch.org/whl/cpu`, 3.13 휠 존재 확인)+transformers 계열 · `server.yaml` 메모리 상한 1Gi→3Gi + hostPath `/opt/callguard/models`→`/models`(읽기 전용, `DirectoryOrCreate`) · 태그 `0.1.25`→`0.1.26` · 런북 머리말 다섯 번째 정정 + 11장 끝 「실물 — CPU 노드에 모델 싣기」(S3 → 노드 → 벡터 재적재 → 하나씩 켜기 → 되돌리기)
  - 확인한 사실: 운영 노드는 Python 3.9·pip 없음(그래서 노드에서 직접 HF 받기는 안 한다) · 디스크 여유 19GB · 메모리 7.6GB 중 2.9GB 사용 · 운영 인덱스에 벡터 없음(재적재 필수) · `/health` 에 뜨는 이름은 `pii_ner`·`retrieval_dense`
- 남은 것(③~⑦): 노드 `aws s3 sync` + 해시 검사 → PR 머지·배포 → 벡터 재적재 → NER·임베딩 켜기 → SYN-010 검증(류준) → `--record` 재측정(류준). 리랭커·생성 모델은 싣지 않는다(`124`)
