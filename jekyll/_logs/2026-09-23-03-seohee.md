---
date: 2026-09-23
author: "조서희"
person: seohee
seq: 3
---

- **w6-admin-agent-hired-on-ui (in-progress)** — 칸반 순서 7번. 서버 API(`decisions/321`)는
  이미 있는데 관리자 화면에 넣을 곳이 없어 운영 상담원 전원이 근속 0년이던 것을 고쳤다.
  - `hubClient.ts`: `AgentSummary`에 `hiredOn` 추가, `updateAgentHiredOn()`
    (`PUT /admin/agents/{id}/hired-on`) 신설.
  - `SettingsTab.tsx`(토큰 발급 화면)에 「상담원 입사일 (J-5 근속)」카드 추가 — 행마다
    이름(가림)·현재 입사일·`date` input·저장 버튼. 입사일 없으면 "근속 0년으로 계산됩니다"를
    카드 상단에 항상 보이는 안내로 못 박았다. `date` input `max`=오늘로 미래 날짜를
    미리 막는다(서버가 최종 422 판정).
  - 검증은 `tsc --noEmit`·`vite build`뿐 — 로컬에 붙는 서버가 없어 실제 저장·오류 응답을
    받아 본 적은 없다. `status: in-progress`로 두고 운영 확인을 남겨 뒀다.
- 남은 것: 칸반 순서 8번(`w6-routing-result-ui`, WS 실시간 배너·상담기록 필드) 또는 9번
  (`w6-admin-stats-wallboard`, 현황판 나머지 5칸)부터 계속.
