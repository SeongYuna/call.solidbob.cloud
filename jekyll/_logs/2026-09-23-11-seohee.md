---
date: 2026-09-23
author: "조서희"
person: seohee
seq: 11
---

- **w7-call-ui-loose-ends (done)** — 칸반 순서 15번. 번호 매긴 결함 둘 다 이미 고쳐져
  있었다.
  1. AgentCallBox가 합성 통화 보기를 가린다 — 오늘 이 세션 앞쪽(`w6-qa-call-screen-fixes`,
     커밋 `e922e1e`)에서 넣은 "자막 도착 시 자동 숨김" 로직이 이 문제도 같이 덮었다.
  2. `customer_id` 해시 노출 — 09-22에 이미 걷혀 있었다(`86a3c27`). `LiveCallHistoryList`는
     해시를 아예 안 그리고 "재문의 고객" 배지만 보여준다. 남아 있던 원본 노출은
     mock 시나리오 라벨이라 실제 해시가 아니었다.
  - 대기 중이던 결정 둘도 정리: "공개 데모 라이브 전환"은 `decisions/127`(09-21)로
    이미 "라이브로 둔다"가 결정돼 있었다(이 티켓의 "mock 유지" 권고보다 나중 결정이라
    그게 이긴다). "상담기록 목록 요약 미리보기"는 새로 넣지 않기로 정했다 — 목록 API에
    요약 필드가 없어 넣으려면 서버 작업이나 N+1 호출이 필요하다.
  - 코드 변경 없음(이미 존재하는 수정 확인만) — 빌드는 이전 상태 그대로 통과.
- 남은 것: 16번(`w7-agent-registration`) — 동명이인 식별자 정책 결정이 필요한 티켓.
