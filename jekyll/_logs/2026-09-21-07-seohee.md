---
date: 2026-09-21
author: "조서희"
person: seohee
seq: 7
---

- **컴플라이언스 파서 PR(06번 로그) 보완.** 한 세그먼트에 위반이 여러 건 잡힐 수
  있는데 스토어가 마지막 것만 남기고 덮어쓰는 구조였던 걸 고쳤다.
- `store/callStore.ts`: `compliance` 상태를 `Record<string, ComplianceFinding>` →
  `Record<string, ComplianceFinding[]>`로 변경. `applyCompliance`는 같은
  `segment_id`에 기존 배열이 있으면 뒤에 붙이고, `rule_code`+`phrase` 기준으로
  중복이면 안 붙인다. `resetCall()`이 `emptyCall`을 통째로 spread하는 구조라
  초기화(통화 전환 시 비워짐) 동작은 그대로 유지됨. `callGuard` 쪽은 손대지 않음.
- `lib/ws/realCallMediatorClient.ts`의 `parseCompliance`: `alternative_source`가
  `undefined`/`null`일 때 정상 파싱되는지 코드 경로로 검증(+ node로 가드 조건
  `v !== undefined && v !== null`을 실제 실행해 확인) — **이미 두 경우 다 정상
  통과하고 있었다.** 버그가 아니라서 코드는 안 고쳤다. finding 하나가 형식 오류일
  때 메시지 전체를 버리는 경로(`parseCompliance` → `null` → `parseByKind` → `null`
  → `parseCallMediatorMessage` → `null` → `handleMessage`의 `onError`)도 그대로 뒀다
  (요청대로 동작 자체는 변경하지 않음).
- 파서 검증용 샘플 메시지 4종(코드에는 안 넣음, 대화 응답으로만 제공) — 정상 1건
  (alternative_source 있음) · alternative_source null · findings 2건(중복 제거
  로직 검증용) · segment_id가 숫자로 오는 잘못된 케이스(onError로 떨어짐, 그
  과정에서 기존 공유 로직인 `readStringValue`의 `alert("데이터 타입이 틀립니다")`도
  같이 뜬다는 것까지 확인해 알림).
- `apps/call`: `tsc --noEmit` + `vite build` 통과.
- `git status` 재확인 — 이번 컴플라이언스 파서 작업 파일 5개(`useCallMediatorSession.ts`·
  `realCallMediatorClient.ts`·`lib/ws/types.ts`·`store/callStore.ts`·`types/contract.ts`)
  외에, **이전 세션의 무관한 변경**(`components/TranscriptPanel.tsx` — detectCustomerRisk
  게이팅, `05`번 로그)과 `flutter/desktop.ini`(윈도 탐색기 잔재, 내가 만든 파일 아님)가
  작업트리에 같이 남아 있음을 확인 — 이번 작업에서는 건드리지 않음.
- 커밋·푸시는 안 함(사용자 지시).
