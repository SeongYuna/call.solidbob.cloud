---
date: 2026-09-11
author: "정성윤"
person: seongyun
seq: 5
---

- **팀원별 할 일 문서를 만들었다** — `_project/team-next-actions.md`(비공개). 1차 자료에서만 뽑았다:
  미완료 티켓 15건(`status`·`assignee`·완료 조건), 마일스톤 4~8주차 목표, 미결 항목, 오늘 교차검증 결과.
  티켓이 없는 일은 기획서 역할표(`plan.md` 7.1절)나 주 담당 영역을 따라 「(제안)」으로 표시했다.
- **작성하다 새로 확인한 것 둘.**
  ① **`w4-schema-qa-followup` ①이 코드에 아직 반영되지 않았다** —
  `server/apps/hub/adapter/outbound/postgres/transcript_segment_repository.py:31`이 여전히
  `ON CONFLICT ("segment_id")`다. 스키마 기본키는 `(call_id, segment_id)`로 바뀌었으니, 티켓 말대로
  새 스키마에서는 전사 저장 UPSERT가 깨진다. **09-10 변경을 운영에 올리기 전에 먼저 고쳐야 한다**
  (운영 DB가 어느 판 스키마인지도 확인 필요). ②(`call` 행 생성)는 `POST /hub/calls`(`decisions/301`)로 풀렸다.
  ② **대시보드 실시간 모드는 WebSocket 게이트웨이(`VITE_GATEWAY_WS_URL`)를 전제한다** — 게이트웨이
  (`services/gateway`)는 코드 0줄이고, 대시보드에서 HTTP로 서버를 부르는 코드는 통화 기록 재생(`coreClient`)
  하나뿐이다. 조서희의 실시간 연동이 A(정성윤)에 걸려 있다. 그래서 발표 자료의 「첫 프론트↔서버 실연동」은
  "실서버 응답을 프론트 파서가 정상 파싱했다"(`e2c0208` 메시지)보다 넓게 읽힐 수 있다 — 표현을 좁힐지
  사용자에게 확인한다.
- 남은 것: 위 ①의 순서(복합키 수정 → 운영 스키마 확인 → 태그 올려 반영)를 장민석과 맞춘다.
