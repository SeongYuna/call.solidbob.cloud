---
date: 2026-09-15
author: "장민석"
person: minseok
seq: 8
---

- **PR #88 을 열었다** — 서버 `0.1.9`(추천 `card_id` · 가드 401) + `frontend` 병합분. CI 전부 초록(`tag-check` 포함). 머지 전.
- **조서희 님 쪽 「끝까지 못 한 것」 4건을 담당별로 갈랐다** — ① 카드 피드백: 서버는 오늘 해결(`card_id`) → 연결은 조서희 ② 지식베이스 공백 탭: 화면 재설계(조서희)
  ③ 상담기록 재생: 서버 조회 API 가 없음(장민석) ④ 블랙리스트 연장: API 없음 + 정책. 사용자 지시로 ③④ 를 했다.
- **③ `GET /hub/calls/{call_id}/record`** — 한 통화의 요약 초안·후속조치·추천(카드 `card_id`)·필요서류 판정(서류별 항목). 셋 다 이미 저장되던 것이라 조회만 붙였다. 감정분석은 저장되지 않아 없다.
  ⚠ **실수**: 새 프로바이더 파일 이름을 `call_record_provider.py` 로 골랐는데 **같은 이름의 기존 파일(통화 시작 기록 프로바이더)을 덮어썼다** — 통화 시작 계약 테스트가 깨져서 알았다.
  `git checkout` 으로 원본을 되돌리고(HEAD 와 차이 없음 확인) `call_record_query_provider.py` 로 옮겼다. 이후 파일을 만들기 전에 이름이 비어 있는지 먼저 확인했다.
- **④ 블랙리스트 만료 연장·단축**(`decisions/309`) — `decisions/205` ⑤ 가 «연장은 새 요청 + 새 근거로만» 이라 정해 둔 것을 **사용자 선택으로 철회**.
  `POST /hub/blacklist-entries/{id}/expiry`(지금부터 N일, 1~365, 사유 필수·마스킹) + `GET …/expiry-changes` + 새 테이블 `blacklist_entry_expiry_change`(27 테이블).
- **버그를 찾아 고쳤다** — 205 가 말한 «새 요청» 경로가 **실제로 막혀 있었다.** 부분 유니크(`customer_ref WHERE released_at IS NULL`)가 만료됐지만 해제 안 된 행도 세서,
  만료 뒤 같은 고객 재승인이 늘 409 였다(오류 문구는 «만료를 기다린다» — 기다려도 안 풀린다). 승인할 때 만료된 미해제 등록을 `released_at = expires_at`·해제자 없음·사유 「만료」 로 먼저 닫는다.
  **그 한 줄을 빼면 integration 이 409 로 실패, 넣으면 통과**함을 확인했다.
- 검증: `server` 739 passed · integration 16 · 계약 4종 KEPT / `ai` 225 · 계약 3종. 마이그레이션 26 → 27 이 새 스키마와 364항목 일치 · 재실행·선행 누락 시 멈춤.
  실제 앱 + postgres:17 E2E — ③ 404·기록 반환 / ④ 요청 201 → 승인 → 연장 200 → 단축 200 → 이력 2건 → 해제 후 409.
- 남은 것: **이번 둘은 운영 반영 전이다. ④는 운영 DB 마이그레이션이 이미지보다 먼저**(런북 19장 주의문 추가) — 이 커밋을 푸시하면 PR #88 에 합쳐져 **#88 도 마이그레이션이 선행 조건이 된다**.
  연장 누적 상한 · 이력 사유 보존 기간은 [미결](/open-items/). 프론트 연결은 조서희 님께 전달.
