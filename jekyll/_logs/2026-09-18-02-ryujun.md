---
date: 2026-09-18
author: "류준"
person: ryujun
seq: 2
---

- **미결을 결정하고 처리했다 — E2E 24건 ✅3·❌21 → ✅22·❌2.** 01 기록의 시스템 결함 7종 중 6종을 서브에이전트 4개(D 콜 미디에이터 · E 서버 규칙 · F 대본·문서 · G 서버 저장)로 고치고 같은 검사를 다시 돌렸다(`data/processed/persona-e2e/2026-09-18-1653.md` + 정합 후 `1655.md`, 로컬 규칙·BM25·NER 없음, `source: synthetic`).
- **콜 미디에이터 `0.2.1`(정성윤 주 담당, `decisions/302`)** — ① 상담원 확정 발화마다 `POST /hub/compliance-checks`(마스킹본, 실패는 warn·통화 유지, 방송은 `announceCompliance` 기본 false — 대시보드 파서가 `compliance` 를 안 받는다) ② 필요서류 절차 후보를 1순위 카드에서 **카드 순서대로 「규칙 있는 첫 조항」까지**(상한 5, 422 는 건너뜀 — 절차를 지어내지 않는다). 테스트 123 → 127
- **서버 `0.1.18`(장민석 주 담당 — 검토 요청을 미결에 적었다)** —
  - `closure_rule` 25 → 29: **2.6 우대용 교통카드 · 4.5 인감증명 · 4.9 취득세 · 4.12 지방세 감면**을 `decisions/305` 의 EXCLUDED 에서 되살렸다. 갈래마다 목록이 다르면 **모든 갈래 공통 서류만 필수**, 갈래별은 conditional. 4.20·4.8·3.3·6.12 는 「— 필요서류」 조항이 아니라 **넣지 않았다** → 그 조항을 절차로 삼던 대본(SYN-006·007·012)은 `required_documents` 를 비워 「판정 0건이 정상」으로
  - C-5 이름: 모음 끝 이름+「고요·구요·에요」 · **가족 호칭 문맥**(「아버지는 X이에요」) · 두 어절 호칭 · `_NOT_NAMES` 보강. P4: 낭독형 휴대전화 뒤 「이에요」의 「이」가 붙어 P3 로 읽히던 것을 P4 로. 합성 대본 24건 라벨 누락/오분류/과잉 **2/5/6 → 0/0/0** · 골든셋 v1-150 규칙만 누락 0·오분류 0 유지 · AI Hub 다산 실제 발화 15,465건(고유) P6/P7 가려진 발화 **38 → 38(신규 0·소실 0)**. 새 테스트 `test_pii_gaps_0918.py`
  - **`compliance_flag` 저장 배선** — 서버가 검사 결과를 응답만 하고 DB 에 안 쓰고 있었다(D 발견). 출력 포트·PostgreSQL 어댑터·합성 루트 배선, `compliance_rule` 카탈로그가 비어 있어 저장 직전 UPSERT(`default_severity` 전부 `medium` — 등급 정의가 없다). 저장 실패는 로그, 응답 그대로. 로컬 실호출로 3행 확인. 서버 전체 테스트 통과(E 시점 1,007) · 계약 4종 KEPT
- **대본 정합(F)** — 규칙표가 정본. `procedure.conditional_documents` 필드 신설(있을 때만 실림), 11건 + SYN-003·024 서류 목록을 규칙표 이름·필수/조건부로 맞췄다(라벨·PII·톤 불변). `seed_documents.py` 를 E2E 스택 절차에 넣어 `document` 98행 → `source_doc_id` NULL 경고 해소
- **결정 기록 `210`** — 모델을 학습하지 않는다·데이터는 S3 측정용(09-16 결정의 기록). 티켓 `w3-a5-translation-spike`·`w3-call-temperature` 메모를 실제 막힘으로, `w5-classifier-ner-benchmark` 는 「측정 불가 — 학습 안 함」으로 닫음(done). 「합성 대본을 하네스에 올릴지」는 **올리지 않는다**(E2E 검사가 그 역할)
- **남은 ❌ 2 = 검색 순위** — SYN-011(3.14)·021(2.9)은 BM25 상위 5 카드에 정답 조항이 없어 판정이 없다 → `decisions/208`(운영 임베딩) 몫. ⚠ 전 24건에 「`/close` 가 `ended_at`·`status` 를 안 바꾼다」 경고는 그대로(09-17 미결)
- 이전 세션의 미커밋 D-5 파일(`ai/apps/evaluation/metrics/call_temperature.py` + 테스트 5)을 이번 커밋에 실었다 — 하네스 배선 전 모듈
- 남은 것: 장민석 님 검토(EXCLUDED 되살림 · 가족 호칭 목록 기반 · `compliance_rule` UPSERT 위치·severity) · 대시보드 컴플라이언스 경고를 프론트 로컬 규칙에서 서버 결과로(조서희) · TTS 키(정성윤)
