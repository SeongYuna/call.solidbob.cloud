---
date: 2026-09-11
author: "정성윤"
person: seongyun
seq: 3
---

- **정정 — 01·02번 기록에 틀린 곳이 더 있었다.** 규칙대로 두 파일은 고치지 않고 여기서 정정한다(CLAUDE.md §4).
  - 01: 훅 스크립트 경로를 `.claude/scripts/check_session_end.py`로 적었는데, 실제 위치는
    **`scripts/check_session_end.py`**다.
  - 01: "티켓 상태 경고 2종"이라고 적었는데, 실제 경고 둘은 **티켓 상태 정합성(②)과 중복 티켓(③)**이다.
  - 01: "기록 누락은 어겨도 아무 일이 일어나지 않아서 조용히 밀렸다"의 출처를 CLAUDE.md §4라고
    적었는데, 이 문장은 **§0.5**에 있다.
  - 01: STATE.md 문구 「운영 AWS 는 코드까지만 … `apply` 안 함, 뜬 리소스 0개」를 인용하면서
    가운데를 생략 표시 없이 줄였다. 원문 사이에는 `infra/terraform/`·`infra/docker/` 설명이 있다.
  - 02 ⑦: "터널은 09-10 오전에 열렸다"는 1차 근거가 없다. 장민석의 09-10 로그에는 시각이 없고,
    가장 이른 증거는 조서희 `e2c0208`(14:08)이다 — **"14:08 이전에 열렸다"**가 맞다.
- **발표 자료 4종을 1차 자료로 문장 단위 전수 감사했다.** 2차 자료(STATE.md·CLAUDE.md·조사 요약)는
  근거에서 빼고, 코드·git 커밋(메시지·작성자·시각)·결정 기록 원문·로그 front matter·티켓 front
  matter로만 대조했다. 틀리거나 1차 근거가 없는 곳이 functional-spec 12건, mentor-presentation
  약 15건, onboarding-briefing 10건, progress-briefing 28건 나왔고 전부 고쳤다. 가장 큰 것들 —
  ① **09-09 저녁 이후 서버 변경은 운영에 배포되지 않았다.** `release.yml` 실행 기록을 보면 PR #57
  (09-09 16:52)·#58(17:18)은 성공이고, **PR #59(18:13)·#61(09-10 22:35)은 실패**다. 코드가 바뀌었는데
  이미지 태그를 올리지 않아 파이프라인이 일부러 멈췄다(`f243a9e`의 설계). 태그는 `e9e4444` 이후 `0.1.1`
  그대로라, 운영에는 류준의 09-09 저녁 커밋과 09-10 서버 변경(통화 시작 API·문자열 응답·
  `DATABASE_URL` 수정·트리거)이 없다. 문서들은 "09-09 이후 자동배포가 계속 된다"는 식으로 읽혔다.
  ② **A-5의 1차 범위는 번역이 아니다.** `decisions/201`이 08-28에 ⓑ(한국어가 서툰 고객 발화의
  정확한 전사)로 정했고, 번역·TTS(ⓐ)는 여유 시 확장이다. 문서들은 A-5를 "외국어→한글 번역 +
  TTS"로 소개하고 있었다. A-5를 「사수」로 적은 것도 1차 근거가 없다(사수는 B·E·C-5).
  ③ **RRF는 함수만 있고 검색 경로에 연결되지 않았다**(테스트 밖 호출 0건). 생성 단계(B-4~6)도
  스니펫 폴백이라, B는 "RAG의 검색 단계"까지만 돈다.
  ④ **통화 종료 API(`/hub/calls/{call_id}/close`)는 스포크가 없어 501**이다. "501 응답이 이제 없다"는
  틀렸다 — 컴플라이언스와 통화 종료, 두 개가 아직 501이다.
  ⑤ 09-08 이후 바뀐 기존 테이블은 3개가 아니라 **6개**다(`masking_event`·`compliance_flag`·
  `knowledge_gap` 추가). 그리고 **`knowledge_gap → transcript_segment` FK가 `db/schema.sql`에 없다**
  — `fd96adc` 커밋 메시지는 "복합 FK로 이었다"고 적고 있다.
  ⑥ 기능명세서 엔드포인트 경로 2개가 틀렸다(`/hub/cards/{card_id}/feedback`,
  `/hub/knowledge-gaps/{gap_id}`). `/health`는 표에서 빠져 있었다.
  ⑦ 주차 표기: 1주차가 08-20~08-26이라 달력상 09-10부터 4주차인데, 팀 티켓은 09-10 작업을
  sprint 3으로 달았다. 문서에 이 사실을 밝히고 티켓 표기를 따랐다.
  ⑧ 그 밖에 인원별 수치(정성윤 로그 43개·류준 37개·장민석 08-27 커밋 29개), `server/.importlinter`
  계약 4종, C-5 누락 두 건의 성격(규칙 폴백이 아는 문맥 패턴 밖), 진행 기록 충돌 사례(`progress.markdown`
  37건 중 16건)와 STATE.md 사례(63건 중 머지 19건)가 섞여 있던 것 등을 고쳤다.
- 고친 파일: `_project/mentor-presentation.md` · `progress-briefing.md` · `functional-spec.md` ·
  `onboarding-briefing.md`. `_project/STATE.md`는 계속 손대지 않았다(사용자 지시).
- 남은 것:
  - **이미지 태그를 올려 09-10 변경을 운영에 반영한다**(정성윤). 운영 DB 저장 여부는 반영 후
    확인한다 — 운영 시크릿에 `POSTGRES_*` 키가 같이 있는지에 따라 지금 저장이 될 수도 안 될 수도 있다.
  - `knowledge_gap` 복합 FK 누락은 생성기(`db/generate_schema_docs.py`)와 커밋 설명이 어긋난 것이라
    류준·장민석에게 알린다.
  - 01의 "남은 것"(STATE.md 인프라 줄 갱신, 훅의 STATE.md 검사 여부)은 그대로 유효하다.
  - `CLAUDE.md`에서 사실과 달라진 곳이 셋이다 — 절대 원칙 5의 C-5 측정 범위 주석, §1의 서류 문의
    수치(174건·69종 → 재측정 146건·64종), 기능 블록 표의 A-5 「사수」 표기. 규칙 파일이라 이번엔 두었다.
