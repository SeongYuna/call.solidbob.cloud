---
date: 2026-09-11
author: "정성윤"
person: seongyun
seq: 14
---

- **12 기록의 「아직 모르는 것」 하나가 풀렸다 — RDS 에 붙은 DB 보안 그룹은 원안 이름 `assist-db` 다.**
  사용자가 CloudShell 에서 `describe-security-groups` 로 확인했다. 5432 인바운드 규칙은 1개이고, 소스는 보안 그룹 하나다.
- **아직 대조하지 않은 것**: 그 소스가 EC2 에 실제로 붙은 `assist-web` 인지. 결과가 ID 로만 나와서 이름을 알 수 없다.
  런북 3-2 에 두 ID 를 나란히 뽑는 명령을 넣었다(`assist-db` 의 5432 소스 · 실행 중 인스턴스의 보안 그룹).
  다르면 17-1 이 5초 뒤 타임아웃이 난다.
- 고친 곳: 런북 머리말·0장 자원표·3-2, 티켓 `w4-rds-prod-db`, `decisions/108` 정정 블록 — 「이름 미확인」 표시를 걷고
  「소스 대조가 남았다」로 바꿨다. 보안 그룹 ID 는 공개 저장소라 문서에 적지 않았다.
- 확인: 17-2 가 받아 가는 `main` 의 `db/schema.sql` 이 작업 트리와 같다 — diff 0줄, `CREATE TABLE` 22개,
  `transcript_segment` PK `(call_id, segment_id)`, 첫 줄이 런북이 확인하는 헤더다.
- **이 내용은 원래 13 번에 썼는데 게이트웨이 세션의 기록이 같은 번호로 덮어썼다.** 두 세션이 거의 동시에
  «비어 있는 다음 번호» 를 골랐기 때문이다(한쪽이 `ls` 로 확인한 직후 다른 쪽이 만들었다). 13 은 게이트웨이 기록으로
  두고 여기 다시 적었다. 같은 작업 트리를 여러 세션이 쓸 때는 **파일을 새로 만들 때 덮어쓰기를 막는다**
  (셸 `set -o noclobber` 등) — 이 파일은 그렇게 만들었다.
- 남은 것: 3-2 대조 → 12-2 → 17 → 19(9·10번) → `0.1.2` 머지 → 19(9·11번) 다시.
