---
date: 2026-09-22
author: "장민석"
person: minseok
seq: 14
---

- **main #133 에서 새로 내 몫으로 온 셋을 이어서 했다** (13번 뒤, 같은 PR #132 · server `0.1.35`)
- **`w6-call-record-customer-id`**(done) — `GET /hub/calls/{id}/record` 에 `customer_id`(HMAC, 없으면 null). 조서희 님께 필드 이름과 「해시라 화면에 그대로 보이지 말 것」을 티켓에 적었다
- **`w6-admin-stats-today`**(in-progress) — 현황판에 오늘(KST 자정부터) 칸 넷. **칸 정의는 조서희 님과 맞춘 게 아니라 티켓 괄호대로 예측했다** — 다르면 바꾼다
- **`w6-server-loose-ends`**(done) — ① `/health` `version`(빌드 인자 `APP_VERSION`) ② `eval_run.components` + 마이그레이션(서버 이미지 무관, `--record` 전에 — 미결로) ③ 보존 기간 `decisions/325` ④ ERD 재생성(생성기와 수동 편집이 일치 · PNG 는 임시 도커로 렌더) ⑤ D-6 주석 + `.importlinter` 계약 **범위를 좁혀** 추가(`hub.app` → 스포크 금지, KEPT — 통째로는 성립하지 않는다: `hub.dependencies` 는 합성, `hub.adapter` 는 인증 가드)
- **런북 19장 9번**에 `version` · `0.1.35` 스키마 변경(서버 무관) 주의 · 기획서 7.3절(`customer_id` · 오늘 칸)
- 검증: **통합 테스트 30건**(로컬 도커 PostgreSQL 17 + 현재 `schema.sql`) · 마이그레이션을 변경 전 스키마에 넣고 재적용이 멈추는 것까지
- 남은 것: 조서희 님 — 현황판 오늘 칸 · 통화 기록 `customer_id` 사용 · 읽기 토큰(`w6-read-path-token-ui`). 운영 — `eval_run` 마이그레이션 · 시드 · `READ_AUTH_REQUIRED`
