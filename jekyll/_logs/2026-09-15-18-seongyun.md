---
date: 2026-09-15
author: "정성윤"
person: seongyun
seq: 18
---

- **정정 — `…-15` 기록의 「복원본을 만들었다」는 틀렸다. `.github/ruleset-main.json` 은 이미 있었다.**
  `22c7983` 에 들어와 있었고, `contexts` 가 **셋**(`server`·`ai`·`jekyll`)인 옛 판이었다.
  내가 한 것은 «만든 것»이 아니라 «라이브와 일치시킨 것»이다. `git status` 가 `??` 가 아니라 `M` 으로
  나온 것을 보고 알았다 — 파일이 없다고 단정하고 `ls`·`git log` 로 확인하지 않았다.
  - 잃은 것은 없다: 최상위 키 여섯 그대로, `rules` 만 바뀌었다(`contexts` 셋 → 다섯 + `integration_id`).
  - **오히려 근거가 세졌다.** `branch-protection.json` 과 `ruleset-main.json` 이 **둘 다 같은 병**으로
    어긋나 있었다 — 룰셋만 콘솔에서 바뀌고 저장소의 사본 둘은 아무도 안 고쳤다.
    `CLAUDE.md` §7 에 넣은 「룰셋을 고치면 함께 갱신한다」가 한 파일이 아니라 **두 파일**에 걸린다.
  - 고친 곳: 미결 항목 · `STATE.md` 인프라 줄 · `w8-final-docs`(대조 검사 부재 사례에 한 건 추가).

- **같은 부류를 더 찾아 고쳤다 — 이름·설정이 바뀌었는데 문서가 안 따라온 것 넷.**
  - `.claude/rules/rfp-harness.md` — 「필수 검사는 `test.yml` 의 job 이름(`server`·`ai`·`jekyll`)」
    → 다섯이고 `tag-check` 는 **`tag-check.yml`** 에 있다. `test.yml` 만 보면 안 된다.
    같은 문단의 「브랜치 이름이 걸린 곳은 push 트리거 목록 하나뿐」도 낡았다 — 트리거가 `[main]` 으로
    좁혀져(`decisions/112`) 이제 브랜치 이름이 걸린 곳이 **없다.**
  - `docs/harness.md` — 「CI `gateway` job(아직 필수 통과 검사 아님)」 → 09-15부터 필수다.
  - `_project/STATE.md` 인프라 줄 — 「룰셋 필수 검사는 아직 셋뿐 · `tag-check`·`gateway` 미등록」
    → 다섯. OIDC 확인 완료도 함께 적었다.
  - `CLAUDE.md` §3 — `apps/` 를 `call` 하나로 적고 있었다. 실제로는 **셋이 배포 중**이다
    (`call`·`admin`·`platform`, 각각 call·admin·www.solidbob.cloud). `apps/admin`(32파일)·
    `apps/platform`(22파일)이 `CLAUDE.md` 어디에도 없었다 — 모든 세션이 처음 읽는 파일인데 빠져 있었다.

- **남긴 것 — 작업 트리의 `apps/dashboard/` 는 잔존물이다.** git 추적 0개, `dist`+`node_modules` **70MB**.
  09-14 개명 때 남은 것이라 지워도 된다. 공유 작업 트리라 내가 지우지 않고 `CLAUDE.md` §3 에 적어만 뒀다.

- 검증: 지킬 빌드 197페이지 · 깨진 링크 0 · JSON 둘 다 파싱 정상 ·
  `ruleset-main.json` 의 쓰기 가능 필드 여섯이 라이브와 동일함을 재대조.
