---
date: 2026-09-17
author: "정성윤"
person: seongyun
seq: 2
---

- **골든셋 남은 오답 2건(`GS-204`·`GS-205`) 원인 분석** — `decisions/206` 「원인 미분석」을 닫는다. 이 머신에 ES·모델이 없어 실측 재현은 못 했고, 206 의 수치 역산 + 지식베이스·골든셋·검색 코드 대조로 추론했다.
  - `GS-204`「필요한 서류를 한 번에 알려주실 수 있나요」→ 지침 1.3. **BM25 는 맞히고 dense 만 놓친 유일한 건.** 원인 ① 지침 1.3 과 매뉴얼 3.2 가 같은 규칙을 적은 쌍둥이 조항인데 골든셋이 1.3 만 정답, 3.2 는 방해 문서로 적었다 — 사유는 항목 메모의 「시민 문의라 지침 쪽」뿐이고 README 규약에 없다(GS-216 도 같은 사유). 본문에 시민용/상담원용 표지가 없어 검색기가 알 수 없는 기준이다. ② 「필요서류」 조항 36개·「○○ — 필요서류」 절차 목록 33개 사이에서 구분 단서가 「한 번에」(2조항) 하나뿐 — BM25 는 희귀 구절 정확 일치로 맞히고 임베딩은 주제에 묻힌다. ③ 206 표 역산으로 리랭커(hybrid cand=20, 94건)도 이 쌍둥이를 못 가른 것으로 보인다(항목별 데이터 없어 미확정).
  - `GS-205`「이건 시에서 결정해주시는 건가요」→ 지침 1.2. **BM25·dense 둘 다 놓친 유일한 건.** 원인 ① 「이건」이 가리키는 앞 맥락이 항목에 없다(`context_utterances` 빈 칸 — 하네스는 맥락이 있으면 질의에 붙인다). ② 겹치는 낱말이 「결정」 하나(1.2·3.8). 「시」는 도시 뜻 1조항 vs 때 뜻 14조항이고 분석기에 품사 필터가 없어 조사·어미까지 질의 토큰이 된다. 조사만 뗀 근사 계산에서는 「결정」 하나로 1.2 가 1위라 **실제 nori 토큰화가 근사와 다르다** — `_analyze` 확인이 먼저다. ③ 「소관 부서」 조항 10개·방해 문서(매뉴얼 1.1·지침 4.2)가 뜻으로 인접.
  - 남은 것: `scripts/compare_retrievers.py` 가 변형별 놓친 ID 만 저장하고 항목별 상위 k 를 안 남긴다 — 다음 측정 때 같이 기록하면 파일만으로 이 분석이 된다. `GS-204` 는 측정이 아니라 **채점 기준 결정**(3.2 를 정답에 추가 / 쌍둥이 조항 통합 / 규약을 README 에 명시)이라 결정 기록으로 남겨야 한다. `GS-205` 는 nori 토큰화 확인 뒤 앞 발화 보강.
- **게이트웨이 단독 검사** — 타입 검사 0 오류 · 유닛 테스트 **101/101** · 운영 `/gateway/health` 정상(키·캡·토큰 2종 설정됨, 활성 통화 0). 구글 키 없이 로컬로 띄우고 가짜 hub 를 붙여 문 검사: 프록시 뒤 토큰 없음 401 · 과금 문에 뷰 토큰 401 · 틀린 토큰 401 · 허용 안 된 Origin 403 · 구글 키 없이 `/ingest` 1011+사유. 전부 설계대로.
- **프론트 실제 코드로 왕복 테스트 (구간별 로그)** — `apps/call` 의 `RealGatewayClient`·`callStore` 를 esbuild 로 번들해 Node 에서 그대로 실행, 홍보 페이지·상담원 박스 훅이 하는 일(주소·서브프로토콜·메시지)을 옮겨 로컬 게이트웨이 + 가짜 hub 에 돌렸다. **프론트→게이트웨이·게이트웨이→서버·게이트웨이→프론트 세 구간 정상, 결함은 스토어 한 곳.** 지금 코드(통화 ID 각자 생성)로는 게이트웨이가 두 통화로 갈라 `/hub/calls` 가 두 번 가고, 대시보드가 `call_id` 없이 구독해 둘 다 받으며, `applyTranscript` 가 `call_id` 대조 없이 `segment_id` 만으로 덮어써서 **고객 1번 줄이 상담원 1번 줄에 덮여 사라지고 `callId` 가 메시지마다 바뀐다**(게이트웨이는 통화마다 1번부터 센다). 같은 ID 를 넘기면 1·2·3 순서대로 남는다. 곁가지: 못 읽는 메시지 하나가 `setError` 로 `connected=false` 를 만든다 — 소켓은 열려 있는데 화면은 끊김 표시. **고칠 곳은 `apps/` 둘뿐(조서희 전담)** — ID 공유 + ID 로 구독/필터.
- **Vercel 환경변수 반영 · 강제 배포** — `call-solidbob-cloud-kxu6`(call.solidbob.cloud) 에 `VITE_CORE_API_URL`·`VITE_GATEWAY_DEMO_BASE_URL` 을 넣었는데 번들에 안 구워졌다. 원인: 대시보드 재배포가 **Ignored Build Step 에 걸려 1초 만에 Canceled**(`apps/call` 안 바뀐 커밋). CLI 로그인 후 저장소 루트를 프로젝트에 링크하고 `vercel deploy --prod --force` 로 올렸다(CLI 배포는 Git 무시 단계를 안 탄다). 링크가 `.gitignore` 두 곳에 `.env*` 를 넣고 `.env.local`(OIDC 토큰)을 만든 것은 되돌리고 지웠다 — 남긴 것은 gitignore 대상 `.vercel/` 뿐. 이어서 사용자가 `VITE_GATEWAY_WS_URL`(뷰 토큰 포함) 을 넣고 대시보드 재배포 → **변수만 바꾼 재배포는 취소되지 않았다.** 번들 `index-CCfzVHkg` → `index-C0zlHR-Y` → `index-DnnU5OWn`, 주소 함수 셋 채워짐 확인.
- **운영 연결 검사** — 상담원 오리진으로 `GET /hub/calls` 200 + 허용 헤더 · `/gateway/dev/text` 토큰 없이 401(오리진 허용, 토큰만 없음) · 번들에 구워진 구독 주소 그대로 운영 `/gateway/ws` 에 열어 **63ms 열림, 헬스 `dashboards` 0→1→0**, 실제 프론트 클라이언트·스토어로 붙여 `mode=live connected=true error=null`. 운영 DB 에 남는 실제 발화는 넣지 않았다.
- **정리한 것** — ① 상담원 박스만 말하면 저장은 `call`·`transcript_segment`(확정만, interim 은 미저장)·`masking_event`(PII 있을 때) 셋뿐 — 추천 트리거가 고객 발화 전용이라 `recommendation`·`closure`·`call_guard_flag` 는 안 생긴다. 고객 발화까지 넣으면 그 넷이 더 는다. 스키마에 ON DELETE CASCADE 가 0 곳이라 지울 때 자식부터. ② 「전화 버튼 → DB」 경로의 경위: 09-11 조서희 첫 게이트웨이 `/dev`(`test-dev-mic-*`) → 같은 날 `decisions/109` 로 정식 게이트웨이 `/dev/text` + 과금 토큰 요구 → 09-14 `f9adace` 로 히어로 「상담원에게 전화 걸기」·상담원 「고객과 전화하기」 박스로 이동(사용자 지시: 방향 전환·텍스트만·팀원 전용). **이 09-14 변경은 진행 기록에 없고 커밋·코드 주석에만 있다.** 운영 `call` 에 `test-web-agent/platform` 행이 0 — 새 버튼으로 운영까지 말해 본 적이 아직 없다.
- 남은 것: `apps/` 두 결함(ID 공유·스토어 필터, 오류 시 연결 표시)은 티켓 미작성 — 조서희 님과 정할 것. `GS-204` 채점 기준 결정 기록. 실제 발화 1건 운영 왕복(팀 토큰 링크). 중복 티켓 `w1-platform-landing` ↔ `w3-platform-landing-refresh` 그대로.
