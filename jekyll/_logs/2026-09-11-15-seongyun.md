---
date: 2026-09-11
author: "정성윤"
person: seongyun
seq: 15
---

- **운영 서버가 처음으로 DB 에 붙었다 — RDS 전환 완료(`0.1.2` 배포 전 단계까지).** 사용자가 운영 인스턴스 SSM
  세션에서 런북 12-2 → 17 을 직접 실행했고, 이 세션은 매 단계 출력을 받아 확인했다([w4-rds-prod-db](/backlog/w4-rds-prod-db/)).
  - 1단계 대조(CloudShell): `assist-db` 의 5432 소스 = 운영 EC2 에 붙은 `assist-web` — 두 ID 일치.
  - 12-2: 백업 → 키 확인 → `DATABASE_URL`·`POSTGRES_*` 를 RDS(사용자 `callguard` · DB `assist` · `sslmode=require`)로 →
    AWS 정적 키 제거(실제로 들어 있었다) → 재시작.
  - 17-1: 서버 파드에서 `('assist', 'callguard') ssl True`. 17-2: `main` 의 `schema.sql` 적용 → **22 테이블**.
  - 19-10(이 세션이 밖에서): 운영 `GET /hub/knowledge-gaps` **500 → 200** (`{"gaps":[],"total":0,…}`).
    09-08 배포 이후 운영이 DB 에서 읽은 첫 응답이다. `/health` `spokes` 는 아직 3종(`0.1.1`)이고
    `/openapi.json` 에 `POST /hub/calls` 는 아직 없다 — `0.1.2` 가 배포돼야 생긴다.
- **막힌 곳 둘 — 둘 다 SSM 웹 터미널 때문이었다. 런북 12-2·17-1 에 넣었다.**
  - 여러 줄을 한 번에 붙이자 `sh` 가 붙여넣기 표시를 명령으로 읽었다(`$'\E[200~': command not found`). 그 바람에
    `K=` 줄이 사라져 `get: command not found`. 한 줄씩 붙이는 것으로 풀었다.
  - **첫 시크릿 교체에서 암호가 빈 값으로 들어갔다** → `fe_sendauth: no password supplied`. 붙여 넣을 때 딸려 온 빈 Enter 를
    `read -rsp` 가 먼저 받았다. 두 번 다시 해도 길이 0 이었다. 빈 입력이면 다시 묻는 반복문
    (`until [ -n "$PGPW" ]; do read -rsp …; done`)으로 바꾸자 들어갔다. 암호 길이는 `read` 결과와 시크릿의 값을 비교해 확인했다.
    **첫 시도는 틀린 값이 운영 시크릿에 들어가 재시작까지 된 상태였다** — 운영은 원래 DB 에 안 붙어 있었으므로 잃은 것은 없다.
- 남은 것: `0.1.2` PR(이 세션이 게이트웨이 변경을 빼고 만든다 — 사용자 요청 대기) → 머지 → `release.yml` →
  19-9(`spokes` 에 `trigger`) · 19-11(통화 → 전사 → 조회). 확인 뒤 인스턴스의 `~/server-env.backup.yaml` 을 `shred -u`.
  운영 ES 지식베이스는 아직 비어 있어 검색은 503 이다.
