---
date: 2026-09-23
author: "정성윤"
person: seongyun
seq: 3
---

- **PR #143(frontend) 태그 검사 실패를 풀었다** — `services/call-mediator/src/` 가 바뀌었는데
  `infra/k8s/base/kustomization.yaml` 의 콜 미디에이터 `newTag` 가 `0.2.7` 그대로였다.
  그 태그는 이미 레지스트리에 있고 파드가 `imagePullPolicy: IfNotPresent` 라, 같은 번호로 다시 구우면
  노드가 캐시된 옛 이미지를 계속 쓴다 — 배포는 초록인데 옛 코드가 도는 상태가 된다. 검사가 막는 게 맞다.
- **`0.2.8` 로 올려 같은 PR 안에 넣었다.** 올리기 전에 네 브랜치의 `kustomization.yaml` 과
  Docker Hub 태그 목록을 둘 다 봐서 `0.2.8` 이 아무도 안 쓴 번호인지 확인했다(`decisions/131` 의
  브랜치 간 태그 충돌 검사에 다시 걸리지 않게). 서버는 `server/`·`ai/` 변경이 없어 `0.1.38` 그대로 둔다.
- 푸시 후 필수 검사 다섯(`server`·`ai`·`jekyll`·`call-mediator`·`tag-check`) 전부 통과.
- 남은 것: PR #143 머지는 지시를 기다린다. 머지되면 `release.yml` 이 `0.2.8` 을 굽고 배포한다.
