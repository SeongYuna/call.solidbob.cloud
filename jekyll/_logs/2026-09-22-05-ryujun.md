---
date: 2026-09-22
author: "류준"
person: ryujun
seq: 5
---

- **정성윤 님 부탁 셋(모델 운영 탑재 뒤 확인)을 처리했다.**
- **부탁 2 — 운영 모델 구성 공식 측정.** `decisions/124` 대로 운영에는 **NER + KoE5 만(리랭커 없음)** 올라갔는데, 오전 run_id 2 는 리랭커를 켠 `rerank-dense` 라 운영과 달랐다 → `--retriever dense` 로 다시 쟀다. 커밋 `e966b62`(깨끗한 트리, `-dirty` 없음) · 로컬 Postgres · 각 3회 최저 `--record`:
  **run_id 3 운영 모델 구성 Recall@5 0.969(93/96) · MRR 0.868** / **run_id 4 운영 구성(규칙+BM25) 0.812(78/96) · 0.635**. 둘 다 C-5 누락 0(n 28, P1~P7 표본 모두) · F-2 1.0(n 99, 규칙표 상한) · 컴플라이언스·콜 가드 1.0(상한) · B-6 기권 0(n 24). 리랭커를 빼도 맞힌 건수는 같고 MRR 만 −0.047(0.914→0.868). 오류 내성(시드 3): dense 는 WER 0~0.201 전 구간 0.969, BM25 는 10% 0.792 · C-5 규칙+NER 누락 0/0/0/2/2, 규칙만 0/1/2/4/4. 측정은 서브에이전트, `eval_run`·`eval_result` 행은 `data/processed/eval-export/2026-09-22-run-3-4.json`. ⚠ `eval_run` 에 검색·NER 구성 컬럼이 없어 **DB 만 보면 run 3·4 가 구분되지 않는다** — 구성은 이 기록과 내보내기 파일에 있다. `golden_set_version` 은 파일 이름이라 `v1-150`(실제 판 `v1-150.1`).
- **부탁 1 — 합성 통화 둘을 운영에 흘렸다**(`wss://server.solidbob.cloud/call-mediator`, `--watch`, `--close`·`--speak` 없음). SYN-010(124 가 허용한 대본) 12:19:59~12:20:31 `syn-prod-syn-010-20260922` · **SYN-008 은 류준 결정으로 추가**(SYN-010 에 이름이 없어 확인 b 를 못 보므로 — 124 의 허용 범위 밖) 12:20:52~12:22:27 `syn-prod-syn-008-20260922`. 통화는 지우지 않았다. 처음엔 서브에이전트에 맡기려 했으나 권한 분류기가 막아(비밀 토큰 + 운영 쓰기) 사용자 지시로 이 세션에서 직접 돌렸다. 토큰은 명령 안 환경변수로만 썼고 저장 파일에 없음을 확인했다.
  - **b 마스킹 통과** — SYN-008 4턴 「이름만 답한 발화 + 주소」가 자막·저장본(`GET /hub/calls/{id}/transcript`) 둘 다 `***이요. ******************.`(P6 0~3 · P7 7~25). 14턴 전화번호 P4 도. 저장본에 원문 이름·주소·번호 0건.
  - **c 통과** — `/health` 전·SYN-010 뒤·SYN-008 뒤 모두 `ok` · 스포크 11개 동일 · `ingest_guard: locked`.
  - **a 주의** — `retrieval_ms` 는 `/ws` 로도 DB 로도 안 나가서 상한인 `recommendation.internal_latency_ms`(`/record`, 인증 없음)로 봤다. **SYN-008 추천 8건은 394~778ms(전부 1,000 미만).** **SYN-010 추천 2건은 3,300 · 2,118ms** — 모델 탑재 뒤 운영이 처음 받은 검색 요청이라 첫 요청 예열로 보이지만 **가설이다.** 두 건의 실제 `retrieval_ms` 는 서버 로그로만 가른다 — 정성윤 님께.
  - **발견 ① 필요서류 절차를 엉뚱하게 잡는다(운영 재현)** — SYN-010 1순위 카드는 정답 `TERM-2.12`(광역 교통)인데 판정은 `TERM-2.9 대중교통 분실물 수령` `incomplete`. 2.12 에 규칙이 없어 「규칙 있는 첫 카드」로 건너뛴 것 — 09-21 로컬 SYN-023 과 같은 원인(미결). SYN-008 도 `6.9 지원금 이의신청`·`3.5 수도 명의변경` 을 번갈아 잡았다(대본은 수도요금 이의신청).
  - **발견 ② SYN-010 마지막 턴(5, 상담원 인사)이 저장되지 않았다** — 저장 4/5 구간, `--watch` 자막도 4개. SYN-008 은 16/16. 재생기가 마지막 확정 직후 연결을 닫는 경쟁으로 보인다(가설).
- **부탁 3 — 모델 폴더 해시** — 정성윤 님 명령 그대로 `data/processed/models-sha256-ryujun.txt`(20파일). 스냅샷 커밋 KoE5 `bc6d284c60fe5a973e74c1751b92594c9f581213` · koelectra-ner `7fe2d3297113e0753716d7f2c85d4880d288542d`(둘 다 받은 파일 전부 한 커밋).
- **정리** — PR #117 태그는 `main` 이 `0.1.28` 을 먼저 써서 `0.1.29` 로 올렸다가, 성윤 님이 이미 `0.1.30` 으로 고쳐 머지한 것을 보고 내 커밋(태그 한 줄뿐)을 버렸다. **04번 기록의 「`0.1.28` 로 올렸다」는 결국 `0.1.30` 으로 머지됐다.** 에이전트 워크트리 4개를 지웠다(전부 머지됨, 브랜치는 남김).
