---
date: 2026-09-15
author: "장민석"
person: minseok
seq: 9
---

- **운영 DB 27 테이블 확인** — 정성윤 님이 서버 파드에서 찍은 `pg_tables` 출력을 `schema.sql` 의 `CREATE TABLE` 27개와 diff, 이름 완전 일치(`blacklist_entry_expiry_change` 포함). 컬럼은 이름만 봤다.
- **PR #88 머지(`76423e8`, 내 계정) → 서버 `0.1.9` 운영 배포.** 머지 직전 main 에 정성윤 님 Redis(PR #89)가 들어와 `BEHIND` 였다 — main 을 당겨(충돌 없음, 태그 검사·server 739 통과) 푸시한 뒤 CI 초록·`CLEAN` 에서 머지했다.
  `release.yml` plan·image·k3s-deploy 성공(gateway 건너뜀).
- **운영 확인(밖에서, 쓰기 없음)** — `/health` 정상(spokes 7) · `/openapi.json` 31 → **34경로**(`/hub/calls/{id}/record` · `…/expiry` · `…/expiry-changes`) · 추천 `CardSchema.card_id` 있음 ·
  **헤더 없는 관리자 요청 401**(`/admin/auth/me`·`/admin/agent-tokens`·`/hub/blacklist-requests`·`POST …/expiry`) — 0.1.8 에서 500 이던 것 · `GET /hub/calls/<없는 통화>/record` 404 · `GET /hub/calls` 200.
- 못 본 것: 로그인한 관리자 요청(구글 로그인 필요)·연장 API 실제 쓰기·추천 `card_id` 실값 — 운영에 쓰기를 하지 않았다. 관리자 로그인이 운영에서 도는지는 PR #89 Redis 뒤로 아직 확인 안 됨.
- `origin/main` 을 `server` 로 당겼다.
