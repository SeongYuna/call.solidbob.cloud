---
date: 2026-09-20
author: "정성윤"
person: seongyun
seq: 2
---

- **운영 DB 를 «컬럼 단위»로 처음 대조했다 — 어긋남 0.** 지금까지는 테이블 **이름**까지만 봤다(09-15 기록에도 *"컬럼은 이름만 봄"*).
  SSM 으로 `information_schema.columns` 를 떠서 `db/schema.sql` 과 맞춰 보니
  **29 테이블 · 198 컬럼 · 타입·길이·NULL 여부까지 전부 일치**했다.
  - **도구로 만들었다 — `scripts/compare_prod_schema.py`**(일회성 명령이 아니라 팀이 다시 돌릴 수 있게).
    이 스크립트는 **DB 에 접속하지 않는다** — 운영 조회는 SSM 쓰는 사람이 하고, 여기서는 **텍스트 둘만 비교**한다(자격증명을 안 다룬다).
  - ⚠ **「어긋남 0」을 그대로 믿지 않고 음성 대조군을 먼저 돌렸다** — `schema.sql` 사본의 컬럼 하나를
    길이·NULL·타입으로 어긋나게 만들고 없는 컬럼을 끼워 넣어 **3건 전부 잡히는 것**을 확인한 뒤에 결과를 받아들였다.
    이 저장소가 반복해서 겪은 「빈 채로 초록불」을 같은 방식으로 만들지 않으려고 한 일이다.
  - 미결 「머지=배포인데 스키마는 사람 손」은 **닫지 않았다** — 구조적 위험은 그대로다. **지금 어긋난 게 없다는 사실만** 적었다.
- **런북 19장 검증 체크리스트를 완주했다 (1·2·4·6·7 · 9·10·12·13·14 전부 통과).**
  - 노드 Ready(12일) · 파드 **4 Running**(ES 재시작 2회) · nori `9.5.1` · 인덱스 `callguard-kb-single` **98 docs green** ·
    RDS **29 테이블 · ssl True** · S3 `assist-apne2/uploads/` 접근 됨 · 서버 env 6종 SET ·
    콜 미디에이터 STT 키 마운트(`callguard-stt.json`) · 토큰 4종 true · 문 2곳 401 · `/dev` 200 · `/dev/text` 401.
  - **건너뛴 것**: 3·5·8 은 이 인스턴스가 `t3.large` 라 해당 없음(`decisions/116`) ·
    **11(DB 쓰기)은 운영 DB 에 행이 남아 일부러 안 했다** · 9-1 은 `0.1.19` 가 배포 전이라 404(정상).
  - **런북을 고친 곳 둘** — 6번 기대값 「22 테이블」이 09-11 값이라 **29** 로 고치고, 「테이블 수만 세면 컬럼 어긋남을 못 본다」를 덧붙였다.
    19장 머리말에 완주 기록을 남겼다.
  - 인스턴스 가동 **6일 9시간**(09-14 기동) — 자동 중지를 안 하기로 한 `decisions/116` 과 일치한다.
- 이 과정에서 **로컬 `aws` CLI 도 cp949 에서 죽는 것**을 봤다(SSM 출력에 `—` 가 있으면). `PYTHONIOENCODING=utf-8` 로 감쌌다 —
  훅에서 겪은 것과 같은 뿌리다(어제 `python3 -X utf8`).
- 남은 것: 커밋 · 콜 미디에이터 `e2e_latency_ms` · 발표 산출물 뼈대 · 상담원 토큰 만료 결정
