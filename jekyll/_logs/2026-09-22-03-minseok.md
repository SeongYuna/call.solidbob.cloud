---
date: 2026-09-22
author: "장민석"
person: minseok
seq: 3
---

- **PR #111 이 머지·배포된 것을 확인했다** — 01:26 머지 → release `image`·`k3s-deploy` 성공 → 운영에서 토큰 없는 `/close`·카드 피드백이 **401**(`0.1.25` 가 떠 있다). 운영 마이그레이션은 정성윤 님이 넣었다(나는 운영 DB 를 볼 수 없어 전달받은 사실이다). ⚠ **초안으로 열었던 PR 이 프론트보다 먼저 머지됐다** — 확인 시점 main 기준 `closeCall` 토큰은 #116 으로 들어왔고, **카드 피드백 토큰 · 관리자 반려 사유 · 온도 null 표시 셋은 프론트에 없다** → 운영에서 카드 사용 표시가 조용히 401, 관리자 반려가 422, 온도 이상이 「0건」으로 보인다. 미결에 적었고 사용자에게 알렸다(프론트는 조서희 님 전담이라 고치지 않았다).
- **`POST /hub/routing-decisions` 를 서비스 토큰 문에 넣었다**(`decisions/126` 이 넘긴 몫) — 부르는 곳(콜 미디에이터)이 붙기 **전에** 잠갔다. 미디에이터의 서버 호출은 이미 `CORE_API_TOKEN` 을 싣는다. 프론트는 부르지 않는다(전수 grep).
- **관리자 현황판 집계 `GET /hub/admin-stats`** 를 만들었다(`w6-admin-stats-api` — 조서희 님이 「장민석 6번」으로 기다리던 것으로 보고 만들었다, 확인은 조서희 님께). 관리자 로그인 · DB 없으면 501 · 문자열 여덟 칸(통화 전체·종료 · 콜 가드 · 승인 대기 · 활성 등록 · 배정 판정 전체·블랙리스트·일반 배정으로 떨어짐) + 센 시각. 문장 하나로 센다. 상담원 단위 숫자는 없다(부록 A-1, 테스트로 고정). 수직 슬라이스 단면(schema → router → dto → 입력 포트 → 인터랙터 → 출력 포트 → postgres → provider → 테스트) 그대로.
- 검증: server **1,290 통과** + integration **28**(새 `postgres:17` + 현재 `schema.sql` — 첫 시도는 테스트 데이터가 `blacklist_entry.request_id` NOT NULL 을 어겨 실패, 고쳐서 통과) · ai 454 · 계약 4종 KEPT. 스키마 변경 없음.
- 남은 것: 이 변경 커밋·PR(태그 `0.1.26`) — 프론트 의존이 없어 초안이 아니어도 된다 · 조서희 님: 현황판 연결 + 운영에서 깨진 셋 · `w6-admin-agent-mapping` 운영 승인 1건 확인(관리자 화면에서 직접).
