---
date: 2026-09-22
author: "정성윤"
person: seongyun
seq: 15
---

- **PR #131(장민석 — server `0.1.34` · call-mediator `0.2.5`, 쓰기 경로 fail-closed · J-5 배정 판정 호출) 머지.** 사용자 요청은 「충돌 해소 뒤 머지」였는데, 열어 보니 장민석 님이 이미 main 을 병합해 STATE 충돌을 풀어 올린 상태(`7cd46c0`)라 GitHub 판정 MERGEABLE·CLEAN, 필수 검사 다섯 전부 통과. 충돌 작업 없이 머지 커밋 `4668d24`(16:02 KST).
- **릴리스 워크플로 성공** — plan · image · call-mediator-image · k3s-deploy 전부 success(es-image 는 skipped, 정상). 운영 `/health` 는 `ok`, 스포크 11종, `ingest_guard: locked` — 이번 PR 로 서비스 토큰이 「비우면 닫힘」으로 바뀌었지만 운영은 이미 잠겨 있어 막힌 경로 없음. `/health` 에 버전 필드가 없어 이미지는 Docker Hub 태그로 확인했다 — `callguard-server:0.1.34` · `callguard-call-mediator:0.2.5` 둘 다 16:03 KST 푸시됨, plan 잡 출력 「배포할 태그: server=0.1.34 call-mediator=0.2.5」. 파드가 그 태그로 떴는지는 SSM 이 없어 안 봤다(k3s-deploy success 까지만).
- 이 PR 이 싣고 온 결정 `320`(J-5 호출자 — 완성본은 교환기, 시연은 콜 미디에이터 대리)은 장민석 님 것 그대로다. 오늘 오후 「126 을 섀도 판정으로 정정」을 권고했는데 `320` 이 같은 자리를 「시연용 대리 + 한계 둘 명시」로 먼저 정리했다 — 화면 문구 정정은 여전히 남아 있다(조서희 님).
- 남은 것: 운영 `ROUTING_CANDIDATES` 값(없으면 늘 빈 목록 → 기존 규칙으로 떨어짐, SYN-007 시연 전에 필요) · 배포 뒤 테스트 통화로 `routing_log` 행 확인 · 상담원·관리자 화면의 「누구에게 배정되는가」 문구 정정.
