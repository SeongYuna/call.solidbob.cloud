---
date: 2026-09-11
author: "정성윤"
person: seongyun
seq: 23
---

- **22 기록의 권고(A — mock 유지 + `?gateway=`)에 빠진 위험을 더했다.** a5 세션이 알렸다 — 대시보드의 `?gateway=` 덮어쓰기
  (`apps/dashboard/src/lib/ws/types.ts`)가 `ws://`·`wss://` 로 **시작만 하면** 어떤 주소든 받아 localStorage 에 남긴다. 링크 하나로
  그 브라우저의 대시보드를 남의 서버에 붙여 **가짜 자막·「필요서류」 카드**를 띄울 수 있다. 앞서 이 코드를 읽고도 A 를 권하면서 이 점을
  적지 않았다. 운영 번들(`index-ZZuCSHQN.js`)에서 `callguard:gatewayUrlOverride` 와 두 `startsWith` 를 직접 확인했다 — PR #70 으로 이미 나가 있다.
- [미결](/open-items/)에 적었다: A 옆에 ⚠ · 권고를 「허용 목록을 먼저 고친 뒤 A」로 · 별도 항목(고치는 법 — `wss://server.solidbob.cloud/gateway/*`
  와 로컬 `ws://localhost`·`127.0.0.1` 만 허용 + 덮어쓰기 중 배너). **코드는 `apps/`(조서희 님 전담)라 고치지 않았다** — 누가 고칠지는 사용자 결정이다.
- PR #71 은 이 보강 전에 머지됐다(`78a505d`) — 그래서 후속 PR 로 넣는다.
