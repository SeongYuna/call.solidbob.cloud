---
date: 2026-09-22
author: "장민석"
person: minseok
seq: 9
---

- **정성윤 님 티켓 둘을 넘겨받았다** — `w7-j5-routing-caller` · `w6-deploy-schema-precheck`. 05번 로그에서 「넘겨받아도 되는지 묻는다」로 미결에 올렸고, 정성윤 님 12번 로그가 「장민석 님께 넘기기로 한 둘」로 답했다. 둘 다 착수 전(`todo`)이라 담당을 옮기고 본문에 넘겨받은 사실을 적었다. 결정(`126`·`128`)은 정성윤 님 것 그대로 따른다. 미결 항목은 닫았다.
- **`w7-j5-routing-caller` 구현** (`decisions/126`) — 콜 미디에이터가 `POST /hub/calls` 성공 직후 `POST /hub/routing-decisions` 를 부른다. 통화당 한 번, 시작이 실패하면 안 부른다, 판정 실패는 로그만(통화는 막지 않는다). 전사는 판정을 기다리지 않는다(`started` 에 묶지 않음). 후보는 `ROUTING_CANDIDATES`(비면 빈 목록 → 서버가 기존 배정 규칙). 결과는 **로그에만** — 화면 표시는 조서희 님과 정한 뒤다.
  - 검증: call-mediator **140 passed** · `tsc --noEmit` 통과. 태그 call-mediator `0.2.3` → **`0.2.4`**
  - 서버 쪽 문은 이미 있었다(`routing-decisions` 는 09-22 서비스 토큰 문에 들어갔고, 방금 fail-closed 로 바뀌었다 — 08번)
- **`w6-deploy-schema-precheck` 는 착수하지 않았다** — 수동 QA 게이트(`decisions/319`) 뒤다.
- 남은 것: 운영에 `ROUTING_CANDIDATES` 를 넣을지(시연 상담원 ID 필요 — 안 넣으면 늘 빈 목록) · 판정 결과 화면 표시(조서희 님) · 배포 뒤 테스트 통화로 `routing_log` 행 확인
