---
date: 2026-09-19
author: "정성윤"
person: seongyun
seq: 3
---

- **이 머신에 운영을 읽을 길을 냈다 — AWS CLI + SSM `send-command`.** `winget` 은 관리자 권한을 요구해 1602 로 실패했고
  **MSI 사용자 설치**(`ALLUSERS=2 MSIINSTALLPERUSER=1`)로 우회했다(`aws-cli/2.36.49`, PATH 등록까지).
  자격증명은 `.env` 의 `AWS_*` 세 키를 **값을 찍지 않고** 환경변수로만 읽어 쓴다. 첫 시도는 `SignatureDoesNotMatch` —
  형식(AKIA+16 / 40자 base64)·시계(오차 1초)가 다 정상이라 **시크릿 오타**로 좁혔고, 다시 넣은 키로 통과했다.
  ⚠ 쓰는 주체는 **IAM 사용자 `admin`**(`arn:aws:iam::621724235711:user/admin`)이다 — 루트 키가 아니다(루트면 ARN 이 `:root` 로 끝난다).
  방식은 릴리스 워크플로와 같은 `ssm send-command` + `get-command-invocation` 이라 **세션 매니저 플러그인이 필요 없다.**
  인스턴스는 `i-034cda2423f65c8bd`(t3.large · 09-14 기동 · **Name 태그 없음** · SSM Online).
- **① 운영 이미지 태그 대조 — 끝.** `callguard-server:0.1.18` · `callguard-call-mediator:0.2.1` · `callguard-es:9.5.1` · `redis:7.4-alpine`,
  파드 넷 Running(서버·미디에이터는 **09-18 08:23 UTC 생성**). **즉 09-17·18 작업이 이미 운영에서 돈다** —
  C-5 이름 규칙 보강 · `closure_rule` 29 · **`compliance_flag` 저장 배선** · 콜 미디에이터의 컴플라이언스 호출.
  STATE 의 「운영 반영 전」 표기는 틀린 것이어서 걷었다(장민석 님 검토 요청은 그대로 남는다).
- **② 옛 오브젝트 청소 — 지울 것이 없었다.** `deploy`·`svc`·`secret`·`ingress` 어디에도 `gateway` 이름이 없다.
  시크릿은 `call-mediator-tokens`·`callguard-server-tls`·`gcp-stt-credentials`·`server-env` 넷뿐 — **`gateway-tokens` 도 이미 없다.**
  적용 스크립트에 prune 이 없어 남을 줄 알았는데 남지 않았다. 로컬 `.env` 도 `CALL_MEDIATOR_*` 뿐.
  **개명 전환에서 남은 것은 Vercel 옛 변수 둘 삭제뿐**이고, 새 변수가 이미 번들에 구워져 돌아 화면에 영향이 없다.
- **③ `document` 98행 — 런북이 맞았다.** `STATE.md` 「막혀 있는 것」과 09-18 미결의 「아직 안 돌렸다」가 낡은 기록이었다. 다시 적재할 필요 없다.
- **덤으로 나온 것 — 운영에서 «한 번도 저장된 적 없는» 경로 셋.** 공개 테이블 29개 ·
  `call` 13 · `transcript_segment` 11 · `masking_event` 5 · `call_guard_flag` 1 ·
  **`recommendation_card` 0 · `closure` 0 · `compliance_flag` 0 · `compliance_rule` 0.**
  코드가 없어서가 아니라 **그 경로를 타는 통화를 운영에서 안 걸어 봤기 때문**으로 보인다(09-17 「바깥 실제 접속 0건」과 같은 이야기).
  ⚠ **측정값이 아니라 «안 돌아 본 것»의 확인이다** — 수치로 인용하지 않는다. 시연 전 합성 통화 한 건을 운영에 태울지는 [미결](/open-items/).
  참고: `closure_rule` 은 테이블이 아니라 코드(`closure_rule.py`)다 — 세려다 `UndefinedTable` 로 알았다.
- 남은 것: Vercel 옛 변수 삭제 · `decisions/208` 합의 · 운영 합성 통화 한 건 여부
