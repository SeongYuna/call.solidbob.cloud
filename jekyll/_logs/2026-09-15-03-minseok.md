---
date: 2026-09-15
author: "장민석"
person: minseok
seq: 3
---

- **운영 `agent_token` 마이그레이션은 «적용했다» 는 보고만 받았다 — 확인 출력은 못 봤다.** 이 머신은 `.env` AWS 키가 비어 있고 Neon 비밀번호도 틀려
  운영 DB 에 닿지 못한다. 밖에서도 못 본다 — 떠 있는 `0.1.7` 은 이 테이블을 쓰는 코드가 없어 어떤 API 로도 드러나지 않는다(`/health` spokes 에 `postcall` 없음 · `/openapi.json` 29경로, `/admin/agent-tokens` 없음 — 옛 이미지 그대로임을 확인).
  사용자 판단으로 **완료로 보고 진행**했다. 배포 뒤 `POST /hub/blacklist-requests` 가 500(`42P01`)이 아니면 간접 확인이 된다.
- **서버 이미지 태그를 `0.1.7` → `0.1.8`**(`infra/k8s/base/kustomization.yaml`). 로컬 `scripts/check_release_tags.py` 통과(`0.1.8` 레지스트리에 없음 · 코드 변경 있음). 런북 19장 `postcall` 주의문을 `0.1.8` 기준으로.
- **로컬 `origin` 을 `SeongYuna/call.solidbob.cloud` 로 바꿨다** — 푸시 때 저장소 이전 안내가 떴다.
- 남은 것: PR #86 머지(= 운영 배포, 사용자 확인 후) → `/health` spokes `postcall` · `/admin/agent-tokens` 확인 · 통화 후 초안 저장
