---
date: 2026-09-15
author: "조서희"
person: seohee
seq: 9
---

- **`AgentCallBox`("고객과 전화하기" 상담원 마이크 박스)를 우하단 토스트에서 중앙
  모달로 재배치**(사용자 지시) — 필요서류 카드·하단 상태바와 겹쳐 보이던 문제.
  `agent-call-backdrop`(풀스크린, `rgba(20,20,18,0.5)`) 안에 기존 `agent-call-box`를
  그대로 두고 `display:grid; place-items:center`로 중앙 배치. 배경 클릭으로는 안
  닫히게 했다(전화는 응답/거절해야 하는 이벤트라 `ConfirmDialog`처럼 배경 클릭
  닫기를 넣지 않음) — 풀스크린 backdrop 한 장으로 뒤 패널 클릭·스크롤도 자연히
  막힌다(추가 JS 불필요, 기존 `ConfirmDialog`와 같은 패턴). 너비 440px, 등장
  애니메이션(scale 0.95→1·opacity 0→1, 180ms) 추가. 콘텐츠·"통화 시작" 로직은
  전혀 안 건드림.
- 헤드리스 크롬+CDP로 실제 클릭까지 검증 — 모달 중심 오프셋 0px, 배경 좌표
  클릭 시 backdrop이 가로챔, "통화 시작" 클릭 시 기존 `no-token` 분기로 정상
  전환(로직 안 바뀜 확인). `tsc --noEmit`·`vite build` 클린.
- 남은 것: J-5 배정 판정(`POST /hub/routing-decisions`) 호출 주체(정성윤 결정
  대기), 블랙리스트 보존 정리 "해제" 관련 요청(의미 재확인 대기), 관리자 구글
  로그인(Google OAuth Client ID 미발급 — `jekyll/open-items.markdown` 2026-09-14
  항목 그대로 남음).
