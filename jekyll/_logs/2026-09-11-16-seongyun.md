---
date: 2026-09-11
author: "정성윤"
person: seongyun
seq: 16
---

- **운영에서 통화 시작 → 전사 저장이 된다 — 보고된 문제가 풀렸다.** PR #64(복합키 · 409 · CI integration · `0.1.2` ·
  RDS 런북)를 머지했다(`3514e18`). `release.yml` 이 `plan`·`image`·`deploy` 모두 성공했다 — 09-09·09-10 에는 `plan` 에서
  멈췄었다. 운영 확인(이 세션이 밖에서, 런북 19):
  - `/openapi.json` 에 `POST /hub/calls` 가 생겼다.
  - 통화 시작 `created "true"`, 같은 통화 재알림 `"false"`(멱등).
  - 전사 **200** — `제 번호는 *********** 입니다`, P4. 조회 `total 1`. 시작 안 한 통화의 전사는 **409**(순서를 알려 준다).
  - 운영 DB 에 `test-deploy-202609111738` 통화 1건이 남았다(지우는 API 는 없다).
- **CI `server` job 에서 integration 테스트가 실제로 돌았다** — 스키마 22 테이블 → **4 passed**, 스킵 0.
  단위 테스트 313 passed · 5 skipped 이고, 5 skipped 는 직전 `main` 실행(307 · 5)과 같아 이번 변경과 무관하다.
- **그런데 `spokes` 에 `trigger` 가 없다 — `0.1.2` 이미지에 `ai/provider.py` 가 빠졌다.** `server.Dockerfile` 은
  `COPY ai/apps/` 만 하고, `main.py` 는 트리거를 `from provider import build_trigger_provider` 로 부른다. 못 부르면
  **오류 없이** 스포크만 빠지는 설계(`decisions/024`)라 배포는 성공으로 보였다. Docker 가 없어 이미지 배치
  (`/app/server` + `/app/ai/apps`)를 흉내 내 재현했다 — `spokes` 3종, `provider.py` 를 더하면 4종이다.
  로컬·CI 는 저장소 전체가 있어 못 봤다.
  - 준비(아직 배포 안 함): `COPY ai/provider.py /app/ai/provider.py` · `newTag` `0.1.3` · `server/tests/test_image_layout.py`
    (합성 루트가 `ai/` 에서 부르는 파일을 Dockerfile 이 전부 복사하는지 본다). 이 테스트는 수정 전 Dockerfile 에서 실패하고
    수정 후 통과한다. `server` 316 passed · 계약 4 kept.
  - 영향: 운영 `/hub/recommendations` 는 지금 501 이다. 트리거가 붙어도 운영 ES 지식베이스가 비어 있어 **503** 이 된다 —
    추천이 실제로 돌려면 적재까지 가야 한다.
- 남은 것: `0.1.3` PR → 머지 → `spokes` 4종 확인 · 운영 ES 지식베이스 적재(이미지에 `knowledge-base/`·`scripts/` 가 없어
  절차 설계가 필요하다) · 인스턴스의 `~/server-env.backup.yaml` `shred -u`.
