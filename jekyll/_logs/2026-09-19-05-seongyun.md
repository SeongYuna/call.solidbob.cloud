---
date: 2026-09-19
author: "정성윤"
person: seongyun
seq: 5
---

- **브리핑(`__006`·`__007`)의 가장 센 문장이 이미 낡았다는 것을 확인했다 — 「운영에서 개인정보 4건이 샌다 = 규칙 위반 상태」.**
  그 문장은 **09-17 오전** 기준이다. 그날 오후·18일의 이름 규칙 보강이 담긴 **`server:0.1.18` 이 운영에 떠 있고**(09-19 SSM 확인),
  **그 규칙만으로** 골든셋 `v1-150` 의 C-5 케이스 **36건(구간·라벨 72판정)이 전부 통과**한다.
  - 재현: 이 윈도 머신에 `.venv` 를 만들고 `server/requirements.txt`(torch 없음)를 깔아
    `pytest apps/masking/tests/domain/test_golden_set_masking.py` → **73 passed**. 그 테스트는 `mask_text` 만 쓴다 —
    **NER 없이, 운영과 같은 구성**이다. 배포 커밋 `a387469` 와 작업 트리의 `server/apps/masking/` 차이는 **0**이다.
  - ⚠ **이건 서버 단위 테스트이고 하네스 기록이 아니다.** 6주차 판정문에 쓸 값은 여전히 재측정 몫이다.
    NER 을 운영에 올리는 결정(`decisions/208`)도 그대로다 — **규칙이 못 잡는 「호칭·문맥 없는 이름」은 남아 있다**.
  - **F-2 는 반대로 확인됐다** — `test_golden_set_closure.py` 가 **3건 전부 skip**(골든셋에 `f2_case` 0건).
    「측정 불가」가 문서상 표현이 아니라 실제로 스킵되고 있다.
  - `STATE.md` 「막혀 있는 것」의 해당 줄을 정정했다.
- **오류 내성 곡선 그림 도구를 만들었다 — `scripts/plot_error_curve.py`**([w5-error-curve-plot](/backlog/w5-error-curve-plot/)).
  기획서가 「필수 도구」로 적은 그래프를 여태 한 번도 안 썼다. 2패널(검색 Recall@5 · C-5 누락 건수)에 x 는 **실제로 잰 WER**,
  판정선은 회색 점선, **그림 안에 「주입기가 실측 오류의 46.9% 를 흉내 못 낸다」는 한계 문장**과 날짜·커밋·골든셋·시드·명령이 함께 박힌다.
  **입력 JSON 이 없으면 그리지 않고 멈춘다**(절대 원칙 2) — 그래서 지금은 가짜 값으로 **스모크 테스트만** 했고
  **저장소에 그림을 넣지 않았다.** 실제 곡선은 류준 님 재측정 뒤 한 줄이면 나온다.
- **미결 항목 10건을 근거와 함께 닫았다** — 통화 목록 API(운영 `/hub/calls` 실측) · P6·P7 NER 구현 · 운영 배포 CI(`decisions/117`) ·
  콜 미디에이터를 같은 EC2 에(넷이 한 노드) · Terraform state(옮길 대상이 없어짐) · `docs` CNAME(200) ·
  `api.solidbob.cloud`(폐기된 주소) · GPU·EXAONE(안 넣었다) · ngrok 인증(터널 닫힘) · J 블록 어댑터(운영에 7경로).
  **지우지 않고 「원문 — 」 뒤에 그대로 남겼다**(절대 원칙 8). 브리핑이 「끝났는데 열려 있다」고 센 20여 건 중 절반이다.
- 이 머신에 처음으로 **서버 테스트를 돌릴 수 있는 `.venv`** 가 생겼다(+ `matplotlib`). ES·모델이 필요한 검색 채점은 여전히 못 돈다.
- 남은 것: 류준 님 재측정 → 곡선 그림 → 6주차 판정 · 미결 나머지 정리 · Vercel 옛 변수
