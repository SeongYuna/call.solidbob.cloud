---
date: 2026-09-23
author: "조서희"
person: seohee
seq: 5
---

- **w6-admin-stats-wallboard (done)** — 칸반 순서 9번. 착수하려고 코드를 열어 보니
  「현황판이 mock 시드」라는 티켓 전제가 이미 낡아 있었다 — `fetchAdminStats()`·
  `adminStore.ts`의 `loadAll()`이 `GET /hub/admin-stats` 여덟 칸 중 다섯을 이미 실제
  값으로 받고 있었고(나머지 셋인 배정 판정은 09-22에 내가 붙였다), 「완료 통화 누적」을
  `calls_total`/`calls_closed` 중 무엇으로 할지도 이미 코드 주석에 결정돼 있었다.
  진짜 남은 건 완료 조건 2번(401 별도 문구)뿐이었다 — `adminStore.ts`의 `loadAll()`에
  401 전용 분기를 추가해 세션을 지우고 로그인 화면에 "세션이 만료되었습니다 — 다시
  로그인해 주세요"를 남기도록 했다(재시도 버튼은 401엔 의미가 없다). `tsc --noEmit`·
  `vite build` 통과 — 401 분기는 실제 만료 토큰으로 눌러 보진 못했다.
- 칸반 순서 5~9번(플랫폼 수치·vitest·입사일 UI·배정판정 배너·현황판 401)을 확인·구현
  완료. 커밋·푸시 예정.
- 남은 것: 10번(`w6-qa-ui-defects-three`)부터 계속.
