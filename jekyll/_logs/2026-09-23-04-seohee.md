---
date: 2026-09-23
author: "조서희"
person: seohee
seq: 4
---

- **w6-routing-result-ui (in-progress, 1/3 조건 완료)** — 칸반 순서 8번. 완료 조건 1번
  (표시 위치·문구 합의)은 사실 09-22에 이미 정해져 있었다 — 그날 J-5 배선을 롤백할 때
  `apps/call`의 WS 파싱·스토어·`TranscriptPanel.tsx` 배너("배정 판정 기록됨…")는 안
  지우고 남겨 뒀다. 진짜 빠진 건 **방송 자체** — `services/call-mediator`의
  `decideRouting()`이 판정을 부르고 로그에만 남기고 있었다.
  - `w6-close-callid-missing`(오늘 오전)에서 쓴 것과 같은 패턴으로 고쳤다: `ports.ts`
    `CallMediatorMessage`에 `routing_decision` 추가, `RegistryDeps.announceRouting`
    (기본 false) 신설, `decideRouting()` 성공 시 서버 응답을 그대로 방송, `main.ts`에서
    켬. 서버 `routing_decision_schema.py`를 확인해 프론트 `parseRoutingDecision`이
    요구하는 필드를 전부 갖고 있는 것도 확인했다(변형 없이 그대로 방송해도 안전).
  - 테스트 3건 추가(꺼져있으면 안 보냄·켜져있으면 방송·판정 실패면 안 보냄) —
    `npm test` 163/163.
  - 남은 것: 운영 재확인(SYN-006→SYN-007에서 `veteran` 배너 실제로 뜨는지)·
    `routing_log` 조회 API(장민석 님 몫) — `in-progress` 유지.
- 오늘 칸반 순서 5~8번(플랫폼 수치·vitest·입사일 UI·배정판정 배너) 커밋·푸시 예정.
- 남은 것: 9번(`w6-admin-stats-wallboard`, 현황판 나머지 5칸)부터 계속.
