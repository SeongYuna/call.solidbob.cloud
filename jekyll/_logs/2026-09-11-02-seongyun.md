---
date: 2026-09-11
author: "정성윤"
person: seongyun
seq: 2
---

- **정정 — 01번 기록의 한 문장이 틀렸다.** 01에 "`_project/STATE.md`는 2026-09-03 커밋(`cc743ef`)
  이후 내용이 실제로 갱신된 적이 없다(merge 커밋만 있고 내용 변경 커밋 없음)"라고 적었는데,
  `git log --no-merges -- _project/STATE.md`로 다시 보니 그 뒤에도 내용 변경 커밋이 9건 있다 —
  조서희(09-04 `9f730f9` · 09-07 `9c97329` · 09-09 `780a979` · 09-10 `61b026c`·`e51e0c5`),
  장민석(09-09 `dd63ffd` · 09-10 `7396275`·`54703c1`), 류준(09-09 `ada6f7d`).
  **정확한 사실은 "파일 전체"가 아니라 "인프라·CI 줄"만 09-03 이후 갱신되지 않았다는 것**이다 —
  그 문구(「운영 AWS 는 코드까지만」)를 `git log -S`로 추적하면 `cc743ef` 한 커밋에서만 나오고,
  09-03 이후 정성윤 계정의 비머지 커밋 가운데 STATE.md를 건드린 것은 없다. 다른 세 사람은 자기
  줄을 갱신했고 인프라 줄만 뒤처졌다. 01의 결론(훅이 STATE.md를 강제하지 않는다)은 그대로다.
  01 파일은 규칙대로 고치지 않고 이 파일로 정정한다(CLAUDE.md §4).
- **발표 자료 4종을 로그가 아니라 커밋 메시지로 한 번 더 대조했다** — 09-07 이후 비머지 커밋
  전체, 특히 류준의 09-09 커밋 6건(`0d9116b`·`e72f085`·`96d88ac`·`af69bf2`·`fd96adc`·`ada6f7d`,
  18:00~18:02 일괄 커밋 → PR #59 18:13 머지). 로그 대조에서 못 잡은 것이 커밋에서 나왔다.
  ① **자동배포 증거의 주체가 틀렸다.** 문서가 "장민석의 CORS·필드명 수정"이라 적었는데 필드명
  수정(`95d930f`)은 정성윤 커밋이고 CORS(`dd63ffd`)만 장민석이다. 게다가 계약과 어긋났던 쪽은
  프론트가 아니라 **서버**였다(계약은 `similarity_score`, 서버만 `score`).
  ② **C-5 측정 범위가 낡았다.** "P1·P2·P3·P5 표본 0건"이라 적었는데 `e72f085`가 "C-5 25(P1~P7
  전부 표본 확보)"로 이미 채웠다. `CLAUDE.md` 절대 원칙 5의 08-28 주석을 옮긴 탓이다.
  ③ **서류 문의 규모가 재측정됐다.** 문서들이 `decisions/201`의 174건·2.60%·69종을 썼는데
  `0d9116b`가 146건·2.18%·64종으로 재측정했다(결정 기록은 절대 원칙 8대로 고치지 않았다). 게다가
  문서가 "서비스 종류가 69종"으로 풀어 쓰고 있었다 — 서류 질문이 흩어진 **민원 의도**의 수다.
  ④ **「감정분석」 표현.** `decisions/203`은 이 말을 쓰지 않기로 했다(심리 상태 판정을 주장하는
  말이라서). 문서에 "상담원의 감정 상태를 음성 톤으로 추정" 같은 표현이 있었다 — 발표에서 그대로
  말하면 팀 결정과 정반대가 된다. D-5 전환 이유도 "반대 결과가 나와서"로 잘못 적혀 있었다 —
  실제 이유는 상담사 톤 일정·그 일정함이 기준선·텍스트 중복·클립 데이터 넷이고, 반대 결과는
  구현 뒤 측정에서 나와 류준이 「가설 기각이 아니라 이 데이터로는 못 잰다」로 적었다.
  ⑤ **C-6 1.0/1.0을 성과로 걸어 두었다.** `96d88ac`가 "교차검수 전까지 목표 달성으로 인용하지
  않는다"고 적었으므로 발표 자료의 「잘 되고 있는 것」에서 뺐다.
  ⑥ **J의 첫 화면은 류준이 만들었다.** `af69bf2`가 대시보드에 관리자 승인 화면·전환 요청 버튼을
  만들었고(사용자 지시로 담당 경계를 넘음), 조서희는 09-10에 그걸 `apps/admin`으로 분리했다.
  J 결정 기록(`204`) 날짜도 08-28이 아니라 09-09다.
  ⑦ **09-10의 첫 프론트↔서버 실연동이 빠져 있었다.** 조서희 `e2c0208`이 장민석의 ngrok 터널로
  실제 응답을 파싱해 확인했다. "authtoken 대기" 문구도 낡았다 — 터널은 09-10 오전에 열렸다.
  ⑧ 그 밖에 — D-5·C-6 "저장은 된다"는 틀렸다(테이블만 있고 저장 코드 없음,
  `w4-c6-d5-persistence`) · F-2 체크리스트 UI 전환은 09-09가 아니라 08-28(`3e919e8`) · 서버 테스트
  290 passed·**3 skipped**는 장민석 커밋 메시지에 있고, 엔드포인트는 코드로 세면 `/hub/*` 14 +
  `/health` = 15 · 조서희의 09-07 「원문 열람」이 SEC-1과 모순된다는 류준의 09-09 미결을 발표
  자료에 경고로 붙였다 · 조서희의 09-09 "여전히 405" 기록(15:41)이 정성윤의 파이프라인 검증
  (16:26~17:11)보다 먼저라는 시점 차이를 커밋 시각으로 확인했다.
- 고친 파일: `_project/mentor-presentation.md` · `progress-briefing.md` · `functional-spec.md` ·
  `onboarding-briefing.md`. `_project/STATE.md`는 계속 손대지 않았다(사용자 지시).
- 남은 것: 01의 "남은 것"은 그대로 유효하다. 덧붙여 **`CLAUDE.md`의 두 곳이 이제 사실과 다르다** —
  절대 원칙 5의 C-5 측정 범위 주석(P4·P6·P7 뿐 → 지금은 P1~P7 전부 표본 있음)과 §1의 서류 문의
  수치(174건·69종 → 재측정 146건·64종). 파생 문서라 고치는 게 맞지만 규칙 파일이라 이번엔 두었다.
