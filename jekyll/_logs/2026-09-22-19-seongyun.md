---
date: 2026-09-22
author: "정성윤"
person: seongyun
seq: 19
---

- **운영 ES 재적재(사용자 요청, 「ES 만」 — DB `seed_documents` 는 안 돌렸다).** 런북 22-② 절차 그대로, 이 머신에서 SSM `send-command` 로 `0.1.36` 서버 파드 안에 main 타르볼(`scripts/index_knowledge_base.py` · `knowledge-base/`)을 풀고 `--to-es --recreate --embed-model /models/koe5`. 결과: **청크 101개(조항 99개) · 임베딩 koe5 1024차원 CPU · `callguard-kb-single` 101건 green · 벡터 101/101**. nori 품사 필터 확인 `_analyze("주민센터에서는 등본을 발급한다")` → `['주민','센터','등본','발급']`(조사 「에서는」·「을」 빠짐). 밖에서 `POST /hub/search` 「주민등록등본 발급 대리 신청 서류」 → `DASAN-TERM-4.3`·`4.1`·`2.5`, 점수 0.847/0.840.
- **이번 재적재에 PR #135(류준, 17:19 KST 머지)의 여권 조항 `DASAN-TERM-4.23` 이 들어갔다.** 재적재 명령은 17:22 KST 로 머지 3분 뒤였고, 파드에 푼 main 타르볼이 그 커밋을 담았다 — 운영 ES `doc_id:DASAN-TERM-4.23` 1건, 밖에서 「여권 재발급 필요 서류」 검색 1위(0.878). 16번 로그(다른 세션, `0.1.34` 파드)의 100건과 이번 101건 차이가 바로 이 조항이다(조항 98→99, 청크 100→101). ⚠ 처음엔 「지난 재적재 이후 KB diff 0」이라고 적었는데 그건 #135 머지 전에 fetch 한 낡은 트리로 잰 값이라 틀렸다 — 여기서 바로잡는다.
- `/health` spokes 에 `retrieval_dense`·`pii_ner` 그대로. `--recreate` 라 인덱스가 지워졌다 다시 생기는 1~2분 동안 검색이 빈다 — 시연 중엔 돌리지 말 것.
