---
date: 2026-09-15
author: "정성윤"
person: seongyun
seq: 11
---

- **`gateway`·`tag-check` 를 main 룰셋의 필수 통과 검사로 등록했다.** 셋(`jekyll`·`ai`·`server`)에서
  다섯이 됐다. 나머지 규칙(`deletion`·`non_fast_forward`·`pull_request`·`strict: true`·승인 0건)은
  그대로다 — 기존 룰셋을 받아 이름 둘만 더해 PUT 했다.
- **왜 지금인가**: 워크플로가 없는 브랜치에 필수 검사를 걸면 검사가 생성되지 않아 **PR 이 영원히
  pending** 이 된다. `tag-check.yml` 이 `6909bde`(09-14 15:31) 로 main 에 들어가 **다섯 브랜치
  전부에 퍼진 것**을 확인하고 걸었다. 열린 PR #92 도 다섯 개 전부 pass 라 막히는 사람이 없다.
- **검사가 실제로 일한 것을 확인했다** — 오늘 07:30 류준 `ai` PR 에서 `tag-check` 가 걸렸다:
  `[callguard-server] 태그 0.1.10 — 레지스트리에 있음: True · 코드 변경: True`.
  장민석이 이미 올린 태그를 그대로 두고 `ai/` 를 고친 경우다. 07:53 에 올려서 통과했다.
  **어제까지는 이게 머지 후 릴리스에서야 드러났다**(실패 5건 중 4건이 이것).
  등록 전이라 그때는 빨간 X 만 뜨고 머지는 가능했다 — 이제 잠긴다.
- 남은 것: **OIDC 신뢰 정책 확인**(`callguard-deploy-role` 의 `sub` 가 `refs/heads/main` 한정인지).
  AWS 자격증명이 없어 아직 아무도 실물을 못 봤다 — [미결](/open-items/) 그대로 둔다.
