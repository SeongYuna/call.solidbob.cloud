---
date: 2026-09-18
author: "류준"
person: ryujun
seq: 1
---

- **합성 통화를 세 갈래로 넓혔다 — 페르소나·대본 확장 · Google TTS · E2E 왕복 검사.** 서브에이전트 3개를 병렬로 돌리고 결과를 내가 대시보드에서 확인했다.
  결정 기록 **`209`(제안)** — 용도는 데모·파이프라인 점검, 소리는 `say` + Google TTS Wavenet(무료 한도 안), 왕복은 규칙 검사.
- **① 페르소나 9명 · 대본 10건(SYN-015~024) 추가** — 상담원 무뚝뚝·사무적(A04) · 과잉 친절·단정(A05) · 외국어 응대 지향(A06), 고객 급한 직장인·의심 많음·일본 출신 초급 한국어·옆길 고령·조용히 비꼼·가족 대리인(C14~C19).
  근거는 `knowledge-base/dasan/terms` 조항만. 생성기 정의를 `scenarios_v0_a.py`(001~014 그대로)·`scenarios_v0_b.py`(신규)로 쪼갰고 **기존 14건 JSON 은 바이트 동일**. QA 페르소나 3종 재검수(SYN-020 카드번호가 Luhn 통과해 교체 · SYN-023 지식베이스에 없는 점검법 삭제).
  폭언·성적 표현은 **추가하지 않았다**. 라벨 합계 24건: P1 3·P2 2·P3 3·P4 17·P5 3·P6 16·P7 5 / C-1 5·C-2 1·C-3 3
- **② Google TTS(Wavenet) — 키만 넣으면 된다.** `replay_persona_call.ts --speak google` · `--prefetch`. REST 직접 호출(의존성 0 → 콜 미디에이터 태그 불변), 페르소나→`ko-KR-Wavenet-A~D` 배분 + 톤→SSML prosody, 결과 캐시(시연 때 호출 0).
  **리밋 2단** — 1차 콘솔 할당량, 2차 `GOOGLE_TTS_MAX_CHARS_PER_MONTH`(기본 900,000, 장부 `data/processed/tts-usage.json`, 넘으면 호출 안 함). 공식 무료 한도는 WaveNet+Standard 합산 400만 자/월(가격표 09-18 확인, `TTS.md`).
  `.env.example` 에 `GOOGLE_TTS_API_KEY`·`GOOGLE_TTS_MAX_CHARS_PER_MONTH` 추가(훅이 편집 도구를 막아 셸로 붙였다 — 값 없음 확인). 테스트 16(콜 미디에이터 합계 123) · tsc 통과.
  **실제 키로는 한 번도 안 불렀다** — 정성윤 님 발급 후 `--prefetch SYN-001` 로 작게 시작(절차 `scripts/persona_sim/TTS.md §1`)
- **③ E2E 왕복 검사 `scripts/persona_sim/e2e_check.py`** — 재생기 → 콜 미디에이터 → 서버 → PostgreSQL → `/ws` → `--close` → `GET /hub/calls/{id}/transcript`·`/record`(대시보드가 읽는 API) 를 대본 `expected` 와 규칙으로 대조. 판정 함수 pytest 13.
  **24건 실행 결과(로컬, 규칙·BM25, NER 없음, `source: synthetic`, 보고서 `data/processed/persona-e2e/2026-09-18-1618.md`): ✅ 3 · ❌ 21.**
  통과한 판정 — 자막 왕복(API=DB=턴 수, 화자 순서) 24/24 · 콜 가드 턴×갈래 24/24 · 요약 초안·`stt_engine=synthetic-script`·고객 연결 24/24 · SEC-1 23/24.
  실패 갈래(전부 시스템 쪽, 대본은 안 고쳤다): **컴플라이언스 탐지 0**(콜 미디에이터가 `/hub/compliance-checks` 를 안 부른다 — 09-17 과 동일, 8건) · **필요서류 판정 없음**(`closure_rule.py` 에 TERM 4.5·4.20·4.12·2.6·4.9 규칙 없음 7건 / 1순위 카드 조항만 절차로 삼아 엉뚱한 절차 5건) · 서류 이름 불일치(대본↔규칙표 5건) · **SEC-1 누출 1**(SYN-020 #16 「저는 강민재고요, 아버지는 강영식이에요」 이름 둘 원문) · `recommendation_card.source_doc_id` 전부 NULL(`document` 테이블 0행)
- **대시보드 실화면 확인(SYN-020, Chrome, 로컬 스택)** — 「통화 시작」→ 자막 실시간 · 주민번호·카드·계좌·전화 `***`+경고 · **#16 이름은 화면·DB 둘 다 원문** · 필요서류 패널이 「지방세 환급」「장애인콜택시」를 진행 중으로 표시(잘못된 절차) · 통화 종료 → 통화 후 처리(규칙 발췌 초안 174자) · 대기화면 → 상담기록 → 지난 통화 요약 · **자막 보기(DB 재생)** 까지 콘솔 에러 0.
  ⚠ 상담원 토큰 로그인이 필요해 검사 DB 에 로컬 토큰 해시를 넣었고, `AgentCallBox` 모달은 `call_token` 없이 「통화받기」를 누르면 닫힌다(마이크 안 켬)
- 이 맥 `git` 이 Xcode 라이선스로 막혀 있던 것을 사용자가 풀었다(`xcodebuild -license accept`)
- **커밋에서 뺀 것**: 작업 트리에 있던 `ai/apps/evaluation/metrics/call_temperature.py`·그 테스트(D-5, 이전 세션 미커밋 — `w3-call-temperature`) — 내용을 검토하지 않았고 `ai/` 변경은 서버 태그를 올려야 해서 별도 세션으로
- 남은 것: TTS 키 발급(정성윤) → 실제 합성 1회 · E2E 실패 21건의 시스템 결함 배정(미결) · 결정 `209` 합의 · 대본 사람 검토
