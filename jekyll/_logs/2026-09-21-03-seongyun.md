---
date: 2026-09-21
author: "정성윤"
person: seongyun
seq: 3
---

- **칸반을 프로젝트 끝까지 채웠다 — 새 티켓 25건** — 보드가 8주차에 다섯 건만 있고 그 사이가 비어 있었다.
  09-15~09-21 로그 73건의 「남은 것」 · 미결 항목 892줄 · 결정 `118`~`123` 을 대조해, **티켓이 없던 일**만 골라 만들었다.
  5주차 3 · 6주차 11 · 7주차 8 · 8주차 3. 담당별로는 정성윤 8 · 류준 6 · 장민석 6 · 조서희 4 · 공동 1.
  - 굵은 줄기는 셋이다 — ① **`decisions/121` 의 실행**(`w6-gpu-model-instance` → `w6-ai-model-http-surface` → `w6-server-remote-model-adapter`.
    `121` 이 「티켓: 없음」으로 남겨 둔 것) ② **열린 문 닫기**(`w5-ingest-auth-fail-closed` · `w6-open-write-guards`)
    ③ **마감**(`w8-final-eval-run` 수치 고정 · `w8-retrospective` · `w8-project-closeout` 자원·키 정리)
  - 한 티켓에 두 사람 작업을 담지 않으려고 `121` 후속을 셋으로, 원문 열람(SEC-1) 건은 프론트 몫만 티켓으로 했다
- **선행 조건 하나가 풀린 것을 확인했다** — `release.yml` 이 PR #106 까지 성공했고(저장소 `newTag` server `0.1.23` · call-mediator `0.2.2`),
  밖에서 본 `/health` 에 `0.1.19` 부터 생긴 `ingest_guard` 가 있다. 값은 **`open`** — 쓰기 일곱 경로가 아직 토큰 없이 받는다.
  `decisions/120` 이 「두 이미지 배포 뒤」로 미뤄 둔 3·4단계를 이제 할 수 있다. ⚠ 파드 이미지 태그를 SSM 으로 직접 본 것은 아니다
  - 같은 자리에서 STT 캡 실측 — 일 600초 · 월 3,600초. 시연일에 리허설까지 하면 모자란다(`w7-demo-infra-readiness`)
- **기존 티켓 상태 다섯을 옮겼다** — 사용자 지시로 남의 티켓도 건드렸다. 옮긴 근거와 「틀렸으면 담당자가 되돌린다」를 각 티켓 끝에 적었다
  - `done`: `w4-admin-google-login`(09-15 운영 로그인 완주) · `w4-dashboard-live-contract`(남은 「통화 목록」이 09-15 에 연결돼 있었다) ·
    `w5-persona-e2e`(완료 조건 전부 체크) · `w1-platform-landing`(08-27 부터 열려 있었다 — 뒤 작업은 다른 두 티켓이 이어받아 끝냈다)
  - `in-progress`: `w6-core-baseline-check`(09-21 재측정으로 이미 착수)
  - **안 옮긴 것**: `w5-c5-masking-gaps` — 코드는 끝났지만 「운영에서 이름·번지가 가려지는가」를 아직 아무도 안 봤다(09-20 왕복은 전화번호만 봤다)
  - 상태는 두고 메모만 단 것 다섯 — `208`→`121` 로 전제가 바뀐 `w5-e2e-findings-ai` · 선행 조건이 풀린 `w6-compliance-alert-ui`(파서 먼저, 방송 나중) ·
    걱정 둘이 움직인 `w8-demo-rehearsal` · `w5-c5-masking-gaps` · `w5-persona-tts-google`
- **TTS 키 발급 착수** — GCP `callguard` 프로젝트에 Cloud Text-to-Speech API 가 사용 설정돼 있는 것을 콘솔에서 확인. 키는 아직 안 만들었다.
  API 세부 화면의 「사용자 인증 정보」 탭에는 API 키 항목이 없다(OAuth·서비스 계정만) — 왼쪽 메뉴에서 만든다
- **미결 13건을 닫았다** — 전부 다른 곳에서 이미 풀렸는데 체크만 안 된 것들이다: B-0 관련 넷(`201` 폐기) · 프론트↔백 계약 · 하네스 C-5 배선 ·
  `generation`·`compliance` 위치 · 중복 티켓 · 카드 피드백 500(`308`) · 모델 업로드(`210`) · PR #86(`114`) · 서버 이미지에 모델(`121`) · F-2(`118`).
  근거를 항목마다 달았다. **애매한 것은 안 닫았다** — 조사에서 「해소된 듯」으로 나온 것이 열 건쯤 더 있지만 파일로 확인 못 한 것은 그대로 뒀다
- 검증: `jekyll build` 통과 · `check_site_links.py` 231쪽 깨진 링크 0 · `check_session_end.py` 통과. `PM` 은 시작할 때 `origin/main` 으로 fast-forward 했다(4커밋, 조서희 09-21 머지)
- 남은 것
  - **팀 확인** — 남의 이름으로 만든 `todo` 16건과 옮긴 상태 넷. 미결에 올렸다
  - `w5-schedule-gap-decision` — 10-14(8스프린트 끝)와 10-27(개발기간 끝) 사이 13일. **이게 정해져야 `w8-*` 마감이 정해진다**
  - 오래 열린 내 티켓 셋(`w2-stt-batch` · `w2-baseline-gate` · `w4-aws-resource-hygiene`)은 상태를 안 바꿨다 — 한 일이 없어서다. `w7-infra-loose-ends` 에서 끝내거나 `cancelled` 로 닫는다
  - 커밋·PR 은 하지 않았다
