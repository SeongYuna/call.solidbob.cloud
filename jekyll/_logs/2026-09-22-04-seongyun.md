---
date: 2026-09-22
author: "정성윤"
person: seongyun
seq: 4
---

- **조서희 님 요청 둘에 답했다**(컴플라이언스 파서 PR 배포 뒤 — `349b18b`).
  - ① **검사 실패 신호** — 콜 미디에이터가 상담원 발화의 컴플라이언스 검사에 실패하면
    `compliance_unavailable` `{call_id, segment_id, status}` 를 방송한다(`ports.ts`·`call_registry.ts`).
    위반 있음 = `compliance`, 검사 실패 = `compliance_unavailable`, 위반 없음 = 침묵 — 셋이 갈린다.
    `status` 는 `"501"`(스포크 없음 = 탐지 자체가 없음) · `"404"`(전사가 먼저 저장 안 됨) · `"503"`류 · `"연결 실패"`.
    스위치는 `announceCompliance` 하나를 같이 탄다. 테스트 2건 보강(실패 두 발화 → 신호 둘, 꺼져 있으면 로그만) — **135/135 통과, tsc 0**.
  - ② **규칙이 실제로 잡는 표현** — `ai/apps/compliance/domain/value_objects/rules.py` 규칙 v1 을 합성 대본 24건의
    상담원 발화 전부에 직접 돌렸다: **라벨 있는 8건(SYN-003·004·008·009·016·020·021·024) 전부 잡히고 라벨 없는 16건 오탐 0.**
    시연 대본은 이 문장들을 그대로 쓰면 된다(답장 본문에 코드별 트리거·예외 정리).
- **방송을 켰다** — `main.ts` `announceCompliance: true`, `kustomization.yaml` call-mediator `0.2.2 → 0.2.3`
  (`services/call-mediator/src` 가 바뀌어 `tag-check` 가 요구한다). 검사·저장은 바뀐 것 없음.
  런북 19-1 을 읽고 고쳤다. 새 티켓 `w6-compliance-broadcast`(in-progress — 운영 확인은 릴리스 뒤).
- **미리 적어 두는 것**: `compliance_unavailable` 은 프론트 파서에 아직 없는 타입이라 실패가 나면 `onError` 배너로
  떨어진다(화면은 안 깨진다 — 조서희 09-21 로그 확인). 운영 스포크는 꽂혀 있어 평시엔 안 난다.
- 같은 PR 에 09-21 세션(06번 로그)의 미커밋 배치(결정 126~129 · 판정문 초안 · 티켓 7건 · 마일스톤 · 미결)를 따로 커밋해 실었다.
- 남은 것: 릴리스 뒤 `/call-mediator/health` · 재생기로 `compliance` 도착 확인(티켓 완료 조건 3) · 조서희 님 파서 추가(4).
