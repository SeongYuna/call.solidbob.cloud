---
date: 2026-09-22
author: "정성윤"
person: seongyun
seq: 10
---

- **SYN-010 을 이 머신(WSL)에서 운영에 다시 흘렸다** — 류준 님 절차 그대로(재생기 `--watch --speed 4`, `--close` 없음, 토큰은 SSM 으로 시크릿에서 꺼내 환경변수로만). `syn-prod-syn-010-seongyun-130124`, 13:01. 통화는 지우지 않았다(시연용 보존 여부 미결).
  - **지연**: 추천 2건 `internal_latency_ms` **1,352 · 655**. 류준 님의 3,300 보다는 낮지만 첫 추천이 여전히 1,000 초과. 같은 파드에서 40분 전 수동 검색은 0.58s 였으므로 **모델 예열이 아니라 CPU 경합**(2 vCPU 에서 NER 마스킹과 임베딩 검색이 동시에) 쪽 가설로 바꿨다. `retrieval_ms`·`generation_ms` 는 record 에 null — 방송에만 실리고 미디에이터가 중계하지 않는다(류준 확인) → 구간을 못 가른다
  - **저장**: 자막 5/5(류준 님의 4/5 유실은 이번엔 재현 안 됨) · 추천 2 · 필요서류 판정 2 · `/health` ok · spokes 11 · 파드 재시작 0 · 파드 703Mi · 노드 여유 3.2GB
  - **재현된 결함 둘**: ① 절차 오판정 — 1순위 카드는 정답 `TERM-2.12` 인데 판정은 규칙 있는 `TERM-2.9`(분실물)로 `incomplete`·「신분증 누락」. 상담원 화면에 틀린 서류가 뜬다(류준 님 PR 대기 — 「규칙 없는 조항이 1순위면 판정하지 않는다」) ② 「이용해 주셔서 감사합니다」에도 추천이 발동해 1.4·6.1·1.1 같은 무관 조항 5장이 뜬다 — B-6 「관련 문서 없음」 문턱이 없어서다(`w6-golden-set-loose-ends`)
- 남은 것: 구간 값을 `/ws`·DB 로 내보내는 것(`w7-latency-budget`) · 위 결함 둘은 류준 님 몫 · 시크릿 백업 `~/server-env.backup.yaml` 은 검증 끝나면 `shred`
