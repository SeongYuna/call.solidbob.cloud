---
date: 2026-09-21
author: "조서희"
person: seohee
seq: 2
---

- **PR #104 머지·배포 후 실제로 발급해보니 동작은 했지만, 사용자 지적으로 하나 더 고쳤다.**
  `resolve_or_create`가 새 상담원을 만들 때 `agent_id`를 `agent-3f9a…` 같은 무작위값으로
  만들었는데, "이름은 상담원 이름으로 해야지"라는 지적을 받았다 — 관리자 화면에 뜨는
  식별자가 사람이 알아볼 수 있는 값이어야 한다는 뜻이었다.
- **agent_id를 이름 그대로 쓰도록 정정했다** — `agent.agent_id`가 `VARCHAR(20)`이라 이름이
  그보다 길 때만(드묾) 무작위값으로 대신한다. `_project/decisions/406`에 정정 이력을 그대로
  남겼다(지우지 않고 "최초 배포판은 …였다"로 남김, 절대 원칙 8).
  `apps/admin`의 토큰 목록도 `agent_id`와 이름이 같으면 괄호 중복("최효원 (최효원)")을
  안 보이게 고쳤다.
- `server/apps/agent_auth` 유닛 24건 재확인, Postgres 통합 테스트에 20자 초과 케이스 1건
  추가(둘 다 로컬 미실행 — DB 없음). `apps/admin` `tsc --noEmit` 통과.
- 배포 확인 겸 조사한 것: PR #104 머지 직후 릴리스 파이프라인이 `server 0.1.21`을 자동
  빌드·롤아웃까지 완료(`01:18:08Z`)한 걸 GitHub Actions 로그로 확인 — 사용자가 처음 봤던
  404는 그 1~2분 창 안에서 테스트하다 옛 코드를 맞은 것으로 보인다(재현 안 됨).
- 남은 것: 아직 로컬/운영 어디서도 브라우저로 실제 발급→화면 표시까지 눈으로 확인은 못
  했다 — 다음 세션에서 admin.solidbob.cloud 로 재확인 필요.
