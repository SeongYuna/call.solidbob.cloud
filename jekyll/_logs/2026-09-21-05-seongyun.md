---
date: 2026-09-21
author: "정성윤"
person: seongyun
seq: 5
---

- **`ai` → `main` PR 의 충돌 5개 파일을 풀었다** — `_project/STATE.md` · `w5-c5-masking-gaps` · `w5-e2e-findings-ai` · `w5-persona-e2e` · `w5-persona-tts-google`. 원인은 같은 날 8분 차이로 두 사람이 같은 자리를 고친 것이다: 류준 님이 `ai` 에서 티켓 넷을 닫으며 「09-21 닫음」 절을 붙였고(11:06), 내가 `PM` 에서 보드 전체를 맞추며 같은 티켓 끝에 「보드 최신화」 메모를 붙였다(11:14, PR #107 로 먼저 머지).
- **푸는 기준**: 티켓 넷은 `ai` 쪽 닫음 절을 살리고, main 쪽 메모는 지우지 않고 그 아래에 머지 주석과 함께 붙였다(절대 원칙 8). 메모의 「상태는 그대로」·「류준 님이 정한다」는 닫음으로 답이 난 것이라 주석에 그렇게 적었다. `STATE.md` 는 표 세 줄이 겹쳤다 — `server/`·인프라 줄은 main(릴리스 `0.1.23` · Neon 은퇴 · 조서희 `agent_auth`)을, `ai/` 줄은 `ai`(류준 님 09-21 측정 여섯 건)를 택하고 main 이 그 줄에 넣은 「(09-18 값)」 표기만 옮겨 왔다. main 의 「09-21 다시 셈 ai 421 · server 1,058」은 류준 님의 더 나중 값(435 · 1,254, 새 골든셋 포함)에 밀려 넣지 않았다.
- **로컬 PM 도 정리했다**: 09-18 세션의 미커밋 진행 기록 6건 중 `2026-09-17-05` 가 원격의 같은 이름 파일(내용 다름 — call-mediator 운영 전환 확인)과 또 겹쳐 `10` 으로 옮겼다. 같은 사람의 동시 세션이 같은 `seq` 를 집는 문제가 세 번째다.
- 남은 것: 09-17·18 브리핑 기록 6건과 `__006`·`__007` 수정은 PM 에 미커밋으로 그대로 있다. 이 머지가 main 에 들어가면 PM 도 다시 pull 한다.
