---
date: 2026-09-14
author: "정성윤"
person: seongyun
seq: 4
---

- **RDS 검증을 운영에서 끝냈다 — 런북 19장 6·10·11번 전부 통과.** 서버 파드가 보는 값으로 `22 테이블 · ssl True`,
  `/hub/knowledge-gaps` 200, 통화 생성(`created: true`) → 전사(`제 번호는 *********** 입니다`, P4 마스킹) →
  조회에 **원번호가 남지 않는 것**까지 확인했다(SEC-1). `0.1.4` + 새 스키마 + RDS 조합이 실제로 관통한다.
  남은 흔적: 운영 DB 에 `test-202609140534` 행이 남았다 — 지우는 API 는 없고 `masking_event` → `transcript_segment` → `call` 순서다.
- **S3 는 「정상」이 아니라 정책이 없었다 — 01 기록의 해석을 정정한다.** 01 은 `aws s3 ls`(버킷 없이) 실패를
  「워크로드 역할에 계정 전역 조회 권한이 없는 게 정상」으로 읽었는데, **버킷을 지정해도 거부됐다**:
  `not authorized to perform: s3:ListBucket … because **no identity-based policy allows** the s3:ListBucket action`.
  런북 4-1 의 인라인 정책 `assist-s3-access` 가 `callguard-ec2-role` 에 **붙어 있지 않았다.**
  콘솔에서 붙인 뒤 `aws s3 ls s3://assist-apne2/` 가 빈 출력으로 성공했다 — **런북 19-7 이 처음으로 통과했다.**
  객체는 0개다. 「계정 전역 조회는 사람 자격증명으로」라는 01 의 결론은 그대로 유효하지만 근거는 이걸로 바뀐다.
- **테스트 음성 업로드·보관을 결정했다** — `_project/decisions/110`, 티켓 [w4-audio-upload-store](/backlog/w4-audio-upload-store/).
  기존 세 경로(`stream_wav.ts`·`/gateway/dev`·`/gateway/ingest`)는 흘려보내고 끝나서 **보관이 없었고**,
  「팀원 넷이 각자 할 수 있어야 한다」(사용자 지시)를 만족하지 못했다. `109` 와 같은 모양으로 간다 —
  브라우저는 AWS 키를 들지 않고, `server` 가 presigned **POST**(`content-length-range` 로 크기 상한을 강제한다 —
  PUT 은 못 건다)를 발급하며, 문은 `UPLOAD_TOKEN` fail-closed 다. 요구 ID 는 **`A-6`** 을 새로 받는다.
- **검토 중 바로잡은 것 셋.** ① 버킷 이름은 `assist-<계정ID>-apne2` 가 아니라 **`assist-apne2`** 다(오늘 런북 3차 정정).
  ② 「이 컴퓨터에 AWS CLI 가 없다」는 틀렸다 — `aws-cli 2.36.38` 이 깔려 있고 없는 것은 **자격증명**이다.
  ③ 「브라우저에 키가 없으니 안전」은 불충분하다 — **잠기지 않은 presign 발급 지점은 익명 업로드 프록시**이고
  막으려던 시나리오가 그대로 재현된다. 그래서 `110` 4번이 fail-closed 토큰을 못박았다.
- **정해지지 않은 것 — 파드가 AWS 자격증명을 어떻게 얻는가.** `108` ③ 으로 `server-env` 에서 AWS 정적 키를
  뺐고, 인라인 정책은 **EC2 인스턴스 역할**에 붙어 있다. IMDS 기본 hop limit 이 1 이라 파드는 막히는 것이 일반적인데
  **실측하지 않았다.** `110` 「미해결」에 확인 명령과 갈림길(hop limit 2 vs 전용 IAM 사용자)을 적어 뒀다.
  **코드는 어느 쪽이든 같다** — boto3 기본 체인이 환경변수와 IMDS 를 모두 본다.
- **GitHub Actions 교차검증**(다른 세션 요청). 확인된 결함 넷: ① `release.yml` 에 `pull_request` 를 그냥 더하면
  image·deploy 에 event 가드가 없어 **머지 안 된 코드가 Docker Hub 에 push** 된다 ② plan 의 `docker manifest inspect` 가
  익명이라 조회 실패가 「이미지 없음」으로 읽혀 게이트가 조용히 새고, 같은 태그를 덮어 구워도 IfNotPresent 라
  **노드는 옛 이미지를 쓴다** ③ `callguard-es` 는 굽는 잡이 없어 태그를 올리면 ImagePullBackOff ④ rebase 머지 +
  `fetch-depth: 2` 면 폴백이 조용히 **마지막 커밋만** 본다(얕은 클론으로 재현: exit 128 → 폴백 exit 0).
  룰셋 실측 — 필수 통과 검사는 `jekyll`·`ai`·`server` 셋뿐이고 `gateway` 는 아직 아니다.
  **`deploy` 라는 job 이름이 `pages.yml`·`release.yml` 양쪽에 있다** — 지금은 무해하지만 필수로 걸 생각이 생기면 이름부터 바꿔야 한다.
- 남은 것:
  - IMDS 확인 명령 실행 → `110` 「미해결」 채우기. 그다음 `server/apps/uploads/` 착수
  - 런북 4-1·19-7 에 「2026-09-14 실물 적용 확인」 표시 · 5-3 데이터셋 프리픽스 이름을 `data/README.md` 와 통일
    (런북은 `aihub-seoul-minwon`·`aihub-505`·`aihub-71479`, 실물은 `aihub-ktelspeech`·`aihub-krespspeech`·
    `aihub-lowquality-phone`·`aihub-minwon-qa` 로 **갈려 있다**. 객체 0개인 지금 정하는 게 싸다)
  - 위 Actions 결함 넷 — 전부 `release.yml` 한 파일이라 **한 세션이 몰아서** 고친다. 룰셋 변경은 admin 몫
