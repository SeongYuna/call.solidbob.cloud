---
date: 2026-09-15
author: "장민석"
person: minseok
seq: 12
---

- **조서희 님 관리자 화면(블랙리스트 분류·재설정·설정 탭)을 서버와 대조했다** — 신규/재범 분류는 실제 API 로 동작 · 재설정은 새 `…/expiry`(일수·사유·누적 상한)로 바꿔야 함 ·
  «만료 후 자동 해제» 는 서버에 없음(새 승인 때만 닫힘) · 만료 기본값은 12개월로 · 베테랑 기준은 서버에 없음. 메시지 초안을 사용자에게 드렸다(보낸 것은 사용자).
- **J-5 를 열었다**(`decisions/313`, 사용자 선택: 인입 전 배정 API · 후보는 요청으로 받기).
  `POST /hub/routing-decisions {call_id, candidates[]}` — 통화의 고객 식별자로 적용 중 블랙리스트 확인 → 후보 입사일로 근속 → 저장된 기준 → **블랙리스트 도메인 `route()`** 가 판정 → `routing_log`.
  `GET·PUT /hub/routing-settings`(관리자) — 새 테이블 `app_setting`, 저장값 없으면 기본 3년. 어댑터·설정 저장소는 기본값을 두 번 적지 않으려고 **블랙리스트 스포크 안에** 뒀다.
- ⚠ **실수 하나 — 새 테이블 순서.** `app_setting` 을 `routing_log` 옆에 넣었더니 생성된 DDL 에서 `admin_account` 보다 **먼저** 만들어져 외래키가 깨질 뻔했다.
  생성 직후 `CREATE TABLE` 순서를 보다가 잡았고 목록 끝(관리자 테이블 뒤)으로 옮겼다. 빈 DB 에 `schema.sql` 적용 성공으로 확인.
- **정정 — 기록 11 의 마이그레이션 파일 이름이 바뀌었다.** `2026-09-15-call-summary-revision.sql` 은 커밋 전이라 `app_setting` 을 합쳐
  **`2026-09-15-summary-revision-app-setting.sql`(27 → 29)** 로 이름을 바꿨다 — 운영 적용을 한 번으로 줄이려고. 결정 기록 311·런북·미결·티켓의 참조도 고쳤다(기록 11 은 고치지 않는다).
- 검증: `server` 780 passed · integration 20 · 계약 4종 KEPT / `ai` 225. 마이그레이션 27 → 29 가 새 스키마와 386항목 일치 · 재실행·선행 누락 시 멈춤.
- 남은 것: **판정을 부르는 곳이 없다**(교환기 없음) · 판정 API 인증 없음 · `fell_back` 집계 화면 없음 · 근속 기준값의 근거 없음 · 설정 탭 연결은 조서희 님 · 커밋·PR 전.
