---
date: 2026-09-22
author: "정성윤"
person: seongyun
seq: 16
---

- **PR #131(장민석 — 쓰기 경로 fail-closed · J-5 배정 판정 호출) 충돌을 풀고 머지했다**(`4668d24`). 충돌은 `STATE.md` 한 곳 —
  `server/` 줄은 장민석 님 것, `ai/` 줄은 main(#129 류준)의 것을 살렸다. **태그가 또 겹쳤다**: #129 가 `0.1.33`·`0.2.4` 를 먼저
  써서 `0.1.34`·`0.2.5` 로 올렸다(오늘 네 번째). 임시 워크트리에서 합쳐 미디에이터 147/147 · `tsc` · server pytest 통과 확인 뒤 push,
  필수 검사 다섯 초록 → 머지 → `release.yml` 성공(run `35697608814`). 운영: server `0.1.34` · call-mediator `0.2.5`, RESTARTS 0.
- **nori 품사 필터 운영 재적재(`w6-nori-pos-filter` 마무리 → done).** `0.1.34` 파드 안에서 `index_knowledge_base.py --to-es --recreate
  --embed-model /models/koe5` — `callguard-kb-single` 100건, 벡터 100/100. `_analyze` 로 「에서는」·「을」 이 빠지는 것 확인,
  `/health` spokes 에 `pii_ner`·`retrieval_dense` 그대로. SSM 명령 두 벌(`reindex.sh`·`verify.sh`)은 세션 스크래치에만 있다.
- ⚠ `kubectl exec` 에 표준입력 파이썬을 넘길 때 `-i` 가 없으면 조용히 아무것도 안 돈다 — 첫 검증 블록이 빈 출력이었다.
- 로컬 `PM` 을 main 과 맞추며 다른 세션의 미커밋 편집(`STATE.md`·티켓 둘)과의 충돌 셋을 풀었다 — 장민석 님 「넘겨받음」 절을 살리고
  재발 사례 절만 보탰다.
- **09-22 저녁 최종 QA 결과로 칸반 티켓 19건을 만들었다**(담당은 «제안»). 류준 5 — `w6-c5-name-tail-leak` · `w6-c5-overmask-request-word` ·
  `w6-passport-clause` · `w6-postcall-golden-cases` · `w6-test-hygiene-eval-wiring`. 장민석 7 — `w6-read-path-auth` · `w6-c5-interim-number-mask` ·
  `w6-d2-inquiry-type-null` · `w6-call-record-customer-id` · `w6-admin-stats-today` · `w6-server-loose-ends` · `w6-routing-candidates-prod`.
  조서희 7 — `w6-close-callid-missing` · `w6-qa-ui-defects-three` · `w6-platform-unmeasured-numbers` · `w6-vercel-json-branch-deploys` ·
  `w6-routing-result-ui` · `w6-frontend-tests-vitest` · `w6-read-path-token-ui`. 이미 티켓이 있는 것(`w6-compliance-alert-ui` · B-6 대화체 재보정 ·
  원문 토글 · 상담원 등록 · 스키마 대조 · 리허설)은 새로 만들지 않았다.
- 남은 것: `w6-procedure-pick-rule` 운영 SYN-010 확인(`closure` 안 뜸 · 5/5 저장) — `0.2.5` 가 떴으니 이제 된다 ·
  `ROUTING_CANDIDATES` 운영 값(장민석 남은 것 ①) · `STATE.md` `ai/`·인프라 줄에 `0.1.34`·재적재 반영(다른 세션이 편집 중이라 안 건드렸다).
