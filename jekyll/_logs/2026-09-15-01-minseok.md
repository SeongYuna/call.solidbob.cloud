---
date: 2026-09-15
author: "장민석"
person: minseok
seq: 1
---

- **`main` 을 `server` 로 당겨왔다** — 충돌 없이 fast-forward(`a6326ee`). 병합 커밋 없음.
- **`transcript_segment` UPSERT 블로커는 이미 풀려 있었다** — STATE 에 옛 줄이 남아 있었을 뿐이다. 어댑터가 `ON CONFLICT ("call_id","segment_id")` 이고
  단위 11 · 새 postgres:17 + 현재 `schema.sql` integration 2 통과(두 통화의 같은 순번 발화가 따로 남는다). 09-11 `w4-segment-composite-upsert` 에서 고쳐졌다.
- **운영·Neon 스키마 적용 여부는 확인하지 못했다.** `.env` 의 Neon 비밀번호가 인증 실패하고, AWS 키 칸은 비어 있어(길이 0) 운영 RDS 에 닿을 경로가 없었다.
  09-14 마이그레이션 적용 후 출력은 여전히 아무도 찍지 않았다 — 「적용했다」 기록만 있고 「됐다」 는 미확인이다.
- **D-1 통화 후 초안 501 을 걷었다 — 규칙 발췌**(`decisions/306`, 사용자 선택: LLM 은 나중). 새 스포크 `server/apps/postcall/`.
  요약은 자막에서 **고르고**(고객 첫 실질 발화 + 상담원 안내 + 건수), 유형(D-2)은 규칙표가 없어 `null`, 후속조치(D-3)는 상담원의 「…드리겠습니다」 약속 중 연락·발송 류.
  `/health` `spokes` 에 `postcall` 이 붙는다 — 런북 19장 기대 출력에 적었다. **품질은 측정 불가**(골든셋 D 0건).
- **상담원 전용 토큰을 만들었다**(`decisions/307`, 사용자 선택). 새 앱 `server/apps/agent_auth/` · 테이블 `agent_token`(해시만) ·
  관리자 API `POST·GET /admin/agent-tokens`·`POST …/{id}/revoke` · 가드 `require_agent`. **`POST /hub/blacklist-requests` 는 이제 토큰이 필요하고
  `requested_by` 를 본문에서 받지 않는다** — 본문에 남의 ID 를 실어도 토큰의 상담원이 저장된다.
  마이그레이션 `db/migrations/2026-09-15-agent-token.sql`: 25 테이블 DB(HEAD 스키마)에 넣으면 새 `schema.sql` 과 **349항목 일치**, 재실행·09-14 선행 누락 시 멈춤 확인.
- **고객 연결(발신 번호 → HMAC)은 코드가 끝까지 있었다** — 로컬 postgres:17 + 실제 앱으로 확인: `customer_linked "true"` · `call.customer_id` 64자 ·
  평문 번호 없음 · `GET /hub/calls?customer_id=` 로 그 통화가 나온다. 같은 실행에서 통화 종료 초안 200 · 토큰 없음 401 · 폐기 후 401 까지 이어서 봤다(16항목 ✅).
  `GET /hub/calls` 의 `customer_id` 설명이 「늘 null」 로 낡아 있어 고쳤다(프론트가 `/openapi.json` 을 읽는다).
- 검증: `server` 696 passed · integration 12 · 계약 4종 KEPT / `ai` 225 passed · 계약 3종 KEPT.
- ⚠ **관찰(이번 변경 아님)**: 마스킹이 「주민센터로 가시면」 을 `*********` 로 가렸다 — 과잉 마스킹. C-5 는 재현율 우선이라 방향은 맞지만 요약 가독성을 깎는다. [미결](/open-items/)에 남겼다.
- 남은 것:
  - **운영 적용 순서** — `agent_token` 마이그레이션을 **이미지 태그를 올리기 전에**(정성윤, 인스턴스 접근). 이미지 태그는 이번에 올리지 않았다 — 머지 = 배포라 순서를 뒤집지 않으려고
  - 프론트(조서희): 블랙리스트 요청에 `Authorization: Bearer cga_…`·`requested_by` 제거 · `apps/admin` 토큰 발급·폐기 화면 · 통화 후 요약 연결
  - 통화 후 초안 **저장**(`w7-postcall-persistence`) · LLM 요약(`w7-postcall-spoke`, 류준)
  - 게이트웨이 알림 3종은 **프론트 수신 뒤에 켠다**(사용자 결정) — 이번에 손대지 않았다
  - `ERD.png` 는 graphviz 가 없어 다시 그리지 못했다(`erd.dot`·`schema.sql` 은 갱신)
