---
date: 2026-09-04
author: "장민석"
person: minseok
seq: 1
---

- **인프라 런북을 네 영역 하네스로 걸었다** — `docs/infra-runbook.md` 가 들어왔지만 읽으라고 적힌 곳이 없어
  아무도 안 읽는 문서가 될 상태였다. 루트 `CLAUDE.md` §0 에 **영역별로 읽을 절 표**(server 12·16·19 /
  ai 11·14·15·22 / apps 16-2·18 / infra 전체)를 넣고, 영역 규칙 네 곳에 「배포 전제」 절을 붙였다 —
  `server/CLAUDE.md` §6 · `ai/CLAUDE.md` §7 · `.claude/rules/dashboard.md` §5 · `infra/CLAUDE.md`(신규).
  표에는 실제로 걸리는 것만 적었다: 주입되는 환경변수 4개와 `core/config.py` 의 대응, `/health` 의 `spokes`
  배열이 배포 검증 항목이라는 것, GPU 1장을 device plugin 없이 나눠 쓴다는 것, `"think": false` 누락이
  4.3절 측정을 무의미하게 만든다는 것, 프론트가 붙는 주소는 443 하나뿐이라는 것
- **훅으로 강제했다** — `.claude/scripts/infra_runbook_guard.py`(PreToolUse). `infra/`·`Dockerfile`·`Caddyfile`·
  `compose*.yml`·`.github/workflows/`·`server/core/config.py`·`.env.example`·`db/schema.sql` 을 고칠 때
  세션 트랜스크립트에 런북을 본 흔적이 없으면 **차단하고 읽을 절을 알린다**. Bash 로 쓰는 경우도 잡고,
  읽기만 하는 명령은 통과시킨다. 입력 8가지로 확인했다(차단 4 · 통과 4). 탈출구는 `CALLGUARD_SKIP_INFRA_CHECK=1`
- **런북과 저장소가 어긋난 4건은 고치지 않고 [미결 항목](/open-items/)에 올렸다** — 전부 인프라(정성윤) 소관이다.
  ① 배포가 `server` 파드 하나(런북)인지 `server`+`ai` 두 도메인(`CLAUDE.md`)인지 ② 클론 주소가 `SeongYuna/…`
  vs `solidbob02/…` ③ 부록 B 결정 기록 9건 미작성 — 런북 18장이 참조하는 `decisions/103` 파일이 없다
  ④ `.env.example` 에 런북 16-1 이 주입하는 `OLLAMA_URL`·`HF_HOME` 키가 없다
- 남은 것: `docs/infra-runbook.md` 는 **아직 커밋되지 않은 파일**이다(untracked). 하네스가 이 경로를 가리키므로
  런북이 같은 PR 에 들어가야 한다. 위 4건은 정성윤 확인 후 한쪽으로 맞춘다
