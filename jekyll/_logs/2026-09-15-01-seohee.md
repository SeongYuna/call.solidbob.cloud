---
date: 2026-09-15
author: "조서희"
person: seohee
seq: 1
---

- **Flutter 개인 실험(`decisions/405`)에 "상담원 계정 생성" 탭 추가** — `requests_tab.dart`의
  폼+목록 패턴(상단 카드 폼, 하단 `ListView`+`Card`)을 그대로 재사용했다. `AgentAccountItem`
  모델(`models/account_models.dart`) 신설, `AdminShell`이 계정 목록을 상태로 들고
  `onCreateAccount`에서 임시 비밀번호(영문+숫자 8자리)를 생성해 함께 저장한다.
  `mustChangePassword`가 true인 계정은 경고색("비밀번호 미변경"), false는 차분한 색("정상
  사용 중") 배지 — 새 색상은 안 만들고 기존 `AppColors`(warnBg/warnFg, okBg/okFg) 그대로 썼다.
- 검증은 안드로이드 에뮬레이터에 실제로 설치해 adb로 조작하며 했다 — 이름/로그인ID 입력 →
  "임시 비밀번호 생성" → 다이얼로그에 8자리 값 표시 → 복사 버튼으로 클립보드 복사 확인(안드로이드
  자체 복사 확인 UI에 같은 값이 뜨는 것까지) → 목록 최상단에 새 계정이 배지와 함께 추가되는 것까지 확인.
  `flutter run` CLI 자체는 이 머신에서 DDS 포트 연결 오류로 두 번 종료됐지만, 앱 프로세스는
  에뮬레이터에서 정상 구동 중이라 adb로 직접 검증했다.
- `flutter analyze` 이슈 0건.
- 남은 것: 없음 — 팀 스코프 밖의 개인 실험이라 이 이상 진행할 계획 없음.
