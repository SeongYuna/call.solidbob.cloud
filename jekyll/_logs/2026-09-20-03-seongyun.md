---
date: 2026-09-20
author: "정성윤"
person: seongyun
seq: 3
---

- **운영에서 파이프라인 전 구간을 처음으로 실제로 돌렸다** (런북 19장 11번의 확장판 — 테스트 통화 1건,
  **끝난 뒤 행 15개를 자식부터 전부 삭제**해 원상복구했다. 삭제 후 `call` 13 · `recommendation` 0 · `transcript_segment` 11 로 되돌아갔다).
  지금까지 운영에서 `recommendation`·`closure`·`compliance_flag` 는 **0행**이었다 — 코드가 없어서가 아니라 **한 번도 안 태워 봐서**였다.
- **돈 것 (전부 운영 실측)**
  - **C-5**: `01012345678` → `***********` 로 가려져 저장. `masking_event` 에 **P4 (6,17)**. 조회해도 마스킹본만 나온다(SEC-1)
  - **B(검색·추천)**: BM25 가 **1순위로 정답 조항 `DASAN-TERM-4.4`(전입신고 — 필요서류)** 를 찾았다.
    카드 5장 저장 · **`recommendation_card.source_doc_id` 가 `document` 98행과 실제로 연결됐다**(09-15 적재 이후 처음 확인).
    **`internal_latency_ms` 227ms** — 4.3절 내부 처리 예산 1,000ms 안이다(표본 1건이라 p95 라고 부르지 않는다)
  - **C-6**: 「멍청한」 → `insult` + 근거 `DASAN-MANUAL-5.1` 저장
  - **C-1~C-4**: 3건 저장(`무조건`·`100% 환급`·`보장해 드`) + `compliance_rule` 카탈로그 UPSERT —
    **09-18 에 새로 넣은 저장 배선이 운영에서 동작하는 것을 확인했다**
  - **F-2**: `DASAN-TERM-4.4` 로 `incomplete` · `missing:["신고인 신분증"]` · 조건부 서류 분리까지
  - **D**: `/close` 가 규칙 발췌 요약 초안을 만들어 저장
- **고쳐야 할 것 넷을 찾았다** ([미결](/open-items/)에 적었다)
  - ⚠ **쓰기 경로에 인증이 없다** — `POST /hub/calls`·`/hub/transcripts`·`/hub/recommendations` 등이 **토큰 없이 200**.
    주소를 아는 사람은 누구나 운영 DB 에 쓴다. 기존 미결 「상담원 토큰을 어디까지 걸지」가 **「실제로 열려 있다」로 확정**됐다
  - **없는 세그먼트를 참조하면 500** — `call_guard_flag` FK 위반이 그대로 터진다(파드 로그 `ForeignKeyViolation`). 4xx 여야 한다
  - **컴플라이언스는 같은 상황에서 200 인데 저장만 조용히 실패**한다(`reason=CallNotStartedError`, 로그에만).
    설계대로지만 **콜 가드와 정책이 반대**다 — 맞출지 정할 것
  - **`/close` 가 `status`·`ended_at` 을 안 바꾼다** — 기존 미결(09-17)을 **운영에서 재현**했다. 요약 초안만 남고 통화는 `in_progress` 그대로
  - `e2e_latency_ms` 는 **NULL** — `decisions/119` 가 적은 그대로다(채울 주체가 콜 미디에이터인데 아직 안 채운다)
- **계약 모양 차이 하나** — `POST /hub/recommendations` 는 **평탄한 본문**(`call_id`·`segment_id`·`text`…)을 받는다.
  로컬 프로브에서 `{"event":{…}}` 로 보냈는데 **501(스포크 미등록)이 먼저 떠서 모양 오류가 안 드러났다** —
  ES 가 없는 로컬에서는 계약 모양을 검증할 수 없다는 뜻이다
- 덤: 윈도에서 한글 본문을 `curl -d` 로 보내면 **400(parsing error)** 이다. UTF-8 파일로 `--data-binary` 해야 한다
- 남은 것: 위 넷 처리 · 옛 `test-deploy-*` 통화 3건 정리 · 커밋
