---
date: 2026-09-22
author: "류준"
person: ryujun
seq: 1
---

- **S3 업로드를 「내가 올린다」에서 「성윤님이 직접 받으신다」로 바꿨다.** 학원이 AWS 계정
  관리자로 지정돼 있어 팀 안에서 자격증명을 만들 수 없다는 회신을 받았다(SSO·IAM 사용자 둘 다 불가).
  **결과적으로 이 방향이 AI Hub 이용정책에 더 맞는다** — 이용정책 5번이 승인받지 않은 제3자에게
  데이터를 제공·양도하는 것을 막으므로, 내 맥의 파일을 넘기는 것보다 **각자 본인 계정으로 신청·수령**하는 편이 깨끗하다.
  이용정책 1번(사업결과임을 밝힐 것)도 확인했다 — 발표 자료에 출처 표기가 필요하다.
- **전달 문서 작성**: `_project/handoff/2026-09-22-ryujun-to-seongyun-dataset-download.md`.
  음성 데이터셋 6종(AI Hub 5 + 서울 열린데이터광장 1, 합 33 GB)의 링크·우선순위·`Validation`만 받는 이유,
  모델 4종(KoE5·bge-reranker-v2-m3·koelectra-ner·kanana GGUF)의 `huggingface-cli` 명령,
  `aihubshell` CLI 로 EC2 에 바로 받는 법, EC2 인스턴스 역할로 자격증명 없이 `aws s3 sync` 하는 법.
  **안 받아도 되는 모델 5종(9.7 GB)** 도 근거(`decisions/009`·`010`·`210`)와 함께 적었다.
- **런북 14-2 가 낡았다** — EXAONE 을 받게 돼 있는데 `decisions/207` 로 kanana 로 바뀌었다.
  인프라 작업이라 고치지 않고 전달 문서에 적어 두기만 했다.
- **AI Hub 원문 공개 범위(`w6-aihub-text-exposure`) 집계를 먼저 돌렸다** — 자격증명 없이 할 수 있는 것부터
  하던 중이었다. 실측: 골든셋 `v1-150.json` 에 **14건**(`source:"aihub"` 12건 + GS-101·102 — 이 둘은
  notes 에 「AI Hub 다산 실제 발화」라고 적혀 있는데 `source` 는 `authored` 다, **라벨 불일치**),
  원문 총 **469자**(건당 12~41자). 그 밖에 `server/apps/masking/tests/domain/test_pii_gaps_0917.py`
  의 과잉 마스킹 목록 **70줄**(P6 41·P7 29)이 AI Hub 실제 발화에서 온 조각이고,
  `name_detector.py`·`address_detector.py` 의 제외 목록도 같은 출처다. 공개 사이트(`jekyll/`·`docs/`)
  쪽은 「카드를 잃어버렸어요」류 **짧은 예시 인용**뿐이다.
  **판단은 아직 안 했다** — 티켓 완료 조건인 결정 기록이 남아 있다.
- **저장소 공개 상태 확인**: 원격은 `SeongYuna/call.solidbob.cloud` **PUBLIC**. `scripts/` 70개 파일이
  전부 추적·푸시돼 있고(`ai` 브랜치 미푸시 커밋 0), 제외되는 건 `__pycache__` 뿐이다.
  자격증명처럼 보이는 문자열은 0건이고 `data/raw/`·`data/processed/`·`models/`·`.env` 는 `.gitignore` 로 빠진다.
- 남은 것: 성윤님 회신(데이터셋 신청 여부) · `w6-aihub-text-exposure` 결정 기록(류준 `2xx`) ·
  S3 경로가 정해지면 미결 항목 닫기.
