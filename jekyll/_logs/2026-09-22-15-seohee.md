---
date: 2026-09-22
author: "조서희"
person: seohee
seq: 15
---

- `apps/admin`의 `RequestsTab.tsx`·`EntriesTab.tsx` 상단 JSDoc을
  `.claude/rules/rfp-harness.md §1` 형식(`Requirement: <ID>`)에 맞게
  고쳤다(주석만, 동작 무변경).
- `RequestsTab.tsx:5` — `"J-3 승인요청창. ..."` → `"Requirement: J-3 승인요청창. ..."`
  — 키워드만 추가, 설명은 그대로.
- `EntriesTab.tsx:37` — `"J-4 블랙리스트 관리창."` → `"Requirement: J-3 블랙리스트 관리창."`
  — 정본(`_project/decisions/204:40`: "관리자 대시보드 — 블랙리스트 관리창 ·
  승인요청창"이 둘 다 J-3) 기준으로 ID 자체도 정정. J-4는 화면이 아니라
  "관리자 판단(승인/반려) 후 상태 전이"(`decisions/204:41`)라 이 컴포넌트를
  가리키지 않는다.
- 오표기 확산 조사(3번, 수정 안 함): 저장소 전체 grep 결과 "J-4"의 압도적
  다수(`server/apps/blacklist`·`server/apps/hub`의 라우터·인터랙터·DTO·
  테스트 수십 건, `db/schema.sql`, `callStore.ts`, `BlacklistRequestButton.tsx`
  등)는 승인/반려·해제·만료변경 등 **실제 J-4(판단 후 상태 전이)를 정확히
  가리켜 오표기가 아니었다.** EntriesTab과 같은 종류의 오표기("화면"을
  J-4로 부름)는 다른 곳에서 발견되지 않았다. 다만 경계가 모호한 관련
  사례 2건을 보고만 하고 손대지 않았다 — `adminStore.ts:44`(주석)와
  `SettingsTab.tsx:65`(화면에 실제로 보이는 `<h3>J-4 블랙리스트 등록 만료
  기간 — 기본값</h3>`)는 "화면"이 아니라 승인 시 정해지는 "만료 기간
  기본값" 설정 하나를 J-4로 부른 것이라 정본의 "판단 후 상태 전이"로 볼
  여지가 있어 EntriesTab만큼 명백한 오표기로 단정하지 않았다.
  (`flutter/lib/screens/tabs/settings_tab.dart:54`도 같은 문구지만
  `decisions/405`로 팀 스코프 밖이라 제외.)
- 빌드 확인: `cd apps/admin && npm run build`(`tsc --noEmit && vite build`)
  통과 — 번들 해시가 직전 빌드와 동일해 런타임 무변경 확인.
