---
date: 2026-09-23
author: "조서희"
person: seohee
seq: 6
---

- **w6-qa-ui-defects-three (in-progress, 2/3 새로 고침)** — 칸반 순서 10번.
  1. 온도 `null`→"0건" — 코드가 **이미 고쳐져 있었다**(`BlacklistRequestButton.tsx`·
     `RequestsTab.tsx` 둘 다 "미측정" 표기). 코드 변경 없음, 확인만.
  2. 위기 신호가 폭언 배지에 덮임 — `callGuard`가 세그먼트당 단일 값이라 두 번째
     신호가 첫 번째를 지우던 버그를 확인대로 고쳤다. `callStore.ts`의
     `callGuard`/`historyCallGuard`를 `Record<string, CallGuardFlag[]>`로 바꾸고
     `applyCallGuard`가 배열로 쌓는다(같은 갈래 중복만 막음). `TranscriptPanel.tsx`는
     세그먼트당 모든 신호를 그리고 위기 신호를 위로 정렬한다. `BlacklistRequestButton.tsx`의
     증거 집계도 배열 평탄화로 맞췄다 — 부수 효과로 블랙리스트 요청 증거(폭언·위협
     건수)도 더 정확해졌다.
  3. 블랙컨슈머 카드·고객 위험 배너의 "관리자에게 전송됨"을 "관리자 알림 연동 전"으로
     바꿨다 — 둘 다 `console.info`뿐인 목업이라(`supervisorAlert.ts`) "전송됨"은 거짓이었다.
  - 검증: `tsc --noEmit`·`vite build`·`npm test`(11/11) 통과. 완료 조건 1번(화면 캡처)은
    로컬에 브라우저 확인 수단이 없어 못 했다 — `in-progress` 유지.
- 오늘 칸반 10번까지 진행. 커밋은 사용자 확인 후.
- 남은 것: 11번(`w6-qa-call-screen-fixes`)부터 계속.
