---
date: 2026-09-22
author: "류준"
person: ryujun
seq: 7
---

- **성윤 님이 올린 류준 티켓 4건을 검토 → 보완 → 서브에이전트로 처리했다**(사용자 지시). 검토에서 고친 것: 절차 고르기는 판정기가 틀린 절차를 못 세서 확인이 불가 → 판정기 보강을 함께 · 재생기는 로컬에서 이미 24/24 라 완료 조건이 버그를 못 잡음 → 원인을 코드로 · B-6 문턱은 dense 안에 넣으면 **0건 → BM25 폴백이 카드를 다시 채워** 무력화 → `FallbackRetriever` 에서 기권 · 0.67 은 같은 표본으로 고른 값 → 새 보류 표본 + 사전 등록. nori 는 「지금은 안 넣음」을 권했으나 **사용자 결정으로 채택**.
- **P 절차 고르기**(`w6-procedure-pick-rule`, in-progress) — 미디에이터가 추천 **1순위 카드에서만** 절차를 잡는다(규칙 여부는 서버 422 그대로, 판정 호출 최대 5→1). 새 테스트 2건은 옛 코드에서 실패 확인. 배포 뒤 운영 SYN-010 확인이 남았다.
- **R 재생기·판정기**(`w6-replay-last-turn`, in-progress) — 마지막 확정이 `/ws` 로 돌아온 걸 확인하고 `end` · `--close` 가 상담원 토큰(`CALL_AGENT_TOKEN`, E2E 는 루프백 검사 DB 에 임시 발급·폐기) · `--watch` 에 `retrieval_ms`… · `--save-ws`. 판정기가 **대본 밖 절차 판정**과 **PII 글자 단위 잔존**을 ❌ 로 잡는다. **티켓 가설(연결 닫는 경쟁)은 코드로 기각** — 남은 가설 둘은 운영 미디에이터 로그로(정성윤).
- **N nori 품사 필터**(`w6-nori-pos-filter`, in-progress · `decisions/214`, 류준 결정) — 기본 제외 품사 그대로. 임시 인덱스 전후: BM25 0.812/0.635 → **0.844/0.682**(얻음 GS-205·219·225·226·290 · 잃음 206·241), dense 불변(순서까지). 같은 골든셋으로 채택을 정했으니 부작용 확인이다. **운영 재적재는 정성윤(`--embed-model /models/koe5` 필수).**
- **B B-6 문턱**(`w6-b6-threshold-decision`, **done — 보류**) — 사전 등록(`695579d`) 후 새 보류 표본 24건으로 한 번 재 **경계값 통과**(걸러냄 12/24 · 손실 1)로 채택했다. 그런데 합친 트리 **E2E 24건에서 대화체 발화의 60%(114/189)가 기권**하고 SYN-015·017 필요서류 절차가 사라졌다 → **운영에 걸지 않는다**(기본 꺼짐, `decisions/215` 「채택 당일의 회귀」). 질문형 표본은 실제 대화 분포를 대표하지 못했다. 재측정은 [w6-b6-conversational-recalibration](/backlog/w6-b6-conversational-recalibration/). 에이전트가 「운영은 BM25 단독」이라 잘못 적은 것도 발행 전에 바로잡았다(운영은 12:10 부터 dense).
- **검증 E2E**(합친 트리 `0b4be57`, 로컬 운영 구성, `source: synthetic`) — ✅13 · ❌11(QA-1 0/24). 엉뚱한 절차 판정 **75쌍·458행 → 10쌍·67행**(남은 67행 중 57행은 누적 재판정) · `/close` 24/24 · 마지막 턴 394/394 · C-5 글자 누출 SYN-017#12 그대로.
- **공식 측정**(로컬 기록 DB) — **run_id 7(`cc572b4`, 운영에 나갈 구성 — NER + KoE5, 문턱 꺼짐): 0.969 · MRR 0.868 · C-5 0 · F-2 1.0 · B-6 기권 0** / run_id 6(`0b4be57`, 규칙+BM25+nori): 0.844 · 0.682 / run_id 5(`0b4be57`, dense + 문턱 켬 — 보류된 구성): 0.958 · 0.862 · 기권 15/24.
- **태그** server `0.1.33`(ai 라이브러리·nori) · call-mediator `0.2.4`(1순위 규칙) — 둘 다 레지스트리·다른 브랜치에 없음.
- **정정** — 05번 기록과 성윤 님 회신의 「`retrieval_ms` 는 `/ws` 로 안 나간다」는 **틀렸다.** 미디에이터가 서버 응답을 펼쳐 방송하므로 실린다 — 재생기가 제목만 찍어 못 봤다(이번 R 수정으로 찍힌다). 06번 기록(B-6 에이전트)은 「채택」 시점의 기록이고, 같은 날 보류로 바뀌었다.
- 오늘 최종 QA(페르소나 4)에서 나온 미해결은 [미결](/open-items/) 「2026-09-22 최종 QA」 블록에 담당별로 올렸다.
