---
date: 2026-09-19
author: "정성윤"
person: seongyun
seq: 6
---

- **F-2 를 「설계 문서 전환」으로 확정했다 — `_project/decisions/118`.** 7주차 체크포인트를 **앞당겨 판정**한 것이고
  새 규칙이 아니다(기획서가 정한 「둘 중 하나라도 미달이면 설계 문서」를 그대로 적용).
  판정 근거는 **구현이 모자라서가 아니라 잴 수 없어서**다 — 골든셋에 `f2_case` 가 0건이고
  `test_golden_set_closure.py` 가 **3건 전부 skip** 된다(09-19 실행). 절대 원칙 5 가 「표본이 없으면 통과가 아니라 측정 불가」라
  적은 그 상태라, **7주차까지 기다려도 결론이 같다.** 비는 시간을 성공 조건과 발표에 쓴다.
  - 산출물은 **시퀀스 다이어그램 + 요건 스키마** 둘. **코드는 지우지 않는다**(돌고 있고 콜 미디에이터가 부른다) —
    다만 「검수 통과한 기능」으로 발표하지 않는다(절대 원칙 10). **F-2 는 성공 조건이 아니다.**
  - 되돌리는 문은 열어 뒀다 — 골든셋에 케이스가 생겨 하네스가 채점하면 「구현 완결」로 되돌린다.
  - ⚠ 그 과정에서 티켓의 **「rev.5 `incomplete` 코드 미반영」이 낡은 서술**임을 확인했다 —
    `gate.py` 가 이미 `verdict="incomplete" if missing else "complete"` 다. 남은 것은 골든셋 케이스와 `DASAN-POLICY-1` 둘.
  - `w7-f2-checkpoint` 를 done 으로, `w8-f2-wrapup` 에 갈래를 적었다(담당·상태는 안 건드렸다 — 착수 전이다).
- **`/health` 가 「설정이 있다」만 보고 「붙는다」는 안 보던 구멍을 막았다 — `GET /health/ready` 신설(server `0.1.19`).**
  2026-09-14 에 그 구멍 때문에 **상태는 「정상」인데 기능이 죽어 있던** 일이 있었다(`connection.py` 주석에 남아 있다).
  - **`/health` 는 고치지 않았다.** 배포 스모크가 매번 치는 싼 기동 확인이라 거기서 DB·ES 를 찌르면 느려지고,
    자원이 잠깐 흔들릴 때 「서버가 죽었다」로 읽힌다. **싼 것과 진짜인 것을 주소로 갈랐다.**
  - PostgreSQL 은 `select 1`, ES 는 ping, **타임아웃 3초**. 설정이 없는 자원은 **실패로 세지 않는다**(`configured: false`).
    설정된 자원이 하나라도 못 붙으면 **503 + `degraded`**.
  - **SEC-2**: 예외 메시지에는 접속 문자열·호스트·비밀번호가 들어 있어 **타입 이름만** 싣는다. 그것을 고정하는 테스트를 같이 넣었다.
  - 검증(이 윈도 머신, 새로 만든 `.venv`): server **1,014 통과**(새 4 — 못 붙으면 503 · 설정 없음은 실패 아님 ·
    접속 문자열 미노출 · ES 미배선 표시) · **import 계약 4종 KEPT** · `check_release_tags.py` 통과 → **server `0.1.18` → `0.1.19`**.
  - 런북 19장에 **9-1 번 수동 검사**로 넣고, `server/CLAUDE.md` 의 「`postgres_configured` 는 설정 여부일 뿐」 줄을 갱신했다.
    ⚠ **k8s 프로브에는 붙이지 않았다** — 붙이면 ES 흔들림에 파드가 서비스에서 빠진다. 그 판단은 [미결](/open-items/)로 남겼다.
- **미결 7건을 더 닫았다**(오늘 누적 17건) — F-2 골든셋 케이스(118 로 정리) · 태그 안 올린 머지(`tag-check` 가 막는다) ·
  「배포를 태그로 확인할 길이 없다」(SSM 으로 생겼다) · 관리자 가드 500(운영 실측 **401**) ·
  `compliance_flag` 미저장(09-18 배선) · 콜 미디에이터 컴플라이언스 미배선(`0.2.1`) · 필요서류 1순위 의존(카드 순회로 교체).
  전부 **원문을 남기고** 근거만 앞에 붙였다.
- 남은 것: 장민석 님 검토(`0.1.19`) · 류준 님 재측정 · `decisions/208` · Vercel 옛 변수 · 커밋
