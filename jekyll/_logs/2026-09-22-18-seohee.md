---
date: 2026-09-22
author: "조서희"
person: seohee
seq: 18
---

- J-5(배정 판정)를 `decisions/126`·`407` 그대로 실제 구현했다 — 팀 합의로
  `w7-j5-routing-caller`(정성윤 담당)를 이어받았다. `services/call-mediator/`는
  원래 정성윤·류준·장민석 영역이라(`decisions/302`) 정성윤 검토를 티켓에 남겨뒀다.
- **콜 미디에이터**(`services/call-mediator`): `call_registry.ts`의 `callFor()`가
  `startCall` 성공 직후 `decideRouting()`을 한 번(화자가 둘이어도) 부르게 했다.
  - `ports.ts`에 `RoutingDecisionRequest`/`RoutingDecisionPayload` 타입과
    `HubPort.decideRouting` 추가, `CallMediatorMessage`에 `routing_decision` 추가
  - `hub_http.ts`에 `POST /hub/routing-decisions` 실제 호출 구현 — 기존
    `checkCompliance` 등과 같은 패턴, 새 인증 안 만들고 `HttpHub`가 이미 붙이는
    서비스 토큰을 그대로 씀
  - `RegistryDeps`에 `routingCandidates`(후보 목록)·`announceRouting`(방송 여부)
    추가. 후보는 `config.ts`의 새 env `ROUTING_CANDIDATE_AGENT_IDS`(콤마 구분,
    비면 빈 배열 → 서버가 기존 배정 규칙으로 떨어뜨린다, 126이 정한 그대로)
  - `main.ts`에서 `announceRouting: true`로 배선(대시보드 파서가 이제 이 타입을
    앎). 판정 실패해도 통화는 안 막고 로그만 남김(126 ③ 그대로)
  - 테스트: `call_registry.test.ts`에 J-5 케이스 6건(한 번만 부름·후보 그대로
    전달·빈 후보 처리·announceRouting on/off·실패해도 통화 계속), `adapters.test.ts`에
    `HttpHub.decideRouting` 계약 테스트 1건 추가 — `npm test` 141/141 통과,
    `npm run typecheck` 클린(이 서비스는 `node_modules`가 없어 `npm install`부터
    했다)
- **apps/call**: WS 파서에 `routing_decision` 추가(기존 `compliance_unavailable`
  추가할 때 쓴 패턴 그대로 — `ParsedMessage` 유니온·화이트리스트·`parseByKind`·
  `parseRoutingDecision`). `CallMediatorListener.onRoutingDecision?` 신설,
  `useCallMediatorSession`에서 `callStore.applyRoutingDecision`으로 연결.
  스토어에 `routingDecision: RoutingDecision | null`(세그먼트 단위 아님, 통화당
  하나라 override) 추가.
- **화면 표시 위치** — `TranscriptPanel.tsx`의 자막 헤더에 배너 하나 추가:
  "배정 판정 기록됨 · (블랙리스트 여부) — 연결은 바뀌지 않습니다(시연용 기록)".
  **"배정됨"이라고 안 쓴다** — `decisions/407`이 정한 원칙(판정이 연결을 안
  바꾼다)을 화면에도 지켰다. 상담기록(history) 재생에는 이 값이 저장 안 돼
  안 보인다 — `!isHistory`로 게이팅.
- **화면 문구 재정정** — 지난 세션에서 "구현 전이라 거짓"이라며 뺐던
  `BlacklistRequestButton.tsx`(apps/call)·`EntriesTab.tsx`(apps/admin)의 배정
  관련 문구를 이제 실제로 구현됐으니 "이후 통화부터 배정 판정이 기록에
  남습니다"로 다시 채웠다.
- `w7-j5-routing-caller.md` 티켓을 `status: done`으로 옮기고 완료 조건 2개를
  체크, 구현 내용과 남은 일(운영 후보 ID 설정·발신번호 있는 시연 리허설)을
  본문에 남겼다. assignee를 조서희로 옮기되 정성윤 검토가 필요하다고 적었다.
- 빌드 확인: `services/call-mediator`(`npm run typecheck`·`npm test` 141/141),
  `apps/call`·`apps/admin`(각각 `npm run build`) 전부 통과.
