---
date: 2026-09-22
author: "정성윤"
person: seongyun
seq: 23
---

- **CI 기준선 게이트를 붙였다 — `w2-baseline-gate` 닫음**(`_project/decisions/134`, 안 C). `test.yml` `server` job 에 「기준선 게이트」 step: 하네스를 ES·DB 없이 돌려 `scripts/check_baseline.py` 로 판정. **C-5 누락 · F-2 판정은 PR 마다 1건 단위로 막는다.** 검색은 러너에 ES 가 없어 「측정 불가 — 검색 엔진 없음」 경고이고 통과로 세지 않는다. 필수 검사 이름은 그대로라 룰셋은 안 건드렸다.
- **검색 기준선은 사람이 본다 — 판정(09-30) · 동결 최종 측정 · 검색을 바꾸는 PR 머지 때.** 사용자 질문 「프로젝트 끝까지 한 번이면 되나」에는 **최소 두 번(판정 · 동결) + 검색 변경 때**라고 답했다.
- **이 머신에서 검색 포함 게이트를 한 번 돌렸다** — Docker 가 WSL 에 없어 ES 9.5.1 + `analysis-nori` 를 세션 스크래치에 직접 풀어 127.0.0.1 에만 띄우고 지식베이스 101청크를 색인(운영 ES 는 건드리지 않았다). 운영 구성(규칙 + BM25) 3회 최저 **Recall@5 0.853 · MRR 0.686(n 102)** · C-5·F-2 뚫린 건 0 → **통과 4 · 측정 불가 0**. 류준 run_id 9 와 같은 값이 재현됐다. ⚠ 기록 DB 없이(`run_id` 없음) — 커밋 `52cb4e6` + 작업 트리(서버·미디에이터 수정은 채점 경로와 무관).
- **CI 조건 재현**: 서버 job 패키지만 든 깨끗한 가상환경에서 돌렸더니 **numpy 가 없어 하네스가 멈췄다** — step 에 `numpy==2.5.2` 를 넣었다. 멈추면 리포트가 없어 게이트도 실패하므로 조용한 초록은 없다. 넣은 뒤 통과 2 · 측정 불가 1.
- **오표기 정정**: ES 가 없을 때 검색이 「모듈 미구현」으로 찍혔다 — 하네스에 `RETRIEVAL_NO_ENGINE` 사유를 두고 `run_eval.py` 가 ES 없을 때 그 문장으로 싣는다. 검증: ai 551 passed · 계약 3 kept · scripts 21 passed.
- 남은 것: PR(전사 덮어쓰기 수정과 함께) · 로컬 ES 는 스크래치에 떠 있다(`es.pid`, 세션 끝에 내린다).
