---
date: 2026-09-15
author: "장민석"
person: minseok
seq: 4
---

- **PR #86 CI 가 전부 초록이 됐다** — 태그 `0.1.8` 커밋 뒤 `server`·`ai`·`gateway`·`jekyll`·**`tag-check`** 통과. 머지(= 운영 배포)는 하지 않았다.
- **통화 후 초안을 저장한다**(`w7-postcall-persistence`) — `POST /hub/calls/{id}/close` 가 `call.summary_text`·`inquiry_type` 을 갱신하고 후속조치를 `follow_up_action`(`status: draft`)에 넣는다.
  새 기록 포트 `PostcallRecordPort` · `postgres/postcall_repository.py` · DB 없으면 로그 어댑터(요약 본문은 로그에 안 남긴다).
  - 다시 닫으면 **확정 전 초안만 교체** — 지우는 후속조치는 `draft` 뿐, 상담원이 손댄 항목은 남긴다
  - **확정된 요약은 덮지 않는다** — `summary_confirmed_at` 을 `FOR UPDATE` 로 읽고 채워져 있으면 409. 통화가 없으면 404(외래키 500 이 아니라)
  - 인터랙터는 판정하지 않는다 — 덮을지 말지는 기록 포트가 예외로 막는다. 저장되는 것은 `confirmed=false`·`call_id` 고정 **뒤의** 초안이다(테스트로 고정)
- 검증: `server` 698 passed · integration 13(새 1) · 계약 4종 KEPT. 실제 앱 + postgres:17: 없는 통화 404 → 통화 시작 → 저장 200(후속조치 1행 `draft`) → 재요청 후에도 1행 → 확정 뒤 409.
- 남은 것: 저장한 요약을 **읽는 API 가 없다**(`GET /hub/calls` 에 `summary_text` 없음) · 상담원 확정 API 없음 — [미결](/open-items/). PR #86 머지는 사용자 확인 뒤
