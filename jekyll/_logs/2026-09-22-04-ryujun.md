---
date: 2026-09-22
author: "류준"
person: ryujun
seq: 4
---

- **`w6-golden-set-loose-ends` 를 닫았다**(in-progress → done). ③ GS-205 는 02번 기록에서 끝났고, 남은 ①② 를 이번에 처리했다.
- **① B-6 — 골든셋이 `v1-150.1` 로 바뀌어 분포를 다시 쟀다.** `scripts/measure_no_answer_threshold.py --device mps`, 2026-09-22 11:30, 커밋 `c526989-dirty`(추적 안 되는 `.claude/worktrees/` 뿐), 정답 있음 B 96건 · 정답 없음 B-6 24건. **결론은 09-21 과 같다** — 세 계열 모두 문턱 하나로 못 가른다. dense 0.671 에서 정답 1건을 잃고 정답 없음 15/24 를 거른다(BM25 는 5.431 → 5/24). 원자료 `jekyll/assets/no-answer-threshold-2026-09-22.json`. 완료 조건은 「케이스 + 측정·기록」이라 충족으로 닫았고, **문턱 값은 팀 결정 그대로 코드에 넣지 않았다.** `decisions/124` 로 임베딩이 운영에 실리게 돼 dense 0.67 안팎이 실제 후보가 됐다는 점만 티켓에 적었다.
- **② C-4 — `decisions/211` 을 채택했다**(사용자 지시). 팀 회의 합의는 없다 — 09-21 전달 문서로 안건을 올렸고 오늘까지 답이 기록에 없어 작성자가 채택했다고 211 에 그대로 적었다. 기획서 C-4 = **「근거 없는 안전 보장 — 의학적 안심 발언 등」 탐지 갈래**(`DASAN-MANUAL-4.1`), 「권장 대체 표현」은 C-1~C-4 공통 출력. 고친 곳: `plan.md` 2.4절 + 수정 이력 · `docs/02` · `docs/domain.md` · `rfp-harness.md` · 골든셋 README · DTO·스키마 주석 · `generate_schema_docs.py` → `schema.sql` 주석 한 줄 · 칸반 배지(「대체 표현」→「안심 발언」) · `rules.py` 갈래표. **코드 동작·골든셋·채점은 한 글자도 안 바뀐다**(하네스는 C 를 이진 채점). 211 의 5번(지식베이스 `MANUAL.md` 표지)만 ES 재적재가 따라 **09-30 판정 뒤**로 미루고 미결에 올렸다. 조서희 님 `w6-compliance-alert-ui` 에는 라벨 기준 메모만 달았다(status 불변).
- ERD 를 재생성하니 `ERD.png` 가 크기만 바뀌었는데 `erd.dot` 은 그대로라 렌더링 차이로 보고 되돌렸다.
- **server 이미지 태그 `0.1.27` → `0.1.28`** — DTO·스키마 주석이 서버 이미지 파일이라 `tag-check` 가 요구한다. `0.1.28` 은 레지스트리·다른 브랜치 모두 비어 있다(`check_release_tags.py`).
- 남은 것: 커밋·푸시(요청 시) · B-6 문턱 값(팀) · 품사 필터 결정 · 211 5번(09-30 뒤).
