---
date: 2026-09-11
author: "정성윤"
person: seongyun
seq: 21
---

- **frontend 브랜치를 직접 합쳤다** (사용자 결정 — 조서희 님을 기다리지 않고). 조서희 님 브랜치 자체는 고치지 않았고, PM 에서
  `origin/frontend` 를 머지해 충돌만 풀었다(`ce7f525`). 조서희 님은 다음에 main 을 받으면 이 정리를 그대로 받는다.
- **살린 것** — 대시보드 `?gateway=` 런타임 주소 덮어쓰기(`apps/dashboard/src/lib/ws/types.ts` — Vercel env 없이 배포된 대시보드를
  라이브로 바꾸는 탈출구), `.vercel` 무시, `decisions/402`, 티켓 `w3-gateway-dev-testcall`, 로그 `09-11-01-seohee`, `w2-dashboard-scaffold` 정리,
  `STATE.md` 조서희 님 줄. **버린 것** — frontend 쪽 `services/gateway` 전부(JS 서버·dev 페이지·Dockerfile·`package*.json`). 충돌은 예상대로
  `README`·`package.json`·락파일 셋이었고 main 쪽을 골랐다. **`services/gateway` 는 main 과 바이트 단위로 같다** — `git diff` 로 확인했다.
  402 의 쓸모는 이미 #69 로 main 게이트웨이에 옮겨 둬서 잃은 것이 없다.
- `decisions/402` 머리에 «게이트웨이 신설 부분은 109 로 일부 대체, 결정 4(`?gateway=`)는 유효» 한 줄만 붙였다. 본문은 그 시점 기록이라 그대로.
- **확인**: 대시보드 `npm run typecheck`·`build` 통과(조서희 님 변경 포함), 게이트웨이 79 tests, 사이트 링크 0 깨짐. 이 머지는 릴리스 경로
  (`server/`·`ai/`·`services/gateway/`·`infra/`)를 건드리지 않아 **재배포가 일어나지 않는다.**
- ⚠ `apps/` 는 조서희 님 전담이다(`decisions/302`) — 이번엔 조서희 님이 이미 만든 변경을 그대로 옮겨 넣었을 뿐 `apps/` 코드를 새로 쓰지 않았다.
  조서희 님께 알려야 한다: ① 게이트웨이는 main 쪽 하나다(`decisions/109`) ② 운영 테스트 페이지는 `https://server.solidbob.cloud/gateway/dev`
  (과금 문 토큰 필요, ngrok 불필요) ③ 대시보드 라이브 주소는 `?gateway=` + `encodeURIComponent("wss://server.solidbob.cloud/gateway/ws?token=<뷰 토큰>")`.
- 남은 것: `?gateway=` 로 운영 게이트웨이에 대시보드를 실제로 붙여 보는 것 — 뷰 토큰이 인스턴스 안에 있어 이 머신에서는 못 했다.
