---
date: 2026-09-21
author: "류준"
person: ryujun
seq: 2
---

- **기록 `01` 에서 착수한 서브에이전트 4개 + B-6 1개가 전부 끝났다.** 결과와 남은 것 —
  ① **모델 계열 오류 내성 곡선**(`w5-error-curve-model-series`, 커밋 `97f7b25`, 시드 3, 검색 n 96 · C-5 판정 24, Apple M5/mps) — 운영 계열(bm25·규칙) 값이 09-21 정성윤 님 JSON 과 **시드별로 완전히 같아** 한 그림에 5계열을 얹었다(`jekyll/assets/error-tolerance-2026-09-21-models.png`). Recall@5/MRR: bm25 0.833/0.666 → dense **0.979/0.885** → dense+리랭커 0.979/0.919, 20% 오류에서 bm25 0.792 · dense 0.969. C-5 누락(시드 최대): 규칙만 0·2·3·4·4, **규칙+NER 0·0·0·1·1**. 리랭커는 Recall 을 한 건도 더 못 맞히고 MRR 만 +0.03(`208`·`121` 「리랭커는 뒤로」와 같은 방향). ⚠ 주입기 낙관(46.9%)·골든셋 상한·운영엔 없는 구성 — `121` §5 의 「로컬 참고치」다
  ② **F-2 골든셋 99건**(`w5-f2-golden-cases`, `GS-601~699`, module F-2, 발화 없이 `f2_case` 만) — 기대값은 규칙표를 손으로 옮겨 적어 먼저 정하고 코드와 대조(어긋남 0). 하네스 `closure_gate accuracy 1.0 · n 99` — **규칙표 상한이지 「안내가 충분했는가」가 아니다**; 발화→evidence 자동 판정은 안 쟀다. 서버 골든 테스트가 `v1-50` 을 읽고 있어 `v1-150` 으로 한 줄 고침(199 passed, `server/` 파일 — 장민석 님께 알림). `DASAN-POLICY-1` 을 305 에 맞춤(골든셋 참조 0건, 다음 ES 재색인 뒤 결과 달라질 수 있음). **`decisions/118` 되돌림은 정성윤 님 판단으로 넘겼다**
  ③ **D-5 하네스 배선**(`w3-call-temperature`) — `VoiceOutlierPort`·DTO 신설(`server/`, `# Requirement: D-5`), 리포트 `call_temperature` 3상태. **구현체가 없어 실제 `run_eval.py` 는 아직 「모듈 미구현」**, 음성 골든셋 0건. 케이스 형식은 골든셋 README 끝
  ④ **B-6 정답 없음 24건**(`w5-b6-no-answer-threshold`, `GS-701~724`, module B-6 — `hit_at_k` 가 빈 정답을 True 로 주므로 B 에 섞지 않았다; **B 96건 값 전후 동일**). 점수 분포(`scripts/measure_no_answer_threshold.py`): **세 계열 모두 문턱 하나로 못 가른다.** BM25 는 정답 1건 잃는 값에서 5/24, dense 0.67 안팎이 손실 1/94 로 15/24. 문턱은 코드에 안 넣었다 — 팀 결정. 같은 24건으로 고르고 잰 자기충족 주의
  ⑤ `decisions/211` **C-4 라벨** 초안(제안) — 저장소 안에 「위반 갈래」·「기능」 두 뜻이 공존, 하네스는 이진 채점이라 점수 영향 0. 권고 ① 기획서를 고친다(문서 12곳, 코드·골든셋 0)
- **Google TTS 키 확인**(사용자 지시) — `--prefetch SYN-001` 7턴 381자 합성, mp3 2.2~9.8초, 상담원 Wavenet-B/고객 Wavenet-C, 재실행 캐시 7·호출 0, 장부 381/900,000. `w5-persona-tts-google` 닫음. SSML `volume` 은 귀로 확인해야 한다
- 티켓 정리: `w5-e2e-findings-ai`·`w5-persona-e2e` 는 남은 항목이 `121`·`210` 으로 대체돼 닫음. 골든셋 156→**279건**. 테스트 ai 435 · server 1,254 · 계약 3+4종 KEPT · 지킬 빌드·링크 0 깨짐. 전달 문서 `_project/handoff/2026-09-21-ryujun-to-seongyun.md` §5 채움
- ⚠ `check_session_end.py` 경고: `w8-f2-wrapup`(장민석)·`w2-baseline-gate`(정성윤) 가 todo 인데 소관 파일(`closure_gate` 테스트 · `harness.py`)을 오늘 고쳤다 — 남의 티켓이라 status 를 안 옮겼고 전달 문서에 적었다
- 남은 것: **AI Hub 505/71479 신청·다운로드(류준 본인)** → `w3-a5-translation-spike` 재개 · 합성 대본 사람 검토 · SYN-001 mp3 듣기 · 정성윤 님 결정(118 되돌림 · 09-30 기준선 구성 · SYN-010 운영) · 장민석 님 검토
