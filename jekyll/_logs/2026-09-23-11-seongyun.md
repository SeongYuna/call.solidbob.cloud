---
date: 2026-09-23
author: "정성윤"
person: seongyun
seq: 11
---

- **미결 항목을 실물로 확인해 16건을 정리했다**(124 → 111). 게이트와 무관한 범위만 골랐다.
- **이미 해결됐는데 열려 있던 것 9건**을 근거를 달아 닫았다 — 원문 열람 SEC-1(`decisions/408` 로 걷힘) ·
  런북 SSH 키 이름(AWS 실물 `callguard-key` 대조) · AgentCallBox 모달 · 컴플라이언스 경고 로컬 규칙 ·
  상담원 토큰 `localStorage` · `closeCall`·카드 피드백 토큰 · 반려 사유 입력 · 현황판 `admin-stats` ·
  블랙리스트 승인·해제 운영 확인(QA Q-62).
- **운영에서 직접 재서 7건 더**: 통화 후 초안 읽기 경로는 `GET /hub/calls/{id}` 에 이미 있고(요약 저장 5/34),
  `gateway=` 잔재 0건, `call`·`platform` 번들에 주소가 들어 있고, `vercel.json` 은 **안 넣기로 한 결정대로** 0개다
  (⚠ 프로젝트를 새로 만들면 대시보드에서 다시 꺼야 한다 — 런북 18-4).
- **운영 DB 29 테이블을 다시 셌다**(09-19 이후 처음). **0행은 넷뿐**이다 — `eval_run`·`eval_result`(하네스는 로컬
  기록 DB) · `knowledge_gap` · `resource_center`(G-2 미구현) · `voice_outlier`(D-5 보류 `317`).
  `document` 99행이 들어와 **`recommendation_card.source_doc_id` NULL 이 445건 중 1건**으로 줄었다(09-19 의 ⑤가 풀렸다).
  `closure` 53 · `closure_item` 131 · `masking_event` 37 · `call_guard_flag` 37 · `routing_log` 33.
- ⚠ **`call.agent_id` 는 34/34 전부 NULL** — Q-14 의 나머지 절반(`w8-call-owner-scope`)이 DB 에서도 그대로 보인다.
  `compliance_rule` 은 C-1 하나가 아니라 **C-1·C-2·C-3 세 행**이 됐고 실제 탐지가 쓰는 카탈로그다.
- 남은 것: 미결 111건 중 팀 결정이 필요한 ~25건은 한 장으로 뽑아 회의에 올린다. 새 측정이 필요한 ~20건은
  발표에서 「측정 불가·한계」로 적는다.
