---
date: 2026-09-11
author: "정성윤"
person: seongyun
seq: 17
---

- **`0.1.3` 배포 — trigger 스포크가 운영에 붙었다.** PR #65 머지(`820d405`) → `release.yml` plan·image·deploy 성공.
  이번 배포부터 스모크 테스트가 `spokes` 4종(`masking`·`closure_gate`·`retrieval`·`trigger`) **전부**를 요구하므로, 초록 자체가
  트리거가 붙었다는 증거다(전에는 `retrieval` 하나만 봐서 `0.1.2` 가 trigger 없이 초록이었다 — a5 세션 지적).
- 운영 확인(이 세션이 밖에서): `/health` `spokes` **4종** · 19-11 재확인(통화 `created "true"` → 전사 **200** `***********` →
  조회 `total 1`, 통화 `test-deploy-202609111747-013` 이 남았다) · `/hub/recommendations` **503**
  「검색 인덱스가 없다 — 지식베이스를 적재해야 한다」. 트리거까지는 돌고 검색에서 멈추는 것이 설계대로다 — 501 이 아니다.
- 티켓 [w4-segment-composite-upsert](/backlog/w4-segment-composite-upsert/) 를 done 으로 옮겼다.
- 남은 것:
  - **운영 ES 지식베이스 적재** — 추천·검색이 실제로 돌려면 필요하다. 서버 이미지에 `knowledge-base/`·`scripts/` 가 없어 절차 설계부터 한다.
  - 인스턴스의 `~/server-env.backup.yaml` `shred -u`(사용자, 이전 시크릿 값이 들어 있다).
  - 이 기록과 티켓 status 변경은 **아직 커밋하지 않았다**(다음 PR 에 함께).
  - 게이트웨이 배포(게이트웨이 세션 몫) — 서버 쪽 선행 조건(`POST /hub/calls`)은 이제 운영에 있다.
