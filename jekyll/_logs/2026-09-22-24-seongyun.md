---
date: 2026-09-22
author: "정성윤"
person: seongyun
seq: 24
---

- **장민석 님께 넘겼던 두 건을 되가져왔다(사용자 지시).** 둘 다 착수 전(`todo`)이라 담당을 옮겼다.
- **① 배포 전 운영 스키마 대조(`w6-deploy-schema-precheck`, `decisions/128` 구현)** — `release.yml` `k3s-deploy` 에 「운영 스키마를 대조한다」 단계: 매니페스트 적용 **전에** 서버 파드 안에서 운영 컬럼을 떠 `compare_prod_schema.py --allow-prod-extra` 로 대조, 어긋나면 적용하지 않고 실패(운영 그대로). 스크립트에 **잘린 덤프 거절**(SSM 24,000자 — 지금 8.8 KB) · 「운영에만 있음」 경고 옵션 · 테스트 5건(scripts 26 passed). 런북 19장 머리에 실패 시 절차. ⚠ `decisions/319` 로 QA 게이트 뒤였던 티켓인데 사용자 지시로 먼저 했다.
  - **이 머신에서 그 단계를 그대로 운영에 돌려 봤더니 멈췄다** — 운영에 `eval_run.components`(PR #132 의 `2026-09-22-eval-run-components.sql`)가 없다. 서버 요청 경로는 안 쓰는 컬럼이라 장애는 없었지만, **이 PR 을 머지하는 순간 배포가 막힌다** → 먼저 넣는다. 해시 대조 후 서버 파드 안에서 넣는 명령을 세션 스크래치에 준비했다(운영 DB 쓰기라 사용자가 `!` 로 돌린다).
- **② J-5 배정 판정 운영 확인(`w6-routing-candidates-prod`)** — 읽기로 보니 후보 값은 들어가 있는데 **운영 `agent` 에 `demo-A01..A06` 행이 0** 이라 판정이 늘 기존 규칙으로 떨어진다(`routing_log` 28행). 시드는 관리자 access token 이 필요해 사용자 몫 — 클립보드에서 읽고 찍지 않는 래퍼를 준비, 드라이런은 정상(베테랑 후보 A03·A04·A06).
- 남은 것: 마이그레이션 적용 → 대조 0 확인 · 시드 → SYN-006 → 요청·승인 → SYN-007 → `routing_log` 베테랑 · PR(오늘 로컬 변경 전부).
