---
date: 2026-09-23
author: "조서희"
person: seohee
seq: 9
---

- **w6-read-path-token-ui (done)** — 칸반 순서 13번. 착수 전 재확인하니 의존 티켓
  `w6-read-path-auth`(장민석)가 2단계 배포(`decisions/322`)로 이미 풀려 있었다 —
  서버는 `0.1.35`로 배포돼 `read_guard: open`(토큰 없이도 통과하는 과도기) 상태고,
  "남은 것: 그 한 줄"이 프론트가 토큰을 싣는 걸 기다리고 있었다.
  - `coreClient.ts`에 `agentAuthHeaders()` 신설(상담원 토큰 있으면 싣고, 없어도
    던지지 않는다 — 과도기 호환) — `fetchCallList`(GET /hub/calls)·
    `fetchCallTranscript`·`fetchCallRecord`·`searchDocuments`(POST /hub/search) 넷에 붙였다.
  - 지식 공백 신고는 `apps/call`에 부르는 화면이 없어 범위 밖. `apps/admin`은 이미
    관리자 토큰을 싣고 있어 손대지 않았다.
  - 완료 조건 2번(로그아웃 탭 오류 문구)은 기존 `try/catch` 구조로 이미 뒷받침됨 —
    실제로 잠긴 뒤 확인은 못 했다.
  - `w6-read-path-auth`에 "프론트 붙었다, `READ_AUTH_REQUIRED=true` 켜도 된다"고
    핸드오프 메모를 남겼다.
  - 검증: `tsc --noEmit`·`vite build`·`npm test`(11/11) 통과.
- 칸반 5~13번(9번은 이미 커밋됨, 이번엔 12·13번) 오늘 세션 진행. 커밋은 사용자 확인 후.
