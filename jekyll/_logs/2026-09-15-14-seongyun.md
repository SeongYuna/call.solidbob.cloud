---
date: 2026-09-15
author: "정성윤"
person: seongyun
seq: 14
---

- **PR #93(`frontend`) 충돌을 풀었다 (`2807827`)** — `origin/main` 을 `frontend` 에 머지했다.
  충돌은 `_project/STATE.md` 영역별 상태 표 **한 곳뿐**이었고, 네 줄을 각자 최신으로 골랐다:
  `server/` 는 frontend 쪽(0.1.10 운영 배포 완료·운영 DB 29테이블·40경로 — frontend 가
  `origin/server` 를 먼저 받았다), `ai/` 와 인프라는 main 쪽(PR #92·#91 로 들어온 값),
  `apps/` 는 frontend 쪽. `jekyll/open-items.markdown` 은 양쪽 순수 추가라 자동 병합됐다.
- **`newTag` 지뢰는 터지지 않았다.** `frontend` 가 `0.1.9` 에 멈춰 있었지만 base 이후 그 줄을
  고친 적이 없어 `infra/k8s/base/kustomization.yaml` 이 자동 병합되며 main 의 **0.1.11** 을 그대로 받았다.
  `tag-check` 도 「코드 변경: False」로 통과했다 — 이 PR 은 `apps/` 만 고쳐 이미지 경로에 안 걸린다.
- **결과: PR #93 `CLEAN` · 필수 검사 5종(server·ai·jekyll·gateway·tag-check) 전부 통과.**
- ⚠ **`STATE.md` 의 `apps/` 줄이 두 물리 줄로 쪼개져 있다** — `origin/frontend` 에 원래 있던 것이고
  머지가 만든 게 아니다. 마크다운 표는 한 줄이어야 해서 지금 이 표가 깨져 보인다.
  **남의 줄이라 손대지 않았다** — 조서희 님 확인 필요.
- 남은 것: PR #93 머지(사용자 판단). 머지되면 main 의 `apps/` 에 상담기록 재생 실연동이 들어온다.
