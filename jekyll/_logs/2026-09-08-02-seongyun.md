---
date: 2026-09-08
author: "정성윤"
person: seongyun
seq: 2
---

- **PR #50 을 열어 머지하고 브랜치를 main 에 맞췄다.** [앞 기록](/logs/)의 대시보드 빌드 복구
  커밋(`08e7d41`)과 조서희의 커밋 2건(`9f730f9`·`9c97329`)이 함께 들어갔다.
- **CI 전부 초록** — `server`·`ai`·`jekyll` 3종이 PR 과 머지 후 main 양쪽에서 통과, Pages 배포도 성공.
  **Vercel 검사 2건도 통과했다** — 이게 이번 수정의 실제 증명이다. 같은 브랜치의 직전 배포는
  `tsc --noEmit` 에서 죽고 있었다.
- **프로덕션 배포 확인**: `call-solidbob-cloud`·`call-solidbob-cloud-kxu6` Production 배포 2건 `success`,
  `call.solidbob.cloud`·`www.solidbob.cloud` 둘 다 200. 라이브 번들 해시 `index-BycDJbT8.js` 가
  로컬 검증 빌드와 **완전 일치** — 고친 코드가 실제로 떠 있다.
- **브랜치 동기화**: `PM`·`frontend` 를 main(`90a83c3`)으로 fast-forward 해 푸시했다.
  `ai`(20 커밋)·`server`(29 커밋)는 **뒤처진 채로 뒀다** — 류준·장민석 소관이라 남의 브랜치를
  말없이 옮기지 않는다. 각자 받아가면 된다.
- 남은 것: **조서희에게 통보.** `apps/dashboard/` 를 PM 이 먼저 손댔다(배포 복구). 겹치는 작업이
  있었으면 알려달라고 해야 한다. 티켓: [w3-dashboard-vercel-build-fix](/backlog/w3-dashboard-vercel-build-fix/)
