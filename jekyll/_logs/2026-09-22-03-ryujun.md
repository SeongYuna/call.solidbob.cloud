---
date: 2026-09-22
author: "류준"
person: ryujun
seq: 3
---

- **PR 을 올리기 전에 `main` 을 `ai` 에 받았다** — 정성윤·장민석·조서희 님 머지분(PR #112·#113, 결정 124~129 · 314~316). 충돌 셋(`STATE.md` · `open-items` · `w6-ai-model-http-surface`)은 **양쪽을 다 살려** 풀었다: `STATE.md` 는 `server/`·인프라 줄을 `main` 것으로, `ai/` 줄만 이쪽 것으로. 미결의 D-5 줄은 장민석 님이 `null`=「미측정」으로 바꾼 내용(`decisions/316`)을 살리고 우리 조사 결과를 그 아래 붙였다.
- **`decisions/124` 를 보고 모델 HTTP 표면을 PR 에서 뺐다.** 124(정성윤, 같은 날)가 모델을 **운영 노드에 CPU 로** 싣기로 하면서 `w6-ai-model-http-surface` 를 취소 대상으로 올렸다 — 원격 어댑터가 없으니 `024`·`105`(ai 는 라이브러리)가 그대로 산다. 에이전트 D 가 만든 `ai/apps/model_serving`·`ai/model_server.py`·FastAPI 의존성은 그 전제를 깨는 코드라 **머지 `121157e` 를 되돌렸다.** `decisions/213` 은 지우지 않고 「철회 — 124 로 대체」로 남겼고(절대 원칙 8), 구현은 커밋 `992b475` 에 있다 — GPU 인스턴스로 다시 가면 되돌림을 revert 한다. 티켓은 124 의 `cancelled` 그대로.
- **server 이미지 태그 `0.1.24` → `0.1.27`.** `0.1.25` 는 `main` 이 이미 구운 것이었고(전 기록의 「누가 구웠나」가 풀렸다), `0.1.26` 은 `PM` 브랜치가 먼저 잡았다고 `check_release_tags.py` 가 경고해 **먼저 손댄 쪽을 살리고 비켜 갔다**(§4). `0.1.27` 은 레지스트리·다른 브랜치 모두 비어 있다.
- **124 ① 「류준이 모델 두 폴더를 S3 에 올린다」는 내가 할 수 없다** — 학원이 AWS 계정 관리자라 자격증명을 못 받는다(01번 기록). 이미 정성윤 님께 드린 전달 문서 §3 에 KoE5·koelectra-ner 받는 명령이 있다 — 124 가 요구하는 「로컬 측정에 쓴 바로 그 폴더」와 같은 HF 저장소다. 해시 대조가 필요하면 내 맥의 폴더 해시를 따로 드린다.
- 검증(최종 트리): ai 454 passed(표면 테스트 42 빠짐) · server 1,283 · call-mediator 135 · scripts 15 · 계약 3+4 KEPT · `jekyll build` 통과 · 태그 검사 통과.
