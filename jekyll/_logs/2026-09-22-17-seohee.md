---
date: 2026-09-22
author: "조서희"
person: seohee
seq: 17
---

- J-5(베테랑 배정) 관련 `decisions/313`(장민석, 09-15)과 `decisions/126`(정성윤,
  09-21)의 충돌을 조사·검증한 뒤(이전 대화에서 인용문·코드를 전부 대조 확인),
  **126을 정본으로 확정하는 새 결정 `_project/decisions/407`을 썼다.**
  - 313은 "인입 전 배정 판정 API"를 선택하며 "통화 시작 때 판정해 기록만"을
    명시적으로 기각했다(근거: 교환기 없음·후보 상담사 불명). 126은 이 문제를
    "콜 미디에이터가 통화 시작 직후 부른다"로 풀었는데, 이건 313이 기각한
    대안과 같은 시점이고 313의 기각 사유(후보 불명)를 재검토하지 않았다.
  - `apps/call`(`AgentStandbyScreen`→`useCallMediatorSession.startCall`)을 다시
    확인해 "상담원이 대기화면에서 스스로 통화를 시작하고, 넘길 대상을 고르는
    단계 자체가 없다"는 것을 근거로 남겼다 — 313이 전제한 "연결 전 배정 순간"이
    지금 구조엔 아예 없다.
  - 407은 313의 "시점" 항목만 철회하고(126을 따름), 나머지(요청 모양·
    `routing_log` 기록·`call.agent_id` 미변경)는 313 그대로 유지했다. **구현
    (콜 미디에이터가 실제로 호출을 배선하는 것)은 여전히 `w7-j5-routing-caller`
    (정성윤) 몫으로 남겨뒀다** — 이 결정은 방향만 정했다.
- `jekyll/open-items.markdown:668`의 기존 J-5 항목에 407 확정 사실과 화면 문구
  수정 사실을 이어 적었다(원문은 그대로 두고 뒤에 덧붙임 — 옛 기록 안 고침).
- **화면 문구도 같이 고쳤다** — `apps/call/BlacklistRequestButton.tsx`·
  `apps/admin/EntriesTab.tsx`의 "바뀌는 것은 누구에게 배정되는가"/"근속 3년
  이상 상담사에게 배정된다" 문구를 뺐다. 126으로 확정했어도 **콜 미디에이터가
  아직 이 API를 한 번도 안 불러서(services/call-mediator grep 0건)** 지금
  당장은 여전히 사실이 아니기 때문이다 — "정상적으로 받습니다(차단 아님)"만
  남기고, 구현되면 다시 채우라는 주석을 남겼다.
- `w7-j5-routing-caller.md` 티켓은 **정성윤 담당·진행 전이라 손대지 않았다**
  (남의 티켓은 이어받을 때만 건드린다) — 407을 참고해 구현하면 된다.
- 빌드 확인: `cd apps/call && npm run build`, `cd apps/admin && npm run build`
  둘 다(`tsc --noEmit && vite build`) 통과.
