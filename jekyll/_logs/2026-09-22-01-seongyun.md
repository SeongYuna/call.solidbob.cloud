---
date: 2026-09-22
author: "정성윤"
person: seongyun
seq: 1
---

- **Windows 머신에 클라우드 도구를 갖추고 실물을 처음 기계로 읽었다** — AWS CLI · SSM 플러그인 · gcloud · Vercel CLI · gh 설치, 넷 다 로그인. 그 전엔 이 머신에서 「확인 불가」였던 것들을 대조했다.
- **랜딩(www.solidbob.cloud)이 09-15 이후 한 번도 배포되지 않았던 것을 찾아 고쳤다.** 원인은 Vercel 프로젝트 `call-solidbob-cloud` 의 Ignored Build Step 이 `-- ./` 가 아니라 `-- apps/admin` 이었던 것 — 09-15 03:51 에 세 프로젝트에 같은 설정을 걸며 관리자 프로젝트 값을 복사한 실수로 보인다(마지막 성공 배포 8초 뒤에 설정이 바뀌었다). 랜딩 기준으로 그 경로는 없어 늘 「변경 없음」이 되고, 09-17 개명·09-21 `?call_id=` 커밋이 전부 `Canceled` 였다. PR 화면엔 초록으로 떠서(런북 18-4 가 경고한 「조용한 초록」) 아무도 몰랐다. 설정을 런북 값으로 고치고 저장소 루트에서 `vercel deploy --prod` 로 강제 배포 — 번들 `index-B9PS_JUI` → `index-CEYo4v58`. ⚠ `apps/platform` 안에서 올리면 「Root Directory 가 없다」로 실패한다 — 루트에서 올려야 한다.
- **랜딩의 라이브 통화는 아직 안 된다** — 프로젝트에 `VITE_CALL_MEDIATOR_WS_URL` 이 없어 화면이 「주소가 설정되지 않았다」를 낸다. 이건 오늘 생긴 게 아니라 처음부터 없었다. 상담원 화면(kxu6)의 값과 같게 넣을지는 사용자 결정으로 남긴다.
- **프론트 3개 Vercel 프로젝트에서 `VITE_` 가 아닌 환경변수 62개를 지웠다**(16·16·30) — AWS 키·DB 비밀번호·HF 토큰·게이트웨이 토큰 등 서버 비밀값이 정적 프론트 프로젝트에 들어 있었다. 정적 빌드는 `VITE_*` 만 읽으므로 동작 변화는 없다. 남은 것: kxu6 `VITE_CALL_MEDIATOR_*`·`VITE_CORE_API_URL`, admin `VITE_API_BASE_URL`·`VITE_GOOGLE_OAUTH_CLIENT_ID`, 랜딩 0개. 지운 키 이름 목록은 로컬에만 남겼다(값은 남기지 않았다).
- **GCP 서비스 계정 `callguard-stt@` 에 `roles/speech.client` 를 붙였다** — 류준 님 Chirp 3 재측정이 403 으로 막혀 있던 것(전달 문서 §1-10). 이제 돌릴 수 있다.
- **운영 실측(SSM 읽기 전용)**: 파드 server `0.1.24` · call-mediator `0.2.2` · redis, 재시작 0(ES 는 2, 09-08 부터). 디스크 30G 중 12G 사용, 메모리 7.8G 중 여유 4.5G. 시크릿 4종(`call-mediator-tokens`·`server-env`·`gcp-stt-credentials`·TLS). `/health` 는 여전히 `ingest_guard: open`. S3 `assist-apne2` 는 `uploads/` 1개뿐이고 `datasets/`·`models/` 는 비어 있다. RDS `db.t4g.micro` 퍼블릭 꺼짐·백업 7일·모니터링 꺼짐.
- **09-21 「남은 일 목록」 셋째 판이 「기록했다」고 적은 결정 124~128·09-30 판정문 초안·티켓 절차는 다섯 브랜치 어디에도 없다.** 그 세션의 머신에 미커밋일 가능성이 크다 — 맥에서 `git status` 를 볼 것. 그 전까지 일정 공백(125)·배정 판정 호출(126)·공개 데모(127)·스키마 대조(128)는 저장소 기준 미결이다.
- **자동 모드 권한**: GCP 권한 변경·Vercel 비밀값 삭제·운영 배포·SSM 읽기·설정 파일 자기 수정이 판정에 막혔다. 사용자가 `.claude/settings.local.json`(gitignore) 에 허용 규칙을 넣어 풀었다. 그 전에 사용자가 직접 친 명령 셋은 붙여 넣기에 줄 끝 역따옴표가 딸려 와 PowerShell 이 줄 잇기로 읽는 바람에 실행되지 않았었다 — 여러 줄 명령은 스크립트 파일 하나로 주는 게 낫다.
- 남은 것: 랜딩 `VITE_CALL_MEDIATOR_WS_URL` 결정 · `ingest_guard` 잠그기(`w5-ingest-auth-fail-closed`) · 셋째 판 미커밋 파일 회수 · 류준 님 요청 답(S3 경로·SYN-010·STT 캡·118·09-30 구성).
