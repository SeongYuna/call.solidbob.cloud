---
date: 2026-09-22
author: "정성윤"
person: seongyun
seq: 26
---

- **운영에 `db/migrations/2026-09-22-eval-run-components.sql` 을 넣었다(19:57 KST, 정성윤).** 세션 스크래치의 명령으로 — 파일 sha256 대조(`HASH-OK`) 후 서버 파드 안에서 `DATABASE_URL` 로 실행. SSM 기록 「migration eval_run.components」 Success. 20:19 에 한 번 더 돌린 것은 파일 안 안전장치(「이미 적용된 DB 다 — 멈춘다」)가 막았다 — 운영은 그대로다.
- 배포 전 스키마 대조 단계를 운영에 그대로 다시 돌려 **어긋남 0 · 29 테이블 · 200 컬럼** 확인(19:5x 에는 199 — `components` 가 빠져 있었다). 오늘 로컬 변경을 머지해도 배포가 대조에서 멈추지 않는다.
- 정정: 처음엔 「누가 넣었는지 기록에 없다」고 적었다 — 넣은 것은 정성윤이고, SSM 명령 기록으로 확인했다.
