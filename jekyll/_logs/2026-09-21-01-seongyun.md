---
date: 2026-09-21
author: "정성윤"
person: seongyun
seq: 1
---

- **「남이 해야 한다 / 이 머신에선 못 한다」로 적어 둔 것들을 직접 했다.** 막힌 이유를 하나씩 파 보니 대부분 **도구가 없어서**였고, 도구는 관리자 권한 없이 깔렸다.
  - **node 24 를 zip 으로 깔았다**(CI 와 같은 버전) → 콜 미디에이터 CI job 재현: 타입 체크 0 · **127/127**. 프론트 두 앱도 빌드된다.
  - **ES 9.5.1 + nori 를 도커 없이 zip 으로 띄웠다**(운영 매니페스트와 같은 설정, 힙만 512MB) → 98조항 적재 → **하네스가 검색까지 돈다.**
- **재측정 — 운영 구성(규칙 + BM25), 3회 중 최저치** (커밋 `261bce4-dirty` · 골든셋 `v1-150` · `run_eval.py --runs 3 --no-ner --retriever bm25`)
  - **retrieval 0.833 / MRR 0.666 · n 96** — 09-17 로컬 측정과 정확히 같다(재현된다)
  - **masking 누락 0 · 오분류 0 · 과잉 0.0 · n 28** — 마지막 공식 기록(09-09)의 **❌ 누락 2 가 풀렸다**
  - compliance·call_guard 1.0/1.0(⚠ 자기충족 — 상한이지 성능이 아니다) · closure_gate·trigger 측정 불가(그대로)
  - ⚠ **기록 DB 가 없어 `--record` 는 못 했다.** §5 의 네 조건(날짜·커밋·명령·표본 수)은 `STATE.md` 에 글로 남겼다
- **STT 오류 내성 곡선 — 성공 조건 1번을 운영 구성으로 쟀고, 처음으로 그림이 나왔다**(`jekyll/assets/error-tolerance-2026-09-21.png` + 원본 `.json`)
  - **검색은 기준을 넘는다**: 0% 0.833 · 5% 0.823 · **10% 0.812**(기준 ≥0.60) · 15% 0.812 · 20% 0.792
  - **⚠ C-5 는 오류가 들어가는 순간 뚫린다**: 누락 **0 → 2 → 3 → 4 → 4**(판정 24). 5% 에서 새는 것은 `GS-033`·`GS-415`(P7 주소), 10% 부터 `GS-056`(P6 이름).
    **「누락 0」은 오류 없는 전사에서만 성립한다** — 실제 통화는 STT 를 거치므로 0% 가 아니다. 절대 규칙을 「통과」로 인용할 때 이 단서를 붙인다.
    09-15 로컬 측정의 규칙+NER 은 0/0/1/1/1 이었다 — **NER 이 필요한 근거가 이것이다**
  - 측정 스크립트가 **모델 파일이 없으면 통째로 멈추던 것**을 고쳤다 — 있는 계열만 재고 빠진 계열은 `meta.skipped` 에 남긴다.
    그림 아래에 「이 측정에서 빠진 계열」로 찍힌다(「안 쟀다」와 「잴 수 없었다」는 다른 정보다)
  - ⚠ 주입기가 실측 오류의 46.9% 를 흉내 내지 못해 **곡선은 낙관 쪽이다.** 모델 계열(dense·리랭커·규칙+NER)은 이번에 못 쟀다
- **쓰기 경로 인증 2단계 — 콜 미디에이터가 서비스 토큰을 보낸다**(`0.2.2`, `decisions/120`). `CORE_API_TOKEN` 이 있으면
  부르는 경로 **전부**에 `Authorization: Bearer` 를 싣고(테스트로 고정), 없으면 헤더를 보내지 않는다. 매니페스트에 optional 시크릿 키로 주입.
  **남은 것은 운영 작업 둘**(시크릿 주입 → fail-closed)이고 **두 이미지가 배포된 뒤에** 한다
- **`e2e_latency_ms` 를 콜 미디에이터가 채운다**(`decisions/119`) — 방송 직전 시각 − 발화 종료 시각, 문자열로. 발동 안 한 응답·발화 종료 시각이 없는 경우는 넣지 않는다.
  통화 기준 시계를 한 곳(`callClockMs`)으로 모았다. 테스트 **132/132**
- **저장 실패 정책을 맞췄다 — 둘 다 「200 + 로그」.** 콜 가드도 DB 가 흔들릴 때 탐지 결과를 돌려준다(전에는 500 이라 **경고가 화면에서도 사라졌다**).
  삼키지 않는 것 둘: 없는 전사 구간(404) · 스포크가 span 을 안 채운 결함(`CallGuardFlagSpanMissingError` — 500 으로 드러낸다)
- **프론트 로직 둘(화면·디자인은 안 건드렸다)** — ① 두 화면이 `?call_id=` 를 읽어 **같은 통화로 붙고**, 상담원 화면은 **그 통화만 구독**한다
  (`WS /ws?call_id=` — 콜 미디에이터가 원래 지원하던 서버 쪽 필터를 안 쓰고 있었다) ② 읽지 못한 메시지 하나가 「연결 끊김」을 만들던 것을 걷었다.
  두 앱 타입 체크·빌드 통과 · 순수 함수 11건 node 로 확인. **남은 것은 진입 경로**(홍보 페이지 버튼이 상담원 화면을 같은 ID 로 열어 주는 것 — 버튼 동작이 바뀌는 일이라 안 했다)
- **운영 DB 정리** — 09-11 배포 검증이 남긴 `test-deploy-*` 3건을 **ID 를 명시해** 자식부터 지웠다(call 13 → 10)
- **`.env.example` 어긋남 0** — 서버가 읽는 26개 키를 전수 대조해 빠진 7개(09-15 부터 미결이던 모델 키 5 + 토큰 2)를 **이름만** 넣었다
- **결정 기록 셋** — `121` 모델은 전용 GPU EC2 에(서버 파드에는 안 싣는다. 함께 열린 질문 다섯을 적었다) ·
  `122` 상담원 토큰에 만료를 두지 않는다(폐기로 관리) · `123` 쌍둥이 조항은 라벨을 고치지 않고 출제 규약을 적는다(판정 직전에 채점 기준을 느슨하게 하지 않는다)
- **내 실수 둘** — `123` 초안에 지침을 `POLICY` 로, `GS-216` 의 짝을 3.2 로 적었다가 골든셋 원본과 대조해 고쳤다(`TERM` · `MANUAL-3.4`).
  `secret.example.yaml` 에 키 13개가 없다고 봤는데 **정규식이 놓친 오탐**이었다 — 고치기 전에 확인해서 걸렀다
- 검증: server **1,054** · ai **421** · call-mediator **132** · 계약 4+3종 KEPT · 프론트 두 앱 타입 체크
- 남은 것: 커밋·PR · 인증 3·4단계(배포 뒤) · 모델 계열 재측정·곡선(모델 있는 머신) · 홍보 페이지 진입 경로 · Vercel 옛 변수
