---
date: 2026-09-22
author: "정성윤"
person: seongyun
seq: 14
---

- **프론트 셋 운영 재배포 완료(15:14 KST) — `w6-vercel-redeploy` 닫음.** Vercel 한도는 「24시간 뒤」가 아니라 **롤링**이었다: 13:39 에 거부된 운영 배포 생성이 15:14 에는 세 프로젝트 다 받아들여졌다(전날 같은 시각 배포가 창 밖으로 나가는 만큼 자리가 난다). `POST /v13/deployments`(git 소스)로 올렸고 셋 다 `READY · PROMOTED`. 운영 번들 `call index-4X0vR1JI` · `admin index-BHChBzPJ` · `www index-De7Koey7` 에서 새 문자열(「재문의 고객」·「변경 이력 보기」·「상담원 링크 복사」)을 확인 — PR #121·#124·#126 의 프론트 커밋 9건이 전부 나갔다.
- **헛돈 한 번: `main` 머리(`f7a2dd1`, PR #128 문서 머지)로 만든 셋은 전부 `Canceled`.** git 소스 배포에도 Ignored Build Step 이 그대로 돌아 첫 부모 diff 에 `apps/` 가 없으면 취소된다 — 그 3건도 한도만 소모했다. **앱을 건드린 마지막 main 머지 커밋**(`68f749a`, PR #126)을 sha 로 주고, `git diff 68f749a..origin/main -- apps/` 가 비는 것을 먼저 확인했다. 런북 18-4 에 적었다.
- 남은 것: `vercel.json` `git.deploymentEnabled` 로 PM·ai·server 브랜치 배포 끄기(미결, 조서희 님과) · 등록 1 해제(관리자 화면) · 수동 QA(`w5-manual-qa-full-stack`)는 이제 새 번들로 해도 된다.
