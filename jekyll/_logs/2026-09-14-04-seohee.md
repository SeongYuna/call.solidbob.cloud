---
date: 2026-09-14
author: "조서희"
person: seohee
seq: 4
---

- **재상담 고객 이력 요약 카드 추가** — 통화 시작 시 이 고객이 재상담 고객이면
  우측 패널(필요서류 카드 위)에 지난 상담 이력 + 메모를 자동으로 보여준다.
  배치는 사용자에게 3안(자막 패널 위 배너 / 우측 패널 상단 / 하단 독 탭)을
  먼저 제시하고 골라달라고 했다 — "우측 패널 상단, 필요서류 카드 위"를 선택함.
- 서버에 F-3(반복 문의 연결)이 없어 실제 고객 매칭은 불가능하다 — `mock/customerHistory.ts`가
  시나리오 ID에 고정으로 매단 mock이다(이력 없는 시나리오는 카드 자체가 안 뜬다 —
  "누구나 재상담"으로 보이지 않게, 절대 원칙 2). `getSelectedMockScenarioId()`를
  `mock/scenarios/index.ts`에 새로 export했다.
  `cd apps/call && npm run typecheck && npm run build` 통과, 사용자가 dev 서버에서
  직접 확인 후 커밋 요청.
- 백로그 티켓: `w4-customer-history-summary`.
- 남은 것: 없음 — 이 세션에서 완결. 실제 F-3이 생기면 `getCustomerHistory()`를
  `customer_id` 조회로 교체해야 한다.
