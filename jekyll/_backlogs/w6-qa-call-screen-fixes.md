---
title: "수동 QA 화면 결함 묶음 — 대기화면 확인창 · 마이크 실패 안내 · 보호 배너 · 통화받기 창 · 문구"
assignee: "조서희"
role: "app"
status: "in-progress"
sprint: 6
priority: 57
date: 2026-09-22
requirement:
  - "C-6"
  - "J-1"
  - "A-3"
paths:
  - "apps/call/src/*"
  - "apps/admin/src/*"
---

> **정성윤이 09-22 수동 QA(`w5-manual-qa-full-stack`) 결과로 만든 티켓이다.** 담당은 «제안»이다. 따로 티켓이 있는 것(경고 방향 · 카드 짝짓기 · 컴플라이언스 경고 · 읽기 인증)은 뺐다.

운영 주소 · 09-22 새 번들(`call index-4X0vR1JI` · admin `index-BVF9FZhk`)에서 본 것. 번호는 QA 티켓의 번호다.

## ❌ — 시연 전에

- [x] **Q-47** 「대기화면」이 통화 중에도 **확인창 없이 바로 나간다** — 코드를 열어 보니 **이미 고쳐져 있었다.** `StandbyBackButton.tsx`가 정확히 요구된 그대로다: `liveInProgress`면 `ConfirmDialog`(제목 "대기화면으로 이동" · 취소 "취소")를 먼저 띄운다. 코드 변경 없음.
- [x] **Q-48** 토큰 없이 「통화받기」 → 안내 없이 창만 닫힌다 — 고쳤다. `useAgentCallSession.ts`의 `start()`가 `no-token`·`unsupported`일 때도 이제 `errorMessage`를 채운다(마이크 거부·WS 거절은 이미 채우고 있었다). `App.tsx`에 별도 배너(`agentCall.errorMessage`)를 추가해 — `AgentCallBox`가 사라져도 안내는 남는다(2026-09-16 결정: 팝업이 로컬 테스트를 막으면 안 된다 — 그 결정은 안 건드렸다, 안내를 배너로 분리했을 뿐).
- [x] **(화면)** 실서버 모드의 「고객과 전화하기」 창이 통화받기를 눌러야만 닫힌다 — 고쳤다. `AgentCallBox.tsx`에 ① 「닫기」(✕) 버튼 ② 자막이 이미 들어오기 시작하면(`utterances.length > 0`) 스스로 사라지는 로직을 추가했다.

## ⚠ — 되도록

- [ ] **Q-29·Q-45** 「상담원 보호 알림」 배너가 안 뜬다 — **못 고쳤다.** `CustomerRiskBanner`(그 라벨을 가진 컴포넌트)는 로컬 정규식 탐지(`detectCustomerRisk`)에서만 나오는데, 실서버 모드에선 그 탐지 자체를 안 돈다(`isCoreApiConfigured()` 분기). 실서버가 주는 `CallGuardFlag`엔 `matchedText`가 없어(`segment_id`·`category`뿐) `CustomerRiskBanner`가 요구하는 인용문을 채울 수 없다 — 배너를 다시 설계하거나 서버가 매칭 텍스트를 보내야 한다. **줄 안 「🚫 콜가드」/「🆘 위기 신호」 핀이 지금 실서버 모드의 유일한 신호다** — `w6-qa-ui-defects-three`에서 위기 신호가 안 덮이도록·여러 건이 다 보이도록 고쳤다(그게 이 항목이 기댈 수 있는 개선의 전부다)
- [x] **Q-54** 「블랙컨슈머로 분류」mock이 "관리자에게 전송됨" — `w6-qa-ui-defects-three`에서 "관리자 알림 연동 전"으로 이미 고쳤다
- [x] **Q-11** `cga_wrong` 로그인 오류가 반말(「…받아라」) — ✅ **2026-09-23 정성윤이 서버에서 고쳤다.** 이 문구는 `apps/` 가 아니라 **서버가 보내는 것**이고 화면은 응답의 `detail` 을 그대로 띄운다(`coreClient.ts:80-85`) — 서희 님 확인이 맞았다. `server/apps/agent_auth/.../agent_guard.py` 등 9곳
- [x] **Q-04** 관리자 토큰 목록의 상담원 이름이 `Q****1`·`최*원` 으로 가려진다 — **의도다.** 2026-09-22 오전 세션(`jekyll/_logs/2026-09-22-01-seohee.md`)에서 조서희가 직접 넣었다. 서버 응답은 그대로고 화면만 가린다.
- [~] **Q-63** 「변경 이력 보기」를 펼치면 줄이 찌그러진다 · 재설정이 "등록일부터" 세어진다 — **절반만.** 코드를 보니 실제로는 "등록일부터"가 아니라 **"지금부터"**(설계 그대로, `extendEntry` 주석에 이미 명시)였는데, 입력칸이 **전역 기본값**(예: 6개월)으로 항상 채워져 있어 안 건드리고 눌러도 만료일이 조용히 바뀌는 게 진짜 문제였다 — 입력칸 옆에 "→ (결과 날짜)로" 미리보기를 추가해 누르기 전에 결과를 보게 했다. **레이아웃 찌그러짐(CSS)은 못 고쳤다** — 로컬에 브라우저가 없어 렌더된 모습을 확인할 방법이 없다.
- [~] **(후속조치)** 저장본에 제목만 있는 빈 항목 — 프론트가 보내는 쪽(`collectFollowUps()`)은 이미 빈 줄을 걸러서 보낸다(`.filter(line => line.length > 0)`). 그런데도 빈 항목이 보였다면 서버 저장 쪽 문제일 수 있는데, 거긴 확인할 방법이 없다.

- [ ] **(시연)** 화면이 통화를 받기 전에 재생이 끝나면 그 통화는 화면에서 종료할 수 없다 — **손 안 댔다.** 코드 결함이라기보다 리허설 운영 절차(누가 언제 화면을 여는지) 결정이 먼저 필요해 보여 미결로 남긴다.

## 완료 조건

- [~] 위 항목마다 고치거나 「의도」 한 줄 — 9개 중 6개 확정(고침 3 · 이미 됨/의도 3), 1개 절반, 2개(보호 알림 배너 재설계·시연 절차)는 결정이 더 필요해 미룸
- [ ] ❌ 셋은 운영에서 그 번호만 다시 눌러 ✅ (QA 티켓 완료 조건 4) — 로컬에 붙는 서버·브라우저가 없어 못 했다

근거: 진행 기록 `2026-09-22-18-seongyun` 결과표.

## 2026-09-23 — 구현 (조서희)

검증: `apps/call`(`tsc --noEmit`·`vite build`·`npm test` 11/11), `apps/admin`(`tsc --noEmit`·`vite build`) 통과.
`status`는 `done`이 아니라 `in-progress`로 둔다 — 운영 재확인이 하나도 없고, 두 항목(보호 알림 배너·시연 절차)은 코드 변경이 아니라 설계 결정이 먼저 필요하다.
