---
title: "수동 QA 화면 결함 묶음 — 대기화면 확인창 · 마이크 실패 안내 · 보호 배너 · 통화받기 창 · 문구"
assignee: "조서희"
role: "app"
status: "todo"
sprint: 6
priority: 57
date: 2026-09-22
requirement:
  - "C-6"
  - "J-1"
  - "A-3"
paths:
  - "apps/call/src/*"
  - "apps/admin/src/*"
---

> **정성윤이 09-22 수동 QA(`w5-manual-qa-full-stack`) 결과로 만든 티켓이다.** 담당은 «제안»이다. 따로 티켓이 있는 것(경고 방향 · 카드 짝짓기 · 컴플라이언스 경고 · 읽기 인증)은 뺐다.

운영 주소 · 09-22 새 번들(`call index-4X0vR1JI` · admin `index-BVF9FZhk`)에서 본 것. 번호는 QA 티켓의 번호다.

## ❌ — 시연 전에

- [ ] **Q-47** 「대기화면」이 통화 중에도 **확인창 없이 바로 나간다** — 누르면 통화 화면 상태를 잃는다. 확인창 「대기화면으로 이동」 → 「취소」면 남아야 한다
- [ ] **Q-48** 토큰 없이 「통화받기」 → **안내 없이 창만 닫힌다.** 콜 미디에이터 로그에 연결 시도 자체가 없다(`useAgentCallSession.ts`). 토큰 없음 · 마이크 거부 · 인식 미지원 · 1006/1008 거절을 문구로
- [ ] **(화면)** 실서버 모드의 「고객과 전화하기」 창은 **「통화받기」를 눌러야만 닫힌다.** 마이크를 쓰지 않는 통화(합성·스크립트)에서도 자막을 가린다 — `421f0d6` 은 mock 만 숨긴다. 첫 자막이 들어오면 닫히거나 「닫기」가 있어야 한다

## ⚠ — 되도록

- [ ] **Q-29·Q-45** 욕설·위협 뒤 **「상담원 보호 알림」 배너가 안 뜬다** — 줄 안 「콜가드」 배지·안내 문구만 있다(`CustomerRiskBanner`)
- [ ] **Q-54** 「블랙컨슈머로 분류」(mock)가 **「관리자에게 전송됨」이라고 표시**한다 — 연동 전이면 「연동 전」. 진짜 「블랙리스트 전환 요청」이 **맨 아래 「콜 라우팅 보호」** 에 있어 처음엔 못 찾았다 — 위치를 올리거나 둘을 합칠지
- [x] **Q-11** `cga_wrong` 로그인 오류가 반말(「…받아라」) — ✅ **2026-09-23 정성윤이 서버에서 고쳤다.** 이 문구는 `apps/` 가 아니라 **서버가 보내는 것**이고 화면은 응답의 `detail` 을 그대로 띄운다(`coreClient.ts:80-85`) — 서희 님 확인이 맞았다. `server/apps/agent_auth/.../agent_guard.py` 등 9곳
- [ ] **Q-04** 관리자 토큰 목록의 상담원 이름이 `Q****1`·`최*원` 으로 가려진다 — 의도인지
- [ ] **Q-63** 블랙리스트 「변경 이력 보기」를 펼치면 줄이 찌그러진다(버튼이 한 글자씩 세로로). 개월 수가 **등록일부터** 세어져 같은 값 재설정도 이력에 남는다 — 의도인지
- [ ] **(후속조치)** 확정 전 「후속조치 0건」인데 저장본에 제목만 있는 `후속 조치` 1건 — 빈 칸이 항목으로 저장되는지 확인

- [ ] **(시연)** 화면이 통화를 받기 **전에** 재생이 끝나면 그 통화는 화면에서 종료할 수 없다(「통화 종료」 꺼짐) — 상담기록 재조회에는 「블랙리스트 전환 요청」도 없다(`decisions/204`). 09-22 SYN-006 에서 겪어 요청을 API 로 보냈다. 리허설에서 같은 일이 나면 막힌다 — 늦게 붙은 화면도 진행 중 통화를 이어받거나, 재조회에서 종료·요청을 허용할지

## 완료 조건

- [ ] 위 항목마다 고치거나 「의도」 한 줄
- [ ] ❌ 셋은 운영에서 그 번호만 다시 눌러 ✅ (QA 티켓 완료 조건 4)

근거: 진행 기록 `2026-09-22-18-seongyun` 결과표.
