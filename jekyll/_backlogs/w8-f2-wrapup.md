---
title: "F-2 마무리 — 구현 완결 또는 설계 문서 산출"
assignee: "장민석"
role: "ai"
status: "done"
sprint: 8
priority: 81
date: 2026-09-15
requirement:
  - "F-2"
depends_on:
  - "w7-f2-checkpoint"
note: "체크포인트 결과에 따라 내용이 갈린다"
paths:
  - "server/apps/closure_gate/*"
---

## 무엇을

[7주차 체크포인트](/backlog/w7-f2-checkpoint/)의 판정에 따라 F-2 를 닫는다.

## 「구현 진행」으로 갔을 때 — 남은 셋

- [x] **`verdict` `blocked` → `incomplete`** — rev.5 에서 「차단」이 「경고」로 바뀌었는데 **코드가 아직 옛 값**이다
      (`plan.md` 7.3절). 응답 계약이 바뀌므로 프론트([필요서류 체크리스트](/backlog/w4-required-docs-checklist/))와 함께 움직인다
- [x] **골든셋 F-2 케이스** — 지금 **0건**이라 전면 측정 불가다. 류준 몫이지만 **케이스 없이는 이 티켓을 닫을 수 없다**
- [x] **`DASAN-POLICY-1` 과 `decisions/305` 의 어긋남** — 지식베이스 문서라 류준과 함께 고친다

## 「설계 문서 전환」으로 갔을 때

- [ ] 시퀀스 다이어그램 + 요건 스키마를 산출한다
- [ ] **이미 만들어 둔 스포크 코드를 지우지 않는다** — 설계 문서의 근거가 된다(절대 원칙 8)

## 주장 범위를 넘지 않는다

어느 쪽이든 이 게이트의 목적은 *"근거 없는 종결의 비용을 올린다"* 이지
*"허위 기재를 막는다"* 가 아니다([2.7절](/docs/02/)·[부록 A-3](/docs/12/)).
**상담원이 필드를 고의로 거짓 입력하면 막을 수 없고, 막을 수 있는 척하지도 않는다.**

## 완료 조건

F-2 의 상태가 **「구현됨 + 측정됨」** 이거나 **「설계 문서로 산출됨」** 중 하나로 확정되고,
`STATE.md` 와 [기능 블록 표](https://github.com/SeongYuna/call.solidbob.cloud/blob/main/CLAUDE.md)에 그대로 적힌다.

---

> **갈래가 정해졌다 — 「설계 문서 산출」 (2026-09-19, `_project/decisions/118`).**
> [7주차 체크포인트](/backlog/w7-f2-checkpoint/)를 앞당겨 판정했다. 이유는 구현이 모자라서가 아니라
> **골든셋에 F-2 채점 케이스가 0건이라 잴 수 없기 때문**이다(테스트 3건 전부 skip, 09-19 실측).
> 그래서 이 티켓에서 할 일은 **시퀀스 다이어그램 + 요건 스키마** 둘이다.
> **코드는 지우지 않는다** — 돌고 있고 콜 미디에이터가 호출한다. 다만 「검수 통과」로 쓰지 않는다.
> ⚠ 「구현 진행」으로 갔을 때의 셋 중 **rev.5 `incomplete` 반영은 이미 되어 있다**(09-19 확인).
> 상태·담당(장민석)은 건드리지 않았다 — 착수 전이고, 갈래만 적어 둔다.

---

> **갈래가 바뀌었다 — 「구현 완결」 (2026-09-22, `_project/decisions/125`, `118` 되돌림).**
> 「구현 진행」의 셋은 전부 끝났다 — 골든셋 케이스 99건(09-21 류준) · `incomplete` 반영(이미 되어 있었음) · `DASAN-POLICY-1` 정정(09-21 류준).
> 그래서 이 티켓에서 남은 일은 코드가 아니라 **발표 문구**다: F-2 수치를 적을 때 「규칙표 상한 · n 99 · 발화→증거 자동 판정은 안 잼」을
> 같은 줄에 붙인다(`125` 인용 규칙). 시퀀스 다이어그램·요건 스키마는 필수에서 내렸다 — 있으면 좋다.
> `CLAUDE.md`·`rfp-harness.md` 의 「측정 불가」·「코드 미반영」 서술은 낡았고 `w8-final-docs` 몫이다. 상태·담당(장민석)은 그대로 둔다.

---

## 2026-09-22 — 발표 문구 (장민석) · 닫음

`decisions/125` 인용 규칙(절대 원칙 10)대로 — **수치와 단서를 한 줄에 붙인다.** 수치는 `125` 가 적은 하네스 값(커밋 `97f7b25`)이고 새로 잰 것이 아니다.

> **F-2 필요서류 체크리스트 — 규칙표 상한 accuracy 1.0 · n 99.** 「팀이 만든 규칙표를 코드가 그대로 따르는가」의 상한이다.
> 통화 발화에서 「서류를 안내했다」는 증거를 자동으로 뽑는 부분(`detection.py`)은 이 케이스로 재지 않았다.
> 판정은 규칙이 하고(절대 원칙 9), 빠진 서류는 **경고(`incomplete`)**로 알린다 — 통화를 막지 않는다.

- 「F-2 100%」 단독 인용 금지 — `w8-presentation` 검토 항목에 넣을 것
- 평가 지표 테스트 픽스처에 남아 있던 옛 값(`blocked`/`approved`)을 `incomplete`/`complete` 로 맞췄다 — 비교 로직만 보는 테스트라 결과는 같다
- 완료 조건의 「`CLAUDE.md` 기능 블록 표」 갱신은 `125` 가 정한 대로 `w8-final-docs` 몫이다(여기서 건드리지 않았다)
