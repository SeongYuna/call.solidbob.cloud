---
title: "J-5 — 블랙리스트 고객을 베테랑 상담사에게 배정"
assignee: "류준"
role: "ai"
status: "in-progress"
sprint: 4
priority: 7
date: 2026-09-09
requirement:
  - "J-5"
depends_on:
  - "w4-blacklist-request-flow"
paths:
  - "server/apps/blacklist/domain/services/routing.py"
note: "09-15 고객 식별(HMAC)은 main 에 들어왔다 · 남은 막힘: 후보 상담원 목록의 출처(설계 결정)"
---

## 무엇을

블랙리스트로 등록된 고객의 전화가 들어오면 **근속 3년 이상** 상담사에게 배정한다.

## 왜

C-6 이 상담원을 보호하는 방식은 「경고」까지다. **가장 확실한 보호는 애초에 배정하지
않는 것**이다 — 신입 상담사가 악성 민원 전화를 받지 않게 한다.

## 완료 조건

- [x] 블랙리스트가 **아니면 아무것도 하지 않는다** — 인입 경로에 얹는 얇은 필터다
- [x] 베테랑이 없으면 **일반 배정으로 떨어뜨리되 `fell_back` 으로 센다**
      (전화를 못 받게 만드는 것이 더 나쁘다. 「베테랑이 부족하다」가 운영 지표가 된다)
- [x] 「3년」을 코드에 굳히지 않고 인자로 뺐다 — **우리에게 3년이 옳다는 근거가 없다**(절대 원칙 2)
- [x] 같은 입력이면 같은 결과 (테스트가 고정)
- [x] `agent.hired_on` 스키마 반영 — 「연수」가 아니라 **입사일**을 저장한다
- [ ] 인입 경로에 실제 배선 — 게이트웨이·라우터가 아직 없다

## 남은 것

- **배정 효과를 실측할 수 없다.** 승인률·베테랑 배정 효과는 운영 데이터가 필요하다.
  재는 것은 `routing_log.fell_back` 집계까지다(`decisions/204` 「남는 위험」)
- **2026-09-14 — 배선은 장민석 님 `server` 브랜치 머지 뒤로 미뤘다.** 블랙리스트 여부를 보려면 통화 시작 때 고객을 식별해야
  하는데(발신 번호 HMAC, `decisions/304`) 그게 그 브랜치에만 있다. main 기준으로 지금 만들면 고객 식별·등록 조회를 한 번 더 만든다.
  **그 뒤에도 먼저 정할 것**: 「지금 받을 수 있는 상담원」 목록이 시스템 어디에도 없다 — `route()` 에 넘길 후보를 어디서 얻는가


## 2026-09-15 — 선행 조건 하나는 풀렸고, 남은 것은 설계 질문이다

- **풀린 것**: 고객 식별(`CallStartCommand.caller_phone` → HMAC, `decisions/304`)이 main 에 머지됐다. 이 티켓을 막던 「server 브랜치 머지 뒤」 조건은 끝났다
- **여전히 막는 것**: `route()` 에 넘길 **후보 상담원 목록이 시스템 어디에도 없다.** `POST /hub/calls` 는 이미 연결된 통화를 기록할 뿐이고(배정 이전 단계가 없다),
  상담원 대기·접속 상태를 아는 곳도 없다(grep 0건). **우리 시스템에는 ACD(자동 호 분배)가 없다**
- 선택지는 둘인데 **둘 다 게이트웨이·전화 연동 계약을 정하는 일**이라 혼자 정하지 않았다 → [미결](/open-items/)
  ① 외부 ACD 가 후보를 넘겨 묻는 API(`POST /hub/routing-decisions` — 후보는 호출자가 준다) ② 상담원 접속 상태를 서버가 갖는다(대시보드 로그인 = 대기)
- 만들지 않은 이유: 부르는 쪽이 없는 API 를 먼저 만들면 **호출부 없는 구현**이 된다(`decisions/302` 가 경계한 구멍의 반대 방향)
