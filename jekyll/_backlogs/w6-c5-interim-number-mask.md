---
title: "C-5 중간 자막(interim)에 번호 앞자리가 가려지지 않고 나간다 — 카드 `9410 0000` · 전화 `010 0000`"
assignee: "장민석"
role: "ai"
status: "in-progress"
sprint: 6
priority: 61
date: 2026-09-22
requirement:
  - "C-5"
paths:
  - "server/apps/masking/*"
  - "services/call-mediator/src/*"
---
> **정성윤이 09-22 저녁 최종 QA(류준 페르소나 4 + 검증 웨이브)·운영 확인 결과를 보고 만든 티켓이다.** 담당은 «제안»이다 — 본인이 확인하고 맞으면 착수, 아니면 옮긴다.

## 무엇을

09-22 최종 QA 표본 6건 중 5건: 번호를 불러 주는 도중의 중간 자막에 앞자리가 그대로 화면에 나갔다. 확정 자막·DB 는 가려진다 — **화면에만** 샌다.
번호가 다 들어오기 전이라 패턴 길이가 안 맞는 것으로 보인다(가설).

## 왜

절대 원칙 3 「애매하면 가린다」. 상담원 화면도 표시 앞단이다(C-5 정의 — 화면·저장 양쪽).

## 어떻게 (후보)

- 중간 자막에는 「숫자 4자리 이상 연속」을 무조건 가리는 보수 규칙을 따로 건다
- 또는 미디에이터가 중간 자막의 숫자열을 서버에 보내기 전에 접는다

## 완료 조건

- [ ] 중간 자막 6건 표본을 테스트로 — 앞자리가 안 보인다
- [x] 확정 자막 채점(누락 0·과잉률)은 변동 없음

근거: 미결 「C-5 중간 자막」. NER 쪽 도움이 필요하면 류준 님과.

## 2026-09-22 — 장민석 착수·구현 (담당 확인)

- `decisions/324` — 후보 1안(보수 규칙)으로 갔다. 중간 자막에만 `InterimMaskingAdapter`: 구분자를 걷고 **3자리 이상** 이어진 숫자(낭독형 포함)를 전부 가린다(4자리보다 한 칸 넓게 — 「010」 만 먼저 들어와도 가린다). NER 이 꽂혀도 그 위에 얹힌다
- 확정 규칙·골든셋 채점은 건드리지 않았다 — `test_golden_set_masking` 그대로 통과
- 표본: 운영 6건 원문은 저장소에 없다(원문을 남기지 않는다, SEC-1) — 미결에 적힌 두 모양(`9410 0000` · `010 0000`)과 낭독형·구분자 섞인 모양을 테스트로 고정했다. **배포 뒤 운영 합성 통화로 첫 조건을 닫는다**
