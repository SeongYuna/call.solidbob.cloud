---
title: "F-2 체크포인트 — 구현으로 갈지 설계 문서로 갈지"
assignee: "정성윤"
role: "infra"
status: "todo"
sprint: 7
priority: 76
date: 2026-09-15
requirement:
  - "F-2"
depends_on:
  - "w6-core-baseline-check"
note: "7주차 중반 판정 — 둘 중 하나라도 미달이면 설계 문서로 전환"
---

## 무엇을

[8주 마일스톤](/docs/08/)의 **7주차 중반 체크포인트**를 실행한다.

```
코어 기준선 통과 + 태스크 궤도 정상  →  F-2 구현 진행
둘 중 하나라도 미달                  →  F-2 를 설계 문서로 전환
                                        (시퀀스 다이어그램 + 요건 스키마 산출)
```

## 판정에 쓸 사실 — 이미 많이 서 있다

F-2 는 **조건부인데도 코드가 꽤 나가 있다.** 2026-08-27 에 `server/` 안에서 모델 없이
완결되는 일이라 먼저 했고([w7-closure-gate-spoke](/backlog/w7-closure-gate-spoke/)),
09-14 에 다산 규칙표로 다시 만들었다(`decisions/305`).

**그래서 판정의 무게가 「만들까」가 아니라 「잴 수 있는가」로 옮겨 갔다** —

- ⚠ **골든셋 F-2 채점 케이스가 0건이다.** 2026-08-28 부터 **전면 측정 불가**이고,
  [미결 항목](/open-items/)에 「류준 님께」로 열려 있다. **케이스가 없으면 절대 규칙을 통과했다고 말할 수 없다**(절대 원칙 5)
- ⚠ **rev.5 에서 「차단」이 「경고」로 바뀌었다** — `verdict` `blocked` → `incomplete`
  (`plan.md` 7.3절). **코드 미반영**이다
- ⚠ **`DASAN-POLICY-1`(「F-2 미적용」)이 `decisions/305` 와 어긋난다** — 지식베이스 문서라 류준과 함께 고친다

## 완료 조건

- [ ] 판정 결과와 근거를 `jekyll/_logs/` 에 남긴다
- [ ] 「구현 진행」이면 위 세 가지(골든셋 케이스·`incomplete` 반영·`DASAN-POLICY-1`)를 [8주차 티켓](/backlog/w8-f2-wrapup/)에 옮긴다
- [ ] 「설계 문서 전환」이면 **시퀀스 다이어그램 + 요건 스키마**를 산출물로 정한다
- [ ] 어느 쪽이든 **F-2 는 성공 조건이 아니다** — 달성 시 추가 성과로 본다([8절](/docs/08/))
