---
title: "필요서류 절차 고르기 — 규칙 없는 조항이 1순위면 판정하지 않는다"
assignee: "류준"
role: "ai"
status: "in-progress"
sprint: 6
priority: 60
date: 2026-09-22
requirement:
  - "F-2"
  - "B-3"
paths:
  - "services/call-mediator/src/*"
---

> **정성윤이 09-22 오늘 진행 기록·미결 항목을 보고 만든 티켓이다.** 담당은 «제안»이다 — 본인이 확인하고 맞으면 착수, 아니면 옮긴다.

## 무엇을

콜 미디에이터가 필요서류 판정에 넘길 절차를 고를 때, **추천 1순위 카드의 조항에 규칙이 없으면 판정하지 않는다.**
지금은 「카드 순서대로 규칙 있는 첫 조항」이라 1순위를 건너뛰고 엉뚱한 절차를 잡는다.

## 왜 — 운영에서 두 번 재현됐다

- 09-22 운영 SYN-010: 1순위 카드는 정답 `TERM-2.12`(광역 교통)인데 판정은 `TERM-2.9`(분실물 수령) `incomplete` · 「신분증 누락」. **상담원 화면에 틀린 「빠진 서류」가 뜬다.**
- 09-21 로컬 SYN-023 도 같은 원인. 검색이 좋아질수록(dense) 더 자주 드러난다.
- 정성윤이 09-22 답장에서 류준 님 제안(「1순위가 규칙 없는 `TERM` 이면 판정하지 않는다」)에 동의했다. [미결 항목](/open-items/) 「필요서류 절차 고르기 규칙」.

## 완료 조건

- [ ] 미디에이터 `src/` 수정 + 테스트(1순위 규칙 없음 → `closure` 방송 없음 · 1순위 규칙 있음 → 그대로)
- [ ] 태그가 오른다 — `kustomization.yaml` 미디에이터 `newTag` 올림
- [ ] 운영에 SYN-010 한 건 흘려 `closure` 가 안 뜨는 것 확인 · 진행 기록

## 2026-09-22 — 미디에이터 수정 (류준)

- `services/call-mediator/src/app/call_registry.ts` — `adoptProcedure` 가 추천 **1순위 카드의 조항 하나만** 받는다(`topSourceDocId`).
  서버가 판정을 돌려주면 절차로 잡고, 422(규칙 없음)면 그 추천에서는 아무것도 잡지 않는다. 2순위 아래는 묻지도 않는다 —
  서버 호출이 추천당 최대 5번에서 1번으로 준다. 「규칙이 있는가」는 전과 같이 `/hub/required-docs-checks` 의 422 로만 안다(미디에이터에 조항 목록을 두지 않는다).
- 테스트 3건 — 1순위 422 → 아무것도 안 잡음·`closure` 없음 / 1순위 규칙 있음 → 그대로 / SYN-010 카드 `[2.12, 2.8, 2.10, 2.1, 2.9]` → 2.9 안 잡음. 옛 코드에서는 앞의 1·3번이 실패하는 것을 확인했다. `npm test` 137/137.
- **남은 것** — 완료 조건 2·3: `kustomization.yaml` 미디에이터 태그 올림(중앙에서 올린다) · 배포 후 운영에 SYN-010 흘려 `closure` 가 안 뜨는 것 확인.
- **이 수정으로 안 고쳐지는 것 — 누적.** 판정 중인 절차는 통화가 끝날 때까지 빠지지 않는다. 앞 발화에서 1순위로 잡힌 절차가
  주제가 바뀐 뒤에도 상담원 발화마다 다시 판정돼 옛 절차의 「빠진 서류」가 계속 뜰 수 있다. 빼는 것은 화면과 맞춰야 해서 이 티켓 밖이다.
