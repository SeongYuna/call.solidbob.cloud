---
title: "최종 문서 정리 — 파생 문서가 기획서와 어긋나 있지 않은가"
assignee: "정성윤"
role: "infra"
status: "in-progress"
sprint: 8
priority: 83
date: 2026-09-15
requirement:
  - "QUA-2"
---

## 무엇을

제출·발표 전에 **문서 사이의 어긋남을 한 번에 훑는다.**

## 왜 이 티켓이 필요한가 — 실제로 여러 번 났다

[미결 항목](/open-items/)과 `STATE.md` 「내부 메모」가 적어 둔 그대로다 —

- **파생 문서가 기획서보다 빡빡해진 사례 3건**(2026-08-27 대조). F·G·H·I 동결의 「6주차 종료 시점에」 누락 ·
  「B·E·C-5 절대 사수」 누락(둘 다 `ee5c137`) · 도메인 4종의 「예시로도」 추가(출처 없음).
  **팀이 「규칙이 빡빡하다」고 겪은 것의 상당수가 옮겨 적기였다**
- **런북의 AWS 자원 이름이 실물과 갈린 것을 세 번 고쳤다**(09-11 · 09-14 두 번)
- **`branch-protection.json` 이 라이브 룰셋과 어긋나 있었다** — 2026-09-15 일치시켜 닫았다(값 셋).
  룰셋 백업(`.github/ruleset-main.json`)**도 같은 병으로 어긋나 있어** 같은 날 일치시켰다 —
  **대조 검사가 없어 우연히 찾은 사례가 하나 더 늘었다.** 남은 것은 형식 차이 하나다 —
  `branch-protection.json` 은 클래식 API 형식이라 라이브 설정 둘을 못 담는다

**대조 검사가 없어서 전부 우연히 찾았다.**

## 훑을 것

- [ ] `CLAUDE.md` · `.claude/rules/rfp-harness.md` · `jekyll/docs/` 를 **`_project/plan.md` + 결정 기록**과 대조.
      **파생 문서가 기획서와 다르면 기획서가 맞다**(`rfp-harness.md §1`) — 단, **같은 사안이면 결정 기록이 더 위다**
- [ ] 수치가 적힌 곳 전부 — **측정일·커밋·명령·표본 수 넷이 붙어 있는가**(§5). 안 붙은 숫자는 지운다
- [ ] 「측정 불가」로 적어야 할 것이 숫자로 적혀 있지 않은가(절대 원칙 2·10)
- [x] `docs/infra-runbook.md` 의 자원 이름이 콘솔 실물과 같은가 — **2026-09-23 AWS 로 대조했다**: EC2 `i-034cda2423f65c8bd` · `t3.large` · 키 `callguard-key` · `ap-northeast-2b` / RDS `callguard-pg` · `db.t4g.micro` · PostgreSQL `17.11` · 20GiB / S3 `assist-apne2`. **런북의 「실물 대조」 표와 전부 일치**한다(키 이름 어긋남은 그 표가 이미 정정해 둔 것이었다)
- [x] `_project/` 의 비공개 내용이 지킬 루트 밖에 있는가(§8). **자격증명 0건** — 2026-09-23 공개 트리 전체를 훑었다(AWS 키 · PEM · `cga_` 토큰 · 슬랙 토큰 패턴) **0건**. 사이트에 남은 `_project/` 문자열 60여 곳은 **결정 기록의 경로 이름을 인용한 것**이고 내용은 지킬 루트 밖에 있다

## 완료 조건

어긋남을 **고치거나, 못 고쳤으면 [미결 항목](/open-items/)에 올린다.**
조용히 넘어간 것이 없다.

## 2026-09-23 — 1차 대조 (정성윤)

낡은 서술을 고쳤다. **원문은 지우지 않고 정정을 덧붙였다**(절대 원칙 8).

- `CLAUDE.md` — 서류 문의 수치 174건·69종 → **재측정 146건(2.18%)·64종**(09-11, `0d9116b`) 병기
- `jekyll/docs/03` — 그림이 원안 그대로였다. 앞에 정정 문단: 도메인 라우팅 폐기(`201`) · **RRF 비채택**(`206`) · 생성은 kanana 이고 운영 미탑재(`207`·`124`) · 게이트웨이 → 콜 미디에이터(`115`)
- `jekyll/docs/06` — 빈 행을 사실로 채웠다: 트리거는 하네스 채점 불가 · E2E 지연 **미측정**(`w7-latency-budget`) · 토큰 비용은 생성 미탑재로 해당 없음 · F-3 **미착수 확정**(`132`) · B-6 문턱 **보류**(`215`) · D-5 발표 전 미배선(`317`)
- `docs/infra-runbook.md` — ⚠ **개명 사고 정정**: AWS `Gateway` VPC 엔드포인트 유형이 09-17 일괄 개명에 휩쓸려 「CallMediator 유형」으로 바뀌어 있었다(5-5 절 3곳 + 자원표). 콘솔에 없는 이름이라 따라 할 수 없었다. 또 **실물 대조 표**를 자원표 아래에 넣었다 — 키 페어 `callguard-key` · EC2 `t3.large`/Amazon Linux 2023/Name 태그 없음 · 탄력적 IP 없음 · AMI 없음 · 네임스페이스 `callguard`
- `jekyll/sprints/01` — 스프린트 페이지 관행을 1주차 한 장으로 닫는다고 명시(진행 기록·마일스톤·칸반이 나눠 갖는다)
- 미결 항목 6건 닫음(여권 조항 · Vercel 옛 변수 · 태그 선점 · J-5 호출 · `eval_run.components` 마이그레이션 · J-5 시연 준비) — 131 → 125

**남은 것**: 런북 GPU 전제 절(11·14장) 재서술 여부 · `docs/06` 실측 열을 09-22 run_id 8·9 로 갱신할지 · `contract.ts` 문자열 계약 · ERD 재생성 · 미결 항목 나머지 정리 · `_project/` 비공개·자격증명 0건 최종 확인.

## 2026-09-23 — 2차 대조 (정성윤, QA 게이트와 무관한 범위만)

- **자원 이름 대조**(위 4번) · **공개/비공개 경계**(위 5번) 둘을 닫았다. 근거는 각 줄에.
- **스키마·ERD 재생성**: `db/generate_schema_docs.py` 를 돌렸다 — `schema.sql`·`erd.dot` 모두 **차이 0**(29 테이블). 저장소가 DB 정의와 어긋나 있지 않다.
  ⚠ `ERD.png` 는 이 머신에 graphviz 가 없어 만들지 못했다 — 그림이 필요하면 graphviz 가 있는 곳에서 같은 스크립트를 한 번 돌린다.
- 남은 것은 1·2·3번(문서 대조 · 숫자 넷 · 「측정 불가」)이다. 2·3번은 하네스 값이 걸려 있어 **6주차 기준선 판정과 같이** 보는 편이 낫다.
