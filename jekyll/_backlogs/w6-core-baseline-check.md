---
title: "코어 기준선 통과 확인 — F·G·H·I 동결 판정"
assignee: "정성윤"
role: "infra"
status: "todo"
sprint: 6
priority: 66
date: 2026-09-15
requirement:
  - "E-1"
  - "E-4"
depends_on:
  - "w2-baseline-gate"
note: "6주차 종료 시점 판정 — 그 전에는 적용되지 않는다"
paths:
  - ".github/workflows/test.yml"
---

## 무엇을

**6주차 «종료 시점»에** 코어(A~E, C-5) 기준선 통과 여부를 판정하고,
`F·G·H·I` 착수 여부를 결정한다(`CLAUDE.md` §1 범위 관리 규칙).

## ⚠ 시점이 있는 조건이다 — 상시 금지가 아니다

2026-08-27 에 이 문장에서 **「6주차 종료 시점에」가 빠져** 있었고, 그 탓에
**시한 있는 조건이 상시 금지로 읽혀 확장 논의가 6건 멈췄다**(`decisions/022`).
원문은 **「6주차까지는 자유, 그때 판정」**이다. 이 티켓은 그 «그때» 를 실행하는 자리다.

## 무엇을 보고 판정하는가

- **B(검색)** — Recall@5 ≥0.70(오류 없음) / ≥0.60(오류 10%)
- **C-5** — 누락 **0건**. ⚠ **잰 범위 안에서만 성립한다** — 표본 0건인 패턴은 「통과」가 아니라 **「측정 불가」**다
- **E** — 하네스가 실제로 돌고 값에 측정일·커밋·명령·표본 수가 붙어 있다(§5)
- **여러 번 실행한 값 중 최저치**로 고정한다(절대 원칙 4)

## 게이트를 켜는 것과 수치가 나온 것은 다른 결정이다

`STATE.md` 가 적어 둔 그대로다 — 절대 원칙 5(미달 시 CI 실패)를 켜면
미구현 항목 때문에 계속 빨간불이 된다. **무엇을 게이트에 넣고 무엇을 「측정 불가」로 둘지**를
여기서 정한다. 선행 티켓이 [w2-baseline-gate](/backlog/w2-baseline-gate/)다.

## 완료 조건

- [ ] 판정 결과(통과 / 미달)를 **숫자와 함께** `jekyll/_logs/` 에 남긴다
- [ ] 미달이면 **F·G·H·I 를 착수하지 않는다** — 대신 [8주차 확장 택1](/backlog/w8-extension-pick/)을 닫는다
- [ ] 통과면 무엇이 통과했고 **무엇이 「측정 불가」인지**를 갈라 적는다
- [ ] 성공 조건은 F-2 가 아니라 **STT 오류 내성 실험 + 검색 품질 개선 수치**임을 판정문에 다시 적는다
