---
title: "블랙리스트 잔여 — display_hint 채우기 · 반려 사유 · 연장 이력 화면"
assignee: "장민석"
role: "ai"
status: "done"
sprint: 7
priority: 84
date: 2026-09-21
requirement:
  - "J-2"
  - "C-5"
paths:
  - "server/apps/blacklist/*"
---

## 무엇을

J 블록에 남은 작은 것들을 닫는다([미결 항목](/open-items/) J 절 ②③).

- **`display_hint`** — 관리자가 고객을 알아볼 단서. 식별자는 HMAC 이라(`decisions/304`) 이것이 비면 목록이 해시뿐이다
- **반려 사유** — 비어 있어도 반려가 된다. 요청한 상담원이 왜 반려됐는지 모른다
- **연장 이력 조회**(`GET …/expiry-changes`) — API 는 있고 화면이 없다(조서희 님이 09-15 에 스코프 밖으로 남겼다). 화면은 조서희 님과 나눈다 — 이 티켓은 서버 쪽만

## ⚠ `display_hint` 는 개인정보 경계다

알아볼 단서를 넣는 순간 HMAC 으로 가린 것을 되돌리는 셈이 될 수 있다. **무엇을 넣어도 되는지부터 정한다** —
전화번호 뒷자리도 P4 의 일부다. 애매하면 넣지 않는다(재현율 우선과 같은 방향).

## 완료 조건

- [ ] 셋 각각 «했다» 또는 «안 한다 + 이유»
- [ ] `display_hint` 에 들어가는 값이 C-5 패턴에 걸리지 않음을 테스트로 고정한다

---

> **2026-09-22 장민석 — 셋 다 정했다(`_project/decisions/316`).** ① `display_hint` — **안 한다**(전화번호 뒷자리도 P4, 늘 null — 테스트로 고정)
> ② 반려 사유 — **했다**: 반려면 필수(422), 마스킹해 `blacklist_request.decision_note` 에, 180일 정리에 포함(스키마 마이그레이션)
> ③ 연장 이력 — 서버 API 는 이미 있다, 화면만 남음. 화면 쪽(반려 사유 입력)은 미결로 조서희 님께. 상담원이 반려 사유를 볼 경로는 없다(미결).
