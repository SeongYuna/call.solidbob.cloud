---
title: "캐싱 — 같은 질문을 두 번 계산하지 않는다"
assignee: "류준"
role: "ai"
status: "todo"
sprint: 7
priority: 72
date: 2026-09-15
requirement:
  - "B-2"
paths:
  - "ai/apps/retrieval/*"
---

## 무엇을

임베딩·검색 결과에 **LRU 캐시**를 붙인다([8주 마일스톤](/docs/08/) 7주차 「운영 관점」).

## 왜 여기가 이득인가

같은 절차 문의가 반복된다 — 다산 서류 문의는 **69종 서비스**에 흩어져 있지만
상위 질문은 몰린다(`decisions/201`). 임베딩 한 번이 검색 한 번보다 비싸다.

## ⚠ 캐시에 무엇을 담는지 먼저 본다

**마스킹 전 원문을 캐시 키로 쓰지 않는다.** SEC-1 은 「마스킹 전 원문이 DB·로그 어디에도 남지 않음」이고,
**메모리 캐시도 어디에 해당한다.** 키는 마스킹 완료본이거나 그 해시여야 한다.

## 완료 조건

- [ ] 캐시 적중률과 **적중/미적중 지연 차이**를 잰다 — 적중률만으로는 이득을 말할 수 없다
- [ ] **캐시를 끈 값과 켠 값을 나란히** 적는다. 품질 지표(Recall@5·MRR)가 **변하지 않는 것**을 확인한다 —
      변하면 캐시가 결과를 바꾸고 있다는 뜻이고 그건 버그다
- [ ] 키에 마스킹 전 원문이 들어가지 않는 것을 테스트가 고정한다(SEC-1)
- [ ] 지식베이스를 재적재하면 캐시가 무효화된다(안 그러면 옛 조항을 계속 돌려준다)
