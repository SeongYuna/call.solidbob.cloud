---
title: "시연 인프라 준비 — STT 캡 · speaker=auto 실검증 · 관리자 토큰 수명"
assignee: "정성윤"
role: "infra"
status: "todo"
sprint: 7
priority: 79
date: 2026-09-21
requirement:
  - "COST-1"
  - "A-2"
paths:
  - "infra/k8s/base/*"
---

## 무엇을

[시연 리허설](/backlog/w8-demo-rehearsal/) 전에 **인프라 쪽에서 막힐 것 셋**을 먼저 푼다.

1. **STT 캡** — 2026-09-21 `/call-mediator/health` 실측: **일 600초 · 월 3,600초.** 하루 약 4통화면 끝난다.
   리허설 + 본 시연을 같은 날 하면 본 시연에서 스트림 오픈이 거부된다(COST-1 2차 가드가 의도대로 동작하는 것이다).
   **시연 기간에만 올리고 끝나면 되돌린다** — GCP 쿼터(1차)와 `STT_MAX_SECONDS_*`(2차) 둘 다
2. **`speaker=auto`** — 모노 녹음 화자 분리(`decisions/303`)가 **실제 구글 응답으로 미검증**이다. 시연은 물리 2채널(브라우저 2대)로 우회하기로 했지만,
   우회가 안 될 때의 대비로 한 번은 실제 응답으로 돌려 본다
3. **관리자 토큰 수명** — access 5분 · refresh 10분은 **테스트 값**이다(`decisions/403`). 시연 중에 로그인이 풀린다. 운영 값은 조서희 님과 정한다

## 완료 조건

- [ ] 캡을 올린 값 · 올린 날 · **되돌린 날**이 `_logs/` 에 남는다
- [ ] `speaker=auto` 실검증 결과 — 안 되면 「미검증」을 「안 됨」으로 고쳐 적는다
- [ ] 관리자 토큰 운영 값 반영
